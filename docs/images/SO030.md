# SO030 处理记录

> 状态：两个「菜单」块已替换为中文并安装；**8 组 16 个按钮（归还/上锁/撤退/开锁/转移/待机/返回×2）已清理+渲染+注入 AGF**（`install\SO030.AGF`，见 §6–§9）。

## 1. 图片信息

- 原图：`install\DATA1-png\SO030.png`，2000×2000，RGBA。
- 原 AGF：`install\DATA1\SO030.AGF`。

## 2. 块识别（连通块查询，alpha≥128）

使用 `.tmp\cc_scan.py`：

| 查询坐标 | 块 | 范围 | 宽×高 | 像素 |
|---|---|---|---|---|
| (650,1400) | A | x=[568,721] y=[1374,1421] | 154×48 | 6278 |
| (800,1400) | B | x=[742,863] y=[1383,1411] | 122×29 | 3114 |

与 SO021/SO025 的「菜单」按钮对完全同型（154×48 / 122×29，像素数 6278 / 3114）。

## 3. 处理记录（已安装）

### 块 A（x=[568,721] y=[1374,1421]，154×48）

- 清理：保留左右各 40px（x=568..607、x=682..721）；中间 x=608..681 逐行复制第 41px 列（x=608，已确认干净）。
- 渲染：居中「菜单」，上半效果（文泉驿 20px、纯黑、字间距 1px、0.5px 黑色描边，行高 48px，top=1374）。
- 校验：黑字中心 (644.5, 1397.5)，与块中心一致。

### 块 B（x=[742,863] y=[1383,1411]，122×29）

- 清理：保留左右各 23px（x=742..764、x=841..863）；中间 x=765..840 逐行复制第 24px 列（x=765，已确认干净）。
- 渲染：居中「菜单」，下半效果（黑字 + 外层约 1px 白色描边 50% 透明度，行高 29px，top=1384 含 +1px 下偏移）。
- 校验：黑字水平中心 802.5（与块中心一致）；垂直中心约 1399（块中心 1397，含 1px 下偏移）；白描边生效。

## 4. 产物与安装

| 项目 | 路径 |
|---|---|
| 清理图 | `.tmp\so030_cleaned.png`（2000×2000） |
| 渲染页 | `.tmp\so030_render.html` |
| 渲染 PNG | `.tmp\so030_cn.png`（2000×2000） |
| 注入 AGF（overlay） | `install\SO030.AGF`（8bpp + ACIF，8,001,152 B） |
| res 产物 | `res\SO030.AGF` / `res\SO030.png` |
| 补丁包 | `patch\AGF\SO030.AGF`（`patch.config.json` 已登记） |

## 5. 复现命令

1. headless Chrome 渲染（需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so030_cn.png" `
     --window-size=2000,2000 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so030_render.html"
   ```
2. AGF 注入（scripts 目录）：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO030.AGF" `
     "E:\Games\Eushully\天結\.tmp\so030_cn.png" -o "E:\Games\Eushully\天結\install\SO030.AGF"
   ```
3. 复制到 res，执行 `npm run sync-patch`。

---

## 6. 8 组 16 按钮（已安装，2026-08）

用 `amayui_uimap` 工具扫描 `install\DATA1-png\SO030.png` 定位 16 个等大按钮（全部 103×73，px=7209），
按坐标配对为 8 组（同列上下相邻两两一组；x=1176 列为特殊交错放置：`#55+#70`、`#66+#79`）。

### 6.1 分组与文案（用户确认）

| No. | 组 | 上半块 | 下半块 | 中文 |
|---|---|---|---|---|
| 1 | G3 | #72 (674,562) | #84 (674,637) | 归还 |
| 2 | G4 | #96 (674,712) | #104 (674,787) | 上锁 |
| 3 | G5 | #73 (781,562) | #85 (781,637) | 撤退 |
| 4 | G6 | #97 (781,712) | #105 (781,787) | 开锁 |
| 5 | G7 | #74 (888,562) | #86 (888,637) | 转移 |
| 6 | G1 | #55 (1176,371) | #70 (1176,521) | 待机 |
| 7 | G2 | #66 (1176,446) | #79 (1176,596) | 返回 |
| 8 | G8 | #148 (1234,1153) | #160 (1234,1228) | 返回 |

> 块坐标为左上角 (x0, y0)，每块 103×73。识别顺序（No.1–8）为 HTML 检查页
> `.tmp\so030_upper_identify.html` 中按视觉顺序（上→下、左→右）的编号。

### 6.2 清理方案（基于 res 版本，`clean_fill.py` 列填充）

- 基础图：`res\SO030.png`（已含此前「菜单」处理）。
- 所有按钮清理后底板一致；上半块用 `#55` 模板、下半块用 `#70` 模板：

| 模板 | keepL | keepR | keepT | keepB |
|---|---|---|---|---|
| 上半（#55 模板） | 17 | 18 | 17 | 17 |
| 下半（#70 模板） | 17 | 18 | 13 | 12 |

- 填充列 `fill-col = x0 + 19`（与 #55=1195 同规则：1176+19）。
- 16 块链式清理脚本：`.tmp\clean_so030_res16.py` → 产物 `.tmp\SO030_res_clean.png`（2000×2000）。

### 6.3 渲染参数（用户逐步确认的最终值）

- 字体：文泉驿（`.tmp\WenQuanYi.ttf` @font-face），**30px**，字间距 1px，行高 73px（按钮高度，垂直居中），
  水平居中，`top = y0 - 1`（居中上移 1px）。
- 双层 span 结构（`.out` 描边层 + `.in` 文字层），两层**整体偏移 `translateY(-0.5px)`**：

| 层 | 上半 | 下半 |
|---|---|---|
| `.out`（描边层） | `-webkit-text-stroke: 4px rgba(255,255,255,1)` + `text-shadow: 0 4px 4px rgba(0,0,0,1)`（阴影施加于白描边层） | `-webkit-text-stroke: 4px rgba(255,255,255,0.5)` |
| `.in`（文字层） | `-webkit-text-stroke: 0.5px #000; color:#000` | 同左 |

> 样式迭代记录：28px→30px；白描边 2px→4px（上下半均 4px，上半 100%/下半 50%）；整体偏移 (0,-2)→(0,-0.5)；
> 上半阴影 (0,4) 晕染 2px→4px（翻倍）；阴影始终施加于白色 stroke 的 `.out` 层。

### 6.4 产物与安装

| 项目 | 路径 |
|---|---|
| 清理底图 | `.tmp\SO030_res_clean.png`（2000×2000） |
| 渲染页 | `.tmp\SO030_render16.html`（生成脚本 `.tmp\make_so030_render16.py`） |
| 最终渲染 PNG | `res\SO030-1.png`（2000×2000，16 按钮全中文） |
| 注入 AGF | `install\SO030.AGF`（8bpp + ACIF，8,001,152 B，由 `install\DATA1\SO030.AGF` 注入） |

### 6.5 复现命令

1. 清理 16 块（基于 res 版本）：`python .tmp\clean_so030_res16.py`
2. 生成渲染页：`python .tmp\make_so030_render16.py`
3. headless Chrome 截图（需提权）→ `res\SO030-1.png`：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --no-sandbox --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\res\SO030-1.png" `
     --window-size=2000,2000 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/SO030_render16.html"
   ```
4. AGF 注入：`node scripts/agf/cli.js inject "install\DATA1\SO030.AGF" "res\SO030-1.png" -o "install\SO030.AGF"`
