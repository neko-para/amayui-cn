# SO030 处理记录

> 状态：两个「菜单」块已替换为中文并安装（install root AGF overlay + 补丁包同步）。

## 1. 图片信息

- 原图：`install\DATA1-png\SO030.png`，2000×2000，RGBA。
- 原 AGF：`install\DATA1\SO030.AGF`。

## 2. 块识别（连通块查询，alpha≥128）

使用 `.tmp\cc_scan.py`：

| 查询坐标 | 块 | 范围 | 宽×高 | 像素 |
|---|---|---|---|---|
| (650,1400) | A | x=[568,721] y=[1374,1421] | 154×48 | 6278 |
| (800,1400) | B | x=[742,863] y=[1383,1411] | 122×29 | 3114 |

与 SO021/SO025 的「菜单」按钮对完全同型（154×48 / 122×29，像素数 6278 / 3114）。

## 3. 处理记录（已安装）

### 块 A（x=[568,721] y=[1374,1421]，154×48）

- 清理：保留左右各 40px（x=568..607、x=682..721）；中间 x=608..681 逐行复制第 41px 列（x=608，已确认干净）。
- 渲染：居中「菜单」，上半效果（文泉驿 20px、纯黑、字间距 1px、0.5px 黑色描边，行高 48px，top=1374）。
- 校验：黑字中心 (644.5, 1397.5)，与块中心一致。

### 块 B（x=[742,863] y=[1383,1411]，122×29）

- 清理：保留左右各 23px（x=742..764、x=841..863）；中间 x=765..840 逐行复制第 24px 列（x=765，已确认干净）。
- 渲染：居中「菜单」，下半效果（黑字 + 外层约 1px 白色描边 50% 透明度，行高 29px，top=1384 含 +1px 下偏移）。
- 校验：黑字水平中心 802.5（与块中心一致）；垂直中心约 1399（块中心 1397，含 1px 下偏移）；白描边生效。

## 4. 产物与安装

| 项目 | 路径 |
|---|---|
| 清理图 | `.tmp\so030_cleaned.png`（2000×2000） |
| 渲染页 | `.tmp\so030_render.html` |
| 渲染 PNG | `.tmp\so030_cn.png`（2000×2000） |
| 注入 AGF（overlay） | `install\SO030.AGF`（8bpp + ACIF，8,001,152 B） |
| res 产物 | `res\SO030.AGF` / `res\SO030.png` |
| 补丁包 | `patch\AGF\SO030.AGF`（`patch.config.json` 已登记） |

## 5. 复现命令

1. headless Chrome 渲染（需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so030_cn.png" `
     --window-size=2000,2000 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so030_render.html"
   ```
2. AGF 注入（scripts 目录）：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO030.AGF" `
     "E:\Games\Eushully\天結\.tmp\so030_cn.png" -o "E:\Games\Eushully\天結\install\SO030.AGF"
   ```
3. 复制到 res，执行 `npm run sync-patch`。
