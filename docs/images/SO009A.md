# SO009A 修改记录

> 用途：设置页文字按钮区域（金色文字三行）中文化。

## 1. 文件与产物

| 项目 | 路径 | 说明 |
|---|---|---|
| 原 AGF | `install\DATA1\SO009A.AGF` | 936,491 B，8bpp + ACIF |
| 原 PNG | `install\DATA1-png\SO009A.png` | 1280×1152（整张 UI 图） |
| 修改后 PNG | `.tmp\so009a_cn.png` | 1280×1152，透明背景 |
| 已安装 AGF | `install\SO009A.AGF` | 2,950,272 B，8bpp + ACIF |

## 2. 文字区域（像素分析确认）

- 内容区域左上角 (974, 687)，右下约 (1130, 763)。
- 三行金色文字（样式与 SO009B 第二列一致，主体 `#FDD7A3`/`#ffd6a4` + 黑阴影）：

| 行 | 原文 | 译文 | y 范围 | x 范围（原文） |
|---|---|---|---|---|
| 1 | 戻る | 返回 | 687-706 | 975-1015 |
| 2 | ページの初期化 | 初始化本页 | 714-733 | 975-1126 |
| 3 | 全体の初期化 | 初始化全部 | 740-760 | 975-1104 |

- 区域内黑色像素为文字阴影（与金色比例约 0.79，与 SO009B 第二列一致），非背景。

## 3. 渲染参数（当前生效）

```css
font-family: "WenQuanYi";          /* 文泉驿微米黑 */
font-size: 20px;
line-height: 1;
color: #ffd6a4;
-webkit-text-stroke: 0.5px #ffd6a4;
text-shadow: 2px 2px 1px rgba(0,0,0,1);
```

布局：

| 行 | left | top |
|---|---|---|
| 返回 | 975px | 687px |
| 初始化本页 | 999px（+24px 偏移） | 714px |
| 初始化全部 | 999px（+24px 偏移） | 740px |

清理范围：x 969-1136、y 683-765（避开 y=682 的一撮无关白色像素；文字阴影到 y=763 已覆盖）。

## 4. 状态与待办

- [x] 三行金色文字已替换为中文并注入 `install\SO009A.AGF`。
- [ ] 第一行（返回）：**未找到实际显示内容**，但已按同样流程处理（占位保留）。
- [ ] 图内还有「初期化」「戻る」两个大按钮文字未处理：含义基本可猜（初始化 / 返回），暂不处理。

## 5. 复现步骤

1. 清理：`python .tmp\so009a_clean.py` → `.tmp\so009a_cleaned.png`
2. 编辑渲染页 `.tmp\so009a_render.html`（CSS / left / top）
3. headless Chrome 截图（1280×1152，透明背景，需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu `
     --no-first-run --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so009a_cn.png" `
     --window-size=1280,1152 --force-device-scale-factor=1 `
     --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so009a_render.html"
   ```
4. 注入：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO009A.AGF" `
     "E:\Games\Eushully\天結\.tmp\so009a_cn.png" `
     -o "E:\Games\Eushully\天結\install\SO009A.AGF"
   ```

## 6. 注意事项

- 调整历史：第二、三行偏移 26 → 24 px。
- 有头注入为无压缩写入，产物 0.94MB → 2.95MB。
- 还原：从 `install\DATA1\SO009A.AGF` 复制覆盖即可。

---

## 7. 「返回」按钮 ×2（已渲染，2026-08）

在 `res\SO009A.png` 基础上处理「初期化」「戻る」两个大按钮中的「戻る→返回」一组（上、下两个按钮）。

### 7.1 块信息（amayui_uimap 定位，用户导出方案）

| 块 | 连通块 | 坐标 (x0,y0) | 宽×高 | 文案 | 效果 |
|---|---|---|---|---|---|
| 1 | #42 | (1025,809) | 117×81 | 返回 | 上半（白描边 100%+阴影） |
| 2 | #44 | (1025,891) | 117×81 | 返回 | 下半（白描边 50%） |

> 两个按钮上下相邻、同一列（x=1025），为一组「返回」；块1 米白底、块2 青蓝渐变底（原图状态）。

### 7.2 清理方案（工作台导出 `.tmp\SO009A_clean.json` / `.sh`）

- 两块均 keepL=20 / keepR=20 / keepT=17 / keepB=17（列填充）。
- fill-col：#42=1049、#44=1052（不同，避免穿过文字笔画）。
- 链式脚本：`.tmp\SO009A_clean.sh` → 产物 `.tmp\SO009A_clean_2.png`（1280×1152）。

### 7.3 渲染参数（用户确认：按 30px 规则 = SO030 第一轮效果）

- 字体：文泉驿（`res\fonts\WenQuanYi.ttf`），**30px**，字距 1px，行高 81px（按钮高度，垂直居中），
  水平居中，`top = y0 - 1`（居中上移 1px），整体偏移 `translateY(-0.5px)`。
- 效果与 SO030 第一轮一致：黑字 `#000` + 0.5px 黑描边（`.in`）；白描边 4px（`.out`：
  上 100% + 阴影 `0 4px 4px` 于白描边层 / 下 50%）。并列结构 `.in` 前 `.out` 后，z-index 分层。

### 7.4 渲染 HTML 核心代码（可复现）

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<style>
  @font-face { font-family: "WenQuanYi"; src: url("../res/fonts/WenQuanYi.ttf"); }
  html, body { margin: 0; padding: 0; width: 1280px; height: 1152px; background: transparent; overflow: hidden; }
  #bg { position: absolute; left: 0; top: 0; }
  .btn { position: absolute; width: 117px; text-align: center; line-height: 81px;
         font-family: "WenQuanYi"; font-size: 30px; letter-spacing: 1px; white-space: nowrap;
         transform: translateY(-0.5px); }
  .out { position: absolute; left: 0; right: 0; top: 0; z-index: 1; }
  .in { position: absolute; left: 0; right: 0; top: 0; z-index: 2;
        color: #000; -webkit-text-stroke: 0.5px #000; }
  .up .out { -webkit-text-stroke: 4px rgba(255,255,255,1);
             color: rgba(255,255,255,1); text-shadow: 0 4px 4px rgba(0,0,0,1); }
  .down .out { -webkit-text-stroke: 4px rgba(255,255,255,0.5);
               color: rgba(255,255,255,0.5); }
</style>
</head>
<body>
  <img id="bg" src="SO009A_clean_2.png">
  <div class="btn up"   style="left:1025px; top:808px;"><span class="in">返回</span><span class="out">返回</span></div>
  <div class="btn down" style="left:1025px; top:890px;"><span class="in">返回</span><span class="out">返回</span></div>
</body>
</html>
```

### 7.5 产物

| 项目 | 路径 |
|---|---|
| 清理底图 | `.tmp\SO009A_clean_2.png`（1280×1152） |
| 渲染页 | `.tmp\SO009A_render2.html` |
| 最终渲染 PNG | `res\SO009A-1.png`（1280×1152，2 按钮中文） |
| 编译 AGF（覆盖） | `res\SO009A.AGF`（bpp=8，ACIF=true，2,950,272 B，由 `res\SO009A.AGF` 注入 `res\SO009A-1.png`） |

### 7.6 复现命令

1. 清理 2 块（基于 res/SO009A.png）：`bash .tmp/SO009A_clean.sh` → `.tmp/SO009A_clean_2.png`
2. 生成渲染页：见 §7.4 HTML（存 `.tmp/SO009A_render2.html`）
3. headless Chrome 截图（1280×1152）→ `res/SO009A-1.png`
4. AGF 编译：`node scripts/agf/cli.js inject res/SO009A.AGF res/SO009A-1.png -o res/SO009A.AGF`
