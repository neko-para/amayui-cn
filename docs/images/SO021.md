# SO021 处理记录

> 状态：块 A 与块 B 已替换为中文「菜单」并安装；**2 个「去城砦」按钮（#28/#29）已替换为中文并安装至 `res\images\SO021-3.png`**（黑`去`18px/红`城砦`20px，见 §8）。（install root AGF overlay + 补丁包同步）

## 1. 图片信息

- 原图：`install\DATA1-png\SO021.png`，1280×1280，RGBA。
- 原 AGF：`install\DATA1\SO021.AGF`。

## 2. 块识别（连通块查询）

使用 `.tmp\cc_scan.py` 查询坐标所属连通块（4 邻接）：

| 查询坐标 | 块 | 完整范围（alpha≥1） | 实体范围（alpha≥128） |
|---|---|---|---|
| (734,768) | A | x=[660,814] y=[748,796]（155×49） | x=[660,813] y=[749,796]（154×48） |
| (727,814) | B | x=[660,787] y=[799,829]（128×31） | x=[663,784] y=[800,828]（122×29） |

结构观察：

- 块 A 为多行文本面板（浅色底，原含 4 段文字行），块 B 为单行文本块（青蓝渐变底）；
- 两块纵向相邻（间隙 2-3px），同列起点 x≈660；上方 y=720..745 有头部块，下方 y=832..855 有两个并排小块（未处理）。

## 3. 块 A 处理记录（已安装）

- 范围：x=[660,813] y=[749,796]（154×48）。
- 清理：保留左右各 40px（x=660..699、x=774..813）；中间 x=700..773 逐行复制第 41px 列（x=700，已确认未穿过文字笔画）。
- 渲染：居中「菜单」，字体效果与 SO020 上半按钮一致：

```css
font-family: "WenQuanYi"; font-size: 20px; line-height: 48px;
letter-spacing: 1px; color: #000; -webkit-text-stroke: 0.5px #000; text-align: center;
```

- 校验：文字水平中心 735（块中心 736.5，偏差约 1.5px 来自字间距）、垂直中心 772.5（与块中心一致）；原 4 行文字全部抹除，左右保留区仅剩边框/斜角装饰。

## 4. 块 B 处理记录（已安装）

- 范围：x=[663,784] y=[800,828]（122×29）。
- 清理：保留左右各 23px（x=663..685、x=762..784）；中间 x=686..761 逐行复制第 24px 列（x=686，已确认是干净的渐变背景）。
- 渲染：居中「菜单」，字体效果与 SO020 下半按钮一致（黑字 + 外层约 1px 白色描边 50% 透明度）：

```css
font-family: "WenQuanYi"; font-size: 20px; line-height: 29px;
letter-spacing: 1px; color: #000; text-align: center;
top: 801px;   /* 2026-08-10 用户确认：在块 B（y=800..828）基础上额外向下偏移 1px */
/* 外层（描边层）：-webkit-text-stroke: 2px rgba(255,255,255,0.5); color: rgba(255,255,255,0.5);
   内层（文字层）：-webkit-text-stroke: 0.5px #000; color: #000; */
```

- 校验：黑字水平中心 723.5（与块中心一致）、垂直中心约 815.5（块中心 814，向下偏移 1px 后）；半透明白描边生效；块 A 区域逐像素差异 0。

## 5. 产物与安装

| 项目 | 路径 |
|---|---|
| 清理+渲染中间图（仅块 A） | `.tmp\so021_blocka_cn.png` |
| 清理+渲染最终图（块 A + B） | `.tmp\so021_cn.png`（1280×1280） |
| 渲染页 | `.tmp\so021_blocka_render.html` / `.tmp\so021_bottom_render.html` |
| 注入 AGF（overlay） | `install\SO021.AGF`（8bpp + ACIF，3,277,952 B） |
| res 产物 | `res\SO021.AGF` / `res\SO021.png` |
| 补丁包 | `patch\AGF\SO021.AGF`（`patch.config.json` 已登记） |

## 6. 复现命令

1. headless Chrome 渲染最终图（需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so021_cn.png" `
     --window-size=1280,1280 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so021_bottom_render.html"
   ```
2. AGF 注入（scripts 目录）：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO021.AGF" `
     "E:\Games\Eushully\天結\.tmp\so021_cn.png" -o "E:\Games\Eushully\天結\install\SO021.AGF"
   ```
3. 复制到 res，执行 `npm run sync-patch`。

## 7. 待办

- 块 A 上方的头部块（y=720..745）与下方的两个并排小块（y=832..855）未处理。

---

## 8. 2 个「去城砦」按钮（已安装，2026-08）

在 `res\images\SO021-1.png` 基础上新增 2 个按钮（#28/#29，93×73，y≈634），文案「去城砦」（去 = E5 上半、城砦 = E6）。

### 8.1 块信息

| 块 | 坐标 (x0,y0) | 宽×高 | 底 |
|---|---|---|---|
| #28 | (794,634) | 93×73 | 米白底 |
| #29 | (889,634) | 93×73 | 青底 |

> 原日文：「城砦へ」；译文：「去城砦」。

### 8.2 清理方案（工作台导出 `.tmp/SO021-1_clean.json` / `.sh`）

- 两块均 keepL=15 / keepR=15 / keepT=16 / keepB=16（列填充，四边保留边框）。
- fill-col：#28=811、#29=905；产物 `.tmp\SO021-1_cleaned.png`（1280×1280）。

### 8.3 渲染参数（用户指定）

- 文案「去城砦」：**「去」= E5 上半**（黑字 `#000` + 0.5px 黑描边）、**「城砦」= E6**（红字 `#FD480A`、无黑描边）；两者共用白描边 4px 100% + 阴影 `0 4px 4px`（`.out` 层按字同拆分、`.in` 层按字分色）；
- 字号（用户调整）：**黑「去」= 18px**（`.s18`）、**红「城砦」= 20px**（基准，未变）；字距 1px，居中，**`top = y0 - 1`（向上偏移 1px）**；行高 = 块高 73px。

### 8.4 产物与安装

| 项目 | 路径 |
|---|---|
| 清理底图 | `.tmp\SO021-1_cleaned.png`（1280×1280） |
| 渲染页 | `.tmp\so021_render.html` |
| 渲染 PNG | `res\images\SO021-3.png`（1280×1280，**当前生效版**，黑`去`18px/红`城砦`20px） |
| 注入 AGF（overlay） | `install\SO021.AGF`（8bpp + ACIF，3,277,952 B） |
| res 产物 | `res\images\SO021.AGF`（= install，MD5 一致） |
| 补丁包 | `patch\AGF\SO021.AGF` |

### 8.5 复现命令

1. 清理 2 块（clean_fill.py 列填充）→ `.tmp\SO021-1_cleaned.png`。
2. headless Chrome 渲染（需提权）→ `res\images\SO021-2.png`（1280×1280）。
3. AGF 注入：`node scripts/agf/cli.js inject install/DATA1/SO021.AGF res/images/SO021-2.png -o install/SO021.AGF`。
4. 复制到 `res\images\SO021.AGF` 与 `patch\AGF\SO021.AGF`，`npm run sync-patch`、`npm run manifest -- SO021.AGF`。
