# SO025 处理记录

> 状态：两个「菜单」块已替换为中文并安装（install root AGF overlay + 补丁包同步）。

## 1. 图片信息

- 原图：`install\DATA1-png\SO025.png`，1280×512，RGBA。
- 原 AGF：`install\DATA1\SO025.AGF`。

## 2. 块识别与确认

使用 `.tmp\cc_scan.py` 查询坐标所属连通块（alpha≥128）：

| 查询坐标 | 块 | 范围 | 宽×高 | 像素 |
|---|---|---|---|---|
| (485,94) | A | x=[418,571] y=[71,118] | 154×48 | 6278 |
| (478,134) | B | x=[421,542] y=[122,150] | 122×29 | 3114 |

确认结果：

- 两块的**形状/尺寸与 SO021 的块 A/B 完全一致**（同为 154×48 / 122×29，alpha≥128 连通块像素数相同 6278 / 3114）；
- 但内部文字内容与 SO021 不同（逐像素对比差异 6050/7392 与 2687/3538），因此按 SO025 自身内容清理后重新渲染。

## 3. 处理记录（已安装）

### 块 A（x=[418,571] y=[71,118]，154×48）

- 清理：保留左右各 40px（x=418..457、x=532..571）；中间 x=458..531 逐行复制第 41px 列（x=458，已确认干净）。
- 渲染：居中「菜单」，效果同 SO021 块 A / SO020 上半按钮（文泉驿 20px、纯黑、字间距 1px、0.5px 黑色描边，行高 48px）。
- 校验：黑字水平/垂直中心均与块中心一致（cx=494.5、cy=94.5）。

### 块 B（x=[421,542] y=[122,150]，122×29）

- 清理：保留左右各 23px（x=421..443、x=520..542）；中间 x=444..519 逐行复制第 24px 列（x=444，已确认干净）。
- 渲染：居中「菜单」，效果同 SO021 块 B / SO020 下半按钮（黑字 + 外层约 1px 白色描边 50% 透明度，行高 29px，top 下移 1px → 123px）。
- 校验：黑字水平中心 481.5（与块中心一致）；垂直中心约 138（块中心 136，含 1px 下偏移）；白描边生效。

## 4. 产物与安装

| 项目 | 路径 |
|---|---|
| 清理图 | `.tmp\so025_cleaned.png`（1280×512） |
| 渲染页 | `.tmp\so025_render.html` |
| 渲染 PNG | `.tmp\so025_cn.png`（1280×512） |
| 注入 AGF（overlay） | `install\SO025.AGF`（8bpp + ACIF，1,311,872 B） |
| res 产物 | `res\SO025.AGF` / `res\SO025.png` |
| 补丁包 | `patch\AGF\SO025.AGF`（`patch.config.json` 已登记） |

## 5. 复现命令

1. headless Chrome 渲染（需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so025_cn.png" `
     --window-size=1280,512 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so025_render.html"
   ```
2. AGF 注入（scripts 目录）：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO025.AGF" `
     "E:\Games\Eushully\天結\.tmp\so025_cn.png" -o "E:\Games\Eushully\天結\install\SO025.AGF"
   ```
3. 复制到 res，执行 `npm run sync-patch`。
