# SO025 处理记录

> 状态：两个「菜单」块已替换为中文并安装（install root AGF overlay + 补丁包同步）。
>
> **输入图片以 `so025-overlay.png` 为准**（overlay 变体）。早期曾误用基础版 `SO025.png` 处理，
> 该版本已废弃并重新构建（见文末「旧记录」）。

## 1. 输入图片信息

- **当前输入**：`install\DATA1-png\so025-overlay.png`，1280×512，RGBA（overlay 变体，游戏实际显示以它为准）。
- 基础版 `install\DATA1-png\SO025.png` 与此变体内容不同（全图差异 72293 像素），不再作为处理输入。
- 原 AGF：`install\DATA1\SO025.AGF`。

## 2. 块识别与确认

使用 `.tmp\cc_scan.py` 查询坐标所属连通块（alpha≥128，so025-overlay.png）：

| 查询坐标 | 块 | 范围 | 宽×高 | 像素 |
|---|---|---|---|---|
| (480,95) | A | x=[418,571] y=[71,118] | 154×48 | 6278 |
| (480,140) | B | x=[421,542] y=[122,150] | 122×29 | 3114 |

确认结果：

- 两块的**形状/尺寸与 SO021 的块 A/B 完全一致**（154×48 / 122×29，alpha≥128 连通块像素数 6278 / 3114）；
- 与基础版 SO025 同位置按钮几何相同，但内容不同（块 A 差异 5328/7392、块 B 差异 592/3538），按 overlay 自身内容清理。

## 3. 处理记录（当前，基于 overlay）

### 块 A（x=[418,571] y=[71,118]，154×48）

- 清理：保留左右各 40px（x=418..457、x=532..571）；中间 x=458..531 逐行复制第 41px 列（x=458，已确认干净）。
- 渲染：居中「菜单」，上半效果（文泉驿 20px、纯黑、字间距 1px、0.5px 黑色描边，行高 48px，top=71）。
- 校验：黑字水平/垂直中心均与块中心一致（cx=494.5、cy=94.5）。

### 块 B（x=[421,542] y=[122,150]，122×29）

- 清理：保留左右各 23px（x=421..443、x=520..542）；中间 x=444..519 逐行复制第 24px 列（x=444，已确认干净）。
- 渲染：居中「菜单」，下半效果（黑字 + 外层约 1px 白色描边 50% 透明度，行高 29px，top=123 含 +1px 下偏移）。
- 校验：黑字水平中心 481.5（与块中心一致）；垂直中心约 138（块中心 136，含 1px 下偏移）；白描边生效。

## 4. 产物与安装（当前）

| 项目 | 路径 |
|---|---|
| 清理图 | `.tmp\so025_overlay_cleaned.png`（1280×512） |
| 渲染页 | `.tmp\so025_overlay_render.html` |
| 渲染 PNG | `.tmp\so025_overlay_cn.png`（1280×512） |
| 注入 AGF（overlay） | `install\SO025.AGF`（8bpp + ACIF） |
| res 产物 | `res\SO025.AGF` / `res\SO025.png` |
| 补丁包 | `patch\AGF\SO025.AGF`（`patch.config.json` 已登记） |

## 5. 复现命令

1. headless Chrome 渲染（需提权）：
   ```powershell
   & "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless --disable-gpu --no-first-run `
     --user-data-dir="E:\Games\Eushully\天結\.tmp\chrome_tmp" `
     --screenshot="E:\Games\Eushully\天結\.tmp\so025_overlay_cn.png" `
     --window-size=1280,512 --force-device-scale-factor=1 --default-background-color=00000000 `
     "file:///E:/Games/Eushully/天結/.tmp/so025_overlay_render.html"
   ```
2. AGF 注入（scripts 目录）：
   ```powershell
   node agf/cli.js inject "E:\Games\Eushully\天結\install\DATA1\SO025.AGF" `
     "E:\Games\Eushully\天結\.tmp\so025_overlay_cn.png" -o "E:\Games\Eushully\天結\install\SO025.AGF"
   ```
3. 复制到 res，执行 `npm run sync-patch`。

## 6. 旧记录（已废弃，勿再使用）

> 早期误用基础版 `SO025.png` 处理并构建，已确认不正确（overlay 版本才是实际显示内容），
> 现已被第 3-5 节的 overlay 构建替换。以下仅为历史存档：

- 旧输入：`install\DATA1-png\SO025.png`；
- 旧产物：`.tmp\so025_cleaned.png` / `.tmp\so025_render.html` / `.tmp\so025_cn.png`；
- 旧校验：块 A 中心 (494.5, 94.5)；块 B 中心 (481.5, 138.0)（几何与 overlay 版一致，但背景内容来源不同）。
