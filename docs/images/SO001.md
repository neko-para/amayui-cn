# SO001 处理记录

> 状态：**15 个按钮中文化并安装**（5 行 × 3 列）（install root AGF overlay + 补丁包同步）。

## 1. 输入图片信息

- **当前输入**：`res\images\SO001-0.png`，1280×1792（原图），RGBA。
- 原 AGF：`install\DATA1\SO001.AGF`（8bpp，795,167 B 有头）。

## 2. 结构（15 块 = 5 行 × 3 列）

| 行 | 原日文 | 中文 | 位置 (x0,y0) |
|---|---|---|---|
| 1 | 設定変更 | 变更设置 | (603/685/767, 795) |
| 2 | 初期化 | 初始化 | (603/685/767, 878) |
| 3 | 全解除 | 全部移除 | (603/685/767, 961) |
| 4 | 閉じる | 关闭 | (603/685/767, 1044) |
| 5 | 戻る | 返回 | (603/685/767, 1127) |

列（每块 77×78，周围花纹三列逐像素一致）：

| 列 | 底 | 文字效果 |
|---|---|---|
| 列1 x603 | 米白/暖棕渐变 | 黄金渐变字（白→金）+ 深棕描边 + **黑阴影** |
| 列2 x685 | 纯浅青 `(1,254,255)` | 白→青渐变字（底部≈背景青）+ 深蓝描边，无阴影 |
| 列3 x767 | 灰白渐变 **（列1 去饱和度）** | **透明→50%黑 渐变**（叠灰底 = 与背景混合）+ 黑描边，无阴影 |

- 列2 的下半部分字内颜色 = 外部背景纯青；（实测 `(4,233,255)`≈`(1,254,255)`）。
- 列3 为列1 灰度化；文字为半透明黑叠于灰底（视觉与背景混合，非 PNG 透明）。

## 3. 清理

三列/五行逐块清理，仅填充文字区（bbox 外扩 2px），四周边框/角纹/侧渐变逐像素保留。

- 列1（暖）：拉普拉斯插值（复用既有模板流程）。
- 列2（纯青）：文字区填纯青。
- 列3（灰）：拉普拉斯插值。
- 清理校验：每块周围区域 diff = 0。

## 4. 渲染参数（CSS，headless Chrome 截图）

- 字体：**Sarasa Gothic SC，Bold**（@font-face 引用 `res/fonts/SarasaGothicSC/SarasaGothicSC-Bold.ttf`），22px，letter-spacing 1px。
- 字宽缩放：2/3 字 `scaleX(0.8)`、4 字 `scaleX(0.6)`。
- 三层 grid 重叠（`grid-area:1/1`）：描边层 `.out` + 填充层 `.gin`（列1 另有阴影层 `.shd`）。

| 列 | 描边 | 填充渐变 | 阴影 |
|---|---|---|---|
| 列1 | `2px #3A2709` | `#FFFFFF→#FDD896(30%)→#FBC568(62%)→#FDAB0A(100%)` | `2px 2px 1px rgba(0,0,0,1)`（仅列1） |
| 列2 | `2px #1438E0` | `#FFFFFF→#C8ECFF(30%)→#4FD4FF(70%)→#01FEFF(100%)` | 无 |
| 列3 | `2px rgba(0,0,0,0.6)` | `rgba(0,0,0,0)→rgba(0,0,0,0.22)(45%)→rgba(0,0,0,0.45)(100%)` | 无 |

- 文字定位：逐块绝对定位、块内 flex 居中；渐变 `background-clip:text` 落在字内。

## 5. 产物与安装

| 项目 | 路径 |
|---|---|
| 清理底图 | `.tmp\so001_clean_full.png`（1280×1792） |
| 渲染 HTML | `.tmp\so001_render15.html` |
| 渲染 PNG | `res\images\SO001-1.png`（1280×1792，**当前生效版**） |
| 注入 AGF（overlay） | `install\SO001.AGF`（8bpp + ACIF，4,588,672 B） |
| res 产物 | `res\images\SO001.AGF`（= install，MD5 一致 `A2AE18822A754F715F46D1A5CA07239B`） |
| 补丁包 | `patch\AGF\SO001.AGF` |

## 6. 复现命令

1. 清理 15 块（`python .tmp/so001_clean15.py`）→ 拼 `so001_clean_full.png`。
2. headless Chrome 渲染（需提权；本次会话已授 safety-full-access）→ `res\images\SO001-1.png`（1280×1792）。
3. AGF 注入：`node scripts/agf/cli.js inject install/DATA1/SO001.AGF res/images/SO001-1.png -o install/SO001.AGF`。
4. 复制到 `res\images\SO001.AGF` 与 `patch\AGF\SO001.AGF`，`npm run sync-patch`、`npm run manifest -- SO001.AGF`。
