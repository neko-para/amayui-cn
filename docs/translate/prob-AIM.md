# AIM 待定内容清单（prob-AIM.md）

> 更新日期：2026-08-08
> 来源：`src/AIM.txt`（目标/采集统计界面，2 处 set-string 字面量，其中 1 处译出、1 处保持原样）
> 用途：记录 AIM 翻译中的存疑点。

## 1. 跨文档一致性

| 词条 | 本次译文 | 候选 / 备注 |
|---|---|---|
| お金 | 金钱 | 与 CDINIT2「金钱卡片・铜/银/金」、keywords-关卡逻辑 一致；REWARD 未译（`draw-string … "お金"`），后续翻译时沿用 |

## 2. 原文自身疑点

1. 界面标题 `(global-string 121e2)` 按当前关卡索引取表，但全部 341 个脚本中均无对该地址的
   `set-string` 定义（SELSTAGE/INFOEN 也引用同一地址）——疑为引擎加载的字符串池数据；
   若后续在游戏内发现标题仍为日文，需另行定位字符串池来源，不属 AIM 脚本范围。

## 3. 工程说明

1. AIM 骨架校验通过，BIN 9064 字节，回读验证 1/1（`お金|金钱`）；`女神力` 保持原样未标记；
2. 胜负条件（bc0–bc5）等显示文本依赖各 SC 脚本在关卡开始时的 `set-string` 定义，
   已翻译关卡（如 SC4070）会显示中文，未翻译关卡暂显示日文——属 SC 进度范畴。
