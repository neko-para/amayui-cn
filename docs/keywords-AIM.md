# 目标/采集统计界面关键字表（AIM）

> 更新日期：2026-08-08
> 来源：`data/AIM.txt`（战前目标/采集统计界面：本脚本仅 2 处字面量文本）
> 用途：AIM 界面标签翻译沉淀；`src/AIM.txt` 已翻译并通过 assemble（回读验证 1/1）。
> 说明：AIM 无 ADV 文本、无 concat；可译字面量仅 2 处 `set-string`，
> 其余显示文本均为全局字符串引用（见 §2），由源表各自翻译。

## 1. 字面量标签

| 日文 | 中文 | 备注 |
|---|---|---|
| 女神力 | 女神力 | 保持原样（资源名，汉字可直接阅读；与 SELSTAGE/关卡逻辑一致） |
| お金 | 金钱 | 通用货币；与 CDINIT2「金钱卡片」、关卡逻辑关键字表一致 |

## 2. 全局字符串引用（不在本脚本翻译）

| 引用 | 内容来源 | 状态 |
|---|---|---|
| `(global-string bc0–bc5)` | 当前关卡的胜利/败北条件 | SC 剧情脚本在关卡开始时 `set-string` 定义，已随 SC 翻译（如 SC4070） |
| `(global-string 121e2)` | 界面标题（按当前关卡索引取表） | 脚本内无 `set-string` 定义，疑似引擎/外部字符串池提供，待确认 |
| `(global-string 125ca)` | 单位名称表 | EBINIT（已译） |
| `(global-string 18e40)` | 道具名称池 | ITINIT（已译） |
| `(global-string 1f5ba)` | 道具名称池（贵重品系） | ITINIT（已译） |

## 3. 绘制约定

- `set-string` 字面量用对语法 `"原文|译文"` 译出（本脚本 1 处）；
- `draw-string` 尾参为寄存器/全局字符串引用时不译（源表已译）；
- 分隔符 `/` 等符号保持原样。
