# FIELD 待定内容清单（prob-FIELD.md）

> 更新日期：2026-08-06
> 来源：`src/FIELD.txt`（场域消息，12 个唯一 set-string + 15 个唯一 concat 片段，全量翻译）
> 用途：记录 FIELD 翻译中**已采用但需游戏内验证**的句式与工程说明。

## 1. 已采用句式（待游戏内验证）

| 组 | 成品句 | 说明 |
|---|---|---|
| 占拠 | 已占领！/ 被占领了！ | 主动/被动后缀式 |
| 发现 | 被发现！ | 被动后缀 |
| 素材 | 素材[N]个已放入采集袋！ | 后缀式 |
| 女神力 | 获得女神力[N]！ | 动词前置由片段改写实现 |
| 道具入手 | 【分类】获得道具名[N]个！ | F 组重排（方案①） |
| 门锁 | 没有『』和『』技能，似乎打不开…… | 三片段前缀/后缀化 |

## 2. 待复核项

| 项 | 现状 | 说明 |
|---|---|---|
| 门锁句行宽 | 约 22 中文字 | 接近 25 字上限，游戏内确认换行 |
| 捕获计数 | `（当前/上限）` 全角括号 + 半角数字 | toString 生成，保持半角 |
| 什么都没找到… | 省略号保留 | 与原文一致 |

## 3. 工程说明

1. **lookup-array 重排暂缓**：本次仅重排 concat 行（本就自由），未改 `translate.js`；
   若后续需要移动 lookup-array，再实现 REORDERABLE 子集检查（基线 ⊆ 当前）；
2. `src/$1$PLINIT.txt` 杂散文件见 `prob-PLINIT.md` §5，与此脚本无关；
3. 拼接消息的成品句已在 `keywords-场域消息.md` 记录，作为后续 SC* 相关消息的统一参照。
