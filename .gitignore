/install
/raw
/raw-parts
/.tmp

/tools/alf
/tools/eushully-decompiler
/tools/SExtractor
/tools/UniversalInjectorFramework
/tools/Eushully-Editor
/tools/ASProtect_unpacker1.7
/tools/unpack

node_modules

# 原生模块的 CMake 构建产物（`.node`/obj/vcxproj）**不进版本库**：
# 本仓的分发口径是「按需构建」（见 native/host-input/README.md 的「预置产物」一节）——缺了它模拟器照旧能跑
# （加载器降级 + 一行诊断），所以不必像 npm 包那样把二进制塞进 git。
/native/*/build/
/native/*/build-*/
/native/*/prebuilds/
# ★例外：macOS 侧的**预编译通用二进制**进版本库（`tickets/T-0117`）。
# 为什么只有这一处开例外：① 模块是纯 N-API ⇒ ABI 稳定，一份产物跨 Node/Electron，没有 node-gyp 那种
# 「换 Node 版本必须重编」的腐坏面；② macOS 支持 fat Mach-O ⇒ 一份 123 KB 的文件同时服务 arm64 与
# x86_64；③ macOS 上「装了 Xcode CLT」并非必然，而缺产物时 0x10A 是**静默降级**（只是玩家看不见光标
# 被挪过去，不报错）⇒ 预置是唯一能让"没编译器的机器"也拿到这条观感的办法。Windows 维持按需构建。
!/native/host-input/prebuilds/

*exe_extracted

/patch/BIN
/patch/AGF

/patch-*.zip
/v*.zip

.DS_Store

__pycache__/

res/fonts/SarasaGothicSC
res/fonts/SarasaGothicJ

/engine/engine.cpp

/src-reflow

/tools/fade_probe_proj
/tools/age_free_fade_probe.cs

/PENDING.patch

# emulator 的运行时状态（诊断日志/轨迹）在 .tmp 下，不进版本库。
# ★玩家数据（SYS4REG.INI / SAVE.DAT）**不在仓库里**：走系统存档目录 + overlay
#   （%LOCALAPPDATA%\Eushully\<game>\ 与同级的 <game>.overlay\，见 src/arch/systemPaths.ts），
#   所以这里不再需要忽略 app/amayui-emulator/SAVE/。
/app/amayui-emulator/.tmp/

# emulator 的**本机私有运行开关**（每个测试者可能不同 ⇒ 不入库；示例见 emulator.config.example.json）
/emulator.config.json
