# CONTEXT —— 工程现状快照（2026-09-24 · 审计实施轮之后）

> **本文件只记「状态 / 结论 / 待办」**；流程、纪律、schema、命令清单一律**不在这里重述** —— 见 §1 的落点。
> 凡推断都写明"候选/未证"；凡结论都带可复算的落点（文件:行 / 命令）。
> 所有数字都是**本轮实测**（2026-09-24）；下一轮开工前先按 §6 重跑一遍基线。

## 1. 该读哪里（本文件不重述这些）

| 你要做什么 | 读它（权威在你/技能那边，本文件不复制） |
|---|---|
| 开票 / 改票 / 收尾 | 技能 **`amayui-ticket-ledger`**（存储模型、状态流转、收尾三连、并行纪律） |
| 读引擎 / 落三层数据层 | 技能 **`amayui-engine-analysis`**（第一层 functions/fields、第二层 capabilities、第三层 scripts） |
| 分析 `src/*.txt` 脚本 | 技能 **`amayui-script-analysis`**（先读文档 → 读脚本 → 落 `analysis/scripts.json` → `build-scripts.mjs` → `docs-new/05-scripts/`） |
| 起/驱动调试实例（agent 自足） | 技能 **`amayui-remote-debug`**（`--attach-headless` + `debug-query` 的 move/click/wheel/key/capture/frame/global） |
| 文档模型（真源/生成物/沿革） | `docs-new/00-overview/authority.md` 附录 **A1–A6** |
| 证据等级 **E0–E4** | `docs-new/03-engine/engine-capabilities.md` 头部 + 台账 schema 的 `evidenceEnum` |
| 其它 | `amayui-mnemonic-rename` / `amayui-ui-text-render` / `amayui-script-translate` / `amayui-script-update` / `batch-task-runner` |

**长期用户要求（仍未撤销）**：**不要 `git add` 任何东西**（本会话同样全程未 `git add`）。

## 2. 本轮（2026-09-24）做完的事

### 2.1 审计 48 条 P1：**全部有唯一处置**

| 处置 | 条数 | 说明 |
|---|---|---|
| 已修（带命名守卫） | 43 | 见报告 §4.8 逐条表；机器可读 = `app/amayui-emulator/.tmp/p1-fix/dispositions.json`（对账脚本 `reconcile.mjs`，输出"✅ 48 条全部有唯一处置"） |
| 经体证**推翻** | 4 | `0x2c0`/`0x2f5` 的"op2 = pan"、`0x260` 的"vPad 无消费者"、`0xfa` 的"3 个待播语音槽"（报告 §4.4 第 11/12/13 行） |
| 有据登记 `n/a` | 1 | 停靠锁 429752 挡住的两段本体（效果计时器调度 / 电影段）未建模 → why + 重开条件进台账 |

代表条目：`0xbc` 音乐开关落空路径、`0xc7` 的 `sound:Music >= 0`、`0x193`/`0x1b2` 字符串族转十进制、
`0x22f` 层 20..29 轴角旋转、`0x1f5` 队列派发三层门、`0x10c` 键位重映射（两张运行期表）、
`0x305` 总门 `(flags & 0x10001) == 0x10001`、`0x222` 3D 层区间提交（新宿主缝）、帧提交门 `frameRenderGate()`。

### 2.2 本轮新增的结论（**不在**任何技能里）

1. **滚轮在 ADV 的真相**（追查全文 `tickets/T-0168/changes.md`，探针 `.tmp/wheel-hist-probe/`）：
   - 引擎**没有**「滚轮 ⇒ HISTORY」内链；`HISTORY.BIN` 全语料唯一入口 = **`call-script 31`**（669 处/335 文件，ADV 菜单 action id 6）。
   - WndProc 把滚轮折算成掩码位 `1 << Conf(set:WheelKeyUp/Down)`（raw 141563-141606）；ADV 脚本用 `0x97` 把位绑到**离屏 1×1 热点**的 labelC：
     `SN0000.txt:94-114` 绑位 0/1/2/3/**8**/7，其中 **位 8 → `label_00002f84` → `call-script 31 // HISTORY`**（位号来源 `SYSTEM4.txt:87-97` 的 `i10c 8 c9`）。
   - 随包 `WheelKeyUp=3 / WheelKeyDown=1`（= ←/→）⇒ **默认滚轮上滚 = 菜单导航**（`label_00002c40` 建/展开侧边栏），真机默认同样如此。
   - 把它配成 8 再滚 ⇒ 脚本**真的进 `HISTORY.BIN`**，随后停在 **`0x1D1` 未实现**（★读体订正：`0x1D1` **不是**"回想页渲染器"，而是 `0x82` 的**孪生** ——「GDI 重画窗 `op1` 的第 `op2` 条 72B 文本项记录」，`sub_4675A0` raw 80524-80532 与 `0x82` 的 `sub_466000` raw 79319+ 逐句同形；语料唯一调用点 `src/HISTORY.txt:1314`）⇒ **画面可见性的最后一环是 `0x1D1`**（脚本命中它即 `NotImplementedOp` **硬停** ⇒ 后续帧不再跑；已由 `T-0170` 按孪生实现、`opcode-gaps.json` 记 `partial`）。★`0x1D1` 的语料调用点是"唯一**静态**调用点"—— 运行时该行会被反复命中（`tickets/T-0168/notes.md` 实测进回想后 4000 帧内命中 **280** 次），别写成"只跑一次"。
   - ④（`#textRewindWheel`）**不负责开画面**（只做回看页游标 ∓1 + `489816` + `0x100000`），且被泵的 ①路由键命中（raw 20242）挡在前面。
   - `0x84`（同一标记的另一置位端）**全语料 0 处**；`0x100000` 的语义 = "文本回调/回看泵进行中"，不是"进入回看画面"。
   - **`CALLBACK_TEXT.BIN` 这个名字的出处**（别再去资源里找）：它是引擎硬编码常量 —— `raw 4344` 的
     `aCallbackTextBi`（`.lst:427230` = `.data:0051F1F8`），exe 里 5 处 `sub_411560(Engine, 该常量)`
     （raw **20044 / 20065 / 20351 / 28852 / 28905**；.lst 0041178D/004117FE/00411E52/0041F921/0041F9ED）。
     全表与"缺失"三步实测见 `tickets/T-0168/changes.md` §五。
2. **帧循环口径差**（→ `T-0169`）：emulator 的逐字显现帧**不跑等待泵**（`src/frame/loop.ts:448` 与 `:457-464` 互斥），引擎同一轮里 20887 与 21226 都会跑到。
3. **三处票面前提已被本轮工作取代**（防重复，见 §4.3）。

### 2.3 台账 / 文档同步（截至 2026-09-24 本轮）

- `analysis/functions.json`：**588 + 4 = 592 条** —— 本轮补 `0x420310`（`0x1D1` handler）、`0x4675A0`（`0x82` 的 `sub_466000` 的孪生体）、`0x45F090`（压 72B 文本项记录，flags `0x4`）、`0x4691D0`（换行记录，flags `0x8`）；并补齐 `0x42DFC0`（`0x1AB`）的**结果码优先级**（`.STH` 覆盖 `.DAT` ⇒ 两份都失败 = 2）。
- `analysis/engine-capabilities.json`：**141 条**；本轮把 `T-0174` 的三格口径落进 `scene-pending-flag-0x400-gate`（`reads` 加 `Scene+46508`；`trigger`/`emulator.note` 写清 `sub_407E20` 的返回值、置 `[11627]`、`Scene+46516 ≡ 引擎基 +369348` 同基、present 每帧清两格）。
- `analysis/opcode-gaps.json`：**118 条**（`partial` 64 / `implemented` 28 / 有据 no-op 8 / `deferred` 18 / 缺口 143）；`0x1D1` 由 `deferred` 定 `partial`（6 条 `missing[]`）、`0x84` 与 `0x2c8` 是两个"静默项"补登。
- `analysis/opcodes.json`：`0x1D1` 语义按体写成「`0x82` 的孪生」+ 三处体证；把不具判别力的"无区域填充"护栏改成如实措辞。
- `analysis/scripts.json`：**35 条**；本轮订正 `NOVEL`/`SYSTEM4` 的 `global 3f90` 门极性（**六处**，含顺带改正的 `4fd9` 反向结论）⇒ `docs-new/05-scripts/` 重生成。
- 六份真源的 `evidence.line` 漂移已清零（本轮刷新 26 条）；指向 `src/**` 的 70 条等 B/C 波收口后刷。
- 报告 `docs-new/99-records/2026-09-impl-audit/impl-audit-2026-09.md`：§4.8 逐条处置表（幂等生成脚本 `.tmp/p1-fix/apply-section.mjs`）。
- `analysis/journal.jsonl`：**seq 45**（实施轮 + 滚轮链各一条）。
- 环境：`emulator.config.json` 的 **`audio.enabled` 已改 `false`**（按用户要求；其 `$comment` 本就写着本机应静音）。

## 3. 台账现状（快照，2026-09-25 **D 波收口 + 台账落库后**实测）

```
票据 175 张：✅done 154  ⬜open 12  🔜doing 7  🚫dropped 2  ⛔blocked 0
  优先级 P0 8 / P1 68 / P2 61 / P3 38
  ★`--validate` ✅ 175 张 / **0 条行号漂移警告**（工具 = `.tmp/settle/fix-lines.mjs --any`）
能力台账 **143 条**：已核验 **79** / 已建模未核验 4 / 部分 **30** / 缺失 6 / n/a 24（needsAttention 40）
缺口台账 **153 条**：**partial 83**（共 140 条 `missing[]`）/ implemented **37** / 有据 no-op 9 / deferred 19 / **unimplemented 5** / unjustified 0
  （★`partial` 是一等处置位：`missing[{what,ticket,raw}]` + 两条棘轮，见 `tickets/T-0149`。
   ★partial 与缺口数**上升**是好事：抄写员把原先藏在 `implemented`/`deferred` 里的"其实还缺东西"
   如实拆出来并逐条挂了票号与 raw —— 这是"未实现 0"不再掩盖缺口的关键。
   ★`unimplemented` 从 0 变 5 = `T-0111` ① 把 5 条**语料 0 处**的未注册指令如实登记（命中即硬报错，非静默））
第一层：`functions.json` **619 条** / `fields.json` **391 条**（D 波两票台账落库：`T-0156` +9/+7、
  `T-0164` +3/+5；`fields.json` 按 scope 分组、组内 offset 升序，见 `.tmp/settle/check-fields-order.mjs`）
脚本台账 **36 条**：已分析 10 / 部分 24 / 仅登记 2（src 分母 941；+1 = `T-0176` 登记 `SETL2DMOC`）
死写基线 13 条（T-0160 给 `Engine.l2dMotionCache` 接了真读者后 14→13，只收缩）
六份真源的 `evidence.line` 漂移 = 0；`app/amayui-emulator/**` 的 = 0
★收口实测：生成物 `--check` 全 ✓ / 守卫电池 **214/214** + `native-tap`/`op-222-scene-commit` 10/10 /
  `tsc -p tsconfig{,.test,.control,.electron}.json --noEmit` **四个 exit 0**
```

其它状态：`test:all` 的 3 条基线红 = **T-0146**（见 §6）；本机 `npm run typecheck` 跑不了（§7），改用 `node node_modules/typescript/bin/tsc`。

## 4. 未完成票据 **19 张（7 doing + 12 open）** = 现状全貌

★**权威清单 = 生成物** `tickets/README.md`（看板）与 `node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --list|--stats`；本节下面三小节是**A 波时的分类**，D 波收口后绝大多数已 `done`，**只当历史分类读**（逐票最新状态一律以上面两条命令为准）。
（A 波时的逐票摘要：`app/amayui-emulator/.tmp/p1-fix/{ticket-survey.md,ticket-groups.mjs}`）

★**D 波收口后仍未完成的 19 张**（`--stats` 实测：done 154 / doing 7 / open 12 / dropped 2 / blocked 0）：
- **doing 7**：`T-0148`（全指令核对主票）、`T-0157`（帧循环批 12 条：P1 已修、P2/P3 在排队）、`T-0154`（场景/转场批：实现已交回并落库，票面**有意留 doing**，剩 E4 类验收）、`T-0158`（面板/菜单/输入批：同上）、`T-0169`（逐字显现帧等待泵口径差，判据已重定待实现）、`T-0091`（转场渲染：只剩 `[4]` 非 create-texture 槽）、`T-0067`（存档页闪一帧：等干净窗口）
- **open 12**：`T-0122`、`T-0142`（工具类）、`T-0168`（滚轮上滚链，研究已收口待排）、`T-0175`（跨文件交接清单）、`T-0019`（拆 msgwin）、`T-0051`、`T-0088`、`T-0118`（外部条件）、`T-0103`（用户点名最后做）、`T-0107`（INFOEN 的 L2D 资产 id 表）、`T-0146`（3 条基线红）、`T-0173`（删两个零读者字段）

### 4.1 审计修复批 17 张（现在：**全部 done**，`T-0157` 除外）——P1 10 / P2 7

`T-0148`（主票，doing）、`T-0149`（P1 结构性：缺口台账加 `partial` 处置位）、`T-0150`（P2 结构性：死写闸门扩到 Engine/SceneState）、
`T-0151` 消息窗/文本（P1，doing）、`T-0152` 音频（P1，doing）、`T-0153` 纹理槽（P1，doing）、`T-0154` 场景/转场（P2，doing）、
`T-0155` 绘制项（P2，open）、`T-0156` 控制流/装载（P1，open）、`T-0157` 帧循环（P1，doing）、`T-0158` 面板/输入（P2，doing）、
`T-0159` 存档槽（P2，open）、`T-0160` Live2D（P2，open）、`T-0161` 字段/配置（P1，doing）、`T-0162` 操作数/内存（P1，open）、
`T-0164` 其余指令（P2，open）、`T-0165` 字符串族根因（P1，doing）。

### 4.2 A 波期间新开的票（4 张，状态已更新）

`T-0168` 滚轮上滚链（位 3 冲突 / `sub_411590` 未建模 / `CALLBACK_TEXT.BIN` 缺失 / `0x1D1` 待办，P2，**open —— 研究已收口，实现待排**）、
`T-0169` 逐字显现帧不跑等待泵（P2，**doing —— 判据已重定，等 B1 放开 `src/vm/engine.ts`**）、
`T-0170` **实现 `0x1D1`（`sub_4675A0` 孪生，raw 80313-81522；语料唯一静态调用点 `src/HISTORY.txt:1314`）**（P1，✅**done**：13/13 守卫含 E3 实测回想画面出 3 行真实序章正文）、
`T-0171` 滚轮「当按键」模式开时位号越界落到累加器（P2，✅**done**：模式开只在 `bit >= 0` 时 `|= 1 << (bit & 31)` 然后一律 return；横滚默认 `-1` ⇒ ADV 里完全无效）、
`T-0172` `docs-new/03-engine/rendering.md` 行号整表逐行核 + B7-B 两向棘轮（P3，✅**done**）、
`T-0174` 图形池旗标 `[11627]`/`[11629]` 基址与三格语义核对（P3，✅**done** —— 结论已落 `engine-capabilities.json` 的 `scene-pending-flag-0x400-gate`）。

### 4.3 老票 15 张（doing 3 / open 12）——P2 5 / P3 10，其中 **3 处前提已被取代（已于 2026-09-24 回写）**

| 票 | 级别 | 现状 / 处置建议 |
|---|---|---|
| `T-0109` 阅读链路清尾 | P2 | ✅**已处置（2026-09-24）**：整票 `dropped`（`droppedWhy` 已写、`links.tickets` 已接 `T-0168`/`T-0166`）—— ① 页表/游标 `T-0095`、滚轮消费者 `T-0167`、滚轮**输入**本轮已接、整链转 `T-0168`、`0x84` 语料 0 处；② `text-layout-wrap-ruby` 已由 `T-0166` 同步为 `modeled-verified`+7 守卫 |
| `T-0111` B7-C | P3 | ✅**判据已重定（2026-09-24）**：③（`0x1F5`→`sub_40FB60`）**已由 `T-0157` 完成**、本票不再承接；② `0x308` 处置定为「**有据 no-op 登记**」（体 = `sub_426B20` raw 12579-12618 = `RegisterTouchWindow`/`UnregisterTouchWindow` + 写 `_this[1954]`，3 写 0 读 ⇒ 改 `stubs.ts` 的 unhandled 桩为显式登记 + 补 `opcode-table.md` 缺行）；剩余 = 0 语料 8 条、`0x1D3` 锚点、三表全量对账 |
| `T-0146` verify 3 条红 | P3 | ✅**已注明（2026-09-24）**：why 开头现在写明「这就是 `npm run verify` 的基线红（`CONTEXT.md` §6），一律以**不新增红**判绿，不算新红、不得重复立项」 |
| `T-0067` 存档页闪一帧 | P2 doing | **阻塞**：需要一次干净窗口/用户 trace（`shot` 与用户实例共用 logs/overlay） |
| `T-0091` 转场渲染剩 4 项 | P3 doing | 只剩 `[4]` 非 create-texture 槽 + E4 可达路径 |
| `T-0103` 章节切换演出不一致 | P3 | ★用户明确"**最后再处理**" |
| `T-0019` 拆 msgwin 两个大文件 | P3 | **与 `T-0151` 同文件冲突** ⇒ 必须排在 T-0151 之后 |
| `T-0122` / `T-0142` | P2 | 调试器：内存快照/恢复；Electron 调试通道收敛到 VM 外层桥 |
| `T-0088` / `T-0118` / `T-0051` | P3 | 需外部条件：真人点选 AGERC 对话框 / Intel Mac（Rosetta）跑 x86_64 slice / E4 真机核验清单 5 项 |
| `T-0108` / `T-0112` / `T-0145` | P3,P3,P2 | 文档类：数据层沿革话术 146 处、B7-B 逐格核 raw 5 项、`global 3f90` 门极性写反等三处 |

## 5. 后续计划

### 5.1 审计批的剩余账：388 条 P2/P3（`app/amayui-emulator/.tmp/p1-fix/p23-worklist2.md`，逐条带锚点）

| 票 | P2/P3 | 票 | P2/P3 | 票 | P2/P3 |
|---|---|---|---|---|---|
| T-0151 消息窗/文本 | 28 / 47 | T-0161 字段/配置 | 8 / 10 | T-0159 存档槽 | 7 / 7 |
| T-0153 纹理槽 | 15 / 10 | T-0156 控制流/装载 | 7 / 28 | T-0167 能力缺口 | 6 / 0 |
| T-0152 音频 | 14 / 25 | T-0163 桩/native | 7 / 22 | T-0164 其余指令 | 5 / 21 |
| T-0160 Live2D | 14 / 5 | T-0158 面板/输入 | 9 / 18 | T-0162 操作数/内存 | 3 / 3 |
| T-0155 绘制项 | 12 / 14 | T-0154 场景/转场 | 10 / 16 | T-0157 帧循环 | 1 / 0 |
| T-0166 能力台账 | 12 / 4 | | | | |

### 5.2 执行顺序（用户认可的框架；★下面每条的"进度"是 2026-09-24 本轮更新）

1. ✅**先防重复**：`T-0109` 整票 `dropped`（droppedWhy + 回链 `T-0168`/`T-0166`）、`T-0111` 判据重定（③ 归 `T-0157`、② 定为「有据 no-op 登记」）、`T-0146` why 开头注明它就是基线红；
2. ✅**两个结构件已落地**：`T-0149`（缺口台账 `partial` 一等处置位 + 两条棘轮）→ 缺口台账现 **118 条 / partial 64 / 缺口 143**；`T-0150`（死写闸门扩到 Engine/SceneState）→ 基线 14 条，逐条带 `reason`+`tickets`；
3. ✅**`0x1D1` 已完成**（`T-0170` done）：按体实现成 `0x82` 的**孪生**（不是"回想页渲染器"）+ 13 例守卫含 E3 实测（回想画面真的出 3 行序章正文）；`functions.json` 补了 4 条（`0x420310`/`0x4675A0`/`0x45F090`/`0x4691D0`）；
4. **P2 优先，一簇一簇收** —— 顺序与进度：
   - **B 波（6 单元）**：`T-0151` ✅ / `T-0153` ✅ / `T-0152` ✅ / `T-0160` ✅ / `T-0155` ✅ / `T-0159` ✅ —— 全部 `done` 且台账已落库（抄写员 A–C）；
   - **C 波（4 单元）**：`T-0161` ✅ / `T-0158` ✅ / `T-0163` ✅ / `T-0162` ✅ —— 同上（抄写员 D/E）；
   - **D 波（4 单元）**：`T-0154` ✅（已落库）、`T-0166` ✅（眨眼，已落库）、`T-0156` ✅（实现+取证已交回：20 例守卫、红→绿 7/20→20/20；**台账正由 Scribe F 回体核实后落库**）、`T-0164`（其余指令）**仍在飞**；
   - **本轮新开的收口票**：`T-0176`（`input.test.ts` 的 TITLE 前提补全 + `SETL2DMOC` 台账登记，✅ done）；
   - **下一批（文件基本已空）**：`T-0167`(6)、`T-0157`(1)、`T-0169`、`T-0173`（删 `Engine.callFlag`/`callLink`）、`T-0019`（拆 `msgwin` 文件）、`T-0175`（B/C 波跨文件交接清单，已做 ④/⑤后半/⑪/⑫）；
5. **P3 收尾**（约 230 条）同法 —— 注意 P3 里绝大多数与 P2 同簇同文件，随簇一起做；文档/沿革类（`T-0108`✅ / `T-0112`✅ / `T-0145`✅）已收口，工具类（`T-0122`/`T-0142`）穿插（`T-0142` 需 Electron 宿主，与用户实例冲突 ⇒ 等实例空闲或用户授权）；
6. 需外部条件的（`T-0067`/`T-0088`/`T-0118`/`T-0051`）与"最后做"的 `T-0103` 不进主序列。

### 5.3 每簇的作业纪律（上一轮验证过、继续保持）

- **一个文件同一时刻只归一个执行者**；跨文件改动先串行化；
- 每个修复单元 = **命名守卫 + 红→绿证据**（先跑出红再改），**不许删/放宽既有断言**；
- 锚点是 ABI：代码被改名/搬家导致票据 `evidence.anchor` 消失 ⇒ **只改指不删**，并跑 `tickets.js --validate`；
- 引擎层结论落 `functions/fields.json` + `engine-capabilities.json`；**脚本层结论落 `analysis/scripts.json` → `build-scripts.mjs` → `docs-new/05-scripts/`**（本轮踩过：只写代码注释 = 违规）；
- 收尾三连（各技能里）：`build-*.mjs` 重生成 + 四份 `--validate` + 对应守卫测试。

### 5.4 第 C 波候选与文件归属（2026-09-24 预映射；用 token 扫描 §T-01xx 各节得出）

> 目的：B 波一收口就能按这张表**立刻**开下一波，省掉每轮重新分析锚点。**"空闲"= 此刻 B1~B6 与 `T-0108` 的清单里都没有它**（随它们交回会变，开波前请复核一次）。

| 簇（P2/P3） | 文件分布（命中数） | 可开性 |
|---|---|---|
| `T-0158` 面板/菜单/输入（9/18） | `src/vm/input.ts`(6)、`handlers/input.ts`(6)、`handlers/menu.ts`(4)、`handlers/panel.ts`(4)、`interpreter.ts`(2)、`handlers/region-hittest.ts`(2)、`route.ts`(2)、**`engine.ts`(1)** | ✅ 除 `engine.ts` 外全空闲（engine.ts 归 B1 ⇒ 开波时禁动、那 1 行写进耦合节） |
| `T-0161` 引擎字段/配置读写（8/10） | `handlers/engine-fields.ts`(12)、`handlers/config-read.ts`(3)、`configRegistry.ts`(1)、`engineConfig.ts`(1)、`engineFieldIds.ts`(1) | ✅ **全部空闲** |
| `T-0156` 控制流/帧管理/装载（7/28） | `handlers/control.ts`(22)、`handlers/frame.ts`(11)、**`engine.ts`(2)** | ✅ 除 `engine.ts` 外全空闲 |
| `T-0162` 操作数与内存原语（3/3） | `src/vm/operand.ts`(5)、`handlers/memory.ts`(1) | ✅ **全部空闲** |
| `T-0163` 桩/native（7/22） | `handlers/stubs.ts`(**18**)、`handlers/agerc.ts`(9)、**`native.ts`(2 — B2 的)** | ⚠ 主战场 `stubs.ts` 空闲 ⇒ ★**它就是把 `T-0111 ②`（`0x308` 改"有据 no-op"）一并做掉的最佳载体**（只剩 `operandPlan.ts` 的注释归 B1、台账归 `T-0108`） |
| `T-0154` 场景状态/转场/混合（10/16） | `src/renderer/scene/ops.ts`(**21**)、`transition.ts`(3)、`blend.ts`(1)、`state.ts`(1) | ⛔ **全在 `src/renderer/**`** ⇒ 等 B4（Live2D 单元有权用 renderer） |
| `T-0164` 跨模块其余指令（5/21） | `handlers/gfx-misc.ts`(10)、`handlers/arithmetic.ts`(6)、`handlers/gfx-cg.ts`(1)、`advState.ts`(1)、`engineFieldIds.ts`(1)、renderer×3、`engine.ts`(2) | ⚠ 大半空闲、少数落 renderer/engine.ts |
| `T-0167` 能力台账缺口（6/0） | `engine.ts`(2)、`engineFieldIds.ts`(1)、renderer×3 | ⛔ 等 B1/B4（且多为台账类，与 `T-0108` 同域） |

## 6. 验证基线与"什么算绿"（2026-09-24 实测）

```
★本机命令（`npm run` 系列因缺 node_modules/.bin 跑不了，见 §7）：
  typecheck      → cd app/amayui-emulator && node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit   （同理 control / electron / test）
  单文件测试      → cd app/amayui-emulator && node --import tsx --test test/<文件>.test.ts        （`npx tsx` 时好时坏）
  全量           → cd app/amayui-emulator && node --import tsx test/run.ts all
  ★生成物重跑：node scripts/build-{opcode-table,capabilities,scripts,opcode-gaps,doc-index,status,tickets}.mjs
    （后四份中 doc-index/status/tickets 分别管 00-overview/index.md、00-overview/status.md、tickets/README.md）
  ★四份台账 --validate（路径别写错 —— 两个在技能目录里，不在 scripts/）：
       node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --validate
       node .agents/skills/amayui-engine-analysis/scripts/capabilities.js --root . --validate
       node .agents/skills/amayui-engine-analysis/scripts/scripts.js --root . --validate
       node scripts/build-opcode-gaps.mjs --check

【★D 波收口实测（2026-09-25，可信）—— "哪些红不是我的"看这里】
  typecheck（tsconfig/tsconfig.test/tsconfig.control/tsconfig.electron） → 四个都 exit 0
  test:all      → 222 文件 / **tests 1544 / pass 1539 / fail 3 / skipped 2**
     ★**3 条红全部 = 既有基线**（票 = `T-0146`），**没有第 4 条**：
       · `engine-slot`（`★E4：本机真槽全部解出…`，SAVE70/71 `storedDwords`）
       · `save-slot`（`E4：真存档槽的头 → 0x1A0 的六个 u16…`，真槽 `format`）
       · `scene-report`（可绘制项 24 ≤ 缺纹理项 26）
  ★本轮**修掉的三条**（都不是基线）：
     ① `doc-model`「index.md 是最新的」—— 我重生成 `docs-new/05-scripts/`（新增 `SETL2DMOC`）后忘了
        `node scripts/build-doc-index.mjs` ⇒ 补跑后 **10/10**。★教训：**动了任何 `docs-new/` 页面就补跑 doc-index**。
     ② `test/input.test.ts` 的 TITLE 例抛 L2D `0x34e` —— `T-0160` 的抛错暴露「单脚本跑 TITLE 缺
        `SETL2DMOC` 前置」⇒ `T-0176` 修（**测试前提不全，非 `src/` 缺陷**）⇒ **14/14**。
     ③ `host-registry` 的「真进程 `--idle-sec 1`」—— 此前被两次独立判成「固有 flaky」；`T-0177` 定位到
        **根因** = `src/web/host.ts` 先 `publishRecord()`（raw 941，同步落盘）再打横幅（raw 990），
        而测试等文件、断言管道累积的 `out` ⇒ 去掉未被保证的时序假设（等横幅）⇒ **14/14 ×3**、全量里消失。
  ★**守卫电池（29 个文件一次跑）→ 214/214 绿**：doc-model / capability-ledger / script-ledger / ticket-ledger /
   opcode-gaps / opcode-arity / opcode-operands / op-underun-fixups / operand-plan / opcode-json-sync /
   registry-classification / texture-create-decision / texture-lifecycle / arch-copy-slot-sth-only /
   op-string-coercion / op-22a-22f-scene-xform / op-22a-22f-scene-world / scene-t0154-scene-state /
   scene-t0154-mesh-transition / live2d-blink / t0156-control-frame / t0164-misc-batch / input /
   live2d-t0160 / live2d-enabled-flag / organization / harness-convergence / no-dead-writes / op-23f-slot-size
   ＋ 台账落库后再跑一轮 **32 文件 239/239 绿**（追加 `op-a2-a3` / `op-1f5-dequeue` / `op-132-134-queue`）
   ＋ `native-tap` / `op-222-scene-commit`（`BRIDGE_METHODS` 两个守卫）**10/10**（`T-0164` 新增桥方法 `hasSlotTexture`）
  ★四份台账 `--validate`：tickets **175 张 / 0 条警告**、capabilities **143**、scripts **36**、
   opcode-gaps **153**（partial 83 / implemented 37 / 有据 no-op 9 / deferred 19 / unimplemented 5；missing 140）
  ★生成物：`opcode-table`/`opcode-gaps`/`doc-index`/`status`/`tickets` 五个支持 `--check`，全 ✓；
   `capabilities`/`scripts` 两个**不支持 `--check`**（无参数即重生成、幂等，跑完即最新）。
  ★死写基线 **13**（`deadWrites.ts` 无新增；棘轮基线 `harness-convergence.baseline.json` 未被任何人修改）

【A 波收口时的基线（2026-09-24，留档对照）】
  test:all      → tests 1310 / pass 1305 / fail 3 / skipped 2（那 3 条 = 上面同一个 `T-0146` 基线）
  四份台账 --validate → tickets 172 ✅ / capabilities 141 ✅ / scripts 35 ✅ / opcode-gaps 118（缺口 143 / partial 64）✅


## 7. 环境事实与操作要点（本机）

| 项 | 值 |
|---|---|
| 资源根 | `install/`（`emulator.config.json` 无 `resources.path` ⇒ 默认）；真机 base = `C:\Users\liaoh\AppData\Local\Eushully\天結いキャッスルマイスター\`（overlay 同名前缀 `.overlay\`） |
| 音频 | **默认静音**（`emulator.config.json` 的 `audio.enabled=false`；单次覆盖用 `AMAYUI_AUDIO_ENABLED=1`） |
| 起调试实例（agent 自足、静音） | `cd app/amayui-emulator && AMAYUI_AUDIO_ENABLED=0 node --import tsx src/web/host.ts --instance <id> --port 0 --attach-headless --idle-sec 0` |
| 驱动 | `POST http://127.0.0.1:3080/dsh-emulator/<id>/api/debug-query`，body `{"args":["frame"|"capture"|"click x y"|"wheel 120"|"key 13"|"global <下标>"]}` |
| 本轮验证过的坐标 | TITLE 的 Game Start `(1180,372)`；GAMESTART 的 ゲーム開始 `(811,605)`（虚拟 1280×720）；右键 = 打开侧边栏（→ `CHARMEDIT.BIN` 层） |
| Web 产物 | 改 `src/vm/**`、`src/renderer/**` 后必须 `npm run build:electron`（重建 `dist/web/renderer.js`）并**刷新页面**才生效 |
| 日志 / 临时产物 | `.tmp/amayui-emulator.log`、`.tmp/instances/<id>/…`；**`shot`/`record`/用户实例共用 log+overlay** ⇒ 同时只跑一个 |
| ★跑 typecheck / 测试 | `app/amayui-emulator/node_modules` 里**没有 `.bin` 目录** ⇒ `npm run typecheck`（以及任何走 `node_modules/.bin` 的 npm 脚本）在本机 shell **跑不了**，报 `'tsc' is not recognized`。**改用绝对入口**：`cd app/amayui-emulator && node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit`（另有 `tsconfig.test.json` / `tsconfig.control.json` / `tsconfig.electron.json`）；测试仍用 `npx tsx --test test/<文件>.test.ts` 或 `node --import tsx test/run.ts all` |
| 已知坑 | 渲染进程**没有** `process.env`（`renderer/**` 里读它会把窗口打白）；`tools/shot.cjs --load` 会回写槽、载入列表记住上次光标（用 `--row`）；**本资源树没有 `CALLBACK_TEXT.BIN`** —— `install/SYS4INI.BIN` 21109 条 / 565 个 `.BIN` 与 `raw-parts/DATA1/` 抽取双向确认，`sub_455000` 找不到返回 **-1**、`sub_40FC90(Engine,-1)` 体首早退 ⇒ 该跳在本树下是 **no-op**（换用带该文件的版本时另当别论） |

## 8. 未决与下一步（自上而下的清单；2026-09-24 **第 39 轮**更新）

> **已交回并全部落库的波次**：A 波 9 单元、B 波 6 单元、C 波 4 单元、**D 波 4 单元全部**（`T-0156`/`T-0154`/`T-0166`/`T-0164`）、`T-0108`/`T-0111①`，以及 **7 个"台账抄写员"（A–G）** 的计划 —— ★**"已交回单元的台账待应用"队列现在是空的**（`T-0156` 由 Scribe F、`T-0164` 由 Scribe G 逐条回体核实后落库；两者都交出了「报告说 → 体里是」的差异清单）。
> **在跑**：**没有代码单元在跑**（本会话的 subagent 全部已结算）。下一步是开新波或按用户指令收口。

1. **本会话最后一段（2026-09-25）做完的事**：
   a. ✅ `T-0156` 台账落库（Scribe F）+ `T-0164` 台账落库（Scribe G 出计划、主 agent 应用）⇒ 函数 **619** / 字段 **391** / 能力 **143** / 缺口 **153**。
   b. ✅ 修掉 `analysis/opcodes.json` 里 `0x7C` 的错误判据（`51848` 是虚拟显示器 A 鼠标游标、`387940` 是恒假门）⇒ 重生成 `opcode-table.md`。
   c. ✅ 订正 `tickets/T-0164/changes-c164.md` 里 `dword_55052C` 的错误结论（它不是死全局；raw 45802+ 一片读点）。
   d. ✅ `T-0156` 置 `done`、开并关 `T-0176`（`input.test.ts` 的 TITLE 前提）与 `T-0177`（`host-registry` 的读-刷竞态）⇒ 看板 **175 张（done 154 / doing 7 / open 12 / dropped 2）**。
   e. ✅ `test:all` 的**红集合 = 纯基线**（3 条 `T-0146`）：1544 例 / 1539 过 / 3 红 / 2 skip。
2. **下一波可开的票**：`T-0157`（帧循环批 12 条：P1 已修、P2/P3 待排）、`T-0167`（能力台账缺口 6 条）、`T-0168`（滚轮上滚链，研究已收口）、`T-0173`（删两个零读者字段）、`T-0019`（拆 `handlers/msgwin.ts`）、`T-0175`（跨文件交接清单，已做 ④/⑤后半/⑪/⑫）、`T-0107`（INFOEN 的 L2D 资产 id 表真源未定位）。★开波前先跑一次 `tickets.js --list` 核状态（本节 §4 的三小节是 A 波分类，只作历史读）。
3. **`T-0148`（主票）仍未关**：它是"全指令实现 × 引擎逐条核对"的总票，剩余 = `T-0167` 承接的那批 + 新发现；关它前应把"还剩哪些没核"写清。
4. **需外部条件（不进主序列）**：`T-0067`（一次干净窗口）、`T-0088`（真人点选 AGERC）、`T-0118`（Intel Mac/Rosetta）、`T-0051`（E4 真机核验 5 项）；`T-0142`（Electron 宿主）与用户实例冲突 ⇒ 等实例空闲或授权；用户点名"最后做"的 `T-0103`。
5. **需用户裁决**：`T-0108` 收尾第 2 项我裁为"保留"（落 `tickets/T-0108/notes.md`）—— 若不同意说一声我再清。
6. **一个待开票的观察**（`T-0111` ① 提出，我认可）：全量对账后"文档 vs 运行时口径不一致"已归零，剩下的是 **159~167 条语料 0 处的 age-shared 指令该不该逐条读体**（其中多数语义全空）⇒ 更像一条新的 `analysis` 票。
7. **在飞期间的越界观察（已接受，不需动作）**：`T-0164` 为实现 `0x23d` 的宿主缝同时改了 `src/renderer/headlessScene.ts`、`src/vm/native.ts`、`src/vm/nativeTap.ts`（`hasSlotTexture` 可选缝）—— 三个文件都无其它 owner、且"宿主缝必须两宿主同改"是硬约束 ⇒ **接受**；若将来要严格化，把它写成单元范围模板即可。
8. **本会话新增的可复用结论**（写进 §9，避免重复踩）：
   ① `TITLE` 单脚本运行必须补 `SETL2DMOC` 的"装 TITLE.MOC 进实例槽 0"前置（`T-0176`）；
   ② `jcc` 是 `jcc cond 真支 假支`、`-1` = 落下句 ⇒ `a9d0 == 0` 走 Live2D、`!= 0` 走静态回落；
   ③ 重生成任何 `docs-new/` 页面后**都要**跑 `build-doc-index.mjs`，改台账计数后**还要**跑 `build-status.mjs`；
   ④ 写台账文本**别用 ASCII 双引号**（用「」）—— 本轮我自己踩了两次（`opcodes.json` 与 ticket JSON），两次都被"写盘前 `JSON.parse`"当场拦下；
   ⑤ 子 agent 报"flaky"只是症状归类：`T-0177` 证明读一次体就能把它变成有根的修复。



## 9. 执行日志（滚动更新；每波做完就补一行）

| 时间 | 动作 | 结果 |
|---|---|---|
| 2026-09-24 | 记录 `CALLBACK_TEXT.BIN` 名字出处 | `tickets/T-0168/changes.md` §五（引擎硬编码常量 raw 4344 + 5 个 `sub_411560` 调用点 20044/20065/20351/28852/28905）+ 本文件 §2.2 |
| 2026-09-24 | 设目标 `goal-729989be-0461-4b33-a86b-2f1e6d5ceb14`（40 轮上限） | 按 §5.2 顺序推进 |
| 2026-09-24 | §5.2-1 三处票面回写（主 agent 亲自做） | `T-0109` → dropped（droppedWhy + 回链）；`T-0111` → 判据重定（③ 归 T-0157、② = 有据 no-op 登记）；`T-0146` → 注明基线红；`tickets.js --validate` 绿（168 张 / 24 条行号漂移警告） |
| 2026-09-24 | 开单 `T-0170`（`0x1D1`，P1，doing） | evidence = raw 80313 `sub_4675A0` + `src/HISTORY.txt:1314` 的 5 操作数；看板已重建（168 张：done 131 / doing 13 / open 22 / dropped 2） |
| 2026-09-24 | 第 A 波 4 个 subagent 并行 | `T-0149`（缺口台账 `partial`）、`T-0150`（死写闸门扩面）、`T-0168` 残留研究（只读）、`T-0170`（`0x1D1` 实现）——文件范围互斥：`analysis/opcode-gaps.json` / `src/tools/**` / 只读 / `src/vm/**`+`opcodes.json`+`capabilities.json`+`scripts.json` |
| 2026-09-24 | 复核自己的票面回写 | `test/ticket-ledger.test.ts` 6/6 绿（含证据锚点棘轮与看板同步）⇒ 三处回写合规 |
| 2026-09-24 | **裁决前提冲突（T-0170）** | `test/op-1d0-1d1-text-metrics.test.ts` 明文钉死「`0x1D1` 必须未注册 + 台账 `deferred`」（62-71、111 行），与 T-0170 判据对立 ⇒ 判为**前提被取代**，授权 A4 做最小 retarget（保留 handler/argc/note 四个锚点与其余断言；`disposition ∈ {implemented,partial}` 一条待我在 T-0149 落地后结算） |
| 2026-09-24 | 第 A 波增补 1 个 subagent（第 5 个） | `T-0151` 的 **`src/text/**` 子集**（6 行：P2 `lazy-gdi-font-set`、P2/P3 `text-font-rebuild-cascade` 台账锚点、P3 `0x2c7`/`0x2c8`×2 sjis 越界与 256B 缓冲、P3 `0x204` BlankExtentMode 量宽）—— `src/text/**` 与 `src/vm/**` 不相交，可与 A4 并行；台账修正交回主 agent 应用 |
| 2026-09-24 | 第 A 波增补第 6 个 subagent | `T-0153` 的 **`src/renderer/**` + `src/arch/**` 子集**（7 行：P3 `0x1f8/0x208/0x20b`（DividedTexture 子纹理 vector、同步 getter、矩形夹取）、P2 `0x23f`（惰性对象槽尺寸）、P3 `0x1fa`（释放时 `slotSizes` 不撤）、P2 `0x1ab`（`DeleteFileA` 只删 overlay）、P2 `0x19e`（overlay 缺失时静默 no-op））—— 票内其余 20+ 行在 `src/vm/handlers/gfx-texture.ts`，归第 B 波 |
| 2026-09-24 | 第 A 波增补第 7 个 subagent | `T-0112`（P3 文档逐格核 5 项）—— 目标是**手写**叙述文档（`adv-text-rendering.md`/`copyright-effect.md`/`engine-reset-mainloop.md`）与 `analysis/{functions,fields}.json`，全部空闲；**不得**碰三份生成物与被占用的 ledger。★`T-0108`（数据层沿革话术 146 处）经核查**不可拆**：6 份真源里 4 份（opcodes 49 / capabilities 37 / opcode-gaps 31 / scripts 1）归 A1/A4，且判据②要求六份全改完再跑四个生成器 ⇒ 留到第 B 波之后 |
| 2026-09-24 | 评估两个工具票 ⇒ **本轮不开**（记下理由，防下次白撞） | `T-0142`：判据④要求 Electron 宿主下跑端到端（截图 + `[main]` 日志），而 **`shot`/`record`/用户实例共用 log+overlay**（本文件 §7）⇒ 与用户当前在跑的 web 实例冲突，须等实例空闲或用户授权；`T-0122`：引擎态快照要落在 `src/renderer/**`（A6 占用）与 `src/vm/**`（A4 占用）⇒ 第 B 波之后 |
| 2026-09-24 | 主 agent **抽检 T-0151 的两行前提**（read-only，为第 B 波去风险） | ① `0x71`（`sub_41ED80` raw 28419-28462）：三条出口**都**写 `_this[122496] = 0`（raw **28444/28449/28457**，与审计给的 raw 行号逐字相同）、跳读支确实 `174801 \|= 0x8000000` + `122455 = 1`、raw 28431 = `sub_48FFB0` 冲刷点、raw 28428 写指令长度槽 ⇒ **审计行准确**。② `0x196`（`sub_41FC20` raw 29032+）：arity 槽写 **7 = 2\*3+1 ⇒ argc 3**（op1 = int，op2/op3 = **字符串**经 `sub_41B640`），第 4 实参确实是 `Engine[97055]`（raw 29078/29085/29091 三处 `v19 = *(_DWORD *)(_this + 388220)`）**不是操作数** ⇒ **审计行准确**。③ 顺带得到可复用事实：`sub_41B640(Engine,k)` = 读**字符串**操作数；0x71 里 `Engine[97055]` 是**作为 `sub_45EC60` 的第 3 实参传入**（raw 28427），而台账 `0x1D0` 条把它写成"受 `Engine[97055]` 门控" ⇒ **措辞存疑，交第 B 波核**。④ 另：raw 29054 写作 `_this + 120*frame + 383220` = **同一个 arity 槽**（120\*frame+383220 ≡ 4\*(30\*frame+95805)）⇒ 两种字节/下标写法等价，别当两处 |
| 2026-09-24 | 早期预警：跑 `npm run typecheck` | **1 条红**：`src/tools/deadWrites.ts(369,45) TS2554: Expected 1 arguments, but got 2.` —— 属 A2（`T-0150`）**在改中**，非最终态；已提示 A2，待其交回后复跑 |
| 2026-09-24 | 主 agent 读体复核 `0x1D1` ⇒ **推翻 T-0170 的前提** | ① handler `sub_420310`（raw 29353-29371）**只是取值转发**：arity 槽写 11（= 2*argc+1，argc 5）+ `sub_41BF50` 读 op5..op1 **顺序直传** `sub_4675A0(Engine+21324, op1..op5, &Engine[21032])`（第 7 参不是操作数 —— 解掉"6 形参 vs 5 操作数"）；`sub_41BF50` = **读操作数**（`functions.json:166` `readIntOperand_41BF50`，体 raw 26555+ 类型检查+抛异常）。② `sub_4675A0` 头部（raw 80524-80532）与 `0x82` 的 `sub_466000`（raw 79319+）**逐句同形**：门 `(3368-3364)/72 > a3 && a3 >= 0` + 窗对象 `+112==1` ⇒ `sub_4634B0` 专用路径（0x82 是 `sub_462040`）⇒ **真语义 = 「GDI 重画窗 op1 的第 op2 条 72B 文本项记录」，不是"回想页渲染器"**。③ ⇒ 已改 `T-0170`（title/why/acceptance/evidence/history）并**发消息叫停 A4 的页面模型**、改为"实现成 0x82 的孪生 + 差异表"；`T-0168` 的"脚本硬停在 0x1D1"结论不变。④ 教训记下：**"回想页/渲染器"这类命名是台账的推断，建票时必须先读被调体头部再写判据** |
| 2026-09-24 | **`T-0149` 交回（A1，done）** | 缺口台账加 `partial` 一阶处置位 + `missing[{what,ticket,raw}]`；生成器加 schema 校验与 §7「部分实现」（按票分组）；`test/opcode-gaps.test.ts` 加 2 条棘轮（有红→绿证据）；首批 **57 条**（改判 13 + 新增 44），条目 71 → 115。★它交回时点名 3 处**范围外**待办（两处守卫把 disposition 钉死、`status.md` 缺 `partial`） |
| 2026-09-24 | 主 agent 收 `T-0149` 的收尾单（3 件全做） | ① `scripts/build-status.mjs:63` 补 `partial` 一列 ⇒ `node scripts/build-status.mjs` 重生成、`--check` **✓ 最新**（A1 说的"陈旧"是我改台账造成的，不是它的问题）；② 两处**钉死 disposition 的守卫**按"新增一阶状态"这一前提 retarget：`op-22a-22f-scene-xform.test.ts` 的 `implemented` → `implemented\|partial`（**`OPS.has(op)` 那条反谎报断言一字未动**）、`op-327-32e-setweather-noop.test.ts` 的 `engine-internal` → `engine-internal\|partial`（**带票 + note 写清"为什么不建模"两条一字未动**）；③ 借 retarget 把 A1 迁不动的 **6 条**补迁：`0x22d/0x22f`（implemented→partial，missing 指向 `T-0154`/raw 117927-117933）+ `0x327/0x328/0x329/0x32e`（engine-internal→partial，missing 指向 `T-0093`/raw 126552-126572）。`missing[]` 内容**不是我新猜的**：取自台账这两族早已写好的缺口 + 我本轮实读的 `sub_4A6EE0`（raw 126522-126574）。⇒ 缺口台账 **partial 63 / 缺口 139**，`build-opcode-gaps.mjs` ✓、`opcode-gaps.test.ts` + 两份被 retarget 的守卫 **19/19 绿** |
| 2026-09-24 | 主 agent 读体实测 `T-0169`（帧循环口径差）⇒ 判据①已答、判据②重定 | 真实次序：跳读内层循环（20840-20886）→ **逐字计时器泵 raw 20887-20895（门 `0x40000000`，到点 `sub_45A940` 推一格）** → `raw 21158-21159 if ((v35 & 0x8000000) == 0) break;` → **显示态主泵 `sub_411900` raw 21160** → 回环；等待泵 `sub_411BC0` 在**外循环之外**（raw 21224-21228，`0x20000000` 为 0 的那一支）。★`sub_411900`（raw 20096+）一轮做四件事：输入提交 / 点击取消跳读 / **3 槽消息交付**（raw 20144-20160，= T-0151 的 `0x72` 那条的**第二处落点**）/ **执行恰好一条指令**（raw 20161-20165 `ip += 4*arity`）⇒ 对 emulator 的准确指控是「`textRevealing` 分支**一条指令都不执行**」，而**不是**「要调 `serviceAdvanceWait`」—— 照后者做会把"每轮一条"误做成"整批派发"并打红 `adv-reveal-under-throttle` |
| 2026-09-24 | 早期预警复查：`npm run typecheck` | **转绿**（A2 已修掉 `deadWrites.ts:369` 的 TS2554，三个 tsconfig 全过） |
| 2026-09-24 | 主 agent 抽检 T-0151 的 `0x72` 三槽交付行（read-only） | `sub_4BB840` 调用点普查（14 处命中）：**0x72 handler raw 28529** ✓（审计给的锚点准确）、**显示态主泵 raw 20153**（我上轮发现）、`0xFA` raw 24973、raw 29902 / 30056 / 33648、raw 19990，以及渲染侧 4 处（76900/77865/79713/80690）。⇒「3 槽消息交付」不是某条指令的私有行为，而是**被至少 5 个状态共用的例程** ⇒ 第 B 波实现 `0x72` 的 missing-consumer 时，**不许只在 `0x72` handler 里补**（否则显示态泵那一份仍然缺） |
| 2026-09-24 | 主 agent 预置 `0x1D1` 结算脚本 | `app/amayui-emulator/.tmp/t0170-settle/patch.mjs`：改判 `partial` + 写单条 `missing`（`sub_4634B0` 专用路径 + GDI 缩放度量分支，票 `T-0170` / raw `80524-80532`）+ 追加 `★T-0149` 说明；★脚本自带**前置断言**：必须保住守卫钉着的 `回看页重绘` / `sub_4675A0` / `raw 80312-81522` 三个字串，否则拒绝写入（等 A4 交回后一条命令跑完） |
| 2026-09-24 | `T-0168` 残留研究交回（A3，只读） | 4 问全部查清（全文 `tickets/T-0168/notes.md`）：① 默认 `3/1` 由 `sub_491880` **写死**（raw 111736-111739），**唯一**覆盖写者 = ini 载入器 `sub_494220`（raw 112705-112718），**游戏内设置界面改不了它**，读端零上界校验；② `0xC9` **不是 VK 而是 DirectInput 扫描码 DIK_PRIOR** —— `sub_476AA0` raw **91413** `_this[1633]=33` ⇒ `Input[1432+0xC9]=33=VK_PRIOR`（emulator 93 条表逐条一致）；③ 受限态下 `0x1D1` 确是唯一未实现 opcode，但 **`0x84` 三表皆无且台账无条目**；④ 泵里翻页只由「上滚位」触发（raw 20343-20348）、下滚位无单独判、翻页**不是 opcode**。★它**独立复核并同意**我对 `0x1D1` 的语义结论（`0x82` 体首 raw 79499-79508 与 `0x1D1` raw 80525-80532 逐句同形） |
| 2026-09-24 | 主 agent 收 A3 的移交（4 件） | ① **新开 `T-0171`**（P2）：滚轮模式开 + 位号越界时 emulator 落到累加器（`src/vm/input.ts:500-512/519-530`），而引擎那个累加支（raw 141580-141583）与模式门 `699204 & 0x90100000` 的 `if` **配对 ⇒ 模式开时不可达**（这一半我亲自读体确认）。★我**收紧了 A3 的一处结论**：位号 `>= 32` 不是「什么都不做」，而是按 x86 `shl cl` 取模（`位 & 31`）⇒ 票里把「先复核 `.lst:004B9CAB`(js) / `004B9CBE`(shl cl)」写成判据；② `tickets/T-0168/changes.md` §三 的 4 个未验证项**全部划掉**并逐条写结论；③ `docs-new/03-engine/input-system.md` 新增 **§15.1**（配置键来源 / 位号出处 / 翻页触发 / `0x84` 登记缺口，全带 raw）；④ `T-0170` 两处「只调 1 次」改成「唯一**静态**调用点 + 运行时 4000 帧内命中 280 次」 |
| 2026-09-24 | `T-0112` 逐格核 5 项交回（A7，done） | ① FontVWindow 字段表 4 处行号全错（90444/90445/90469/78866 → **90446/90447/90463-90464/78861**）+ 3 处二级证据一并改；② `0x204` 的「GDI 一次性整串」**被推翻**（体：整串一次调用、内部逐字软件光栅化；`TextOutA` 那一版在 `sub_456820` raw 68616）；③ `set:AutoLineFeed` 字段 = **`Font+1404`（`Engine+86700`）**（raw 23660-23662 + 缺省 `v13=1` raw 111548-111549）；④ `copyright-effect.md` §6 索引表 **11 行全部偏低**（含函数名 `sub_49A300` → **`sub_49AA30`**）整表核改 + 正文 20 处；⑤ `sub_40DF10` 队列/栈构造点各 1 处、各跑 10 次 ⇒ 对象各 10 个（计数没错，尺寸标注改实测 `0x1C`/`0x14`）。★它新增 `test/doc-model.test.ts` 的 **B7-B 两向行号棘轮**（24 锚点：文档该行必须写着该行号 **且** 该行号处真含该代码串）+ 改前 26/26 ❌ 的机械取证 |
| 2026-09-24 | 主 agent 收 A7 的移交 | ① **新开 `T-0172`**（P3）：`docs-new/03-engine/rendering.md` 行号整表逐行核（8 处已知错 + 8 处从未核过的；另含 `04-app/emulator-copyright-effect.md` 的 3 项）；② ★**真红待修（追踪中）**：`test/doc-model.test.ts` 现有一例 ✖ = `opcode-table.md` 与 `analysis/opcodes.json` 不同步（A4 在 00:40:10 改了真源、尚未重跑 `build-opcode-table.mjs`）⇒ **A4 交回后必须重跑该生成器**；③ `npm run typecheck:test` 的红全在 A6 在改的 `test/texture-renderer-gaps.test.ts`（预期，待其交回） |
| 2026-09-24 | 主 agent 补缺口台账的 **`0x84` 静默项** + 修掉 `index.md` 那例真红 | ① 实证：派发表项 `_this + 676524`（= `675996 + 4*0x84`，raw **22833**）= **`sub_41F790`** ⇒ **`0x84` 就是 §2.2 记的「`0x100000` 置位端」**（argc 1、op1 = **带符号方向**、推回看游标 `sub_459770(Font, ±1, 2)`、写 `122454 = ±1`、`174801 \|= 0x100000`、调 `CALLBACK_TEXT`（本机 no-op））。它**既不在三张运行时表、也不在缺口台账** ⇒ 语料 0 时棘轮看不见、命中即硬停，而 `unimplemented: 0` 会把这件事**掩盖**成"没有缺口" ⇒ 已补条目（`deferred` + `why` + 扩展点 = 抽出滚轮 ④ 路那套体后注册 `[0x84, …]`；票 `T-0168`）：缺口台账 **116 条 / deferred 19**，`opcode-gaps.test.ts` **6/6 绿**。② doc-model 的那例红**不是** opcode-table（A4 已重生成，00:42:06），而是 `docs-new/00-overview/index.md` 过期 ⇒ 生成器 = `scripts/build-doc-index.mjs`，已重跑（109 份文档）⇒ `doc-model.test.ts` **9/9 绿**，树红清空 |
| 2026-09-24 | `T-0151` 的 `src/text/**` 子集交回（A5，6 条全处置） | ① P2 `lazy-gdi-font-set`：`src/text/fontSet.ts` 落成引擎的句柄组（主套 **10** 支 + 注音 4 支，逐格带 raw）+ `metricFaceSlot`（"量宽用哪支句柄"）+ 位图缓冲算术 + 缩放修正；无等价物的 6 格**有据登记**。② P2/P3 `text-font-rebuild-cascade`：实为「5 setter + 1 查名被调 → 2 重建体」（`engine.raw 70940-71273` 本身没错），两个错锚点**实测确指错**（`msgwin.ts:1146-1189` 现是 `op_msgwin_obj_*`；`text-layout.test.ts:248-264` 现是游标/面名用例）⇒ 真锚点与真 guard 在 `changes-text.md` §8。③ P3 `0x2c7`：负起点现抛 `SjisSubstrOutOfRangeError`（`sub_40C120` 无符号比较）。④ P3 `0x2c8`：字节长 > 255 抛 `SjisSubstrSourceTooLongError`；下界外读**登记为不复制**。⑤ P3 `0x204`：按体分两条路（`Font+201680 <= 1` 这道门**只在描边路径** `sub_471180` raw 87269）、mode 1 取 `cx`（**全角也取 cx**）⇒ **顺带修正了审计摘要两处**。命令：3 个新守卫 **16/16** 绿（每单元先贴红）、文本族回归 **95/95** 绿、`typecheck` exit 0 |
| 2026-09-24 | 主 agent 应用 A5 的缺口台账移交 | **`0x2c8` 原本不在缺口台账**（当时判为"完整实现"）⇒ 按 T-0149 口径（"某处为披露的近似"也算 `partial`）补条目：`handler=sub_434260` / `argc 4` / `missing[1]`（raw **42420** 的缓冲下界外读 = 未初始化栈字节 ⇒ 不复制）/ 票 `T-0151`。缺口台账 **117 条 / partial 64 / 缺口 140**，`opcode-gaps.test.ts` **6/6 绿**。★另 3 条 capabilities 修正（`text-font-rebuild-cascade` 加 `engine.calls` + 换 guard + 换两个错锚点；`lazy-gdi-font-set` 的 ×8→×10 + 补 16 个 reads + 降 `partial`；`text-blank-extent-mode-gate` 改 note）**待 A4 释放 `engine-capabilities.json`** 后再落 |
| 2026-09-24 | 主 agent 处理 A5 报的两处**跨 agent 红** | ① A4 的 `test/recall-page-0x1d1.test.ts` 是 `typecheck:test` 与 `harness-convergence` 的唯一红（+ `check:dead-writes` 新增 `Engine.recallRepaint` 无读者）⇒ 已提醒 A4；② A6 的 `test/save-delete-slots.test.ts` 缺 `@tier` pragma（`npm run test:org` 红）⇒ 已提醒 A6 |
| 2026-09-24 | 新开第 8 个 subagent：`T-0172`（docs 行号整表核） | `docs-new/03-engine/rendering.md` 是 T-0112 同批结论的**未同步文件**（8 处已知错 + 8 处从未核过），外加 `docs-new/04-app/emulator-copyright-effect.md` 三项 ⇒ 交给一个新 agent（这些文件此刻无人占），要求把 `rendering.md` 也纳入 `doc-model.test.ts` 的 **B7-B 两向行号棘轮**，并**先跑出"改前 ❌"**再改 |
| 2026-09-24 | 主 agent **抽检 T-0153 的单位差前提**（read-only） | 审计声称 0x245 族「op2 要 ÷ `dbl_51FB50`」而 0x246 族「÷ `dbl_5201F0`」（emulator 原值直传 ⇒ 单位差 1000× / 100×）：体证 —— `dbl_51FB50 = 1000.0`（raw **4393**）、`dbl_5201F0 = 100.0`（raw **4430**）；写端 raw **32568 / 32640 / 32673** 全是 `sub_4081B0(对象, (double)v / dbl_51FB50)`（÷1000）、raw **32696-32697** 是 `(double)result / dbl_5201F0`（÷100）；读端 raw **40013 / 40028** 是 `sub_4080B0(v) * dbl_51FB50`（×1000）⇒ **审计行准确**（两个常量确实不是同一个）。可复用：`sub_4081B0(obj,v)` = 按 ÷1000 写节点尺寸、`sub_4080B0(obj)` = 读尺寸（0x23E/0x23F 用） |
| 2026-09-24 | 复核 A1（`T-0149`）**在改中**的产物 | `partial` 一阶处置位已落：`dispositions.partial` 带精确 schema 说明（`missing[]` = {what, ticket, raw}，raw 须匹配 `^\d+(-\d+)?$` 单段）；已把若干 `implemented` 条目改判 `partial` 并逐条写 `missing[]`（**带票号 + raw 锚点**，例如 0x7C 的 4 条缺分支 → `T-0156`，raw 14001-14008 / 25798-25807 / 25812-25815 / 25816-25822）⇒ **我给 `0x1D1` 的结算照这个形状写即可**（单条 missing 指向 `T-0170`、raw 用 80524-80532） |
| 2026-09-24 | 主 agent **抽检 T-0151 的 `0x197` 行**（read-only） | 审计声称「`sub_418680` 把新字号写进 **10 个字体对象**的 `+200`（字号）与 `+204`（`_this[327]`）并逐个生效；另写派生态 `_this[324] = _this[25509] = a2/-2`、`_this[323] = _this[25508] = -a2`（整数除法）」。体证（raw 24085-24154）：**逐个点清** `_this[261].._this[270]` 共 **10 个**对象的 `*(obj+200) = a2` / `*(obj+204) = _this[327]`，四格派生态逐字命中（`a2 / -2`、`-a2` 的向零截断），收尾 `sub_45A6E0(_this)`（= 注音字体重建，与 `0x2be` 的早退门同族）⇒ **审计行准确**。handler = `sub_41FDD0`（raw 29117-29124，arity 槽 3 ⇒ **argc 1**，只读 op1） |
| 2026-09-24 | 保护性检查：A4 的 `src/vm` 在改产物是否偏离修正后的语义 | `git diff --stat` = `textItems.ts` +271 / `msgwin.ts` +25 / `engine.ts` ±10。`textItems.ts` 的新增**不是页面模型**，而是把 `sub_4675A0` 真正读的东西建出来：① 记录 `flags` 位 `ITEM_ROW_TEXT=0x4`（带正文串的已画行，写者 `sub_45F090` raw 74360-74400）与 `ITEM_ROW_LINE=0x8`（换行记录，写者 `sub_4691D0` raw 81530-81553）；② **op3（引擎 `a4`）的位单独命名**（`REPAINT_ALLOW_REFLOW=1` / `SET_COLORS=2` / `SUPPRESS_ROW=4` / `VOICE_ICONS=8` / `RUBY_RANGE=0x30` / `KEEP_SURFACE=0x40`，每格带 raw）并明确写出「**op3 的位与记录 flags 的位数值相同但语义不同，四对纯属巧合**」；③ `TextItemRecord` 新增 `+44 std::string`（`text?`）⇒ **方向正确**（含 0x82 的 `op3 & 0x30 ⇒ win+276/+280` 那一族） |
| 2026-09-24 | 主 agent **独立复核 A5 的三处关键论断**（read-only，三处全部命中） | ① 「`Font+201680 <= 1` 的门**只在描边路径**」—— raw **87269**（`sub_471180` 内）确实是 `if ( *(int *)(_this + 201680) <= 1 && GetConfig(set:BlankExtentMode) == 1 )`，而 raw **86057**（`sub_46F2D0` 内）**只有** `GetConfig(...) == 1` ⇒ 无该门 ✓。② 「直绘空白字**全角也取 `cx`**」—— 两条路都写 `sub_404EE0(_this, &psizl, ch); cx = psizl.cx;`（全角/半角走同一支）；`else` 支里的除数 `((ch < 0x100) + 1)` 才是"全角减半"的估算 ✓。③ 「`0x2c7` 负起点必抛」—— `sub_40C120`（raw 16219-16225）`v6 = (unsigned int)a2[4]` 取**无符号**长度后 `if ( v6 < a3 ) std___Xout_of_range(aInvalidStringP)`，负 `a3` 被提升成巨大无符号数 ⇒ **必抛** ✓（随后 `v7 = v6 - a3; if (a4 < v7) v7 = a4;` + 两次 `sub_40B420` 的"保留 `[a3, a3+v7)`"语义也对得上）。★顺带一眼：`sub_46F2D0` 的 `else` 支用 `*(__int16*)(Font+201704)`（**16 位有符号**），与描边路的 `*(int*)(Font+201684)` **不是同一格** ⇒ 与 A5 报的"未建模 `(int16)Font+201704`"一致 |
| 2026-09-24 | 主 agent 为第 B 波**预写两份作业说明书**（把已核实的前提固化，免得交回时重复讲） | `.tmp/wave-b/T-0151-msgwin-brief.md`：3 槽交付的 **5 处落点**、`0x197` 的 10 个字体对象 `_this[261..270]`、`0x196` 的 **argc=3** 与第 4 实参来自 `Engine[97055]`、`0x71` 的 `Engine[97055]` 是**参数不是门**、`sub_41BF50`/`sub_41B640` 两个取操作数器、arity 槽的两种等价写法（`Engine[30f+95805]` ≡ `*(Engine+120f+383220)`）、与 `T-0019`/`T-0169` 的边界。`.tmp/wave-b/T-0153-gfx-texture-brief.md`：`÷1000`（`dbl_51FB50` raw 4393）vs `÷100`（`dbl_5201F0` raw 4430）**两个常量不许合并** + 写/读两侧 raw、`0x1fa` 的外层门 `!_this[a2+11676]`、对象表 `Engine[槽+94672]` 与类型标记门、`0x1F8` 还擦颜色/状态两格、`0x249` 的 `a6=1 ⇒ imgid=−1`、以及"renderer 半边归别人、同文件不许并行"。两份都写进同一条规则：**不许写 `analysis/*.json`，台账内容交回主 agent 串行应用** |
| 2026-09-24 | 主 agent 向 A4 提**提前解锁台账**的协调请求 | 问它 `analysis/{engine-capabilities,opcodes,scripts}.json` 是否定稿（最后写入 00:42:00-00:42:03，之后它只在 code/test 上迭代）：若定稿就可立刻应用 A5 的 3 条 capabilities 修正（不同条目、同一文件 ⇒ 必须串行），否则等它交回 |
| 2026-09-24 | A4 回话：**ledger 定稿** + 死写已清 + 命令全绿 + 列全它改过的测试文件 | ① 三个 `analysis/*.json` **它不再动** ⇒ 解锁我应用 A5 的 3 条修正（它对它们的改动 = `opcodes.json` 的 `0x1D1` semantics 整段按体重写、`engine-capabilities.json` 新增 `backlog-drawn-row-recording` + 改 `text-item-record-table` 的 `emulator.note`/把 `engine.raw` 收成单区间、`scripts.json` 新建 `HISTORY` 条目）。② `Engine.recallRepaint` 的死写**已清**：**删掉该字段**，切片只留一条通路（`TextItemTable.repaintRange` → `MsgWindow.setPageText` + `emitWin`），`check:dead-writes` = **14（= 基线）无新增**。③ `typecheck:test` exit 0、`harness-convergence` **3/3**（★它踩的坑值得记：时钟源抽成常量后，**注释里**仍留着扫描器认的那串字面量 ⇒ 第一次改完还是红 —— 那个扫描器读整份文件文本含注释）。④ 它实际改过的测试文件**只有两个**：新建 `test/recall-page-0x1d1.test.ts`(13 例) + retarget `test/op-1d0-1d1-text-metrics.test.ts`（按我给的书面契约）；`test/save-slot.test.ts`、`test/native-tap.test.ts`、`test/no-dead-writes.test.ts` **不是它改的** ⇒ 前两个待 A6 归因，`no-dead-writes` 剩的两条红是 A2 在改的**检测器自检** |
| 2026-09-24 | 主 agent **应用 A5 的 3 条 capabilities 修正**（A4 一解锁就做） | 脚本 `.tmp/t0151-text/apply-capabilities.mjs`（内容逐字取自 `tickets/T-0151/changes-text.md` §8 的 7-A.1/2/3）：① `text-font-rebuild-cascade`：+`engine.calls`（8 条，带调用点 raw）+ guard → `test/config1-chain.test.ts#排版结果进入渲染模型` + note 重写（换掉两个**指错**的锚点）；② `lazy-gdi-font-set`：`name` 的 `×8` → **`×10`（主套）+ `×4`（注音套）**、`engine.fns` 8 项、`reads` 26 项、**`status` → `partial`**、guard → `test/text-font-rebuild-set.test.ts` + note 重写（含 `GDI_FACE_REBUILD_NOT_MODELED` 的 6 格缺口与 recheck）；③ `text-blank-extent-mode-gate`：note **保留前半**并追加 T-0151 段（两条路的 `Font+201680` 门不同、**全角取 `cx`**、宿主调用点未接线、`(int16)Font+201704` 与 `Font+218520` 未建模）。⇒ 计数需要 `--recount`（`partial 28→29`、`needsAttention 39→40`、`modeled-verified 78→77`）后重生成：`capabilities --validate` **[ok] 141 条**、`build-capabilities.mjs` ✓、`build-status.mjs` ✓、`build-doc-index.mjs` ✓；`doc-model.test.ts` **9/9**、`capability-gap.test.ts` **6/6** 绿。★7-A.4（`0x2c8` 条目）已在前一步单独做完 |
| 2026-09-24 | **主 agent 结算 `0x1D1`（`T-0170` 的收尾步）** | 跑 `.tmp/t0170-settle/patch.mjs`：`deferred → partial`，`missing[]` **不新编** —— 逐条搬自 A4 自己读体后的 `tickets/T-0170/changes.md` §六（`sub_4634B0` 专用路径 raw 77500-78716／栏带四边形循环 raw 81498-81509／`sub_404CB0` 语音占线无宿主缝 raw **80683**（我自己查了调用点就在那段 voice-icon 分支里）／逐字 GDI 度量不等价 raw 80725-81014／`win+136/+132` 记账 raw 81220-81224／自动换行第三个 push 点 raw 81530-81553），note 保住被钉的三个字串并追加"已按体实现的 0x82 孪生 + 仍缺 6 条"。⇒ 缺口台账 **partial 65 / deferred 18 / 缺口 146**、`build-opcode-gaps.mjs` ✓。★**守卫 24/24 绿**（`opcode-gaps.test.ts` 6 + 被我 retarget 的 `op-1d0-1d1-text-metrics.test.ts` + A4 新建的 `recall-page-0x1d1.test.ts` 13 例）—— 其中"`disposition` 必须已结算"那条正是 A4 §八 列为"预期仍红"的唯一一条 |
| 2026-09-24 | 主 agent 授权 A4 retarget 两条 `config1-chain-advreturn-*` 断言 | A4 的全量跑出 **1309 / 1299 pass / 8 fail**，其中它自己的新增红只有 2 条：`recordsAtI082` 由 `3` 变 `6` —— 因为按体实现后 `0x6E`/`0x196` 也会 push **已画行**记录（`sub_45F090` 家族），而旧断言钉死的是"记录表里只有探针塞的 3 个种子"这个**简化前提**；★决定性证据是那两条测试**自己的注释**就写着引擎真路径实测 `2 → 600 帧后 6`。⇒ 判为**前提被取代**，授权只改那一条（要求：消息写清因果与引擎实测值；能钉 `=== 6` 就钉死，别只留 `>= 3`） |
| 2026-09-24 | ★**主 agent 修掉自己造成的一处回归**（`input.test.ts` 的 0x10C 红） | A4 归因说它"是 T-0163/A5 的 in-flight"，我追下去发现**是我不久前改的**：`src/vm/input.ts` 的 `flushPending`/`flushHeld` 在把 `wheelKeyBits` 并进掩码时我写了 `>>> 0`，而引擎的掩码是 **int32**（位 31 = `1 << 31` = 负值）⇒ 整张掩码被转成无符号，`test/input.test.ts` 的 `★0x10C ③`（`assert.equal(flushHeld(), 1 << 31)`）从 `-2147483648` 变成 `2147483648` 而红。红→绿证据：改前 `test/input.test.ts` **13/14**（`+ 2147483648 / - -2147483648`），去掉两处 `>>> 0` 并**写明"这里不许 `>>> 0`"的理由**后 → `input.test.ts` + `wheel-as-key.test.ts` **23/23 绿**。★教训：**"我自己的改动也要重新量基线"** —— 这条红不在我记的 3 条基线里，我最初差点顺着 A4 的归因把它算作别人的 |
| 2026-09-24 | `T-0153` 的 renderer/arch 子集交回（A6，7 条） | ① `0x1f8` mode==3 ⇒ **DividedTexture** 修好（建类 + 子纹理表 `ceil(w/tile)×ceil(h/tile)`、末行末列按余数截断）；② `0x208` 同步 getter **登记为有意近似**（两条扩展点 + 重评条件）；③ `0x20b` 矩形夹取/空矩形早退修好；④ `0x23f` **renderer 半边修好**（两宿主对象表 + `slotNodeSize` 缝）／**VM 半边待第 B 波**；⑤ `0x1fa` 释放撤尺寸缓存修好，且**订正审计**"Pixi 侧已一致"的判断（它靠 `#healSlot` 答旧尺寸；引擎此时必答 0×0）；⑥ `0x1ab` 删槽修好（**overlay∪base 都删**，base 先隔离到 `SAVE/.deleted/`）；⑦ `0x19E` 缺 overlay 由静默 no-op 改为抛（残留精确码 2 vs 引擎 1 已登记）。命令：两个新守卫文件 **19/19**、纹理族+宿主 10 文件 **47/47**、存档族 14 文件 **90 pass / 2 fail（= 基线红）**、`typecheck`/`typecheck:test` exit 0、`test:org` ✅ 0 问题。★它答了我上轮的归因问题：`test/save-slot.test.ts`(2 处) 与 `test/native-tap.test.ts`(登记册同步) 确实是它改的 |
| 2026-09-24 | 主 agent 对 A6 的**策略面请求**做裁决（`0x1ab` 删 base） | A6 的 ⑥ 突破了 `T-0018` 的旧决定"删也只碰 overlay"，并给了替代方案（墓碑机制，未采纳）。**我的裁决 = 采纳 A6 的做法**：① 引擎 `DeleteFileA`（raw 38477-38481）打在**真实存档目录**，而读会回落 base ⇒ 只删 overlay 会让槽"复活"且返回码相反；② A6 的 base 那份**先隔离到 `<overlay>/SAVE/.deleted/` 再 unlink**，不是静默丢字节 ⇒ 可恢复；③ 已在 `changes-renderer.md` 与台账 note 里披露。**边界**：这是"删"而不是"写/改"，与 `T-0018` 要防的"乱改用户安装"不同类；若将来要更保守，走 A6 给的墓碑替代（但那会被下一次审计归为 host-invented） |
| 2026-09-24 | **`T-0150` 交回（A2，done）—— 目标第 (2) 项完成** | 死写闸门从 `Item`/`MeshObj` 扩到 **`Engine` + `SceneState`（含嵌套 `SceneState.render4`）+ `SceneXform` + `TextFrame`**；扩面后 **14 条存量死写**逐条登记（**a 类真死写 10**：`Engine.callFlag`/`callLink`/`dispatching`/`texSlotFlags`/`l2dMotionCache`、`render4.primReset`/`primTransform`/`released3D`、`SceneXform.maskA`/`maskB`；b 类记录·计数型 4），并**修掉扫描器两处真盲区**：① 容器变更方法（`.set()/.clear()/.push()` 与 `++/--`）修前**不算写** ⇒ `Engine.texSlotFlags`、`render4.commits/.transitionClears` 永远逃过闸门；② **嵌套对象类型**（`SceneState.render4` 的 `{` 在下一行）修前整段解析不出来、`render4.*` 六条全看不见。`check:dead-writes` **基线 14 / 当前 14 / ★无新增**（扫 141 文件 / 154 字段）、`no-dead-writes` **7/7**、两个 typecheck exit 0、票据 ✅。红→绿：临时注入 `Engine.t0150TemporaryDeadWrite` ⇒ exit 1 + 守卫 6/7；撤销 ⇒ exit 0 + 7/7。★它**没动** `src/vm/**`（只登记 + 回链），并交回"必须改 `src/vm` 才能修"的清单：`Engine.dispatching`→`T-0157`、`Engine.texSlotFlags`→`T-0154`、`Engine.l2dMotionCache`→`T-0160`（**审计没点到的新发现**）、`render4.{primReset,primTransform,released3D,entryParams,slotParams,commits,transitionClears}` + `SceneXform.{maskA,maskB}`→`T-0154`（其中 `primTransform` 是**语料 7 处的 185° 旋转真的不生效**）。两个临时脚本已删 ✓ |
| 2026-09-24 | 主 agent 收 A2 的移交 | ① 跑了 `node scripts/build-tickets.mjs`（它置 done 后看板陈旧；现 **171 张**：done 134 / doing 14 / open 21 / dropped 2）；② **新开 `T-0173`**（P3）：删掉两条纯冗余字段 `Engine.callFlag`/`Engine.callLink`（T-0150 扩面新发现，0 读者，写点在 `handlers/control.ts:508-509`）—— 删字段比在基线里长期挂 reason 干净（基线留给真缺口）；③ A2 那份 `src/vm` 清单登记为第 B 波输入 |
| 2026-09-24 | 主 agent **二次授权** A4 的 retarget（`-seed` 的另两条对照组） | 我读了那 50 行：`:83` 的理由文本（"`show-text` 不 push 文本项记录"）**正是 `T-0170` 推翻的那一句**，`:68` 的"记录表仍空"同属"修前 emulator 缺陷"这个前提 ⇒ 都判**前提被取代**；分支区分仍由未动的 `:69-72`（`!includes(0x6e)`）与 `:78-79`（`includes(0x71)`/`includes(0x6e)`）保证 ⇒ 覆盖不降。**附加要求**：两条 `=== 1` 之前各加一条**因果断言**（`recordsAtI082 >= 3`），把"0→1"变成"1 + 为什么"，不许只改数字。★同时**更正它的归因**：`input.test.ts` 的 0x10C 红**不是 A5/T-0163，是我造成的**（`>>> 0` 那个回归，已修绿）—— 它和 A6 都归错了，已在消息里要求写进交回报告 |
| 2026-09-24 | **主 agent 亲自做掉 `T-0171`（P2 滚轮位号越界）** | 体证（逐行读）：WndProc 滚轮支 raw 141563-141583 —— 模式开（`699204 & 0x90100000`）时**只**走 `LABEL_147`，累加支是模式门的 `else`（raw 141580-141583／141608-141611）⇒ **模式开时永不累加**；`LABEL_147` = `if ( v19 >= 0 ) Engine[699208] \|= 1 << v19;`（raw 141604-141606）⇒ 负位号什么都不做、**≥32 按 x86 `shl cl` 掩到 5 位 = `bit & 31`**。**内建缺省实测**：`WheelKeyUp=3 / WheelKeyDown=1 / HWheelKeyUp=-1 / HWheelKeyDown=-1`（raw 111736-111743）⇒ **横滚在 ADV 里默认完全无效**（这也是 ④ 那条旧断言错的地方）。修法：`addWheel`/`addHWheel` 改为「模式开 ⇒ 只在 `bit >= 0` 时置 `1 << (bit & 31)`，然后**一律 return**（含负位号与零增量）」+ 去掉两处会把 int32 掩码转成无符号的 `>>> 0`。守卫改造（我自己的 `wheel-as-key.test.ts`）：④ 由"缺省 ⇒ 退回累加器"改为"缺省 ⇒ 什么都不做"并**补模式关对照**（`i2e5` 那条路），⑤ 由"+120/−120 抵消"改为**单方向喂** + 负位号 + `99 ⇒ bit3`（取模）。**红→绿**：临时装回旧口径 ⇒ `wheel-as-key` **7/9（②/⑤ 红）**；再单独去掉横滚那条无条件 `return` ⇒ **8/9（④ 红）**；恢复 ⇒ `wheel-as-key` + `input.test.ts` **23/23 绿**、`typecheck` exit 0。★教训：**`Set-Content` 会把 LF 改成 CRLF**（实测造出 1545 行假 diff）⇒ 之后改文件一律用 `[System.IO.File]::WriteAllText` 或写完转回 LF（我为此多花两步把两个文件的行尾还原，最终 diff 收敛为 input.ts 30 行 / 测试 43 行） |
| 2026-09-24 | **`T-0108` 现在可派了**（六份 ledger 首次全部空闲）⇒ 起第 9 个 subagent | A 波交回后：`analysis/{opcodes,engine-capabilities,scripts}` 由 A4 释放、`opcode-gaps` 归我、`functions/fields` 随 `T-0112` 释放 ⇒ 六份真源**同时无主** ⇒ 把「数据层沿革话术 146 处 → 结论留原文、沿革落 `journal[]`」整票（P3，非代码类）交给一个 agent。prompt 三条护栏：① **不许碰 `missing[]`**（现行事实，不是沿革）；② **保住被守卫钉住的字串**（`0x1D1` 的 `回看页重绘`/`sub_4675A0`/`raw 80312-81522`；`0x1D0` 的 `回看页索引表`/`sub_459860`/`raw 70629-70724`/route-c 文档名）；③ A5/T-0149/T-0170 刚写进去的「★…第 N 次订正」段落里**当前事实（真锚点/新值）必须留下**，只搬沿革。另附我自己的教训：**改文件别用 `Set-Content`**（LF→CRLF，实测 1545 行假 diff） |
| 2026-09-24 | A4 已落两条对照组 retarget（在它最后那次全量跑之前） | `config1-chain-advreturn-seed.test.ts` 的 `:68`/`:80-84` 已改，且**按我的要求补了因果断言**：`:74` `x.recordsAtI082 > 0`、`:77` `x.recordsAtI082 === 3` —— 那条注释还给出一个比我的假设更准的解释：**该组没有塞种子**，所以 3 条正是"那页样例正文 push 出来的已画行记录"（组一 = 3 种子 + 3 行 = 6，两组自洽）。⇒ 我们的"0→1"不是"改数字"，而是把因果链写进了测试 |
| 2026-09-24 | **`T-0170` 交回（A4，done）⇒ 目标第 (3) 项完成** | `0x1D1` 按体落地（`OPS` 真实现 + `opcodes.json` 记 `emulator: partial` + 6 条 `missing[]`）；**E3 实测回想画面真的出字**（`win=3`、切出 13 条记录、**3 行真实序章正文 52 字**）；红→绿：临时注掉注册 ⇒ 该文件 13 例里 **3/13**，恢复 ⇒ **13/13**。`test:all` = **1310 / 1304 / fail 4** = 3 条基线红 + **1 条是我 T-0171 造成的** ⇒ **本票新增红 0**。它交回时点名 4 件范围外：① `0x1D1` 结算（我已做）；② **7 条 `evidence.line` 漂移**（它插了约 200 行：`handlers/msgwin.ts` 的 T-0016/T-0031/T-0104/T-0085/T-0094/T-0147×2 + `textItems.ts` 的 T-0102）；③ **两处过期 note**：`T-0102 ev[17]`（"records 仅由 pushText/pushVoice 填"已被证伪）与 `src/tools/config1Chain.ts:297-311`；④ `T-0171` 需 retarget `adv-msgwin.test.ts`。另：`analysis/functions.json` 缺 4 条（`0x420310`/`0x4675A0`/`0x45F090`/`0x4691D0`）—— 那文件现在归 `T-0108`，等它交回再补 |
| 2026-09-24 | 主 agent 修掉 T-0171 造成的第 4 条红（`adv-msgwin.test.ts`） | 那条断言「逐字期间**下滚** ⇒ 贴完整页」原来靠"模式开也落回累加器"这个**修前缺陷**才过。读了 `serviceRevealAdvanceInput`（`src/vm/engine.ts:846-859`）确认它只认三条路：`mask & 0x10`、`set:WheelKeyDown` 的**掩码位**（还要求 `message:AdvanceMesOnWheel & 1`）、以及**累加器 < 0**（模式关那一支）。模式开 + 下滚位未映射 ⇒ 引擎**什么都不做**（raw 141604-141606 的 `if (v19 >= 0)`）。⇒ 同纪律 retarget：下滚改断言 `false`（写清因果 + 引 T-0171），**另补**左键按下沿（`mask & 0x10`）那条真正的"贴完整页"路；测试名同步改成"只认引擎那三种输入"。结果 **37/37 绿**、diff 18+/3-。全量 `test:all` 已在后台重跑复核 |
| 2026-09-24 | 主 agent 顺手核实 A8 交回的卡点（`rendering.md` line 84 的字节歧义） | 结论：**A8 的怀疑成立，文档自相矛盾** —— 同一份 `rendering.md` 第 92 行自己写着"mesh-draw **以池基**读成 `[46500]`"（池基 = 引擎+322832），而 line 84 又用"base=引擎(0)"去解释同一个 `_this[11629]` 并把 `46516` 与 `369348` 判成**两个不同字节**。按它自己的口径统一：池基 + 46516 ≡ 引擎基 + 369348 = **同一个 dword** ⇒ line 84 的 ⚠️ 结论要改（**未改代码结论**，只改这句内部矛盾；该行的 `cites` 十个行号必须保留，`T-0172` 的三向棘轮钉着） |
| 2026-09-24 | 主 agent 收 A8 的卡点 ⇒ **新开 `T-0174`（P3）** + 把 line 84 的自相矛盾改成"待核" | ① 开票 `T-0174`：核对 `sub_407E20`(raw 12762) 读的 `[11629]`(=引擎+369348) 究竟由谁置位。★**锚点表暴露的新线索**：131881/131977/132823/132844 四个 raw 行含的是 **`11627`**（= 池+46508，Scene dirty 旗标），而 `sub_4AF1C0` 在 133528 写 `_this + 46516`（= `[11629]`）⇒ 图形池里**两个相邻 dword 被不同写者置位**，line 84 那句把它们混成了一个 —— 这比"基址口径"更深一层。② 把 line 84 的「是**两个不同字节**」改成「**是不是同一个字节取决于两处的基址口径**……留待专门核对（`tickets/T-0174`）」：不擅自断定反向结论，同时保留 `T-0172` 棘轮钉的 10 个 `cites`（`check-anchors.mjs` 复跑 **46 条 / ❌ 0**）。③ ★**紧接着就被自己的守卫抓到**：`ticket-ledger.test.ts` 的证据锚点棘轮报 `T-0174: "两个不同字节" 不在 rendering.md` —— 这就是"**锚点是 ABI，改动 ⇒ 只改指不删**"那条纪律的现场演示；已把 `evidence[0].anchor` 改指为新措辞 ⇒ 守卫 **6/6 绿** |
| 2026-09-24 | ★**全量基线复核通过 ⇒ A 波正式收口，第 B 波开跑** | `npm run test:all` = **1310 / 1305 pass / 3 fail / 2 skipped**，3 条 fail 逐条就是 §6 记的基线红（`engine-slot` 的 SAVE70/71 `storedDwords`、`save-slot` 真槽 `format`、`scene-report` 可绘制项 24 ≤ 缺纹理项 26）⇒ **"不新增红"成立**。A 波 **9 个单元全部交回**（T-0149 / T-0150 / T-0112 / T-0151-text / T-0153-renderer / T-0168 研究 / T-0170 / T-0172 ＋ 我自家的 T-0171），外加我做的 8 处集成（`0x1D1` 结算、A5 的 3 条 capabilities、A6 的 7 条 gaps、`0x84`+`0x2c8` 两个静默项、`build-status`/`doc-index` 两个生成器、`input.ts` 的 `>>> 0` 回归、`adv-msgwin` retarget、`rendering.md` line 84 的自我矛盾）。**第 B 波开两个文件互斥的单元**：① `T-0151` msgwin 主体（`src/vm/{handlers/msgwin,msgwin,textItems,engine,operandPlan}.ts`，28 条 P2 优先）；② `T-0153` 的 VM 半边（`src/vm/handlers/gfx-texture.ts` + `native.ts`/`nativeTap.ts`，含 `op_get_slot_size` 读对象表与 `op-23f-slot-size` 的连带 retarget）。两者**都只写自己的 `changes-*.md`**（`analysis/*.json` 此刻归 `T-0108`，台账由我串行应用）；`T-0169` 暂缓（它要和 `T-0151` 争 `src/vm/engine.ts`） |
| 2026-09-24 | 第 B 波开到**第 3 个单元**（`T-0152` 音频，14 P2 / 25 P3） | 核过文件分布：`T-0152` 的 39 行落在 `src/vm/handlers/audio.ts` + `src/audio/**`，**两者都空闲**（B1 占 `msgwin`/`textItems`/`engine`/`operandPlan`，B2 占 `gfx-texture`/`native`/`nativeTap`）⇒ 开第三个单元。prompt 里两条特别提醒：① 本会话更早的 **P1 修复轮已经动过 `0xC4`/`0x1BD`/`0x2F6`/`0x2F7`/`0x2FF`/`0x302`/`0xBC`/`0xC7`** ⇒ 每条动手前先核**当前**代码状态（可能已修/只修一半，报告要能区分"复核后已修"与"本轮修"）；② **新加的引擎字段必须有真读者**，否则 `T-0150` 刚扩面的死写闸门立刻报红。⇒ 现在 4 个 agent 并行：`T-0151` / `T-0153` VM 半边 / `T-0152` / `T-0108`（四者文件互斥，台账由我串行应用） |
| 2026-09-24 | 主 agent 收 A4 移交的第 ③ 项（两处**过期 note**） | 都改完了（**只改事实、不动锚点**）：① `src/tools/config1Chain.ts` 的两段文档 —— 原文写「本探针链路上记录表多半是 0」「`records` 只有文本项入队时才 push ⇒ TITLE→CONFIG 这条链里它是空的」，**与 `T-0170` 的体证相反** ⇒ 改为「**画正文行**时经 `sub_45F090`（raw 74360-74400）push 带正文串的已画行记录（`flags & 4`），实测 **3**（= 样例正文 3 行）/ 开 `seedRecords` 时 **6**」；② `tickets/T-0102/ticket.json` 的 3 条 evidence note（`:101` 重新入队本体、`:119` 判决实验的辨别力证明、`:125` 「记录表只有谁能填」）—— 都补上「★2026-09-24 被 `T-0170` 取代」的指针 + 实测值（`recordsAtI082` = 3/6、`republishByI082` 由 0 变 1），**原结论文本保留**（`T-0102` 是 done 票，锚点一个字没动）。`git diff --stat` = 17 行 / 6 行；票据 `--validate` ✅ 172 |
| 2026-09-24 | 主 agent 亲自做掉 **`T-0174`**（P3 分析：图形池旗标基址核对）⇒ 三问全收敛 + **顺手收敛了一个"未收敛项"** | ① `sub_407E20`（raw 12762-12786）**返回 `[11629]`**，但它自己先**置 `[11627] = 1`**（raw 12773）⇒ 门 = 「等待计时器在跑（`[11631]` 非零；超时判据 `[11625] > [11630] + [11631]`，raw 12768-12779）」**或**「`[11629]` 非零」。② present 族每帧把**两格都清 0**（raw 136792-136793 `46508/46516 = 0`）⇒ 两者都是**帧内瞬态**，不是跨帧累积量。③ **同基**：`sub_407E20` 的调用点 raw **21111** 写作 `sub_407E20((_DWORD *)(_this + 322832))`（**显式传池基**），而 `sub_4AF1C0` 的 9 个调用点全在同一大渲染函数、与 `sub_4AEEA0(_this, …)`（raw 135581）共用同一个 `_this` ⇒ **`46516` 与 `369348` 是同一个 dword**。⇒ 推翻 `rendering.md` line 84 的两处旧说法（"两个不同字节"、"由绘制项 setter 置 `[11629]`"），并**收敛了 line 84/89 挂着的「~5s 精确驱动者」**：mesh#2 的动画窗（delay `0x1194`=4500ms ＋ dur `0x1f4`=500ms）在活跃的每一帧重新把 `[11629]` 置起，门持续到该窗走完。落地：改写 line 84（**保住 `T-0172` 钉的 10 个 cites** —— 脚本 `fix-rendering.mjs` 自带"替换后 cites 必须仍在"的断言）⇒ `check-anchors` **46/0**、`doc-model` **10/10**；票以 `doneWhy` 收口。★**锚点棘轮又一次当场抓到我**（我把它指向的那句话改写了）⇒ 按纪律**改指**（换成无 backtick 的稳定子串 `两处就是同一个 dword`）⇒ 守卫 **6/6**。台账部分（`engine-capabilities.json` 里 `[11627]`/`[11629]`/`[11631]` 三格的语义）等 `T-0108` 释放后再搬 |
| 2026-09-24 | 主 agent **把 `T-0145` 判据 1 的证据链自己走完了**（read-only；落地只等 `scripts.json` 释放） | ① 两条脚本原文一致：`src/NOVEL.txt:33-34` 与 `src/SYSTEM4.txt:184-185` 都是 `eq (local-int 0) (global-int 3f90) 0` + `jcc (local-int 0) ffffffff label_XXXX`；② **判据回到真源**（不靠"对反编译的解读"）：`analysis/opcodes.json:1075` 的 `0xA0` 行写死「`op1≠0` → 跳 `op2`（`op2 == 0xFFFFFFFF` ⇒ **落下句**）；`op1==0` → 跳 `op3`」⇒ 这两处 = 「`3f90 == 0` ⇒ `local0 = 1` ⇒ **落下句、整块执行**；`3f90 != 0` ⇒ 跳 `label_XXXX`、跳过」；③ 运行期反证（票里已有）：`NOVEL.txt:191` 的 `i1f6` 在 SN0000→SC0000 边界**确实执行**过（`tickets/T-0100/evidence/sn0000-sc0000-boundary.md` 日志 7953）⇒ **旧文档「`3f90==0` 时整段跳过」是反的**。⇒ 判据 1 的结论与改法都确定（改 `analysis/scripts.json` 里 NOVEL/SYSTEM4 条目的 note/invariants + 重生成 `docs-new/05-scripts/`），**只等 `T-0108` 交回** |
| 2026-09-24 | 主 agent 核 **`T-0145`** 四条判据（**3 条闭合、1 条被 `T-0108` 挡**；判据 1 的证据链已单独走完，见上一行） | ① 判据 2 = **no-op**（`grep -r 3f90 app/amayui-emulator/test/` **零命中** ⇒ 没有任何测试钉着旧极性，原票点的 `t0102-chapter-chain` 也不含它）；② 判据 3 **复核确认已落地**（`src/vm/handlers/save-slot.ts:316-318` 与 `src/live2d/runtime.ts:188` 都已改引正确依据 `sub_410160` raw 19385-19388，并把 `sub_403EF0` 在同段标为**假依据**）；③ 判据 4 **本轮改掉**：`src/renderer/pixiBackend.ts` 的 `#holdFrameAfterCurtainDrop` 文档把 `clearDrawContainer` 写成"留帧的**解除点**"，而实现是 `this.#holdFrames = Math.max(this.#holdFrames, HOLD_MAX_FRAMES)`（**续期 60 帧、绝不解除** —— 它自己的行内注释 `:1145-1148` 早就写明）⇒ 改成一致口径；④ 判据 1（`global 3f90` 门极性）**被 `T-0108` 挡着**（`NOVEL.md`/`SYSTEM4.md` 是 `analysis/scripts.json` 的生成物，该文件此刻归它）⇒ 票置 `doing`，`changes.md` 写明重开条件与判据链（`jcc` 表口径 + 运行期反证 `i1f6`） |
| 2026-09-24 | ★**第三次被锚点棘轮抓到** ⇒ 这次把**流程**改对了 | 我改写 `pixiBackend.ts` 那句话，正好把 `T-0145 evidence[7]` 的锚点串吃掉 ⇒ `--validate` 报 ✗。这次不再"事后救火"，而是用 ledger CLI **本来就有的** `--anchors-in <file>`（技能文档原话："改被锚文件前先跑它"）：对我动过的两个文件各跑一遍 —— `pixiBackend.ts` **15 条**锚点、`rendering.md` **4 条**，逐条比对后**只有 T-0145 那一条**被破坏 ⇒ 改指为同义新串「不是解除点」⇒ ✅ 172 张、`ticket-ledger` **6/6**。**新工作规则（写进本节）**：任何"改写文档/注释正文"的动作 —— **动手前先 `tickets.js --anchors-in <file>`，动手后立刻 `--validate`** |
| 2026-09-24 | 第 B 波开到**第 4 个代码单元**（`T-0160` Live2D，14 P2 / 5 P3） | 核过锚点分布：全在 `src/live2d/**` + `src/vm/handlers/live2d.ts`（+ `test/live2d-*.test.ts`）—— 与 B1（`msgwin`/`textItems`/`engine`/`operandPlan`）、B2（`gfx-texture`/`native`/`nativeTap`）、B3（`audio`/`src/audio/**`）**无一相交** ⇒ 可安全并行（`src/renderer/**` 的上一波 owner 已交回，允它按需用）。prompt 里三条特别要求：① 本票**点名了一条存量死写** `Engine.l2dMotionCache`（`live2d/runtime.ts:237` 只 `.set()`、全仓无 `.get()`）⇒ **要么给真读者、要么删字段并把它从 `dead-writes.baseline.json` 移除**（基线只许收缩）；② `0x34a` 要置的脏位就是 `Scene+46508`（= `_this[11627]`）—— 与我刚核完的 **`T-0174` 同一格**（该格由绘制项 setter 与 `sub_407E20` 自己置、每帧被 present 清）⇒ 实现时可直接引 T-0174 的结论；③ 把**新工作规则**（`--anchors-in` → 改 → `--validate`）也写进了它的 prompt（我自己本轮已因此被棘轮抓三次）。⇒ 现在 **5 个 agent 并行**（4 个代码单元 + `T-0108` 台账），文件两两互斥 |
| 2026-09-24 | 主 agent 抽检 A5 的一条关键前提（`Font+201680` **恒 0**） | 它用这条把 `0x204` 的门简化成「只看 `set:BlankExtentMode == 1`」⇒ 值得独立核：`grep 201680 engine/*.c` = **63 处命中**，其中**只有 raw 78769 / 78895 两处是写**、且都写 `= 0`；其余 60+ 处**全是读**（`== 0/1/2`、`!= 1`、`<= 1`…）⇒ **该字段在本 build 里恒 0**，**A5 的简化成立** ✓。★附带结论：那 60 多个读里凡判 `== 1` / `== 2` / `!= 1` 的分支**在真机上也永不可达**（引擎自带的死门）—— 后续谁再读到它们，**别当成"活的另一条路"**（可作 P3 登记素材；台账此刻归 `T-0108`） |
| 2026-09-24 | ★主 agent 复核 `T-0153` 的 ⑥（`0x1AB` 删槽）时**读体发现并修掉一处结果码优先级分歧** | 引擎（`sub_42DFC0` raw 38463-38483）：`v4 = !DeleteFileA(.DAT)` ⇒ 1，**随后** `if ( !DeleteFileA(.STH) ) v4 = 2;` ⇒ **`.STH` 的失败码覆盖 `.DAT` 的** ⇒ **两份都失败时返回 2**；而 emulator 的实现**与 A6 的守卫**都写成 `r.dat ? (r.sth ? 0 : 2) : 1` ⇒ 那一格给 **1**。★同型错误也在 `0x1AC`（`sub_42E0A0` raw 38505-38511：`v5 = !CopyFileA(.DAT)` → `if (!CopyFileA(.STH)) v5 = 2`）。修：`src/vm/handlers/save-slot.ts` 的 `op_slot_delete`/`op_slot_copy` 改成 `r.sth ? (r.dat ? 0 : 1) : 2` 并写明覆盖顺序；`test/save-delete-slots.test.ts` 的 `op1Of` 与"两侧都没有"那条断言同步（**旧断言 1 → 2**，消息里附体证据）。守卫 **7/7 绿**。⇒ 这是"**看别人的交付要连体一起看**"的又一例：A6 的红→绿自证是自洽的（实现与守卫同错），只有回到 `raw 38477-38481` 才发现。★顺带记：本轮 `npm run typecheck` 有一条红在 `src/vm/handlers/msgwin.ts(249,5)`（TS2322 `0\|1\|2`）—— 那是 **B1 在改中**的文件，不是本改动 |
| 2026-09-24 | 主 agent 又抽检两处已交付前提（A4 与 A5） | ① **A4 的「`0x1D1` 体内无区域填充」**：限定 `80312-81522` 查 `FillRect\|Rectangle\|PatBlt\|BitBlt\|StretchBlt\|FillRgn\|FrameRect` = **0 命中** ✓ **字面成立**；★但**全库也是 0 命中**（这份反编译里根本没有这些名字）⇒ 该条**不构成判别证据**（不能用它支持"不是页面/高亮模型"）。A4 的结论仍然站得住，但真正撑住它的是**别的东西**（记录表门 `(Font+3368−Font+3364)/72 > op2 && op2 >= 0`、窗对象 `+112 == 1` 的专用体 `sub_4634B0`、以及与 `0x82` 的逐句同形）⇒ 待 `T-0108` 释放 `opcodes.json` 后把那条"护栏"改成如实措辞（**"全库都没有这些名字 ⇒ 本条不判别，结论依据见前两条"**）。② **A5 的「主套 10 支 / 注音 4 支」**：`CreateFontIndirectA` 全库 24 处命中里，`sub_459F40`（raw **71042** + **71171-71186** 九处）正好 **10** 支；`sub_45A6E0`（**71229** + `v10(&lf)`/`v10(&v15)`/`v10(_this+102032)` = **71265/71266/71269**）正好 **4** 支 ⇒ **逐个数对** ✓；顺带确认它的缩放修正公式 `lf.lfHeight = (int)((double)size * aspect + 0.5)`（raw 71240-71242，`v15.lfWidth = lf.lfHeight / 2`）与「scale=1 时因 +0.5 也**不是**恒等」的说法 ✓ |
| 2026-09-24 | 主 agent 抽检 A6 的 ①（`0x1f8` mode==3 ⇒ DividedTexture 的分块公式） | 两处都对：① raw **122855-122863** 确实是 `if ( a5 == 3 ) { v7 = operator new(0x460u); … sub_43A5C0(v7, Engine+1860, a2); }`，`else` 支是 `operator new(0x450u)` + `sub_48AB20`（普通纹理类）⇒ **"mode == 3 建的是另一个类"成立** ✓；② 分块数在 `sub_43A5C0`（定义 raw **46562**）的 raw **46675-46677**：`v35 = a2 / dword_55052C + (a2 % dword_55052C != 0); v11 = a3 / dword_55052C + (a3 % dword_55052C != 0);` ⇒ **`ceil(w/tile) × ceil(h/tile)` 逐字成立** ✓；`dword_55052C = 256`（raw 5663）且有写点（raw 32711 = `0x248` 的 setter）⇒「tile 默认 256、`0x248` 可改」也成立 ✓；末行/末列的余数截断见 raw 46776-46782 ✓。⇒ **A6 那一簇到目前为止只有 `0x1AB` 的结果码一处错**（且是"实现与守卫同错"型），其余抽检项（`0x1f8`/`0x20b`/`0x19e` 依据/分块公式）全部命中 |
| 2026-09-24 | 主 agent 核 A2 的 `T-0150` 死写基线（14 条 + 纪律） | ★先说**我自己的一个读文件失误**：我先跑 `node -e "... b.known \|\| b ..."`，只看到 `known` 那个**裸数组** ⇒ 一度以为"报告说新增了 `reason`/`tickets`，文件里却没有"。**改成 `read` 原始文件**才发现两个 map 都在：`reason`（14 条逐条、长度都 >20 字）与 `tickets`（12 条回链 `T-0154`/`T-0160`/`T-0157`，2 条 `"none"` 并说明为何暂无票），而且 `src/tools/deadWrites.ts:681-686` **强制校验**它们（缺 `reason` / `reason` < 20 字 / 缺 `tickets` 都会让闸门报问题）⇒ **A2 的声明与文件一致** ✓。**教训**：先用 `read` 看文件本体、再用脚本做统计，**别拿截断/选错字段的 JSON 片段下结论**。抽检两条 reason：① `Engine.callFlag`/`callLink` —— `grep -r` 只有声明（`engine.ts:263-264`）与复位（`control.ts:508-509`）、**零读者** ✓；② `Engine.l2dMotionCache` —— 只有声明（`engine.ts:1738`）+ `.clear()`（`live2d/runtime.ts:207`）+ `.set()`（`:237`）、**全仓无 `.get()`** ⇒「动作缓存从未命中、每次重新解析 `.mtn`」✓。另：基线的 `_removed` 把**已毕业出基线**的字段逐条写清"谁接上了消费者"，`_note_2026_09_25` 记下**刻意不进**基线的两个字段与理由 ⇒ 这份棘轮台账的规范度是本工程目前最好的之一 |
| 2026-09-24 | 第 B 波开到**第 5 个单元**（`T-0155` 绘制项的 **VM 半边**） | 核过锚点：`T-0155` 的 26 行里约 **10 行**在 `src/vm/handlers/gfx-state.ts` + `gfx-item.ts`（**都空闲**），其余在 `src/renderer/drawitem/**` + `pixi/presenter.ts`；而 `src/renderer/**` 我**允许了 B4（Live2D）按需使用** ⇒ 只开 VM 半边、**明确禁止**它动 `src/renderer/**`（renderer 半边留待后续）。含 P2 的 `0x32`/`0x321`/`0x32d`/`0x33f` 与 P3 的 `0x20e`/`0x21e`/`0x24f`/`0x258`×2/`gfx-prim-mesh-and-render-state`。★特意提醒它：`Engine.texSlotFlags` 已在死写基线 `known` 里挂着并回链 `T-0154`，那条 `0x258` 的 P3 要的是**如实标注"建模 ≠ 有消费端"**、**不是**假装它有消费者（否则会去编一个消费者）。⇒ 现在 **6 个 agent 并行**（5 个代码单元 + `T-0108`），文件两两互斥 |
| 2026-09-24 | 第 B 波开到**第 6 个代码单元**（`T-0159` 存档槽链路，7 P2）—— ★**本波并发到此封顶** | 核过锚点：几乎全在 `src/vm/handlers/save-slot.ts` + `src/save/**`（**都空闲**，B1~B5 的清单里都没有），只有一条 missing-consumer 落在 `src/renderer/pixi/textureCache.ts` ⇒ 要求它**只登记、不动手**（renderer 可能被 B4 占着）。prompt 里三处特别交代：① **别改回**我刚修的 `0x1AB`/`0x1AC` 结果码优先级（`.STH` 的失败码覆盖 `.DAT` 的 ⇒ 两份都失败 = **2**；`op_slot_delete`/`op_slot_copy` 与 `test/save-delete-slots.test.ts` 的 `op1Of` 都已同步）；② 本票有**两处必须如实登记的"做不到"** —— `0x19E` 的 `MB_YESNO` 覆盖确认框（emulator 无宿主对话框 ⇒ 现在恒按"玩家点是"）与 `0x1AE` 取不到像素时自造的 `AMYTH1\\n{…}` 块（现为 `host-invented`），**不许假装做到**；③ `save-slot` 那条**基线红本来就在**（`T-0146` 的真槽 `format`），别算成自己新增、也别去修它。⇒ 现在 **7 个 agent 并行**（6 个文件互斥的代码单元 + `T-0108`）—— **这是本波的并发上限**：再加就只能抢 B1~B6 或 `T-0108` 手里的文件了 |
| 2026-09-24 | **`T-0108` 交回（第 9 个 subagent，done）⇒ 主 agent 复核树状态** | 181 处沿革话术逐条判定（移 286 条进 217 个实体的 `journal[]` / 删 15 / 留 3 类），六份真源 `journal[]` 之外 **0 处**，四份生成物 `49/8/1/8 → 0/0/0/0`。我复核：四份 `--validate` 全绿（tickets 172 / capabilities 141 / scripts 35 / opcode-gaps 缺口 143 partial 64）、三份生成器 `--check` 最新、`git diff --stat` = 89 files / 9020+ / 1263-。★它的 diff 数是**上界**（六份真源本会话开始时已是 modified） |
| 2026-09-24 | 主 agent **串行结算四份真源**（本波第一次"我自己动手"的台账集成；脚本 `.tmp/settle/{patches.json,new-functions.json,apply.mjs}`，两阶段校验 + 每个 old 必须恰好 1 次 + 拒绝 CRLF + JSON 兜底） | ① `analysis/functions.json` **新增 4 条**：`0x420310`（0x1D1 handler；arity 槽 11 = 2*argc+1、`sub_41BF50` 读 op5..op1 顺序直传、第 7 实参 `&Engine[21032]` 不是操作数 —— raw 29353-29371）、`0x4675A0`（0x82 的 `sub_466000` 的**孪生体**，门 raw 80524-80532 与 raw 79499-79508 逐句同形）、`0x45F090`（压 72B 文本项记录，flags `0x4` 由 raw 76381/79181 的 `a4 \| 4` 传入）、`0x4691D0`（换行：几何下移 + `sub_4691A0(..., a3 \| 8, ...)` ⇒ flags `0x8`）；并把 `0x42DFC0`（`0x1AB` 删槽）的 purpose/notes 补齐**结果码优先级**（`.STH` 失败码覆盖 `.DAT` 的 ⇒ 两份都失败 = **2**，raw 38477-38481 —— 与我上轮修的 handler/守卫同口径）。② `analysis/engine-capabilities.json` 的 `scene-pending-flag-0x400-gate`：`reads` 补 `Scene+46508`；`trigger` 补 **T-0174** 全文（`sub_407E20` 返回 `[11629]` 但**先置 `[11627]`**（raw 12773）⇒ 返回值 = 「计时器在跑且未到点 ⇒ 1」否则「`[11629]`」；到点判据 `[11625] > [11630]+[11631]` 或 `[11628]==1` ⇒ 清的是**起点·时长**两格不是挂起位；调用点 raw **21111** 显式传池基 `_this+322832` ⇒ `Scene+46516` ≡ 引擎基 `+369348` **同一个 dword**，推翻 `rendering.md:84` 的「两个不同字节」；present 族每帧把两格都清 0（raw 136792-136793）⇒ 帧内瞬态）；`emulator.note` 把「到期清两格」改成逐句口径 + 记 `[11627]` 无条件置位。③ `analysis/scripts.json` 的 `3f90` 门极性**六处**全部订正（SYSTEM4 的 layout/`what` + gotcha；NOVEL 的 layout×2 + invariant），并顺手改正原写的 `4fd9 != 0 时才走 i1f6` —— **也是反的**（`4fd9 == 0` 才走 `i1f6`，`label_00000f9c` 在 `src/SYSTEM4.txt:211-213`）⇒ `build-scripts.mjs` 重生成。④ `analysis/opcodes.json` 的 `0x1D1` 语义把**不具判别力**的护栏改成如实措辞：`FillRect`/`Rectangle`/`PatBlt`/`FrameRect`/`FillRgn`/`BitBlt`/`StretchBlt` 在**全库 184091 行实测 0 命中**（GDI 库符号，反编译里只以 `sub_*` 出现）⇒「搜不到」不能证明什么；结论依据改为三处体证（记录表门 raw 80529 / 专用体 `sub_4634B0` raw 80531-80532 / 与 `sub_466000` 逐句同形） |
| 2026-09-24 | 主 agent **刷新六份真源的 `evidence.line` 漂移**（`.tmp/settle/fix-lines.mjs`：只在**串仍在文件里**时改行号；多命中优先取 `"id": "<anchor>"` 定义行；串真没了就报告不改锚点） | `T-0108` 点名的 **25 条** + 我本轮编辑新增的漂移，一次刷新 **26 条**（capabilities 13 / opcodes 3 / functions 2 / opcode-gaps 3 / fields 1 / …）；警告 **95 → 70**，且六份真源**零漂移**（剩下 70 条全部指 `app/amayui-emulator/src/**`，是 B 波 6 个 agent 在改中的文件，等它们交回再刷）。★顺带发现 `T-0011` 的 `auto_line_feed` 原本**没有 line 字段**，补上 = 404 |
| 2026-09-24 | **`T-0145` 四条判据全闭合**（判据 5 由本轮结算覆盖） | ① 3f90 极性按 `opcodes.json` 的 `0xA0` 行订正 + 两份生成物重生成；② no-op（`grep 3f90 test/` 零命中）；③ 早前已落地（`save-slot.ts:316-318` / `live2d/runtime.ts:188` 引 `sub_410160` raw 19385-19388）；④ 早前已落地（`pixiBackend.ts` 的 `#holdFrameAfterCurtainDrop` 改成「续期 60 帧、不解除」）。`evidence[4]`（`SYSTEM4.md` 的锚点）按棘轮纪律 retarget 到新结论串 `3f90 == 0 ⇒ 落下句、整块执行`。守卫：`doc-model` + `capability-ledger` + `script-ledger` + `ticket-ledger` + `opcode-gaps` + `op-1d0-1d1-text-metrics` + `operand-plan` **46/46 绿** |
| 2026-09-24 | ★**环境事实（新增，写进 §7）**：`npm run typecheck` 在本机 shell **跑不了** | `app/amayui-emulator/node_modules` 里**没有 `.bin` 目录**（26 个包都在，`typescript/bin/tsc` 也在）⇒ npm 脚本里的 `tsc` 找不到，报 `'tsc' is not recognized`。**改用** `cd app/amayui-emulator && node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit`（另有 `tsconfig.test.json` / `tsconfig.control.json` / `tsconfig.electron.json`）。实测：`tsconfig.json` / `control` / `electron` **exit 0**；`tsconfig.test.json` 有 **1 条红**在 `test/gfx-state-operand-io.test.ts(71,12) TS4117`（B5 的 `T-0155` 在改中，非最终态） |
| 2026-09-24 | 主 agent 裁决 `T-0108` 的收尾第 2 项（「04-app 与 00-overview 里的 12 处『订正』清不清」）⇒ **保留** | 理由：它们是**手写叙述/设计文档**，不在 I9 射程内（本票判据只点 03-engine + 生成物 + 数据层）；逐条读过后 12 处全是**现在时语义**（`emulator-frame-loop-design.md:93`「✅ 已订正（T-0015）」、`emulator-copyright-effect.md:164`「★2026-09 订正：不再是…而是…」）或**流程词**（`emulator-refactor-plan.md:211` 的任务项、`live2d-support-assessment.md:138` 的验收项），删掉反丢信息。第 4 项（长 note 句序重排）不做。⇒ 裁决已落 `tickets/T-0108/notes.md` |
| 2026-09-24 | **第 C 波开 4 个单元**（B 波 6 个仍在跑；C 的 4 份文件清单与 B1~B6 **两两互斥**，且 §5.4 复核过"此刻空闲"） | ① **`T-0162`** 操作数与内存原语（P2 3/P3 3）：只许改 `src/vm/operand.ts` + `handlers/memory.ts`；核心 = 把 `sub_41B9B0`（读字符串操作数）与 `sub_42A420`（任意操作数**强制转串**）逐 case 读完做「tag → 语义」表。② **`T-0161`** 引擎字段/配置（P2 8/P3 10）：只许改 `handlers/engine-fields.ts` + `handlers/config-read.ts` + `configRegistry.ts` + `engineConfig.ts` + `engineFieldIds.ts`；含三条"只写无读"（`0xd9`/`0x142`/`0x2e9`）⇒ 明确要求**如实登记缺口、不许编消费者**。③ **`T-0158`** 面板/菜单/输入（P2 9/P3 18）：只许改 `handlers/{menu,panel,input,region-hittest}.ts` + `interpreter.ts` + `src/vm/input.ts` + `route.ts`；**`src/vm/engine.ts` 那条 P2 `0x94` 明令只登记不动手**（B1 占着）；并提醒它 `0x10d`/`0x2e5` 的滚轮门**我已在 `T-0171` 修过** ⇒ 报告必须区分"复核后已修"与"本轮修"。④ **`T-0163`** 桩/native（P2 7/P3 22）**+ `T-0111` 判据 ②**：只许改 `handlers/stubs.ts` + `handlers/agerc.ts`（`native.ts`/`nativeTap.ts` 归 B2 ⇒ 只登记）；判据 ② = 把 `stubs.ts` 的 `[0x308, op_stub_unhandled]` 移进 `ENGINE_INTERNAL_OPS` 做**有据 no-op**（体 `sub_426B20` raw 12579-12618 = `RegisterTouchWindow`/`UnregisterTouchWindow` + 写 `_this[1954]` **3 写 0 读**，语料 31279 处 ≈ 无宿主触点源）并交回 `opcode-gaps.json`/`opcodes.json`/capabilities 三份内容给我落库；★`[0x308, op_stub_unhandled]` 正好是 `T-0111` 的 evidence.anchor ⇒ 已交代按棘轮**改指不删**。4 份 prompt 都写进同一条新工作规则（改被锚文件前 `--anchors-in`、改完 `--validate`）与"先读票、再读体、再动手" |
| 2026-09-24 | 主 agent 复盘：**`T-0156` 故意没开** | 它 7 条 P2 落在 `handlers/control.ts` + `handlers/frame.ts`，但两条 P3 与 `T-0158` 的 `interpreter.ts` 同域 —— 三处讲的是**同一件事**（引擎把「调用方恢复位置」编码成调用方帧的**步长槽**由主循环 `ip += 4*step` 消费，`0x91`/`0x92`/`0x100` 三条都靠"handler 决定 ip 是否前进"）。⇒ 等 `T-0158` 交回、step-slot 口径定下来再开，避免两个 agent 各造一套 ip 前进模型 |
| 2026-09-24 | 主 agent **收 `T-0151`（B1，msgwin 主体，P2 28 + P3 47 全部处置）** 并落库它的 A/B 两节 | 修好 13 / 登记 4 / 推翻 8 / 交他人 3；新建守卫 `test/t0151-msgwin-vm.test.ts` **22 例**（红 15 → 22/22）。它**反向 retarget 了 `T-0170` 留下的 4 处断言**（`config1-chain-advreturn-{real,seed}` 的 `recordsAtI082` 6→3 / 3→0、`republishByI082` 1→0）—— ★**我独立复核后维持它的口径**：引擎的每一处记录 push 都查 `Engine[97055]`（`0x6E` 传参 raw 28372-28375 → `sub_46BE30` 第 5 形参 → `if (a5 >= 0) sub_4691A0(...)` raw 83941-83942；`0x6F` → `sub_46AF90(_this, v4, a3)` raw 82667 → `sub_4691D0` 的 `if (a3 >= 0)` raw 81549），而 `0x1BB` 体（raw 29223-29242，**我亲自读体**）写 `Engine[97055] = op1==1 ? 0 : (op1==0 ? 0x80000000 : 错误串)`，`CONFIG.txt:41 i1bb 0` … `:355 i1bb 1` **罩住整段 CONFIG 正文**（含 172-179 的 ADV 样例）⇒ CONFIG 链路正文**一条都不记**；真实 ADV→CONFIG 路径不受影响（那些行在 ADV 阶段已记下）。⇒ `T-0102 evidence[17]` 的 note/`T-0170` 的 note 要按此写清区分（**待办**）。落库：`engine-capabilities.json` **8 条**（含 `adv-text-reveal-progress`/`msgwin-object-table`/`msgwin-text-method-opcodes`/`msgwin-attr-font-opcodes`/`msgwin-config-gates`/`text-aa-config-gate`/`msgwin-text-object`/`text-layout-wrap-ruby`）+ `opcode-gaps.json` **新增 `0x7D`**（`deferred`：与 0x6E 逐句同形、语料 0 处、保持硬报错） |
| 2026-09-24 | ★主 agent 被 **I9 棘轮**（`doc-model` 的「沿革不进叙述文档」）当场抓到 ⇒ 改成 T-0108 的正确形状 | 我第一版把 T-0151 的 note 写成「★T-0151 订正（P2 stale-ledger）：原 note 说…已过期…」⇒ 生成物 `engine-capabilities.md` 出现 5 行「订正」⇒ `doc-model` 红。修法 = **被渲染字段只写现在时事实**（`emulator.note`/`trigger`/`whySilent`/`name`），把「以前写错了什么」搬进该条目的 `journal[]`（`{at, field, what}`，**永不渲染**）。为此给 `.tmp/settle/ledger-set.mjs` 加了 **`add`（数组 append；字段不存在时自动插入）** 与 **`unset`（删字段 + 自动处理逗号）**。改后 `doc-model` + `capability-ledger` + `opcode-gaps` + `script-ledger` + `ticket-ledger` **34/34 绿** |
| 2026-09-24 | ★主 agent **替别人的语法错做了最小修复**（阻塞全体，已告知 owner） | `src/vm/handlers/config-read.ts` 第 217 行的块注释里写了路径 ``.tmp/instances/*/overlay/SYS4REG.INI`` —— 其中的 **`*/` 提前闭合注释**，第 218 行起的注释被当代码 ⇒ esbuild `Expected ";" but found "cfgStr" @line 218`。它经 `handlers/index.ts → ops.ts` 被**所有** VM 测试传递导入 ⇒ **任何** `node --import tsx --test` 都 TransformError，至少 3 个 agent（T-0158/T-0153/T-0160）被卡。修法：`*/` → `<id>/`（只改注释里一个词，CRLF=0）。★教训：**块注释里写 glob 路径必须避开 `*/`**；★流程教训：**这条由"文件归属"边界挡住太久** —— 一个字符的注释错让 3 个 agent 白等，说明"阻塞全体"应当**立即由主 agent 修 + 同时告知 owner**，我这次做对了但晚了一步 |
| 2026-09-24 | 主 agent **修 `opcode-gaps` 的两处口径红**（`0x7D` / `0x308`） | ① `0x7D`：T-0151 给的 `deferred` + 1 条 `missing[]` 与台账口径冲突（`partial` 才是"还缺东西"、`deferred` 不许带 `missing[]`）⇒ 删 `missing[]`、把扩展点写进 note。② `0x308`：B 波 `T-0163` 已把它从 `STUB_NATIVE_OPS` 的 `op_stub_unhandled` 移进 `ENGINE_INTERNAL_OPS`（有据 no-op），但**缺口台账还没有条目** ⇒ 校验器报「在 `ENGINE_INTERNAL_OPS` 里但没登记进 opcode-gaps.json」⇒ 我按 T-0111 判据② 补条目（`sub_426B20` raw 12579-12618 / 派发表项 raw 33808-33815 = `RegisterTouchWindow`/`UnregisterTouchWindow` + 写 `_this[1954]` **3 写 0 读**；语料 **31279** 处；附影响面评估与扩展点）。⇒ 缺口台账 **120 条 / 有据 no-op 9 / deferred 19 / partial 64 / implemented 28**，`build-opcode-gaps.mjs --check` ✓ |
| 2026-09-24 | 主 agent 收 `T-0111` ② 的锚点棘轮（**预判到并当场改指**） | `stubs.ts` 的 `[0x308, op_stub_unhandled]` 正是 `T-0111 evidence[2]` 的锚点 ⇒ 它随 T-0163 的改写消失、`tickets --validate` 报 ✗。按"**锚点是 ABI，只改指不删**"改为 `[0x308, op_engine_internal]`（line 355）+ note 写清新登记内容与守卫 `test/stub-308-touch-register.test.ts` ⇒ ✅ |
| 2026-09-24 | ★**B 波 6 个单元全部交回** + 主 agent 收 `T-0166`（能力台账陈旧，10 条定点补丁） | 交回：`T-0151`（22 例守卫）/`T-0153` VM 半边（10/10 先红 → 79/79 绿）/`T-0152` 音频（24/24）/`T-0160` Live2D（16/16；顺带给 `Engine.l2dMotionCache` **真读者** ⇒ 死写基线 14→13，并把「缺省轴 `[0,0,1]`」按体 raw 118487-118490 最小 retarget 到 `(0,0,0)`）/`T-0155` gfx-state（3/3；修掉 `tsconfig.test.json` 的 TS4117）/`T-0159` 存档槽（15/15，含 5 条变异证明）。`T-0166` 的 16 行里 **4 行"前提被推翻"**（台账早就改好了 / 审计把 `365288` 认成 `Scene+42456` 的槽表，实为 `Engine+4*slot+91322` 影片资源表）⇒ 我剔掉它自算的 `counts` 条目（改用 `capabilities.js --recount`）后**应用 9 条**，`capability-ledger`/`doc-model`/`ticket-ledger` **22/22 绿**，能力台账 **141 条：已核验 77 / 未核验 4 / 部分 30 / 缺失 6 / n/a 24** |
| 2026-09-24 | ★主 agent 把**票据 evidence 行号漂移清到 0**（82 → 0 条警告） | 给 `.tmp/settle/fix-lines.mjs` 加了 **`--any`**（把 `app/amayui-emulator/**` 也纳入范围）与**多命中取"离原行号最近者"**（代码文件没有 `"id": "<anchor>"` 定义行，只能靠距离）。一轮刷新 **87 条**（含 `T-0159` 的 16 条、`T-0160`/`T-0155` 的、以及 5 条原本 `line: undefined` 的补全）⇒ `tickets.js --validate` **✅ 172 张、0 条警告**、`ticket-ledger` 6/6 绿。★这是本工程第一次让票据锚点全绿；**纪律**：波次收口后必跑 `--any` 一次 |
| 2026-09-24 | 主 agent 开 **3 个"台账抄写员"（scribe）subagent**（只读核实 + 产出可机械应用的补丁 + 自证 dry-run，**不许碰任何真源/代码/测试**） | Scribe A = `T-0152`（capabilities `audio-device-init` / `opcodes.json` 9 条语义 / `opcode-gaps` 6 条 `partial`+`missing[]` / `fields.json` 3 条）；Scribe B = `T-0153` §6（`opcode-gaps` 504/506/575/581/582/585 的 `missing[]` 增删）+ `T-0155` §3（capabilities `gfx-prim-mesh-and-render-state` 的 reads + 5 条 `missing`）；Scribe C = `T-0160` §5（capabilities 2 条 + `opcode-gaps` i341/i34e 的 9 条 `missing[]` 全删改 `implemented`）+ `T-0151` §6 **C 节 18 条 opcodes 语义**+ `T-0159` 的 opcode-gaps 复述订正。三者都抄了同一条硬纪律：**`missing[]` 口径（`partial` 必须有、其它不许有）+ 沿革话术不进被渲染字段（I9）+ 禁用 `Set-Content`/`git add`**；产出格式 = 我的 `ledger-set.mjs` 计划 JSON（`match` 必须恰好命中 1 条）或 `apply.mjs` 的整串补丁，**必须跑 dry-run 自证零报错** |
| 2026-09-24 | 主 agent 收 `T-0159` 的 **`test/opcode-operands.test.ts` 8 条豁免文案** | 原 8 条写「合成指令里没有 fileSource/slot 数据 ⇒ 提前返回（真实路径读 op1..）」—— **实测口径相反**：handler **已读 op2/op3**，缺的是**写侧输出格 op1**（合成指令注入的 `fileSource` 是 `new Proxy({}, { get: () => () => new Promise(() => {}) })`，写调用**永不结算** ⇒ 挂在 `await fs.write*` 上、`plan.setInt(1, …)` 执行不到；引擎侧同序 raw 38303/38383/38530）。改了文案（**键 `'0x19e'`…`'0x1af'` 一字未动、顺序未动**），`opcode-operands` + `op-underun-fixups` + `doc-model` **19/19 绿**。★`test/op-underun-fixups.test.ts` 的「`0x1d3/0x1d4/0x2f3/0x33f` 必须留在表里」那条**按 T-0159 的告示刻意没动** |
| 2026-09-24 | 主 agent **同步 `docs-new/03-engine/sound-system.md`**（T-0152 的叙述层，用户既有纪律："分析后必须更新文档"） | 新增 **§12「2026-09 逐条核体后的口径（`tickets/T-0152`）」**：`0xC2` 的 op2 **不是每帧步长**（节流毫秒 `op2<1000 ? op2/10 : op2/1000`、进度增量 `? 10 : 1`；ADV 位在场走另一条路 raw 29815-29823）、`0xB5/0xBA` 值域 **0..14**（raw 138599/138605-138612）、`0x2F4` 按 op3 二态翻转（raw 33620-33648）、`0x1BC` 的释放段被**全库无写者**的 `+85160` 门住 ⇒ 死代码（raw 24851-24869）、`0xC3` 单条写（raw 29856-29857）、`0x302` 只写两格（raw 33767-33777）、`0x2BF` 值域门 `a2 < 10`（raw 137725/137736）、`0x1C9` op1 = 驱动 id（raw 140301）、`0xBB` vs `0xBC` 开关族不同（raw 13551-13568 / 13525-13540），末尾列 6 条**仍在案的近似**（回链 `opcode-gaps.json` 的 `partial`+`missing[]`）。★写前跑 `--anchors-in`（该文件被 `T-0105 evidence[8]` 锚着第 175 行）⇒ 新增在**文件末尾**、锚点行号未动、`doc-model` 16/16 绿。★叙述层按 I9 写成**现在时**（不写「订正」，沿革留 `journal[]`） |
| 2026-09-24 | 主 agent 收 `T-0102` / `T-0170` 的口径回写（T-0151 §6 D） | ① `T-0102 evidence[17]` 的 note 按 `i1bb` 记账门的体证整体改写（`records.length` **3/6 → 0/3**、`republishByI082` **1 → 0**；引 `sub_46BE30` raw 83941-83942 / `sub_4691D0` raw 81549 / `0x1BB` raw 29223-29242 / `CONFIG.txt:41`-`:355`；**anchor 串 `pushText(...)` 一字未动**）；② `tickets/T-0170/changes.md` 追加「与 T-0151 的口径区分」一节：本票的 E3 实测走 **SN0000 的 ADV 路径**（`i1bb` 正常、记录照记），那个「6」与 CONFIG 探针链路不是同一条。③ `T-0151` 登记 `tests[]`（6 个文件）并留 `doing` —— 它三条判据里只剩第 3 条「`verify` 全绿」不可达（波次在飞） |
| 2026-09-24 | `T-0151` 的 **B-3** 落地（`0x196` 的 `missing[]` 重写） | 原 `missing[0]`「第 4 实参 a5 = `Engine[97055]` 未建模」**已由 T-0151 接上**（`recordGateOpen`）⇒ 删除；改成两条：①第③路 bit16 的「本行有内容」判据（`addRuby` 无返回值语义，raw 29104-29110）、②窗对象 `+112 == 1` 的专用路径（`sub_46B100` raw 83509-83514）。note 用现在时重写 + 沿革落 `journal[]`。`build-opcode-gaps.mjs` ✓、`opcode-gaps`+`doc-model` **16/16 绿** |
| 2026-09-24 | 主 agent 收 **C 波两份中报**，并**当即裁决一个新发现的真缺陷** | ① `T-0158` 收尾（P2 6 修 / 1 部分 / 1 只登记 / 1 复核后已修；P3 8 修 / 4 复核 / 1 推翻 / 5 登记；4 个新守卫 **21 例**，红 20/3/17 → 绿 20/20/0；`deadWrites` 无新增）—— 它**不改** `src/vm/input.ts`（那是 `T-0171` 的滚轮门，只复核），并指出 `src/vm/operand.ts` 正在被全角化（`toFullWidthNumber`）⇒ 若跟进会让 `test/menu.test.ts:36-39` 新增红，故它只把**字符串族**键接上 `readStringOperand`（语料里菜单键 0 处字符串族）。② `T-0161` 在读体时**发现 `src/configRegistry.ts` 里 `set:DependMovieSound` 出现两次**（新加的 `def: 0` 与原 `def: 1`）⇒ **静默双真源**：`CONFIG_REGISTRY_DEFAULTS` 是 `new Map` ⇒ **后写者胜（1）**，而 `formatIni` 的 `put()` 有 `seen` 去重 ⇒ 缺值时写进 INI 的是**先出现的（0）**。raw 判据（注册表构造 raw 111728-111739：`v13 = 1; sub_434D00(v2, aSetDependmovie, &v13);`）⇒ 正确形态 = **只留 `def: 1`**。★它按纪律**没去踩别人在写的文件**（该条重复是 `T-0152` 刚加的）⇒ 我**授权它自己修 + 加静态守卫**（扫 `CONFIG_REGISTRY_KEYS` 任何 key 重复即红 + 断言 `CONFIG_REGISTRY_DEFAULTS.size === KEYS.length`，先跑红再改绿），并要它顺带核全表还有没有别的重复键 |
| 2026-09-24 | ★**三个"台账抄写员"中 B、C 已交回，主 agent 一把落库**（`ledger-set.mjs` 逐个计划 dry-run 校验后连续应用） | 应用顺序与字节变化：**Scribe B**（`T-0153` §6 + `T-0155` §3）⇒ `opcode-gaps` +8343B、`capabilities` +1068B；**Scribe B 追加**（`T-0163` §3）⇒ `opcode-gaps` +727B、`opcodes` +1014B、`capabilities` +803B；**Scribe C**（`T-0160` §5 + `T-0151` §6 C 节 18 条 + `T-0159`）⇒ `opcodes` **+8468B**（18 条语义）、`capabilities` +1844B、`opcode-gaps` **−1890B**（`i341`/`i34e` 的 9 条 `missing[]` 全删 ⇒ 改回 `implemented`）。落库后 `capabilities.js --recount` + 七份生成器重跑 + 四份 `--validate` + **50/50 守卫绿**。缺口台账现在 **有据 no-op 9 / 已实现 30 / deferred 19 / partial 66（缺口 134 条）** |
| 2026-09-24 | 主 agent 收 `T-0163` 的读体纠正并落库（**三处前提被它推翻**） | ① ★**`0xDD` 不是本作指令**：派发槽 `675996 + 4*221 = 676880` 整份反编译**零赋值**（邻槽同），只被 raw 22722 `memset32(..., sub_418E30, 0x400)` 填默认 ⇒ 我改了 `T-0111` 的 `why`（撤销「`0xDD` 同缺，一并核」）并回链 `T-0163`；② ★**`0x308` 的 `opcode-table.md` 表行本来就存在**（`opcodes.json` 的 `{opcode:776, argc:1, handler:"sub_426B20", status:"已核对"}` 早已渲染）⇒ 判据② 只剩「桩 → 有据登记」；③ `scene-3d-effect-level-writer` 已被 `T-0167` 修掉（现 `modeled-verified` + guard 已填）。落库：`opcode-gaps` 的 `0x308` note 补全门形状（`op1 \|\| (GetConfig("system:LimitTouch") & 1)` bit0）+ 两个 USER32 导出名的真假分支 + 语料**31279 处 / 345 文件** + 「不建模 `_this[1954]`（3 写 0 读）」的理由；`opcodes.json` 的 `0x308` semantics 同口径重写；`capabilities` 的 `scene-3d-weather-effects-rain-snow-leaf` 补 `engine.fns += sub_4531B0` 并把「并有模型消费者」**收紧**为「模型侧函数已就位、opcode → 模型未接线」（`scWeatherDestroyAll`/`scWeatherSetDestroyThresholds`/`scSet3DEffectSnow` 除定义外无产品调用点，每帧唯一被调的是 `scEnsureEffect3DSlots`） |
| 2026-09-24 | ★主 agent 收 `T-0151` 对**唯一新增红**的裁决（**它判对了**） | `test/op-10-002-adv-sleep-order.test.ts` 的旧期望 `['adv','adv']` 成立的前提是「`0x101`（poll-input）无副作用」—— 而 `0x101` 的体**自己清 ADV 位**（raw 25077 `_this[174801] &= ~0x8000000u;`），`T-0158` 的 owner 按体补上后 ⇒ 第 1 帧派发 `0x101` 即清 ADV、第 2 轮不再进 ADV 循环（raw **21158**）而落到 raw **21176** 的 `0x20000000` 支 ⇒ `['adv','sleep']` **正是引擎行为**。它做的 retarget 是本工程最漂亮的一次：**拆两个子例** —— ① 次序证明改用**惰性载荷** `0x1a7`（`CONTROL_OPS` 的 `op_comment`，体 `() => undefined`）⇒ **原断言原样保留**（`['adv','adv']` + `steps === 2`）；② 同一场景 + `0x101` ⇒ 新钉 `['adv','sleep']` + `advActive === false`（raw 25077）+ `steps === 1`。**没有**放宽成恒真，也**没有**去动本来就对的 `src/frame/loop.ts`。⇒ `op-10-002` **5/5 绿**、+`t0151` 守卫 +`adv-msgwin` **64/64 绿**。★方法论收获：**"预期数值"依赖"载荷无副作用"时，必须把载荷换成惰性的再做次序证明** —— 否则测的是载荷而不是次序 |
| 2026-09-24 | 主 agent 收 `T-0162`（操作数/内存原语，**done**）—— 它**推翻了两条审计前提**并找到一条真根因 | ① **`0x1B0` 的 memcpy 方向确实反**（体 raw 37991-37995 `memcpy(v2=addr(2), v4=addr(1), 4*op3)`；语料 `^i1b0` **0 处** ⇒ 只能合成守卫）。② ★**字符串族的真根因不是「没做数值转串」（T-0165 已修），而是三条取串原语的**分叉**从未建模**：`sub_41B640`/`sub_42A420` 的**每条数值路径**都过 `sub_41A6C0`（ASCII → **全角**，本机 exe 是中文版 ⇒ GBK `0xA3xx`），而 `0x1B2` 走的 `sub_41B9B0` **不**过它 ⇒ **0x1B2 必须半角**。③ 前提被推翻：`LABEL_40` 的空串 fallback 只属**数组族**（`0x8005/0x800B` ⇒ 空串；`0x8003/0x8009` ⇒ 全角 `０`/半角 `0`），**指针族 case 6/12 无任何 null 检查**。④ 读体新发现：**float 指针族 tag 7/13 的读/写此前都在截断**（读经 `readRef` 的 `(raw)\|0`、写走 `default: writeIntOperand(…, v\|0)`），**语料真命中 `src/SETPOLYGON.txt:40/43`** ⇒ 修前每次 fdiv 都在除截断后的整数。红→绿：转串 **9/9 红 → 9/9 绿**、memcpy **2/2 → 2/2**、float 指针 **2/2 → 2/2**，合并 **41/41 绿**；5 条既有断言按全角化 retarget（`'42'→'４２'` 等）。★**它的台账内容（`functions.json` 新增 `sub_41A6C0`/`sub_41B9B0`/`sub_42A420` 三条 + 4 段缺口 JSON + `opcodes.json` 全角化补句）尚未落库** —— 见 §8 的集成队列 |
| 2026-09-24 | 主 agent 复核：**B/C 波的台账已全落，四份台账 + 九份生成物 + 50 例守卫全绿** | `tickets --validate` ✅ 172 张、`capabilities --validate` ✅ 141、`scripts --validate` ✅ 35、`build-opcode-gaps --check` ✅（有据 no-op 9 / 已实现 30 / deferred 19 / partial 66 / 缺口 134）、七份 `--check` 全 ✓；`doc-model`+`capability-ledger`+`script-ledger`+`ticket-ledger`+`opcode-gaps`+`opcode-arity`+`opcode-operands`+`operand-plan`+`op-1d0-1d1-text-metrics` = **50/50 绿**；`fix-lines --any` 又刷 9 条（含 `T-0161` 新测试的两条 `undefined → N`）。★**此时仍红的两条都不是台账的**：`organization.test.ts` 的 T1 档位诚实 + `harness-convergence` 的自造 `mk()` 变体，**都点名 `T-0161` 新建的 `test/config-t0161.test.ts` / `test/engine-fields-t0161.test.ts`** ⇒ 已发消息要它按判据修（不许靠改基线过关） |
| 2026-09-24 | ★**抄写员 A 交回，主 agent 落库 ⇒ 三个抄写员的计划全部应用完毕** | Scribe A（`T-0152` 音频）⇒ `capabilities` +1664B、`opcodes` +2393B、`opcode-gaps` **+7653B**（6 条 `partial`+`missing[]`）、`fields.json` +707B（3 条字节偏移）；连同本轮的 B（+8343/+1068/+727/+1014/+803）与 C（+8468/+1844/−1890）—— 三份计划共改写**五份真源**。★三个抄写员都独立发现"**我的补丁已经被应用了**"（其实是**我自己**在上一轮落的）⇒ 它们按设计**拒绝重放**（`add.journal` 非幂等；`counts` 会被旧版本抹掉）并给了哨兵脚本。**纪律收获**：`ledger-set` 的 `add` 对**数组字段**（`journal`/`missing`/`fns`/`reads`）是**追加语义**、不幂等 ⇒ 同一计划**只能应用一次**；应用后必须在工具里留 `--dry` 的字节快照以便判断"是否已应用" |
| 2026-09-24 | 主 agent 收 `T-0161` 终报（18 行全处置；**顺手修掉一个真缺陷**） | 10 修 / 3 复核后已修 / 3 前提被推翻 / 2 如实登记。它**按我的批准修掉了 `configRegistry.ts` 的 `set:DependMovieSound` 重复键**（保留 `def: 1`，raw 111734-111735）并加了守卫（`config-keys` 的新用例：键表不得有重复 key + 断言 `CONFIG_REGISTRY_DEFAULTS.size === CONFIG_REGISTRY_KEYS.length`）—— 全表已核**只有这一处**重复（116 字面量 / 115 唯一）；**为什么静默** = `Map` 后写者胜（`registryDefault` → 1）而 `formatIni` 的 `seen` 先写者胜（→ 0）。**我点的两条硬红已清**：`organization` + `harness-convergence` + `config-keys` = **14/14 绿**。它推翻的三条前提：`0x2eb` 第 2 层**不是** INI `[set]`（INI 装载器 raw 111846-112310 无该键，大写 `GAMEVERSION` 只走 `sub_494220` 的注册表数据块）、`0xd9` 的返回值被**强转 `void` 丢弃**（raw 20161-20165 ⇒ 不是缺口）、`0x141` 的"玩家可见提示"实为 `WriteFile` 诊断链。它还确认了 `0xDD` 非本作指令（槽 `676880` 全篇零出现 ⇒ 保持默认 `sub_418E30`，命中即抛"このコマンドはサポートされていません．"） |
| 2026-09-24 | 主 agent 收 `T-0160`/`T-0163`/`T-0159` 的收尾助手动作并**收掉最后三处 owner 动作** | ① `src/vm/native.ts:450` 的注释「★只给 `0x203` 用」→ 改成「本缝的消费者 = `0x203` **与** `0x34f`（`op2 < 0` 的 `sub_4ADD60` 回退，raw 34808）」（`T-0160` §5.3-1 的告示）；② `docs-new/03-engine/live2d.md` 补 **「逐条核体后的三条口径」**（`0x346` 的缺省轴 = **`(0,0,0)`** 而非 `(0,0,1)`，raw 118487-118490；`0x34F` 走 `getDrawItemColor` 宿主缝、`op2<0` 按 `(unsigned)−1` 回退；`0x352` **不建槽**，只落 `motion+4/+8` 等下一次 `0x34E` 消费）；③ `test/opcode-operands.test.ts` 的 `'0x308'` 白名单文本按 `T-0161` 给的 `old`/`new` 换掉（条目本身仍需要）。★写前都跑了 `--anchors-in`、写后 `--validate` |
| 2026-09-24 | ★★**第 40 轮收口实测：四份台账 + 七份生成物 + 66 例守卫 + 两份 tsconfig 全绿** | 四份 `--validate`：tickets **172 张 / 0 条行号漂移警告**、capabilities **141**、scripts **35**、opcode-gaps **124 条**（未实现 0 / unjustified 0 / 有据 no-op 9 / **已实现 30** / deferred 19 / **partial 74（缺口 142 条）**）—— ★`partial` 与缺口数上升是**好事**：抄写员把原先藏在 `implemented`/`deferred` 里的"其实还缺东西"**如实拆出来**并逐条挂了票号与 raw。七份生成器 `--check` 全 ✓。守卫 **66/66 绿**（含 `doc-model`/`capability-ledger`/`script-ledger`/`ticket-ledger`/`opcode-gaps`/`opcode-arity`/`opcode-operands`/`operand-plan`/`op-1d0-1d1-text-metrics`/`harness-convergence`/`registry-classification`/`organization`/`config-keys`）。`tsc -p tsconfig.json` 与 `-p tsconfig.test.json` **均 exit 0**。死写基线 13（无新增）。`fix-lines --any` 又刷 5 条 ⇒ 票据锚点再次归 0。 |
| 2026-09-24 | 主 agent 给下一轮留的**明确交接**（本目标轮次用尽） | ① **`T-0156` 现在可以开**（`T-0158` 已交回，step-slot 口径已定；只剩 `src/vm/engine.ts` 那 2 行等 `T-0161` 释放 —— 而 `T-0161` 已交回 ⇒ 全开）；② 下一批 P2 簇：`T-0154`（10，全在 `src/renderer/**`，现在无人占）、`T-0164`（5）、`T-0166`（已收口）、`T-0167`（6，`engine.ts` 已释放）；③ **未落库的台账内容**（三个抄写员只覆盖了 T-0152/0153/0155/0160/0163/0151C/0159）：`T-0158` §3（`scene-norender-mode` 的 `fns`/`raw`、`adv-advance-route-table` 的 `reads`、4 个新引擎字段）、`T-0161` §3（`opcode-gaps` 增删 5 条 + `opcodes` 2 条 + capabilities 2 块 + `functions.json` 10 条 + `fields.json` 4 条）、**`T-0162` §4**（`functions.json` 新增 `sub_41A6C0`/`sub_41B9B0`/`sub_42A420` + 4 段缺口 JSON + `opcodes` 全角化补句）；④ 波次收口后重跑 `test:all` 复核"只余 3 条基线红"；⑤ 票据收尾：`T-0151`/`T-0152`/`T-0153`/`T-0155`/`T-0159`/`T-0160`/`T-0161`/`T-0163` 的 `verify` 判据（本机跑不了 `npm run`，用直接入口替代）+ `--set-status` |
| 2026-09-24 | ★★**主 agent 抓到并修掉自己工具里的一个数据损坏 bug**（本会话最严重的一次自身失误） | 症状：`scene-3d-weather-effects-rain-snow-leaf` 的 `engine.fns` 从 **8 项变成只剩 `["sub_4531B0"]`**（抄写员 B 在读盘核验时发现并报回）。根因在 `.tmp/settle/ledger-set.mjs` 的 `add` 分支：它只查**扁平键**（`parsed2['engine.fns']` ⇒ undefined）⇒ `Array.isArray` 为假 ⇒ 走 `applySet(entryText, 'engine.fns', ['sub_4531B0'])` 的**整体替换**，把原数组抹掉。修法：`add` 改为**按点路径取值**（`k.split('.').reduce(...)`），并在每次 append 时打印 `· add <path>: 数组 N → M（append K）` —— 同类错误**不可能再静默发生**。修复：按原 8 项恢复并把 `sub_4531B0` 插到 `sub_453150` 之后（共 9 项，与其它 452/453 族顺序一致），沿革落该条 `journal[]`。★**教训**：台账改写工具的"追加"与"替换"必须在**同一个点路径解析器**上实现；任何"取不到就当不存在"的兜底都会静默降级成破坏性操作 —— 这类工具**必须打印它做了什么**。★另外它暴露了流程价值：**抄写员读盘复核**（而不是只对接报告）能抓住主 agent 自己的破坏 |
| 2026-09-24 | 主 agent 收 **抄写员 A/B 的完整报告并按其发现收尾** | ① **A 报 10 处不符**（最有价值的三条：`engine.raw` 必须**单段**否则 `capability-ledger.test.ts:65` 必红；`emulator.note` 不能写「订正」否则撞 I9；`0x103C` 这个 offset **算不出来**）—— 它自证 `check-applied.mjs` = **已应用 23 / 未应用 0 / 有出入 0**（即"被并行写者应用"其实就是**我自己**上一轮落的）。② **B 报 11 处不符**，并**修掉它自己上一轮留下的两处缺陷**（`.tmp/scribe-b/plan-delta.json`：`0x246` 的 note 计数句 `replace('仍缺 **3** 条', …)` **静默未命中**（该条写的是「仍缺 3 条」**没有** `**`）⇒ 现盘 note 写「仍缺 3 条」而 `missing[]` 只有 2 条，已改成「仍缺 **2** 条」；以及上面那条 `engine.fns` 破坏）—— ★它给 delta 计划加了**前置断言**（现盘对不上就报错，绝不静默 no-op），这正是那两处缺陷的成因。③ 我另按 A 的提示做了两条**缺口台账清理**：`0x1c9`(457) 的 `missing[0]`（"op1 只写日志不落字段"）**已由本批修掉** ⇒ 删除（该条仍 `partial`，保留 DirectSound 设备丢失那条）；`0xbf`(191) 的唯一 `missing[0]`（"体内有语音状态位翻转"）**前提被 T-0152 推翻**（那段 raw 29872-29880 其实属 `0xC4`）⇒ 清空 `missing[]` 并改回 `implemented`。⇒ 缺口台账 **有据 no-op 9 / 已实现 31 / deferred 19 / partial 73（缺口 140 条）** |
| 2026-09-24 | 主 agent **亲自读体订正三条 `fields.json`**（抄写员 A 说"两行 offset 站不住"） | 读 `sub_4B8490`（raw 140293-140312）确认：它的**第一个形参 `_this` 就是驱动对象本身**，写的是 `_this[2179]` ⇒ 落在**对象内 `+2179*4 = +0x220C`**（成功写 id / 失败写 −1，raw 140301/140306）；若驱动对象是嵌在 `Engine+7912` 的那一份，则等价于 **Engine 下标 4157 = `0x40F4`**（与 `engineFieldIds.ts` 的 `audioDeviceDriverId` 一致）。⇒ ① `audio_driver_key` 的 `offset` 由 **`0x103C` 改成 `0x220C`**（原值 4156 既不是对象内偏移也不是 Engine 下标；全库无其它地方引用 `0x103C` ⇒ 不会造成悬空引用）；② `voice_pan` 的 meaning 拆清**写读不是同一格**（写 `设备[375+ch]` = byte **1500**；读 `设备[387+ch]` = byte **1548** —— 原句把 1548 安在 375 上，自相矛盾）；③ `dependent_movie_sound`(0x3BD8) 的 evidence **不成立**（`15320` 在整份反编译里**零出现**，raw 12055/12093 只是 `GetConfig` 调用点）⇒ meaning 改成"**运行态副本（待证）**"并写明**重新评估条件**（保持 `status=tentative`）。 |
| 2026-09-24 | **第 34 轮（重开目标后）开第 D 波 + 2 个抄写员** | 代码单元 2 个：**`T-0156`** 控制流/帧管理/脚本装载（P2 7 / P3 28；`src/vm/handlers/{control,frame}.ts` + `src/vm/engine.ts` + `engineFieldIds.ts` + `src/frame/loop.ts`）与 **`T-0154`** 场景状态/转场/混合（P2 10 / P3 16；`src/renderer/scene/{ops,transition,blend,state}.ts` + `renderer/drawitem/**`）—— 两者文件**互斥**，且与在跑的抄写员（只写 `.tmp/**`）不冲突。**故意没开 `T-0167`**：它要 `engine.ts`（T-0156 占用）+ `renderer/**`（T-0154 占用）⇒ 排下一波。抄写员 2 个：**D** = `T-0158` §3 + `T-0161` §3；**E** = `T-0162` §4（含 `functions.json` 追加 3 条 `sub_41A6C0`/`sub_41B9B0`/`sub_42A420`）。四份 prompt 都写进了本轮新学到的两条：**"若断言预期依赖某指令无副作用 ⇒ 换惰性载荷再证"** 与 **"`add` 数组字段是追加语义、计划只能应用一次"** |
| 2026-09-24 | ★**主 agent 做「票据收尾三连」的最后一步：把 B/C 波 8 张票收口 + 建交接票 `T-0175`** | 8 张票（`T-0151`/`T-0152`/`T-0153`/`T-0155`/`T-0159`/`T-0160`/`T-0161`/`T-0163`）的**票内判据全部落地**（守卫 + 三层台账 + 红→绿），此前只差两件事：①「`npm run verify` 全绿」这条判据在本机**原理上不可达**（`node_modules/.bin` 缺失，见 §7）；② 各票在**可写范围外**留下的一批跨文件/跨宿主项。处置：用 `.tmp/settle/close-wave.mjs`（**先自检**：`tests[]` 非空且文件真实存在、`doneWhy` 非空 ⇒ 不满足就拒绝写盘；再**追加**而不覆盖 `doneWhy`；写 `history{kind:status}` 记明为什么现在能关）⇒ 8 张全部 `done`，看板 **done 140 → 148 / doing 14 → 9**。★替代口径写进每张票的 `doneWhy`：`tsc -p tsconfig.json` + `-p tsconfig.test.json` 均 exit 0、本票守卫全绿、四份台账 `--validate` + 七份生成物 `--check` 全绿、`check:dead-writes` 无新增（基线 13）。★5 张票原先**没有 `tests[]`**（`T-0152`/`T-0153`/`T-0155`/`T-0159`/`T-0160`）—— 我从它们的交回报告里填了守卫文件（每个都先确认文件真实存在），并**没有**为过关而编造用例名 |
| 2026-09-24 | 主 agent **新开 `T-0175`**（P2，**跨文件/跨宿主交接清单**）—— 让"谁还欠谁"可查询 | 动机：那批"别人该接"的项原先只写在各自的 `tickets/T-XX/changes-*.md` §4/§5 里，**不会被任何机械检查扫到**，后续读者容易漏；而继续把原票挂 `doing` 会让看板失真。`T-0175` 把它们集中成 **12 条 acceptance**（每条给「文件 + 改什么 + 判据 + 原始出处」，不重复技术细节）：`engine.ts` 的 `scriptEngineFlag` 初值/复位与三态机极性、消息窗**行淡入色窗接线**（`msgwin.ts` + `animWindow.ts`）、**宿主媒体/触摸/呈现缝**（`confirmSlotOverwrite`/`slotWriteFailed` 进桥、`0x25b` 加载 + 抛异常、`setTextureObject*` **拆缝**）、`nodeFileSource.copySaveSlot` 漏复制 `源槽只有 .STH` 时的那份 ``textureCache.setSlotPixels` 静默忽略 + DestroyQueue 不可测、`src/live2d/**` 的 `+16`/`+23` 眨眼、`session.ts` 屏障②可观测 + `presenter.ts` 四路同键次序、`0x345` 失败支抛错、Pixi 端 `mulColor` 当 tint、`0x137`/`0x30a` 越界诊断（需先给 `operandPlan` 声明计划）、两处测试注释漂移、`toFullWidth` 两份实现去重。★每条都要求"回到出处文件核实现状"（很多项可能已被后续波次顺手做掉）。看板 **172 → 173 张**（done 148 / doing 9 / open 14 / dropped 2） |
| 2026-09-24 | 主 agent 修掉两处**过期措辞**（抄写员 B 的超范围发现） | `test/opcode-operands.test.ts:56`（`0x325`）与 `:57`（`0x326`）以及 `test/opcode-gaps.test.ts:100-101` 的注释仍写「emulator **无 Effect3D 子系统**」—— 而 `T-0163` 已把 `stubs.ts` 的口径改成「Scene 模型侧自 `T-0167` 起已有 Effect3D 半边，缺的是 **opcode→模型**那一半」⇒ 三处同步（**只改注释、条目本身仍需要**；每处保持单行以免顶掉 `T-0082` 锚在 `opcode-operands.test.ts:142` 的行号）。改后 `opcode-operands`+`opcode-gaps`+`op-underun-fixups`+`operand-plan` **22/22 绿**、`tickets --validate` ✅ |
| 2026-09-24 | 主 agent **关掉 `T-0165`**（P1「字符串族不转十进制」根因票）—— 三判据逐条核过后才关 | 判据 1（`readStringOperand` 按引擎 tag 分派）：**已落地**（int/指针族 `_itoa_s` 十进制、float `%lf` 精度 6、local-string 走解析期解出的 `a.str`），且 `T-0162` 补上了**三条取串原语的分叉**（`sub_41B640`/`sub_42A420` 每条数值路径都过 `sub_41A6C0` 全角化，`0x1B2` 走的 `sub_41B9B0` **不**过 ⇒ 必须半角）。判据 2（三族各一条守卫 + 两个语料锚点）：**已落地** —— `test/op-string-coercion.test.ts` 的 `0x192`（int/指针/立即数都转十进制、不再回槽号）/ `0x193`（复刻 `src/COMMITDR.txt:9`）/ `0x1B2`（复刻 `src/SYSTEM4.txt:464`，断言**半角**）+ 浮点族与"字符串族原样取串"对照；另 `op-1b2-text-buffer.test.ts` 覆盖缓冲累加、`T-0162` 新补 `operand-string-primitive.test.ts`(9 例) 与 `operand-float-ptr-io.test.ts`。判据 3 按 §6 替代口径。⇒ 看板 **done 148 → 149 / doing 9 → 8**，`ticket-ledger` 6/6 绿 |
| 2026-09-24 | 主 agent **亲自做掉 `T-0175` ④**（并发现它其实**已经修好**） | 先写体证式守卫再判：`T-0159` §4.2 报的「`copySaveSlot` 里 `if (d) writeSaveSlot(...)` ⇒ 源槽只有 `.STH` 时**漏复制**」**在现盘已不成立** —— 现在是两条**独立**写（`if (d) …DAT` / `if (t) …STH`，返回 `{dat: d!==null, sth: t!==null}`），与引擎 `0x1AC` 的两次独立 `CopyFileA`（`sub_42E0A0` raw 38498-38512）**同口径** ⇒ 本条=**已落地、只缺守卫**。新增 `test/arch-copy-slot-sth-only.test.ts`（**4 例全绿**，用隔离的 base/overlay 临时目录）：① 只 `.STH`（★判据：目标必须真的拿到 `.STH`，不许被「源没有 `.DAT`」**连带门掉**）② 只 `.DAT`（不凭空造缩略图）③ 两份都在（**逐字节相同**）④ 两份都没有（`{false,false}` 且两处都不落文件）。★同时把**更深一层的遗留**写进该票 `notes.md`：`writeSaveSlot`/`writeSlotThumb` 返回 `void` ⇒ **写失败无法上报**，返回值只反映**源侧是否存在**、不反映目标侧写是否成功；与引擎完全对齐需给这两条写路径（含 `OverlayDir.write`）加成败返回。★这条再次演示了"**报回来的缺陷要先复核再动手**"：报告里的那行代码已经不存在了 |
| 2026-09-24 | 主 agent 清掉两处**过期测试注释**（`T-0175` ⑪） | ① `test/engine-field-store.test.ts:112-113` 的 `0x2E9` 注释写「消费端（自动翻页时长）emulator **尚未实现** ⇒ 见 T-0076」—— 而 `T-0151` 已落地 `armCoexistAutoMessage` + `#autoMessageInterval`、`T-0161` 又加了"生产读者棘轮" ⇒ 改成"**已落地**；仍未建模的是**第二个计算点** raw 20416-20426（被 `Engine[122501]` 门控、「行数 − 基准」**不减 1**）"。② `test/ptr.test.ts:110` 的注释把 ptr0 写成 `dest`、ptr1 写成 `src` —— 而 `T-0162` 读体订正了 `0x1B0` 的方向（`memcpy(dest = addr(op2), src = addr(op1), 4*op3)`，raw 37991-37995）⇒ 改成正确方向并注明"本用例两指针同址、两种口径下断言都成立；用不同地址区分方向的新守卫见 `test/operand-memcpy-direction.test.ts`"。两文件都先跑 `--anchors-in`（都是 0 条锚点），改后 `engine-field-store`+`ptr`+`arch-copy-slot`+`opcode-operands` **17/17 绿** |
| 2026-09-24 | 主 agent **派第 5 个单元**：`T-0111` 判据 ①（它唯一剩下的判据） | 三件事：**(a)** 8 条「语料 0 处」的 `仅映射` 行（`0x105`/`0x2CA`/`0x309`/`0x339`/`0x026`/`0x144`/`0x2FD`/`0x22E`）逐条**读体**后二选一：补语义（`status → 已核对` + 一句话语义 + raw + **自己数出来的语料计数** + emulator 现状，未注册的还要给缺口条目）或转 `deferred`（带语料计数 + "为什么不影响"）；**(b)** `0x1D3` 的 handler 锚点统一（票面记「表 `sub_42D4A0` vs 真源 `sub_457960`」，要读体判定谁是 handler、谁是被调体）；**(c)** **全量对账**：现盘所有 `status: 仅映射` 的行 ⟷ emulator 运行时三表（`OPS`/`NATIVE_OPS`/`ENGINE_INTERNAL_OPS` + 各族表），列出"文档仍 `仅映射` 但运行时已注册"的过期行。产出 = 可机械应用的补丁 + 自证 dry-run（落库归我）。★prompt 里明确要求"**不许凭空说语料 0 处**，必须自己用 `Select-String` 数并把命中数写进台账" |
| 2026-09-24 | ★**抄写员 D 交回，主 agent 按它给的顺序一把落库**（`T-0158` §3 + `T-0161` §3） | 顺序与字节变化：`ledger-set --plan plan-ledger.json`（12 ops + 6 条 appendEntries）⇒ `capabilities` +3611、`opcode-gaps` **+9274**、`opcodes` +1083；`apply.mjs` 追加 `fields.json` **+4 条**（`draw_mode`/`auto_message_baseline`/`dispatch_saved_flags`/`msg_media_mode`）与 `functions.json` **+10 条**；随后 `build-opcode-gaps.mjs` 回填 counts ⇒ 缺口台账 **138 条 / partial 77 / 缺口 142**、有据 no-op 9 / 已实现 33 / deferred 19；七份生成物全部重跑 + 四份 `--validate` ✓。★它自带**两份独立自证**（`verify.mjs` 复算计划 + `rawcheck.mjs` 抽查 **11 条 `missing.raw` + 31 处关键行**），并且**明确警告了三件我必须在落库前后处理的事**（见下三行） |
| 2026-09-24 | ★主 agent 收 `T-0158`/`T-0161` 报告里的**三处"照抄就会红/破坏 JSON"**（抄写员 D 的发现，逐条已修） | ① **`raw` 必红格式**：`T-0161` §3.1 的 `0x142` 片段写 `raw: "22591,17961"`，而棘轮要求 **`^\d+(-\d+)?$`（单一段）** ⇒ 拆成两条单段（`17961` / `91057-91061`），构造点 22591 写进 `what`。② **全角引号**：`0x148` 片段用**全角引号**包 ASCII 双引号内容 ⇒ 照抄会提前闭合 JSON，已改写。③ **`implemented` 不许带 `missing[]`**：`0x201` 升 `implemented` 并删 `missing`；而 `0x147` 的 host-invented 只是"最接近的可表示行为" ⇒ **保持 `partial` + 1 条 missing**（不升）。另：`dispatch_saved_flags`(0x5D88C) 与现盘既有条目**同格重复**（未新增）、`msg_media_mode` 的「0x25A 写 1 raw 33193」应为 **33196**、片段里「byte 383836」与自身换算 383116 矛盾（用了 383116） |
| 2026-09-24 | ★主 agent 修掉**三处"字段里塞了说明文字"**导致的 schema 红（都是我落库后才被 `--validate` 抓到） | ① `scene-norender-mode` 的 **`emulator.evidence`** 被写成 `E1 · src/engineConfig.ts:235（…）· test/engine-config.test.ts:160（…）` —— 该字段**只接受枚举** `E0/E1/E2/E3` ⇒ 改回 `E1`，理由落 `journal[]`。② `msgwin-line-fade-window` 的 **`emulator.guard`** 被写成 `test/draw-item-anim-window.test.ts（动画窗机制）· 缺：消息窗行接线（T-0161 登记）`（说明文字当路径）⇒ 改成**空 guard**（本条确实没有守卫）并把"该补什么守卫"写进 note；同时它**声称 `E2` 却没有 guard** ⇒ evidence 改回 `E1`。③ `msgwin-cancel-key-state` 的 guard 被写成**两条**用 ` · ` 拼接 ⇒ 改成单条真实文件 `test/config-t0161.test.ts`（#锚点写进 note）。⇒ `capabilities --validate` **✅ 141 条**、`capability-ledger`+`doc-model`+`opcode-gaps`+`script-ledger`+`ticket-ledger` **34/34 绿**。★**教训（第三次同型）**：**"报告里的建议值"不是台账值** —— 建议常把"改什么 + 为什么"写在一行里，落库时必须**逐字段按 schema 拆开**（枚举归枚举、路径归路径、理由归 note/journal）。这条已与「`add` 必须按点路径取值」一起，构成本轮对"抄写→落库"这条流水线的两条硬规矩 |
| 2026-09-24 | 主 agent 记下一项**待裁决**（抄写员 D 提，未擅自新造条目） | 现盘 `opcode-gaps.json`（138 条）里**没有** `0x91/0x92/0x93/0x94/0xff/0x108/0x10d/0x2e5/0x2eb/0x2fc/0xa2/0xa3/0xd9` 中任何一条 ⇒ `T-0158` §3.3 的 13 条处置建议里 **11 条"无处可改"**；抄写员只把 `T-0158` 自己写明"新开 partial"的两条（`0x93` 的 `sub_403DD0`、`0xff` 的读者）整条 append，其余 11 条**故意不新造**。⇒ 我的裁决：**先不批量新造**（那需要逐条重读体判 disposition），改为在 `T-0175` 里加一条 acceptance：**逐条核实这 11 个 opcode 是否仍有"已注册但缺分支/消费端"的实情** —— 多数（`0x91`/`0x92`/`0x10d`/`0x2e5`/`0x2eb`）已被 `T-0158`/`T-0171`/`T-0161` **真修**⇒ 无需条目；少数（`0x2fc` 无触摸缓冲、`0xa2`/`0xa3` 的野跳偏差、`0x108` 的宿主时序补偿）可能确实该登记。 |
| 2026-09-24 | ★**抄写员 E 交回，主 agent 落库**（`T-0162` §3/§4/§5 的台账） | 顺序：`check-fresh.mjs`（**并发新鲜度护栏**，✅ 通过）→ `ledger-set --plan plan.json`（**14 ops**：`opcode-gaps` 的 434(0x1B2) 改判 `implemented→partial` + `missing×3` + `journal`，`opcodes.json` 的 **13 条 `semantics`**；**`appendEntries` 3 条**：432/402/403 整条）⇒ `opcode-gaps` **+7867B**、`opcodes` **+5280B**；→ `apply.mjs` 追加 `functions.json` **+3 条**（`sub_41A6C0` `toFullWidthAscii` / `sub_41B9B0` / `sub_42A420`）+ 补 `0x41B640`/`0x42D150` 的 `notes`（+5230B）；→ **再应用可选 `plan-extra.json`**（其余 **15 条** `sub_41B640` 宿主的同型 `semantics`，+6014B）⇒ `opcodes.json` 累计 **+11294B**。生成器全部重跑 + 四份 `--validate` ✓ + **47/47 守卫绿**。缺口台账现在 **141 条 / partial 81（缺口 152 条）/ implemented 32 / 有据 no-op 9 / deferred 19** |
| 2026-09-24 | ★主 agent 做掉 `T-0175` ⑫（`toFullWidth` 两份实现去重） | 读体确认两份**逐字节相同**（`handlers/msgwin.ts` 的 `toFullWidth`（`0x205` 逐字直绘用）与 `operand.ts` 的 `toFullWidthNumber`（取串全角化用））⇒ 合并为**唯一一份**：`operand.ts` 里 `export function toFullWidthAscii`（按引擎符号名改名，并写明"谁要过这道、谁不要由**取串原语**决定：`sub_41B640`/`sub_42A420` 都过、`sub_41B9B0`（只有 `0x1B2`）不过"），`msgwin.ts` 改成 `import { toFullWidthAscii as toFullWidth } from '../operand.js'` 并删掉本地副本（**别名保留 ⇒ 调用点零改动**）。动机写进注释：两份一旦漂移，症状是"同一个字符在 `0x205` 与 `0x192` 下宽度不同"——极难察觉。实测：esbuild 解析 OK、`op-string-coercion`+`operand-string-primitive`+`adv-msgwin`+`t0151-msgwin-vm` **73/73 绿** |
| 2026-09-24 | ★主 agent 按抄写员 E 的**机械复扫**订正 `src/vm/operand.ts` 头注里 5 处**错误计数** | E 指出 T-0162 报告里的「全库经这三条原语读串的共 **30** 条 / 其余 **23** 条走 `sub_41B640`」**统计错了**（实测 **29** / **22**：`sub_41B9B0` 1 条 + `sub_42A420` 6 条 + `sub_41B640` 22 条 = 29），而这个错**已经抄进了源码注释**（`operand.ts` 的 5 处，含表头 `sub_41B640（23 条）`）⇒ 用带**前置断言**的脚本逐处替换（每处必须恰好 1 次，否则拒绝写盘），改成 29/22。★**教训**：报告里的统计数字也会被"抄进源码注释"从而**获得第二次生命** —— 凡是写进代码注释的计数，都要在写之前自己数一遍（或至少标注它是报告口径） |
| 2026-09-24 | 主 agent 记录抄写员 E 报的 **15 处不符**中最该知道的一条 | ★**`0x7D` 也全角化**：`T-0162` §3 说"调用点全库 8 处"但只枚举了 **7 处**，漏掉 raw **25625** —— 那处在 `sub_41A780` 里（= **`0x7D` 的十六进制原语**，`_itoa_s(..., 16)` 之后）⇒ **`0x7D` 的 hex 输出同样过全角化**。这条对本工程有直接后果：`0x7D` 现在登记为"未实现（命中即硬停）"，将来照 `0x6E` 落地时**必须记得它也全角化**（否则又是"同一个字符两种宽度"）。其余重点：缺口① 的 `handlerBodyLine` 惯例应取函数**标记行**（37984 而非 37985）；缺口② 的 raw `36394-36411` 挂 `0x1B2` **不成立**（那是 `sub_42A420` 的区间，而 `0x1B2` 走 `sub_41B9B0`）⇒ 已改用 `26538-26547`；`sub_41A6C0` 的字符域体里是 **`48..90` 整段**（含 `: ; < = > ? @ [ \ ] ^ _`）+ `97..122`，不止 `0-9/A-Z/a-z`（对 emulator 是**收窄**，已登记）；★另**推翻我的简报一处**：`functions.json` **有** `journal` 先例（实测 36/592 条），所以三条新函数的沿革写进了 `journal[]` 而非 `notes` |
| 2026-09-24 | 主 agent 做掉 `T-0175` ⑤ 的后半（把"换尺寸 ⇒ 旧纹理入队"这条接线变成**可测的决策**） | 读体确认 `create()` 里那段决策是一个复合 `if`（`old && old.w===cw && old.h===ch && old.res===res && typeof document!=='undefined' && w>0 && h>0`），而它**只能在有 DOM 的环境里跑**（要 `document.createElement` + `PIXI.Texture.from`）⇒ Node 测试里 `old` 永远是 undefined，**"同尺寸复用 / 换尺寸把旧纹理推入 `DestroyQueue`"从来没有断言**（`T-0166` §4-② 点的就是这条）。处置：抽成**纯函数** `decideCreateTexture({had,w,h,res,hasDom})`（三态 `reuse-clear` / `replace` / `fresh`），`create()` 改为按它的返回值走；新增 `test/texture-create-decision.test.ts` **7 例**（同尺寸同 DPR+DOM ⇒ 复用；尺寸变 / DPR 变 / **无 DOM** / `w`或`h` 为 0 ⇒ `replace`；无旧表面 ⇒ `fresh`；`\|0` 归一与 `max(1,·)` 的口径）。★**我第一版有一例断言写错**（以为 `w=0.4` 会因"尺寸归一为 1"而 replace，实际 `w > 0` 成立 ⇒ 仍是 reuse）—— 是**测试自己抓出来的**，说明这组纯函数断言真的有判别力（不是恒真）。★**仍未覆盖的那一格如实保留**：真正 `#pendingDestroy.push(old.tex)` 只在真宿主走到 ⇒ 现在断言的是**决策**、不是 push 本身（要断言 push 需注入假 canvas 工厂 + 假 PIXI Texture，代价大于收益）——写进 `T-0175/notes.md` |
| 2026-09-24 | 主 agent 做掉 `T-0175` ⑫（`toFullWidth` 两份实现去重）—— 已在上一段详述，此处只记结论 | 两处**逐字节相同** ⇒ 合并为 `operand.ts` 的 `export function toFullWidthAscii`（按引擎符号名改名，注释写明"谁要过这道由**取串原语**决定"），`msgwin.ts` 改成 `import { toFullWidthAscii as toFullWidth }` 并删本地副本（**别名保留 ⇒ 调用点零改动**）。esbuild OK、四个相关守卫文件 **73/73 绿**。★顺带发现：`src/renderer/pixi/textureCache.ts` 被 `T-0029`/`T-0050`/`T-0051`/`T-0062`/`T-0102` 共 **6 条** evidence 锚着 ⇒ 我的插行让它们全体偏移 ~52 行（`--validate` 警告 24→30）⇒ 用 `fix-lines.mjs --any` 一轮刷完（本轮共刷 **30 条**）⇒ 票据锚点再次归 0 警告。★**流程提醒**：往被锚文件里**插函数**（不是改字串）也会让锚点漂移 —— 插完必须跑一次 `fix-lines --any` |
| 2026-09-24 | 主 agent 派 **`T-0164`**（跨模块其余指令，P2 5 / P3 21）—— ★它的 P2 五条**全部落在空闲文件上** | 逐条核过文件归属后确认可开：`0x20f`（play-movie 的影片槽建/复用 + `effectFlags \|= 0x2000`）与 `0x23d`（释放 slot 42..999）在 `handlers/gfx-misc.ts`；`0x19d`（缺键时缺省应为引擎内建 `set:SaveVersion1 = 1`）在 `handlers/resource-usage.ts`；**`0x60`（引擎随机数是 CRT `rand()` 进程级 LCG，emulator 用 `Math.random()` ⇒ 同存档两次跑随机流不同、破坏 `T-0005` 的 G3 回放判据）**在 `handlers/arithmetic.ts`；`0x21d`（`CopyScene` 的顶点缓冲创建/加锁两条失败路径）在 `renderer/pixiBackend.ts`。⇒ 此刻 **4 个单元在跑**（`T-0156`/`T-0154`/`T-0164` + `T-0111` ①）。★prompt 里点名 **3 条只登记不动手**（`src/vm/engine.ts` 的 `0x14b`/`0x14d` 与 `engineFieldIds.ts` 的 `0x2f6` —— 都归 `T-0156`），并把"写进注释的统计数字也要自己数一遍"这条教训写进了要求 |
| 2026-09-24 | ★★**`T-0111` 判据 ① 交回并落库 ⇒ 该票三条判据全闭合、已关单**（看板 done 149 → **150**） | 落库 **17 处改写**：`opcodes.json` **9 条**（8 条 `仅映射 → 已核对` + 语义 + journal；另 `0x222`）+ `opcode-gaps.json` **3 条 note 追加**（`0x026`/`0x144`/`0x2FD`：补**自数的语料计数** + "为什么不影响"）+ **新增 5 条 `unimplemented`**（`0x105`/`0x2CA`/`0x309`/`0x339`/`0x22E`）。生成器按它给的顺序跑：`build-opcode-table`（**恰好 9 行变化**）→ ★**`scripts/asm/build-opcodes.js`**（它同时写 `scripts/asm/opcodes.json` 与包内 `src/generated/opcodes.json`，`opcode-json-sync` 要求两份**逐字节相同** —— 这条依赖我此前不知道，是它点出来的）→ `build-opcode-gaps`（回填 counts）。守卫 `doc-model`/`opcode-gaps`/`opcode-arity`/`opcode-operands`/`operand-plan`/`opcode-json-sync`/`capability-ledger`/`script-ledger`/`ticket-ledger` **47/47 绿**。缺口台账现在 **146 条：未实现 5 / 有据 no-op 9 / 已实现 32 / deferred 19 / partial 81（缺口 152）** |
| 2026-09-24 | ★★**全量对账的数字（本轮最有价值的产出）**：`仅映射` 168 条里**只有 1 条**是真过期 | 现盘 `opcodes.json` **544 条**，`status == 仅映射` = **168 条**；与 emulator 运行时三表（`OPS` 299 / `NATIVE_OPS` 54 / `ENGINE_INTERNAL_OPS` 13，并集 **366**）逐条对账 ⇒ **「文档仍 `仅映射` 但运行时已注册」恰好 1 条 = `0x222`**（dec 546；体 raw 31923-31933 → 真身 `sub_4B4460` raw 136968-137285；语料 10 处/10 文件；emulator 早就是 `OPS` 的 `op_scene_commit_range`）⇒ 已改 `已核对` + 语义。**其余 167 条是真的没注册**（命中即 `NotImplementedOp` 硬报错，不是"文档过期"）。⇒ **「文档 vs 运行时口径不一致」这一项归零**。★它**刻意没有**把 167 条批量转 `unimplemented`（那会造 160+ 条噪声，且它们全是语料 0 处）—— 这条判断我认可：判据要的是"逐条三选一"，不是"批量改状态位"。★它另提一个**下一轮观察（未开票）**：剩下的是「**167 条语料 0 处的 age-shared 指令该不该逐条读体**」（其中 160 条语义全空）⇒ 更像一条新的 `analysis` 票，而不是继续挂 `T-0111` |
| 2026-09-24 | ★主 agent 收 `T-0111` 报告里**推翻票面两处 + 订正三处**的结论（都有读体证据） | ① ★**「语料 0 处」对 3 条不成立**：`0x026` = `SC0000.txt:13127`（1 处）、`0x144` = `SAVE.txt:560`（1 处）、`0x2FD` = `ALLMAP:361`/`CGVIEWER:94`/`FIELD:1286`/`REIGN:468`（4 处）—— 三种口径（`Select-String` / 自写扫描 / 真源 `scanCorpus`）一致；★**票面自己又在别处记着"语料 4 处：ALLMAP/CGVIEWER/FIELD/REIGN"**（在册 `0x2FD` 的 note 里）⇒ **同一事实两处自相矛盾**。后果：这 3 条不是"排期"问题而是"宿主缝不存在"问题 ⇒ 处置必须留在 `deferred`（已按此落库）。② ★**`0x1D3` 的 handler/被调体被票面写反了**：派发槽实读 raw 22875 `*(_DWORD *)(_this + 677864) = sub_42D4A0;`，`sub_42D4A0`（raw 38111-38127）读 op5/op4/op3 后调 `sub_457960(_this+21324, &v7, v2, v5, v6)` 并写回 op1/op2 ⇒ **`sub_42D4A0` 是 handler、`sub_457960` 是被调体**，现盘真源本来就对 ⇒ **不改任何字段**。③ `0x2FD` 的"5 个 float 操作数"说法误导：`sub_42AEA0`（raw 36755-36960）是**按操作数类型取指针** ⇒ op2..op5 是**输出缓冲指针**（`sub_41BF50` 只读 int），只有坐标值是 float。④ `0x105` 的语义只能写到"读某个标志"：`_this[122246]` **全库只有读、无写者** ⇒ 语义无法再往前推（这条本身就是有价值的事实）。⑤ `0xDD` 的订正**独立复核通过**（`_this + 676880` 全库 0 命中、`fallbackDefault` 与语料 `^idd\b` 都不含它）⇒ 与 `T-0163` 一致 |
| 2026-09-24 | ★主 agent **再次代做"阻塞全体"的一字修复**（`T-0156` 在飞文件里的重复导入） | `src/vm/handlers/control.ts` 第 13 行 `import type { Engine, Frame } from '../engine.js';` 与第 30 行 `import { QUEUE_INT_SLOTS, type Engine } from '../engine.js';` **都导入 `Engine`** ⇒ `tsc -p tsconfig.json` 报 `TS2300: Duplicate identifier 'Engine'`（第 13、30 行）⇒ **任何 agent 跑全树 typecheck 都会看到它、`verify` 不可能绿**（它在飞期间我至少两次看到这条）。修法：第 30 行改为 `import { QUEUE_INT_SLOTS } from '../engine.js';`（保留第 13 行的类型导入）⇒ `control.ts` 报错清零（随后的报错全在 `T-0164` 在飞的 `handlers/gfx-misc.ts`，属预期）。★**但这次的"告知 owner"没送出去**：`send_message` 对 `T-0156` 两次返回 `subagent ... is unavailable`（它状态仍是 `running`，可能正在长工具调用里、消息队列暂不可投递）⇒ **改走 CONTEXT 留痕**：如果 `T-0156` 交回时发现它又把 `type Engine` 加回第 30 行，请按本行再修一次；同时提醒它每次改完自己文件立刻跑一次 `tsc -p tsconfig.json`。★累计教训（第三次"阻塞全体"）：**一个 token 的导入冲突能让全树 typecheck 变红、并被每个 agent 误当成"别人的红"** ⇒ 发现即修 + 留痕，比等 owner 更省 |
| 2026-09-24 | 主 agent 派 **第 4 个 D 波单元**：Live2D **眨眼（`+16` `EyeBlinkMotion` / `+23` 门控）** | 这是一条**已登记**的缺口（不是新发现）：`analysis/engine-capabilities.json` 的 `live2d-node-draw-advance` note 明写"**真正的剩余缺口 = `+16`/`+23` 眨眼未建模**"（出处 `tickets/T-0166` §4-①；引擎 `sub_4783D0` raw **92578-92615** 的 `+23` 分支；`src/live2d/` 全库**无 blink 符号**），`T-0175 acceptance ⑥` 也点了同一条。文件范围 = `src/live2d/**` + `handlers/live2d.ts` + 新测试（**不碰** `src/renderer/**`，那里两个单元占着）。prompt 要求：读清眨眼链**每环带 raw**、`+23` 的**极性与来源不许猜**、先跑红再改绿、并要求它给出 **`live2d-node-draw-advance` 那段"剩余缺口"该怎么改写**的可照抄片段（含要不要新增 `functions.json`/`fields.json` 条目）。⇒ 此刻 **4 个单元在跑**（`T-0156` / `T-0154` / `T-0164` / `T-0166-blink`） |
| 2026-09-24 | ★主 agent **审计"已交回单元的台账待应用队列"⇒ 确认为空**（用 6 项机械抽查佐证，不是凭记忆） | 逐票核过 `T-0151`/`0152`/`0153`/`0155`/`0158`/`0159`/`0160`/`0161`/`0162`/`0163`/`0166`/`0111`/`0170`/`0108`/`0112`/`0145` 的"台账待应用"节**全部落库**。抽查（`node -e` 直读真源）：① `scene-norender-mode`（抄写员 D）= `fns 14 项` / `raw 11132-141165` / `evidence E1` ✓；② `0x246`（抄写员 B）= `missing 2 条` + note 写「仍缺 **2** 条」（此前是"仍缺 3 条"与 `missing 2` 不一致）✓；③ `functions.json` 的 `0x41A6C0`/`0x41B9B0`/`0x42A420` 三条（抄写员 E）都在且 `status=ANALYZED` ✓；④ `0x222` = `已核对` + 1060 字语义（`T-0111` ① 唯一那条"真过期"）✓；⑤ `unimplemented` 5 条 = `0x105/0x2ca/0x309/0x339/0x22e` ✓；⑥ 仍为 `仅映射` 的条数 **168 → 159**（`T-0111` ① 把 8 条 + `0x222` 共 9 条补成 `已核对`）—— **算术自洽**。★这条审计本身值得记：在"多单元并行 + 台账集中结算"的模式下，**"队列是否为空"必须机械抽查，不能凭记忆**（本轮我差点把已落的当成待落） |
| 2026-09-24 | 主 agent 重写 `CONTEXT.md` **§8**（原先新旧条目混排、编号 1-4 与 5-8 重复） | 现在是一份**可续跑的执行清单**：① "已交回并全部落库"的波次清单 + **"台账待应用队列为空"的结论**（带上面那次抽查）；② 在跑 4 个单元；③ **它们交回后我要做的 a–d**（落库两条硬规矩 / **`0x20f` 的 SKIP 豁免** / 生成器全套 + `fix-lines --any` / `test:all` 复核 + `close-wave.mjs` 收票）；④ **下一波可开的票**（`T-0167`/`T-0157`/`T-0169`/`T-0173`/`T-0019`，D 波一交回文件就全空）；⑤ `T-0175` 的现状（已做 ④/⑤后半/⑪/⑫）；⑥ 需外部条件与需用户裁决的票；⑦ **待开票的观察**（167→159 条语料 0 处的 age-shared 指令该不该逐条读体 ⇒ 更像一条新 `analysis` 票）；⑧ **在飞期间的越界观察**（`T-0164` 为实现 `0x23d` 的宿主缝同时改了 `src/renderer/headlessScene.ts` +79 行 —— 该文件无人认领、且两个宿主必须同口径 ⇒ **接受**，并写下"将来把'宿主缝必须两宿主同改'写成单元范围模板"的建议） |
| 2026-09-24 | 主 agent 核 `T-0156` 对 `test/input.test.ts` 的改动（**它越过了我给的清单**，但判为**合理的最小 retarget**） | 它把 TITLE 那条 `sleep` 断言改了：`sleep(0xC8)` 的实参是 **1**，而引擎 `sub_4218D0` raw **30304** 只在 `v3 >= 10` 时进"帧节流计时器"支、`n < 10` 走 raw **30311** 的 `Sleep(v3)`（**同一次派发内的进程级硬阻塞**，不置 `effect_flags \|= 0x20000000`、不装计时器）⇒ 旧断言「sleep 应置 `SLEEP_GATE` / 应设 `sleepUntil`」的**前提被体推翻**（语料 385 处里 **51 处**落在 `n<10` 支）。它的做法符合纪律：**保留节流支（`n>=10`）的覆盖**（同段用 `sleep 1000` 的合成样本）+ 写明因果 + 引 raw。★这条同时解释了本会话早前那一族 `op-10-002` 红的来源，值得记进 §9：**"`sleep` 有两种语义"是 `0xC8` 的 P2 行（`T-0156`），不是 T-0151 的回归** |
| 2026-09-24 | ★主 agent 收口自审：**我自己新加的两个测试文件触发 R1 分类头棘轮** ⇒ 立刻补上 | `test/organization.test.ts` 的 R1 要求**每个测试文件首行**是 `/** @tier T? @kind ? @subsystem ? */`。我在 `T-0175` ④/⑤ 里新建的 `arch-copy-slot-sth-only.test.ts` 与 `texture-create-decision.test.ts` **都缺这个头** ⇒ 连带顶红 R1 + 「轴的取值白名单」+「默认档有实质覆盖」三条（它们统计的是"有头的文件"）。修法：各补 `/** @tier T0 @kind core @subsystem save\|texture */`（都用临时目录/纯函数 ⇒ 不需要真游戏资源 ⇒ **T0** 才诚实）。⇒ `organization`+`harness-convergence` 的通过数 **15 → 18**，我自己那两个文件的红清零。★**教训**：新建测试文件时**第一行就得写分类头**（`kind ∈ core\|ratchet\|tool`、`subsystem ∈ frame\|vm\|ops\|adv\|text\|render\|texture\|transition\|l2d\|save\|audio\|config\|input\|host\|ledger\|tool`）—— 这是本仓库最容易漏的"入场券" |
| 2026-09-24 | ★主 agent **逐条归因剩下 3 条仓库级棘轮红**，并**指名通知了 owner** | 三条红全部落在**在飞单元新建的测试文件**上（不是台账/文档的）：① **T-0020 棘轮「不得新增自造 `mk()`/`ctx` 变体」** = `live2d-blink.test.ts` + `t0156-control-frame.test.ts` + `t0164-misc-batch.test.ts`；② **「基线只许收缩」** = 同三个文件把基线撑长了；③ **R2 档位诚实**（声明 T1 但机械判据看不出资产依赖）= `live2d-blink.test.ts` + `t0164-misc-batch.test.ts`。处置：给 `T-0164` 与 `T-0166-blink` 的 owner **各发一条指名消息**（要素：改用 `test/harness.ts` 的工厂、**不许**改基线、档位诚实二选一 T0 / 机械可见的资产依赖、首行格式与枚举值、改完跑哪条命令到绿）。★**`T-0156` 的消息第三次投递失败**（`subagent unavailable`）⇒ 改为在 CONTEXT 留痕：它新建的 `t0156-control-frame.test.ts` 需要同样处理，下一位接手者请补发/直接按上面三条修。⇒ **在飞单元的四条红全部有名有姓**（另见 §6） |
| 2026-09-24 | 主 agent 复核 **`0x20f` 的守卫红已自行消失** ⇒ 确认我给 `T-0164` 的处置建议被采纳 | 上一轮我实测到 `test/opcode-operands.test.ts` 报「`0x20f` 在合成指令上抛错」（因为它给 `0x20f` 补了引擎那两条"抛 C++ 异常"分支）。本轮实测该文件 **1/1 绿**，且 `0x20f` **仍是注册的 `op_play_movie`**（`OPS.size` 299 未变）⇒ 说明它把抛错条件收在了**合成夹具到不了**的路径上（而不是把抛错删掉、也不是把 opcode 撤注册 —— 两种"假绿"都没有发生）。同时实测 `analysis/opcodes.json` 仍为 **159 条 `仅映射`**、`unimplemented` 仍 5 条 ⇒ **台账侧没有被在飞单元污染**。★我已要求它在报告里说明具体怎么处理的，以便复核 |
| 2026-09-24 | ★**第 40 轮（目标轮上限）收口实测** | 四份台账 `--validate`：tickets **173 张 / 5 条警告**（全部指在飞 `src/**`）、capabilities **141**、scripts **35**、opcode-gaps **146 条**（未实现 5 / 有据 no-op 9 / 已实现 32 / deferred 19 / partial 81 / 缺口 152）—— 全部 ✅；七份生成物 `--check` 全 ✓。**已完成单元相关的守卫 15 个文件 76/76 绿**（doc-model / capability-ledger / script-ledger / ticket-ledger / opcode-gaps / opcode-arity / opcode-operands / op-underun-fixups / operand-plan / opcode-json-sync / registry-classification / texture-create-decision / texture-lifecycle / arch-copy-slot-sth-only / op-string-coercion）。**唯一红组 = 上面那条仓库级棘轮**（3 条，全在 4 个在飞单元新建的测试文件上，已指名通知/留痕）。★判绿口径：**基线 3 条 + 在飞单元的 4 条红**，其余全绿 |
| 2026-09-24 | ★**`T-0166` 眨眼单元交回 ⇒ 主 agent 一把落库（3 份真源，六处改写）** | 该单元读体（含反汇编）证明：`+16` = `live2d::EyeBlinkMotion`（`sub_4BC3E0` 建，104B）、门 `+23` **极性 = 非 0 即真**（`.lst` 00478429 `cmp byte ptr [esi+17h],0` + `jz`）而**来源只有构造时那一条 dword 存** `mov [esi+15h], ebx`（`ebx = 0`，`.lst` 00478309，把 `+21..+24` 一并清 0；**Hex-Rays 的 `_DWORD` 视图里 `+23` 整个不出现** ⇒ 只读 `.c` 会误判成未初始化堆字节）+ 此后**全库 0 写者** ⇒ **这一支在随包游戏里恒不执行**，`blinkEnabled` 缺省 `false` 是**引擎真实行为、不是未建模**。它仍按体把整条链建模并接进 `l2dAdvance`（`src/live2d/blink.ts` 新 214 行 + `mtn.ts`/`runtime.ts` + `test/live2d-blink.test.ts` 新 321 行；红→绿 = 插 `return null` ⇒ 0/3，恢复 ⇒ 3/3；11 个既有 L2D 文件 **77/77**；`src/renderer/**` 零改动）。落库六处：① `live2d-node-draw-advance` 的 `emulator.note` **只换尾句**（删「仍未建模 `+16`/`+23`」，换成「眨眼那一支已建模 + 门恒 0 是引擎行为 + 唯一剩余缺口是无 E4」）；② 同条 `guard` → **`test/live2d-blink.test.ts`**；③ 同条 `engine.fns` **补 `sub_4BC550` / `sub_478270`**（现 6 项）；④ 同条 `journal` +1；⑤ `lazy-live2d-slot` 的 note 补一格「`+16` 的眨眼态也一并回缺省（`newBlinkMotion()`；引擎 `new(0x4C)` + `sub_478270` 清 `+23`）」+ journal +1；⑥ `functions.json` **新增两条**（`0x4BC3E0` live2dEyeBlinkMotionCtor / `0x4BC500` live2dEyeBlinkNextTime，后者是 `dbl_52E310` 的唯一读者）+ 给 `0x4783D0`/`0x478270` **各追加一段 notes**（把「`+23` 的清零点只在反汇编里」这条**从 `.c` 读不出来**的事实写进第一层）；`fields.json` **新增三条**（`+16 eyeBlinkMotion` / `+23 eyeBlinkGate` / `EyeBlinkMotion+84..96 blinkTimings`，经 `sort-fields.js` 归位）。⇒ `functions 605→607`、`fields 376→379`，四份台账 + 七份生成物 + **66/66 守卫全绿**。★**一处我自己造成并当场修掉的错**：我用「全局替换」改 guard 时**误伤了 `lazy-live2d-slot`**（它也用 `live2d-chain.test.ts`）⇒ 变成 `… + …` 这种非法路径、`capabilities --validate` 报「guard 不存在」⇒ 立刻用 `ledger-set` 按 id 定点修回（`lazy-live2d-slot` 恢复单条、`live2d-node-draw-advance` 才拿新守卫） |
| 2026-09-24 | ★主 agent **修掉最后一个非在飞红**：`arch-copy-slot-sth-only.test.ts` 的档位谎报 | 我上一轮给它写 `@tier T0`，而 R2 的机械判据在它的 **import 行**里看见 `import:nodeFileSource` ⇒ 报「声明 T0 但有真资产证据 ⇒ 应改声明为 T1」。★**它其实真的不需要真资产**（全程用 `mkdtempSync` 临时目录）—— 但仓库内**同类文件的既有约定就是 T1**（`save-delete-slots.test.ts` / `append-packs.test.ts` / `agf-size.test.ts` 全都 import `NodeFileSource` 且都是 T1）⇒ 按约定改成 `@tier T1` 并在报告里说明这是**约定而非需要**。⇒ `organization`+`harness-convergence` 通过数 **8/10 → 10/10**（只剩下面那一条） |
| 2026-09-24 | ★主 agent 定位**全树最后一条红**：`t0156-control-frame.test.ts`（`T-0156` 在飞、消息不可达 ⇒ 留精确配方） | `test/harness-convergence.test.ts` 的两条断言（T-0020 + 「基线只许收缩」）**同根**：该文件自己写了一个 `function mk(calls): Fixture`，里面**手搓** `new Engine(new StubNative(() => {}), new InputManager())` + 整份 `ScriptBinary`。扫描口径是纯**名字正则**（`/function mk\(\|const mk = \|function mkEngine\(\|function makeCtx\(/`，实现 `test/harnessScan.ts:16`）。⇒ 配方：改用 `test/harness.ts` 的 **`mkEngine(ops, 'T0156.BIN')`**（它做的正是同一件事；★且 `Engine` 的 ctor 是 `(native, input?)`、`input ?? new InputManager()` ⇒ 那里显式传的 `InputManager` 本来就是多余的），把本地 helper 改名（例如 `fixtureFor`）并让 `Fixture.bin` 取自引擎的当前脚本（`e.curScript().bin` 一类）。★**发现一条仓库自相矛盾**：guard 的报错文案给的逃生口是「加进 `harness-convergence.baseline.json` 的 `mkVariants` 并写清为什么」，但**同文件另一条断言写死「基线只许收缩」**（`now.mkVariants.length <= base.mkVariants.length`）⇒ 对**新**文件这条逃生口是关的 ⇒ **唯一解就是改用 harness**。★我**没有**动这个文件：`T-0156` 仍在跑且**消息三次投递失败**（`subagent unavailable`），改它可能把它自己的红→绿证据搅乱；等它交回（或下一位接手者）按上面配方改即可 |
| 2026-09-24 | ★**`T-0154` 交回 ⇒ 主 agent 落库它的台账（P2 10 / P3 16 的引擎层结论）** | 落库两处：**`opcode-gaps.json`** —— `0x1f6`(502) 改写 `missing[130763-130766]`；`0x223`(547) **删** `missing[132603-132615]`（missing 清空 ⇒ `partial → implemented`）；`0x229`(553) 改写 `missing[117080-117090]`（raw 收到 `117080-117112`）；`0x244`(580) 改写 `132436-132441`（→ `132428-132501`）；`0x320`(800) **删三条**（`41037-41040`/`41073-41077`/`122409-122412`）+ 改写 `132727-132758`（原判"重建先析构 ⇒ 几何被清、bit0 归 0"**被推翻**：析构在 `sub_4ADFE0` 体内，进门是 `sub_432150` 的 `if (v2 > 0)` ⇒ `vcount <= 0` 既不析构也不清 bit0）；`0x20d`(525) **删两条** + 改写 `117375-117380`（→ `117375-117423`）；`0x321`(801) **删** `missing[17798-17805]`。**`engine-capabilities.json`** —— `render-3d-layer-dual-commit` 的 `name`/`trigger`（原 name"对偶逐帧提交"失真：`sub_4B4460` 的唯一调用点就是 `0x222` handler raw 31933，逐帧提交是 `sub_4B4040`）；`scene-flag-46528-bits` 的 `trigger`（bit2 极性原来写反：体是 **bit2 == 0 才清零冻结值**）；`bullet-dirty-from-freeze-or-pending` 的 note（冻结那一半改成**无条件置脏**，与 raw 136718-136719 的"8 字节窗口一起判、无 per-item 条件"逐字一致）；`scene-draw-total-gate-1056` 的 `reads`（补 1052/1860/1112/1116）+ `emulator` 段收成 modeled-verified/E2/新守卫；`scene-slot-release-1000` 的 `status→absent` + `evidence→E0`（note 自述"完全没有"与 partial 自相矛盾）；`world-matrix-identity-refresh` 的 note 半段替换（区间门已建模 + 残留 + ★新披露 **0x22A/0x22D 与 0x22C/0x22F 抢同一个矩阵格**：目标块靠动画窗到期的 `qmemcpy` raw 117496/117646 才进 current）；`lazy-transition-map-node` 的 `fns` 去掉 `sub_4AA190`（它是**只读查找**，不插入）+ `raw` 补 `130197-130236`。★为落这一批给 `.tmp/settle/ledger-set.mjs` **加了 `mutate` 能力**（按 `raw` 删/改数组项，作用于 `missing[]`），因为这份台账的排版不是标准 `JSON.stringify(·,2)` ⇒ 不能整体重写。结果：缺口台账 **partial 80 / implemented 33 / deferred 19 / 有据 no-op 9 / unimplemented 5（缺口 145）**；能力台账 **已核验 78 / 部分 29** |
| 2026-09-24 | ★**我在落库过程中犯了三个错，全部由自己的断言当场抓住并修掉** | ① **误翻处置位**：我的收尾脚本第二次运行时用「`opcode: 547` 之后第一条 `"disposition": "partial"`」定位 ⇒ 547 已是 `implemented`，于是翻掉了**下一条**（`0x1ba`）⇒ 它变成"`implemented` 却带 `missing[]`"。审计脚本（`非 partial 却带 missing`）当场列出它 ⇒ 已恢复 `partial`。★**教训**：改台账**必须按条目边界定位**（先切出该条目文本段再在段内找），**永远不要用"下一条匹配"代替"本条匹配"**。② **ASCII 引号**：我在 note 里写了 `按"四块叠加"近似` ⇒ 直接把 JSON 串提前闭合（`JSON.parse` 在写盘前抛 `position 60212`）⇒ 靠"先解析再写盘"这条断言**没有写坏真源**；改成「四块叠加」。③ **标记写漏一个字**：world-matrix 的真实标记是 `★仍属近似的只有两处`（带「的」），我写成 `★仍属近似只有两处` ⇒ 命中 0 次、脚本拒绝写盘（又是断言救的）⇒ 用真实标记重做 |
| 2026-09-24 | ★主 agent **retarget 掉 `T-0111` ① 造成的那条外部红**（`op-22a-22f-scene-xform.test.ts` 的「unimplemented 清零」） | `T-0154` 报它时把它归为"外部红"，我核后确认**根因是我**：那条断言写死「台账里不再有 `unimplemented`」，而我按 `T-0111` ① **故意**登记了 5 条**语料 0 处**的未注册指令（口径 = 命中即 `NotImplementedOp` **硬报错**，非静默）。⇒ 按"前提被取代 ⇒ 最小 retarget"改，而且改成**更强**的两条：① `unimplemented` 集合必须**恰是**那 5 条（`0x105/0x2ca/0x309/0x339/0x22e`）——增删都要人过目；② **每一条都不得进三张运行时表**（`OPS`/`NATIVE_OPS`/`ENGINE_INTERNAL_OPS`）——这才是"未实现"的本义，**旧断言反而允许"注册了却算未实现"蒙混过去**；`deferred` 必须带 note 那条原样保留。⇒ 该文件 **11/11 绿** |
| 2026-09-24 | ★**收口实测：102/102 守卫全绿、票据 0 警告、四份台账 + 七份生成物全 ✓** | 本轮最后一遍：`doc-model`+`capability-ledger`+`script-ledger`+`ticket-ledger`+`opcode-gaps`+`opcode-arity`+`opcode-operands`+`opcode-json-sync`+`operand-plan`+`op-22a-22f-scene-xform`+`op-22a-22f-scene-world`+`scene-t0154-scene-state`+`scene-t0154-mesh-transition`+`live2d-blink` = **102/102 绿**（含 `T-0154` 的 24 例新守卫与上一条的 retarget）。`fix-lines --any` 又刷 **15 条** ⇒ 票据回到 **0 条警告**。四份 `--validate` 全 ✅、七份生成物 `--check` 全 ✓。**唯一剩下的红仍是那一条**：`test/harness-convergence.test.ts` 点名的 `t0156-control-frame.test.ts`（`T-0156` 在飞、消息不可达；配方已写在 §9 的上一条里） |
| 2026-09-24 | ★★**收口实测（第二轮）：`test/run.ts fast` 全绿** | `cd app/amayui-emulator && node --import tsx test/run.ts fast` ⇒ **tests 1209 / pass 1208 / fail 0 / skipped 1**（exit 0）—— 这是本会话第一次让**快速档全绿**（此前每一轮都有在飞单元造成的红）。同时 **14 个守卫文件 102/102 绿**、票据 **173 张 / 0 条警告**、四份 `--validate` + 七份生成物 `--check` 全 ✓、`fix-lines --any` 无需再刷（0 条漂移）。工作树 **293 个文件改动 / 218 files in `git diff --stat`（+14594 / −2201）**。★注意到 `test/harness-convergence`+`organization` 也 **10/10 绿** ⇒ 连 `t0156-control-frame.test.ts` 的"自造 `mk()` 变体"那条也在 `T-0156` 自己的迭代里被修掉了（它此前把基线撑长的那两条断言随之归零）⇒ **当前唯一的红集合 = 空**（`test:all` 的 3 条基线红另计，见 §6） |
| 2026-09-24 | ★**`T-0156` 交回（D 波 · 控制流/帧管理/脚本装载）** | 实现+取证完成：**P2 七条全落地**（`0x7c` 深度校验改无条件 raw 25798-25807；`0xc8` 两支语义拆分 + `Sleep(0)` 不再被抬成 1ms；`0xae` 版本门三规则；`0x3` 的 `ShowMessageError`(码 65537 raw 26776-26783) + 装载失败 `ExitScript`(码 2 raw 26794-26798)；`0x9` 的 exit-script 重建 **10** 个 Queue_int（raw 18080-18109）+ `op1=0xA` 拒绝越界（raw 18110）），守卫 `test/t0156-control-frame.test.ts` **20 例，红→绿 = 7/20 → 20/20**（用 `git show HEAD:` 取修前源码）。**最小 retarget 仅一处**：`op-132-134-queue.test.ts` 的「队列族规模 11 → 10」（raw 18081/18111/22658 的 `v15 = 10; do{…}while(v15-- == 1)`）。★**它的报告与我的独立核查一致**：`test:all` 的第 4 条红（`input.test.ts` 的 L2D `0x34e`）**A/B 证明与它无关**（HEAD 版测试在当前源码上逐字同错）⇒ 转 `T-0176`。★六份真源它**一个都没动** ⇒ 已派 **Scribe F**（`5d249bed`）逐条回体核实 §4.1-§4.4 后落库。★读体新条目里有一条**归属纠正**值得记：`sub_41C7C0`（含 `if (v4>=40)` 深度门）是 **`0x6`** 不是 `0x8`；**`0x137`/`0x138`** 才是 Stack_int 族（`sub_4222B0` raw 30704-30730 / `sub_4223A0` raw 30733-30750），而 **`0x13E`/`0x13F` 是作用在 Queue_int 容器上的 TOTALQ/check-bit**（`sub_42FAC0`/`sub_42FB40`） |
| 2026-09-24 | ★★**主 agent 定位并修掉「第 4 条红」⇒ 开单 `T-0176`（P2，已 done）** | **症状**：`test/input.test.ts` 的 TITLE 端到端例抛 `L2Dモーションファイル TITLE.MTN の読み込みに失敗しました（opcode 0x34e：槽 0 不存在或槽里没有模型）`。**定位**（四步实证）：① HEAD 版测试在当前源码上 **pass 13 / fail 1** ⇒ 与测试改动无关、属 `src/` 变化；② 逐步 trace 证明 TITLE 在 `ip=418` 执行 `i34e` 前，`src/TITLE.txt:544-547` 那轮 20 次 `call-script` **全被 `jcc` 跳过**（`global 708ab6` 无写者）；③ 手工铺 `708ab6 = 0x522d` 也不够 —— `SETL2DMOC` 本体在 **append pack** 里，`call-script` 它报「拡張ファイル情報ファイル 72 は読み込まれていません．」；④ 真链路走法 = `INIT2.txt:115` 灌表 + `TITLE.txt:533-547` 预设 `f8c46=0x4f9e`/`f8c47=0` ⇒ `SETL2DMOC.txt:22-28` 的 `i341 4f9e <槽>` + 三条 `i345`。**判定**：`T-0160` 把 `0x341`/`0x34E` 的失败改成按引擎 `_CxxThrowException` 抛 `ShowMessageError`（raw 34482-34491 / 34722-34731）是**对的**，红的是**测试前提不全**（旧实现静默吞掉 ⇒ 掩盖了"槽 0 必须先有模型"）。**修法**（`src/` 一字未动）：测试内跑一段合成前置 `i341 4f9e 0` + `i345 4f9f/4fa0/4fa1`（= `SETL2DMOC` 对 TITLE 做的事），并断言 `l2dSlots.get(0)?.modelId === 0x4f9e` ⇒ **14/14 绿**。**顺带纠正一条我早前的判读**：`jcc` 的签名是 `jcc cond 真支(=-1 落下句) 假支`（`operandPlan.ts:1136`）⇒ `TITLE.txt:527` 的 `jcc (global-int a9d0) ffffffff label_00002058` = **`a9d0 == 0`（默认）走 Live2D、`!= 0` 走静态贴图回落**（与 `test/live2d-enabled-flag.test.ts` 的 2×2 断言一致）——此前笔记里"`a9d0 != 0` 走 Live2D"是**反的**。**台账**：`analysis/scripts.json` 新增 `SETL2DMOC`（stub，含 TITLE 支形状 / `f8c46`/`f8c47` 输入槽 / 扩展包那条坑）⇒ 脚本台账 **35 → 36 条**，`scripts.js --validate` ✅、`build-scripts.mjs` 重生成（顺带补上 6 份 md 的陈旧计数头与 2 处早前未重生成的正文） |
| 2026-09-24 | 主 agent 顺手修了 `ledger-set.mjs` 的一个**硬缺陷**（防下次落库白撞） | `appendEntries` 的去重键原先**硬写 `opcode`** ⇒ 对 `functions.json`（键 `addr`）/`fields.json`（键 `offset`）**恒判"已存在"而拒写**。改成按该文件**既有条目的自然键**推断（`opcode`/`addr`/`offset`/`id`），并支持 `appendKeys` 覆盖；头部注释同步。★这条是 `T-0156` 的落库（9 函数 + 12 字段 + 2 能力）必须走的路 |
| 2026-09-24 | ★**`T-0164` 交回（D 波 · 跨模块其余指令）并自结 `done`** | P2 五条全落地：① **`0x20f`**（raw 31667 `effectFlags \|= 0x2000` + raw 31668 `_this[675972] = 1`；op3 按 raw 31655 `sub_4054D0` 位解码 `0x10000/0x20000/0x40000/0x80000 → 0/1/2/3`，四位都不置时取 `set:DependMovieSound` **原值**，再按 raw 31656 `sub_405460` 映射音源 BOOL —— ★模式 1 是 `sound:Music >= 0`、**不是** `!= 0`）；② **`0x23d`** 的宿主缝 `releaseMovieSlots` 两个宿主都缺 ⇒ 实现在 `headlessScene.ts` + `pixiBackend.ts`（槽 **42..999**：析构影片对象 + 解绑槽→imgid + CTexture 表面；**不删** `slotSize` —— 那是 `0x208` 的答案缓存，`T-0153` 的 `op-23f` 守卫钉了它）；③ **`0x19d`** 的 `cfgInt` 缺省值改用 `registryDefault(CFG.setSaveVersion1)` = **1**；④ **`0x60`** 用真 MSVCRT LCG（`seed*214013 + 2531011; (seed>>16) & 0x7FFF`，初值 1、模块级状态）替换 `Math.random()` + 导出 `reseedCrtRand()` 供"模拟进程重启"；⑤ **`0x21d`** 钉"三表各自独立"、顶点缓冲两条错误路径（依赖真 D3D VB）**如实登记**。红→绿：`t0164-misc-batch.test.ts` **2/13 → 14/14**、`opcode-operands.test.ts` **0/1 → 1/1**。★**它没用我给的 `0x20f` SKIP 豁免**：改用**可选宿主缝** `NativeBridge.hasSlotTexture?(slot): boolean \| undefined`（`undefined` = 宿主不建模 ⇒ **不抛**，与修前逐字相同；`false` ⇒ 抛 `asc_520248`）⇒ `opcode-operands.test.ts` **一行未动、也不需豁免**。越界三处（`native.ts` +39 / `nativeTap.ts` +7（`BRIDGE_METHODS += 'hasSlotTexture'`）/ `headlessScene.ts` +92）**已复核接受**（P2 的宿主缝不给实现等于没修）。★它的两处**表混同纠错**要落台账：`Scene/54708` 是灯光 **enabled 位**、与 `Scene[13687]` 的 26-dword/条灯光**记录**不是同一张表（审计原文混成"数组"）；`Engine/365288` 与 `378688` 也是两张不同的表。★我已复核它的清单：`git diff --stat` 的 7 个 src 文件 = **+433 / −28**，与它报告的数字**逐字一致** |
| 2026-09-24 | ★主 agent 复核 `T-0164` 落地后的全绿面（`tsc` ×4 + 29 个守卫文件 + `test:all`） | `tsc -p tsconfig{,.test,.control,.electron}.json --noEmit` **四个 exit 0**；守卫电池（29 文件，含 `t0164-misc-batch` / `native-tap` / `op-222-scene-commit` / `no-dead-writes` / `op-23f`）**214/214 绿**；`test:all` → **tests 1544 / pass 1538 / fail 4 / skipped 2**，4 条红 = **3 条 `T-0146` 基线**（engine-slot 真槽 / save-slot 真槽 format / scene-report 24 ≤ 26）+ **1 条固有 flaky**（`host-registry.test.ts` 的「真进程 `--idle-sec 1`」：连跑 4 次 = 红/绿/绿/绿，失败那次 223ms 就返回 ⇒ 子进程没在窗口内起来）。★把它记成 **flaky 而不是基线红**是有据的：`T-0156` 与 `T-0164` 各自做过 A/B（HEAD 红 / 实现单跑绿）⇒ **与本轮任何改动无关** |
| 2026-09-25 | ★**Scribe F 落库 `T-0156` 的三层台账（回体核实后）** | 落库：`functions.json` **607→616**（+9：`0x4223A0` Push / `0x407BD0` stackIntCtor / `0x453A60` frameTimerSet / `0x42FAC0` TOTALQ / `0x42FB40` check-bit / `0x42F990` ACQUIREQ / `0x422860` REWINDQ / `0x40EA00` frameRelease(PARTIAL) / `0x407C50` queueIntCtor）；`fields.json` **379→386**（+7：`0x5EC9C` queue_int_family / `0x5ECC4` stack_int_family / `0xCA88` display_a_cursor / `0x5EB64` list_finish_gate / `0x5EE04` frame_ret_stack_depth / `0xA30CC` redisplay_state_frame / `0xA537C` handler_table_B8，并对 `ScriptContext/0x34·0x38·0x4C·0x60` **4 条既有 meaning 按体改写**）；`engine-capabilities.json` **141→143**（`queue-int-family-reset-on-exit-script` modeled-verified/E2 + `script-request-queue-drain-dispatch` partial/E2）；`opcode-gaps.json` **146→151**（`124(0x7c)` missing 4→2、`3(0x3)` → **implemented**、`9(0x9)` missing 5→2、`306/307` 容器 11→**10**，新增 `200(0xc8)` partial / `174(0xae)` / `8` / `6` implemented / `5(ret)` partial）。命令全绿：两个计划 `--dry` 零 problem、`sort-fields.js`、`capabilities --recount`、两个生成器 + `--check`、三条守卫 **18/18**、`doc-model` 10/10。★**它的 16 条核实差异是本次最有价值的产出**，其中三条**主 agent 另作处置**：① `95804` 是**写**不是读（真读点是成对的 `95803`，8 处）——原报告把方向写反；② `51848` 不是"只写不读"（= **虚拟显示器 A 的鼠标游标**，读者 raw 20005/20013/20175/20286）、`387940` 有写者但两处都写 0（**死分支**不是缺口）⇒ 原报告「选择/列表框」的归属方向也不对；③ §4.4 片段用的是**另一套 schema**（`kind/status`）且 `raw` 双段（违反单段规则）⇒ 已按真源 schema 改写 |
| 2026-09-25 | ★主 agent 修掉 `analysis/opcodes.json` 里 `0x7C` 的**错误判据**（Scribe F 发现、不在它可写面） | 原文写「未建模的 `81776/81768/51848/51840/387940` 按**只写不读不建模**跳过」——按体不成立（见上条）。改成：五格未建模，其中 `51848` = 虚拟显示器 A 鼠标游标（读者 raw 20005/20013/20175/20286；界 = `Engine[23008]`＝raw 258；写者还有 raw 43703）、`387940` = 恒假门（写者 raw 22605/25818 都写 0）；未建模的真实理由是「消费端（虚拟显示器/列表子系统）本身没建」。★**踩坑记录**：我第一次改写时在 JSON 字符串里用了 ASCII 双引号 ⇒ `JSON.parse` 立刻抛（position 30180）⇒ 改用「」；修完 `build-opcode-table.mjs` + `--check` ✓、`doc-model` + `opcode-json-sync` **12/12** |
| 2026-09-25 | ★**Scribe G 出 `T-0164` 落库计划（只写计划、不写真源）⇒ 主 agent 应用** | 计划 = 4 处 `ops`（含 `mutate`：`96(0x60)` 删 2 条 missing + 改写 1 条、`541(0x21d)` 删 1 条 + 改写 1 条、`fields 0x77A5C agerc_export_table` 的 meaning+evidence、`functions 0x41A300` journal）+ 7 条新条目（`opcode-gaps` 新增 `527(0x20f)` partial / `724(0x2d4)` partial；`fields` +5：`0x5C740` movie_object_table / `0xD5B4` light_enabled_bits / `0xD5DC` light_records / `0xD9F0` render_state_22 / `0xA5084` movie_playing；`functions` +3：`0x4054D0` / `0x405460` / `0x4237B0`）。应用后：`opcode-gaps` **151→153**（partial 81→**83**）、`fields` **386→391**、`functions` **616→619**；`sort-fields.js` 后**按 scope 组内 0 断点**（`.tmp/settle/check-fields-order.mjs`）、生成器 `--check` ✓、`capabilities/scripts --validate` ✅。★**它的三条核实结论都改变了盘面**：① Scribe G 判定报告 §1#5「`dword_55052C` 一个读点都没有 ⇒ 引擎侧本来就是死全局」**按体不成立**（raw 45802-45810/45856/45886/45929/46134/46675/46776 是 `x / dword_55052C` 的格子除数读点；raw 5663 定义、raw 32711 是 `0x248` 唯一写点）⇒ 该判据作废（`0x248` 仍不动，但理由改成"宿主缝无对应物"）；② `fields.json` 的 `0x77A5C` evidence 写了**不存在的读点** raw 43055（那是 `SHGetFolderPath` 调试报告路径）⇒ 真读点 = raw 39839（`0x14D` handler `sub_430170`）；③ **它自己发现一条报告没提的真偏差**：`0x2D4` 的 C 体是 `fmod`（raw 40171）而 emulator 用 JS `%`（`arithmetic.ts:262`）⇒ 负被除数结果不同（`fmod(-7.5,2)=0.5` vs `-7.5%2=-1.5`），现有守卫覆盖不到 ⇒ 已作为 `0x2d4` 的 `partial` missing 落库 |
| 2026-09-25 | ★主 agent 订正 `tickets/T-0164/changes-c164.md` 里那条**错误判据** | 按 Scribe G 的读体证据改写 §1 第 5 行（原判据作废）+ 新增 5b 行写明「报告说 → 体里是」。★修复过程中我把第 6 行的行首误并进 5b ⇒ 当场按行结构检查抓回并复原（表格列数复核通过） |
| 2026-09-25 | ★**`T-0156` 置 `done`**（主 agent 收口） | 判据①②（实现 + 取证）达成；判据③「`npm run verify` 全绿」的本机替代 = `tsc` ×4 全 0 + 守卫电池 214/214 + 四份台账 `--validate` 全 ✅ + 生成物 `--check` 全 ✓；剩余红 = 3 条 `T-0146` 基线 + 1 条 `host-registry` flaky ⇒ 按"已知基线不计"口径置 done。另补一条 `--note` 更正计数笔误（准确值：+9 函数 / +7 字段 / +2 能力 / +5 缺口条目）。⇒ 看板 **174 张：done 151→153 / doing 8→7** |
| 2026-09-25 | ★★**D 波 + 台账落库收口实测** | 看板 **174 张（done 153 / open 12 / doing 7 / dropped 2 / blocked 0）/ 0 警告**；四份台账 `--validate` ✅（capabilities **143** / scripts **36** / opcode-gaps **153**：partial 83 · implemented 37 · 有据 no-op 9 · deferred 19 · unimplemented 5 / missing 140）；生成物 `--check` ✓；守卫电池 **214/214** + `native-tap`/`op-222` 10/10；`tsc` ×4 exit 0；`test:all` = 1544 例 / 1538 过 / **4 红 = 3 基线 + 1 flaky**。★**A 波时那张"在飞单元造成的红"清单已全部清零**（`live2d-blink` / `t0156-control-frame` / `t0164-misc-batch` 的自造 `mk()`、基线与档位三条棘轮都已由各自 owner 修掉） |
| 2026-09-25 | ★★**主 agent 把最后一条非基线红也修了 ⇒ `test:all` 只剩 3 条基线** | 开单 `T-0177`（P3，已 done）：`host-registry.test.ts` 的「真进程 `--idle-sec 1`」不是"固有 flaky"而是**读-刷次序竞态** —— 读体定位 = `src/web/host.ts` 先 `publishRecord()`（raw 941，**同步落盘**）再打启动横幅（raw 990），而测试等的是**注册表文件**、断言的是**管道累积的 `out`** ⇒ 「记录已出现」**不蕴含**「横幅已进 `out`」。修法 = 断言前单独 `await waitFor(() => /就绪 http:\/\/127\.0\.0\.1:/.test(out) ? out : null, 30_000)` + 自己的 `assert.ok`，**原 `assert.match` 与其余断言一字未动**（判据强度不变，只去掉未被保证的时序假设）。红→绿：修前 4 连跑 = 红/绿/绿/绿（失败样本 223~430ms 返回、`out` 里只有 `[web] 实例 …`）、修后 **14/14 ×3**。⇒ **`test:all` = tests 1544 / pass 1539 / fail 3 / skipped 2**，3 条红**全部**是 `T-0146` 基线（engine-slot 真槽 / save-slot 真槽 format / scene-report 24 ≤ 26）—— 这是本会话第一次让**全量档的红集合 = 纯基线**。★教训：两次子 agent 的 A/B 都只做到"这是 flaky"，**症状归类 ≠ 根因**；一条无法与真实回归区分的红会长期污染判绿口径，值得花十分钟读体定位 |


