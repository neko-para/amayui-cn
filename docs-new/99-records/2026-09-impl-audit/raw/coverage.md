# 覆盖账（机器生成）

- 未产出任何批的批次（0）：（无）
- 部分覆盖的批次（0）：（无）

| opcode | 批 | verdict | findings | verdictWhy |
|---|---|---|---|---|
| 0x1 | t01 | 一致 | 0 | 无操作数、无字段、无分支三条都成立；唯一的引擎副作用（抛 Command_Exit ⇒ 程序结束）在 emulator 有等价物（抛 ExitScript ⇒ 中止运行并关切主窗口），故判一致。 |
| 0x2 | t01 | 有缺口 | 3 | 三支语义、`-10` 现场还原、`-11` 的只读配置与分派表都被 emulator 覆盖（`tickets/T-0092`/`T-0125`）；缺口剩两处引擎事实：`v2 >= 0` 时引擎不直接切帧，而是（a）相邻返回先 `sub_40EA00` 释放被放弃帧、（b）目标帧没脚本时逐级下退、（c）`_this[66 |
| 0x3 | t01 | 有缺口 | 2 | 深度门槛（cur >= 39 ⇒ 报 40）、操作数读位、压帧（cur+1 与 caller/调用点）、装载与文件使用记账、装载失败即中止，这些都对得上；缺口只有「错误通道的种类」：引擎把「太深」做成可恢复的 ShowMessage（报错串），emulator 与装载失败一样抛普通 Error ⇒ 与 0x1/顶层 e |
| 0x5 | t01 | 有缺口 | 2 | 栈空 no-op、返回点存 dword 偏移并换算回下标、`v2 != -1` 才跳这三条一致；缺口是引擎的两条额外事实：返回点 == -1 时的哨兵语义（emulator 会抛错）、以及 `107437/107436` 这层全局末层栈 + `_this[174801]` 写回（emulator 无对应物，且全库**没 |
| 0x6 | t02 | 一致 | 3 | 可观测语义逐项对上：读两格、写 cur 备份/恢复、装载目标帧、越界保护都在；emulator 不建模 _this[388248]/[388236]/[388240] 三道前置门（引擎侧是宿主/版本门，与 op6 无关，另一个 handler 同族也一样没建模）⇒ 不单独开 finding；差异只在两处保护分支的形态（ |
| 0x8 | t02 | 一致 | 2 | 读/写/分支逐项对上：预装校验、caller 回填、ip 归零、cur 切换、调用方恢复位置（帧状态=3 ⇒ ip 前进 3 dword = 下一条，与 emulator 的 ip+=1 等价）。emulator 另加 frameIdx 范围守卫（引擎无），属更严的近似，语料 116 处帧号都合法 ⇒ 不单列 find |
| 0x9 | t02 | 有缺口 | 2 | 体里可观测的「写引擎字段」逐条对上了大部分（logoEnabled、帧数组、整体复位、重载脚本 0、失败即 Exit）：但有两处引擎副作用在重写侧没有对应——0x132/0x133/0x134 的队列容器在引擎 teardown 里被析构置 NULL 而重写侧不清（findings 1），体首那次 sub_419720 |
| 0x32 | t11 | 有缺口 | 1 | 十格读法与两组矩形的化开逐字对上，夹取的"按比例跟随 + `(int)` 截断"也由 `clampScaledBlit` 实现（0x207 同源）。缺的是**引擎真正走的那条支**：emulator 不读 `set:DrawMode`，恒走 `!= 0` 的 `sub_4A87A0`；而本机 `SYS4REG.INI` |
| 0x50 | s01 | 一致 | 0 | 六项判据逐条对应：① 读的操作数 = op2/op3（顺序也与体一致：先 3 后 2）；② 写的操作数 = 仅 op1，且读在写之前（op1 与 op2 同槽时也正确）；③ 无读写引擎字段（步长槽 `95805 = 7` 由 emulator 的 `operandCountSlotValue(0x50,3)=7` 表达 |
| 0x51 | s01 | 一致 | 0 | 读 op2/op3（顺序 3→2 与体一致）、只写 op1、无引擎字段、体无分支、ENC/DEC 由计划层读写门面承担、步长槽值 7 与 `operandCountSlotValue(0x51,3)` 一致 ⇒ 判一致。★语料里 `sub (local-ptr 0) 0 64` 这类「op1 是 local-ptr」的 |
| 0x52 | s01 | 一致 | 0 | 读 op2/op3、只写 op1、无引擎字段、无分支、DEC/ENC 由计划层承担、步长槽 7 与 `operandCountSlotValue(0x52,3)` 一致；`Math.imul` 的溢出回绕与 x86 `imul` 低 32 位逐位相同 ⇒ 判一致。 |
| 0x53 | s01 | 有缺口 | 1 | 读 op2/op3、只写 op1、C 截断语义（`Math.trunc`，T-0057 R4 已订正过 floored 版）、除零抛错而非静默得 0 三条都对；不一致只剩 `INT_MIN / -1` 这一个没有分支可循的硬件例外，见 finding（属「体里读不出的硬件行为」）。既有白名单「除数为 0 ⇒ 引擎抛除零 |
| 0x54 | s01 | 一致 | 0 | 读 op2/op3（顺序 3→2 同体）、只写 op1、C 截断/符号语义（`-5 % 3 = -2`，`(l % r) \| 0` 与 `idiv` 余数同值）、模数为 0 抛错四条都对；`INT_MIN % -1` 与 0x53 的 `INT_MIN / -1` 是**同一条 `idiv`** 的同一例外根因，已在 |
| 0x55 | s01 | 一致 | 0 | 读 op2、只写 op1（不含 op1 读，`io:['w','r']` 与体的覆盖写一致）、无引擎字段、无分支、步长槽 5 与 `operandCountSlotValue(0x55,2)` 一致；语料 472360 处里的 `mov (global-int …) N` / `mov (local-int …) N` |
| 0x56 | s02 | 一致 | 0 | 引擎的每个可观测项都有对应：读 op2/op3 ⟷ `p.int(2)/p.int(3)`、写 op1 ⟷ `p.setInt(1,…)`、`v3 & v2` ⟷ `l & r`（JS 的 `&` 即 ToInt32 结果，与 C 的 32 位 `&` 同口径）、无分支 ⟷ 无分支；类型口径也一致（写 float 型目 |
| 0x57 | s02 | 一致 | 0 | 读 op2/op3、写 op1、单条无分支的 `op2 \| op3` 逐项对上；JS `\|` 的结果已由 ToInt32 归 32 位，与 C 的 32 位 `\|` 同口径；float 型目标的双向隐式转换也一致。引擎帧槽 95805 的运行时写入在 emulator 由解析期 `index` 表承担（`test |
| 0x58 | s02 | 一致 | 0 | 被移值 op2、移位量 op3、写 op1 三项对上；emulator 的 `>>` 是 JS 算术右移（操作数 ToInt32、结果 int32），引擎是 C 的 `int` 算术右移，负数与超 31 位移（x86 低 5 位 vs `& 31`）两支都等价；`char` 截断只影响高位、而高位不进移位量 ⇒ 无可观 |
| 0x59 | s02 | 一致 | 0 | 读 op2/op3、写 op1、`<<`（含超 31 位移的 x86 低 5 位口径 = `& 31`、以及 `\| 0` 的 32 位回绕）都逐项对上；语料 `shl (global-int f7fff) 1 (global-int f8000)` 的立即数移位量 1 更是无歧义。引擎帧槽 95805 的运行时写由  |
| 0x5a | s02 | 一致 | 0 | 语义（值相等 ⇒ 1/否则 0）、位数（op2/op3 读、op1 写）、整数比较口径（emulator 显式 `asI32` 两侧，与 C 的 `int == int` 一致）全对上；无遗漏分支。 |
| 0x5b | s02 | 一致 | 0 | 与 0x5a 同形只差取反：读位、写位、0/1 结果、有符号 32 位比较口径全对上，无遗漏分支。 |
| 0x5c | s02 | 一致 | 0 | 有序比较**有符号**（emulator `asI32` 两侧 ⟷ C 的 `int` 比较），方向 `op2 < op3`（⟷ `v3 < v2`，v3=op2、v2=op3）一致；读/写位与 0/1 结果无缺口。 |
| 0x5d | s02 | 一致 | 0 | 有符号 `<=`、方向 `op2 <= op3`、读 op2/op3 与写 op1 全部逐项对应；语料 `lte (local-int 0) (global-int f801d) b` 的两侧都是 int 型操作数，无类型歧义。 |
| 0x5e | s02 | 一致 | 0 | 有符号 `>`、方向 `op2 > op3`、写 op1（0/1）逐项一致；语料 `gr (local-int 0) (local-ptr 0) 0` 的 op2 是指针解引用，emulator `readIntOperand` 对 `TYPE_LOCAL_PTR` 亦解引用取所指值，两边同口径。 |
| 0x5f | s03 | 一致 | 0 | 引擎可观测项逐条对上：读 op2/op3、写 op1（0/1）、argc 3、有符号 int 比较；emulator 的 asI32 对应体的 `int` 语义、p.int 对声明 int 的操作数遇到 float 型抛 PlanKindError（对应体的 Type_Exception）、无引擎字段/消费端/被调副作 |
| 0x60 | s03 | 有缺口 | 2 | 主路径（argc 2、读 op2、写 op1 = rand()%op2、值域含 0）一致；但对差后有两处真实分叉：mod==0 分支**少了引擎在抛之前那一次 op1=0 的写**（全局 int 槽在引擎里是持久池、会被加密落盘，见 sub_42B4B0 case 3），以及 RNG 用宿主 `Math.random( |
| 0x61 | memory | 一致 | 1 | 读序（op3→op2）、写目标（op1 指针池）、元素步长（int/float 4、string 28）与引擎体逐项对应；op1 在引擎体里确实**不被读**，emulator 也只写不读。语料核对：143885 处 `lookup-array` 的 op1 **全部是指针型**（`local-ptr`/`local- |
| 0x63 | memory | 一致 | 0 | 取址语义、写目标（op1 指针池）、无 ENC/DEC 三点与体一致；语料 173 处样本（`src/ADDEXP.txt:77` 的 `lea (local-ptr 0) (local-ptr 2)`、`src/ADDEXP.txt:78` 的 `lea (local-ptr 1) (global-int 5469e |
| 0x64 | memory | 有缺口 | 2 | 引擎把池里的原始槽位做一次 `ROL4(key ^ ROR4(v,7),21)` 后**存裸值**；emulator 把同一批原始值经 `writeRef` 存成 `ENC(v)` ⇒ 后续任何按 int 槽解引用读回都得到 `DEC(ENC(v)) = ROR(key ^ ROL(v,11), 25)`，只有 `ke |
| 0x6c | memory | 一致 | 0 | 读操作数（op1 取址 + op2 个数）、写内容（ENC(0)）、零个数短路三点均对应；语料形态核对：1322 处 op1 均为 `global-int`/`local-int`/`global-string`/`local-ptr` 这类“基址型”操作数，没有出现“op2 被当值用”的用法。 |
| 0x6e | t17 | 有缺口 | 2 | argc/操作数（op1 槽、op2 字符串）、体①的 `flags \|= 0x10000`、LABEL_11 的节流门（`MessageSpeed == 0 或 ADV 已置 ⇒ 不装门`）与 else 支的「置 0x20000000 + 起计时器」在 emulator 都有对应且已按体订正。缺口在两处引擎可观测项 |
| 0x6f | t17 | 有缺口 | 2 | op1 的读、0⇒默认窗（`endLine` 内部用 `resolveWin(i)`，`emitWin` 也 `resolveWin`）与「按窗独立」都对得上。缺口是 `sub_46AF90` 在非注音支**必然**做的两件事：当前文本记录的笔位推进（`-20`/`-16`/`-4`/`+71`）与 gate ≥ 0  |
| 0x70 | t17 | 有缺口 | 2 | 5 个操作数的读、`op1=0 ⇒ 默认窗`、w/h/x/y 的落点（含 `+36/+40` 当换行边界）、以及末尾**不过门**的回看页 push + 组首标记都与体一致（`T-0095` 已订正顺序/门）。缺口在体里另外两类可观测项：`+20/+24`（w/h 的另一组副本）与 D3D/GDI 互斥支里的 `+12 |
| 0x71 | t17 | 有缺口 | 2 | op1 的读、`0⇒默认窗`、清该窗文本记录+复位显现游标（`beginNewMessage`）、记账门 `textBaseGate >= 0` 才 push 回看页 + 置组首（`T-0095` 已订正）都对得上。缺口是体的其余可观测项：`122496 = 0`、`122455` 的置 1/清 0、`174801 \ |
| 0x72 | t17 | 有缺口 | 3 | argc、`0x80000000` 等待门、bit30 武装与 `107704 = 0`、`sub_45A940` 的字格数查询、`!122496 && !(mask & 0x40)` 这条早退条件（emulator 用 `m.alt === 0 && m.skipMirror === 0`）都对应上了。缺口：①3 个 |
| 0x73 | t17 | 有缺口 | 2 | 十个操作数的读、`op1=0 ⇒ 默认窗`、`win+88 = 1` 总门、`win+92/96 = op9`（`0x72` 的模数来源）、`win+60/64` 原点、`win+80/84`、`win+68/72` 与 op10 节拍（含 0⇒1）都在 emulator 有对应。缺口：①`Engine[166964] |
| 0x74 | t17 | 一致 | 0 | 引擎可观测项只有「读 op1」「写 `Engine[21668]`」两项，emulator 一一对应，且都**不写配置注册表**（与 `0x1B5` 区分）；无分支需要对应。 |
| 0x75 | t17 | 有缺口 | 1 | op1 的读、`Font+201684` 这一格的语义与「不重绘」都对应；但引擎还写 `Font+1232`/`Font+101972`（= -op1）两格、在 `Font+201680 == 0` 时再写 `Font+1236`/`Font+101976`（= op1/-2），emulator 这四格都没有对应物（渲 |
| 0x76 | t03 | 一致 | 0 | 逐项对差成立：① 读 op1（计划 `declarePlan(0x76)` 的 argc/类型与体一致）；② 写 `_this[21664]` ↔ `ENGINE_FIELD.colorFill = 21664`，值的 BGR→RGB 口径与体逐字同构（`bgrToRgb` 的三段移位就是体那行）；③ 分支：体无分支， |
| 0x77 | t03 | 一致 | 0 | 与 0x76 同型：读 op1（1 位 int）、写单一字段 21665 ↔ `ENGINE_FIELD.colorOutline = 21665`、同一 BGR→RGB 变换、无分支；被调 `sub_459F40` 的字体重建属宿主渲染物（emulator 的描边色由 `msgwin.ts` 的 `globalFon |
| 0x78 | t03 | 一致 | 0 | 读 1 位 int、写单字段 21667 ↔ `ENGINE_FIELD.outlineMode = 21667`、写原值不重排、无分支 —— 全部对应；引擎体也不钳位（`& 3` 出现在 emulator 的读取端、不是写入端，不会与「存原值」冲突：只有 0..3 有绘制语义，其它值在引擎里同样落进 `*(Font+ |
| 0x79 | t17 | 一致 | 0 | op1..op3 的读、`0 ⇒ 默认窗`、`+28 ← op2` / `+32 ← op3` 的落点（不是旧注所说的「子项」）与两个字段的消费端（排版起点）都对得上；体里没有其它可观测项（无分支可漏、无错误处理）。emulator 额外调一次 `emitWin` 发布该窗文本，而引擎只写字段不重绘 —— 这是发布时机 |
| 0x7a | t18 | 一致 | 1 | 读格数/顺序/类型、op1=0 的默认窗回退、两个 dword 的落点（该窗文字块原点）在 emulator 都有对应且消费端已接（styleOfWin → 排版 originX/originY）。引擎那一格是「缓冲头」而下一条排版会把 buf[-20] 复位成 obj[28]（raw 82684-82685 `*(_ |
| 0x7b | frame | 一致 | 0 | 两个操作数的类型（int）、顺序（op1→主游标、op2→备用游标）与两个写入目标字段都逐一对应；两个读者（0x199 与主循环 raw 14001-14008）在 emulator 侧分别由 op_redisplay_text 与帧循环承担。 |
| 0x7c | frame | 有缺口 | 3 | 模式门（0x2000000）与 ip 回跳、effectFlags 还原、redisplayMode 清零四条主路径一致；两处缺口：① 深度校验在 emulator 侧有不校验的空档（want===-1），② 三个状态字段与队列收尾派发未建模。`51848` 在引擎侧有真实读者（raw 20005/20286），所以「 |
| 0x7f | t18 | 一致 | 0 | 唯一可观测项（回写 op1 = `Engine[21668]`）在 emulator 有对应，字段来源同为 `message:MessageSpeed`（引擎 raw 23736-23738 把该配置灌进 `Engine+86672`，emulator 的 ENGINE_FIELD.messageSpeed 同源，且带 |
| 0x80 | t18 | 一致 | 0 | 读 1 格、写该格到「默认窗」字段、以及该字段的消费端（所有 `op1 ?: 默认窗` 的回退）在 emulator 都有对应；引擎体无分支可漏。语料 10 处（`CALLBACK_LOAD.txt:82` 等 `i080 8`/`i080 1`、`CONFIG.txt:42` `i080 9`）全部落在 emulat |
| 0x82 | t18 | 有缺口 | 4 | 越界门（①）与 `op3 & 2` 的设色（③）已接，读格数与类型与引擎一致；但 `sub_466000` 体里另外四条分支（②专用路径、④bit6 的起始下标偏移、⑤bit4/5 的 `sub_4ABB60(win+276,win+280)`、⑥bit0 的记录过滤）以及「从第 a3 条记录起重画」的粒度在 emul |
| 0x85 | text-items | 一致 | 0 | 逐项对上：① argc 核验 N=1 与计划 `[0x85, 0, [], []]` 同（argc=0 ⇒ operandCountSlotValue=1，与体一致；`frame.operandCount` 只作报告，ip 推进用下标制）；② 读操作数：两边都零格；③ 写操作数：两边都零格；④ 引擎字段：两边都清同一对 |
| 0x88 | t18 | 一致 | 0 | 读 1 格、写 1415/97050 两格、以及非 0/为 0 的两条分支在 emulator 都有逐项对应（`skipMirror`=1415、`skipMode`=97050、`skipping`=122368、`clearAdv`=清 `0x8000000`）。复核过 122368 的其余读者/写者（引擎仅 ra |
| 0x8b | t03 | 一致 | 0 | 逐项对差成立：① 读 op1；② 写 `_this[21669]` ↔ `ENGINE_FIELD.lineSpacing = 21669`（原值、无 BGR 重排 —— 与 0x76/0x77 的区别在 emulator 里也保住了：本条 `map` 无 `transform`）；③ 无分支；④ 本条**没有** ` |
| 0x8c | t02 | 一致 | 1 | 读/写/分支逐条对上：-1 落下句、非 -1 跳转、跳转后不再自动推进都在；唯一差是「目标不是指令边界」时 emulator 抛错而引擎会跳到任意 dword 偏移，语料 160687 处 jmp（614 文件）的目标都是汇编器生成的指令边界 label，不可观测。 |
| 0x8f | t02 | 一致 | 1 | 读/写/分支逐条对上：先压后判、-1 时弹回等价 no-op、非 -1 跳转、返回点按 dword 偏移存（与 0x5 ret 的 dwordToInstr 换算配套）都一致；与 0x8c 同性质的「目标必须命中 labelMap」差异见 finding。 |
| 0x90 | t18 | 一致 | 0 | 七格操作数、x+w/y+h 的合成、第 9 实参 = 脚本身份 token、入队失败抛异常、以及入队后 `[7468]=-1`/`[7466]=0` 的两格写都在 emulator 有对应（route.ts 自己那两格就是 `cursor = -1` / `enterPending = 0`，见其 push 尾三行）。 |
| 0x91 | panel | 有缺口 | 2 | ①②两个分支与其中四个可观测项（effect_flags 掩码含 0x8000000 的清除、op1 读取、sub_404020、`[12958]` 清零）在 emulator 都有对应，`[12957]/[12956]/[12958]` 的槽位也对（5494+7463=12957、+7462=12956、+7464= |
| 0x92 | panel | 有缺口 | 2 | 与 0x91 同型：①分支、effect_flags 掩码、op1/op2 读取、sub_404020 都在位。缺口三处：与 0x91 同源的 sub_4098E0 未实现（0x800000 无消费者）；`[12961]` 写入在 emulator 是**死写**（`p.fallbackLabel` 全 src 无读者 |
| 0x93 | panel | 一致 | 0 | 体的三项在 emulator 一一对应：①清 0x800000 一致（且同样不碰 0x8000000）；②`sub_403EF0` 的 7 个复位格 = `routes.reset()` 覆盖的 `[258]/[959]/[960]/[7467]/[7468]/[7466]/[7464]`，且与体一样**不**动 `[ |
| 0x94 | panel | 有缺口 | 2 | 体里三格（`12957`、`[960]`、`[7464]`）与「首次显示时按当前鼠标重做一次命中测试」都在 emulator 有对应，语料 334 处/334 文件（每条 i094 前都是 i090 登记 + i097 绑位的同型 UI 例程）也证实这条链必须在。两处缺口：(a) 引擎在 WM_MOUSEMOVE 里对 |
| 0x97 | panel | 有缺口 | 1 | 体读的 5 格（含 op1/op2 的重复读）、写目标 `[7361+i]`、四字段全等判据、无匹配静默、`[258] == 0` 时整段跳过，五点在 emulator 都有对应（重复读唯一残留差别是那个全局操作数地址，0x97 不写操作数、无消费者）。缺口只在**消费端的输入来源**：语料每条 i097 的 6 条绑 |
| 0xa0 | t02 | 一致 | 1 | 两个目标方向、-1 落下句、非 -1 跳转、跳转后不自动推进全部逐条对上；同上「未知 label ⇒ emulator 抛错 vs 引擎跳到任意 dword 偏移」的差异见 finding。作业书提到的 T-0145（文档/注释与体不符：global 3f90 门极性、留帧注释）是**文档侧**票据，本次逐行读体未发现 |
| 0xa1 | menu | 一致 | 0 | 可观测项逐条对上：①无操作数（引擎 argc 0 ⟷ 计划 `argc: 0`，handler 一格都不碰）；②唯一效果是复位该哈希表，`menuMap.clear()` 与之同义（容量参数 0xFFF 只是重建桶，复位后无条目 ⇒ 不可观测）；③该表在引擎里的访问点全仓恰好三处（grep `107679`：raw 3 |
| 0xa2 | menu | 有缺口 | 1 | argc、读格数/顺序、不写操作数、同键覆盖、键由 int 值十进制化（`sub_41B640` 的 `_itoa_s` 分支 ⟷ `String(number)`，负值两边都是 `-1` 形态）都对得上；语料 87 处 op1 全是立即 int 或 `local-int` 槽，两者 emulator 都取到同一个运行 |
| 0xa3 | menu | 有缺口 | 2 | ①格数与 argc 一致；②命中/未命中二选一、未命中回退到 op2、命中用表内值这三条语义都在；③『值 = 脚本 dword 偏移 ⇒ ip = base + 4*值』的换算在 emulator 里由 `labelMap`（键正是 `(byteOffset-headerLen)>>2`）+ `jump(数组下标)`  |
| 0xad | t03 | 一致 | 0 | ① 无操作数（argc 0 与体的 N=1 一致，`test/opcode-arity.test.ts` 自动核验）；② 两个写的槽都在：`_this[5450] ← _this[5451]` ↔ `timerSecondsPrev(5450) ← timerSeconds(5451)`（`engineFieldIds |
| 0xae | frame | 有缺口 | 3 | 门控、三条版本支、两条落点表（call 表步长 3 ⇒ 落到下一条；消息表步长 0 ⇒ 停在本条）与收尾写 `95777=savedRet`/清门/`97054=1`（仅 sv1==2）都对得上。缺口有三处：① sv1=1 时引擎只认 `sv2==20`，emulator 的 `direct` 直落旁路了版本选组；②「 |
| 0xaf | t24 | 一致 | 0 | 引擎体唯一可观测项 = 步长槽写 1（= 1 个 dword = 1 条指令），emulator 由 stepOnce 对每条指令统一写 2*argc+1 并按下标推进 1 ⇒ 观测等价（与 stubs.ts:274-286 的 op-6-05 订正一致）。 |
| 0xb4 | s04 | 有缺口 | 2 | 读位数与次序一致（op1=id、op2=通道）；写操作数：两侧都没有。缺口有两处：① 引擎在 `sub_4559C0` 里**必然**打「已使用文件」标志（`sub_454960`），emulator 这条路上没有任何 `markFileUsed` 等价物；② 引擎有「设备/文件库未初始化 ⇒ 静默跳过」与「装载失败  |
| 0xb5 | s04 | 有缺口 | 1 | 读 op1、`sound:SE` 开关这条门对上（宿主用 `#enabled.se` 表达 `SE[261]`）；缺的是：① 通道值域 0..14（emulator 只 10 条通道）；② 「设备未初始化 / 该通道没绑过缓冲」这两条静默与错误串分支（宿主只置 pending 等装载，永远不会走 dsPlay/dsPl |
| 0xb6 | s04 | 一致 | 0 | 读 op1 ⟷ `p.int(1)`；无操作数写 ⟷ 无；「不查 SE 开关」也对上 —— 宿主 `seStop` 里确实没有 `#enabled.se` 判据，与引擎体一致。引擎写的两格运行态（`SE[303+通道]`、`SE[262+通道]`）在 emulator 无对应数值槽：ETC 的 `SE[303+ch]` |
| 0xb7 | s04 | 一致 | 0 | ①②③④ 四条分支都对上（op1 非 0 ⇒ 换曲并登记 id；op1 = 0 ⇒ 用 `Music[259]` 起播；两者皆 0 ⇒ 什么都不做＝引擎的 `sub_489B50` 停一条此刻无曲可停）；循环位恒 1 与体调用 `sub_489F80(…, 1)` 一致（`b7`/`b9` 的差别在 opcode，`b |
| 0xb8 | s04 | 有缺口 | 1 | 读/写操作数：两侧都无；`Music[259] = 0`、`Music[260] = 0`、清 bit0x200 + 推进淡出三条都对上。缺口在宿主消费端：`op_bgm_stop` 只是**发意图**，而宿主 `bgmStop()` 是 `#stopBgm()` 的纯停播，**没有**任何清 BGM 模式/开关的等价 |
| 0xb9 | s05 | 一致 | 0 | 引擎体的每个可观测项都有对应：argc/读格、bit 0x200 读改写、`Music[261] = 0`、`Music[260] = 0`（起播分支）、`Music[259]` 直接写/重播/停止三分支、`Music[264]/[265]` 由宿主 `bgmPlay` 的播放状态等价承担（音量运行态在重写侧无对应物， |
| 0xba | s05 | 一致 | 0 | argc 1 与读格一致，`loop=true` 常量化一致，无写格/无分支可漏。语料 154 处全是 `i0ba 9`（通道 9），与实现口径一致；已由 `test/audio-opcodes.test.ts:62-63` 覆盖。 |
| 0xbb | s05 | 有缺口 | 1 | 主体一致：`(a2 != 0) == v3` 的幂等提前返回、关分支的十通道释放、开分支写 1、`sound:SE` 配置写回都有对应；`Engine[83920]` 与配置 `sound:SE` 是同一判据的两个副本（boot raw 23689-23691），emulator 只留配置一份属有据收敛。唯一缺口是写回 |
| 0xbc | s05 | 有缺口 | 1 | argc/读格/`<= 2` 门/±3 口径都有对应，但 `sub_408CF0` 的两条外层条件被落成了内层 if（见 finding）：引擎在「开而当前已非负」「关而当前已负」时整条早退，emulator 却照样「切模式 + 停 + 重播」。语料 0 处 `i0bc`（玩家音频开关实际走 `0x1BA` 音乐分支， |
| 0xbf | s05 | 有缺口 | 2 | argc/读格/`sub_489C20` 的四分支（重起、幂等不重起、op1=0 重播、op1=0 且无曲则停）、`Music[261]` 旧值判据、严格 == 1 的两级配置门、bit 0x200 清理都有对应。缺两处：① `Music[122503]`（跳读态）在本体里被读，但 emulator 从未给该字段赋值  |
| 0xc0 | t03 | 一致 | 0 | ① 读的引擎字段 174713 ↔ `ENGINE_FIELD.musicField = 174713`（`engineFieldIds.ts:98`）；② 写的操作数 = op1（计划 `declarePlan(0xc0)` 声明 1 位 int + `w` 方向，与体「只写 op1、不读操作数」一致）；③ 无分支； |
| 0xc1 | s05 | 一致 | 0 | argc 0、翻转语义、写 `Music[260]`、按音源槽判「有没有后端」都有对应；后端对象差异（CD/MIDI/PCM 分派）在重写侧收敛成一条 Web Audio BGM 通路，属有据豁免（`sound:Music` 配置仍照引擎 ±3 写）。语料 1 处 `src/MMODE.txt:440`；已由 `tes |
| 0xc2 | s05 | 有缺口 | 2 | argc/读格、`Music[265] == 0 ⇒ Music[259] = 0`、`Music[259] != 0` 时起播当前曲都有对应；缺两处：① 非 ADV 路径的 `set:TransferMusicVolume` 分支与节流计数（`_this[107503]`）整段未建模（host 的 fade 近似代替 |
| 0xc3 | s05 | 一致 | 1 | argc/读格/字段号/bit 0x200 清理/「只登记不起播」全部一致。唯一不精确处是源码注释与计划表里把它描述成「`Music[259] = op1`」——`Music[259]` 按结构体基址算是 `_this[174713]` 本身，所以不构成行为缺口，仅注释口径（见 finding）。语料 28 处（如 ` |
| 0xc4 | s05 | 有缺口 | 2 | argc/读格、ADV 寄存 vs 立即起播两分支、`sub_45EEA0` 文本项记录（含 `Engine[97055]` 门控）、循环位 0/1 都有对应；`sub_407120` 淡变推进与主机音频引擎的淡变是重写侧收敛（宿主自己按帧推进）。缺两处：体开头的 `Engine[21315]/[21318]` 一次性 |
| 0xc5 | config-read | 有据豁免 | 1 | 有据豁免：唯一分歧是「非法 selector 的错误报告」这一非状态分支 —— emulator 已按 `onBadSelector: 'skip'` 有意豁免（config-read.ts:47 注释「引擎在 0xC5 里是报错路径」、:108 行内注释），且它不改变任何脚本可见状态（两边都不写 op2）。本轮新核出 |
| 0xc6 | s06 | 一致 | 2 | 引擎每个可观测项都有对应：op1 的「值即类别」在 emulator 收敛成「0..4 = 类别」（唯一差别是 op1=0 时引擎走 `if` 的假分支、emulator 走 `case 0`，结果同为写 `sound:Volume0`）；op2 两侧都写进配置并被消费；非法类别两侧都不写；引擎 `sub_4092C0 |
| 0xc7 | config-read | 有缺口 | 1 | 有缺口：四个键名映射、读 op1 / 写 op2、写 0/1 字面量、非法 selector 不写都对，但 **Music 支的判据写错了** —— 引擎是 `v2 >= 0`，emulator 的四条统一按 `!== 0` 归一。两者只在 `sound:Music > 0` 时一致；值为 0 时引擎 1 / emul |
| 0xc8 | frame | 有缺口 | 1 | ADV 门（0x8000000）、操作数读取（只读 op1、ADV 时完全不读）、以及「暂停 ≈ n 毫秒」的落点都一致；缺口在**两条路径被压成一条**：引擎的 n>=10 走定时器对象节流（同时置 `effect_flags \|= 1`，由主循环 raw 21184-21188 按 `sub_453AF0` 的帧 |
| 0xcc | t13 | 一致 | 0 | 两格操作数全读、三个字段（目标/身份 token/节流间隔）都有对应，`0 ⇒ 1` 的兜底也照抄；`sub_453A60` 顺带写的「上次推进时刻」（`Engine+429808`）emulator 侧由上一格 fallback 未改动，寄存器时钟由 `input.lastAdvance` 承载（`getInputT |
| 0xcd | t13 | 一致 | 1 | 引擎侧可观测项逐条对上：推进门（时间节流 + `0x8000000` OR 兜底）、刷新上次推进时刻、未注册目标不跳、`Depth が不正です` 身份守卫、压返回点（`>>2 + 1` = dword 偏移，emulator 用 `index + 1`）、`ip = base + 4*目标`（emulator 用 la |
| 0xd0 | t04 | 一致 | 0 | 逐项对齐：唯一可观测项「op1 ← 墙钟毫秒」在 emulator 是 `setInt(1, …)`（写 int 第 1 位）；不读操作数两边一致；无分支可漏。时钟取值口径：引擎 `int Time = timeGetTime()` 收 DWORD ⇒ emulator 的 `Math.trunc(...) \| 0` |
| 0xd3 | t23 | 一致 | 0 | 三格写逐项对上（`cursor ← −1`、`exitLabel ← −1`、`index ← 0`）✓；vector 清空 ⇒ `entries.length = 0` ✓；不动门 ✓（`reset()` 注释与实现都不碰 effectFlags，引擎体也不碰 `699204`）✓；返回值无消费者故不复刻（emula |
| 0xd4 | t23 | 一致 | 0 | 读的 4 格 ✓、`op2 <= 0` 不追加 ✓（循环 k < count 直接不进）、时刻基线 = 上一条的 t（跨 `i0d4` 继续累计）✓、每条相对前一条加 op1 ✓、写游标 `cursor++` ✓、op3 = body / op4 = **落后时**用的 tail ✓（`nextStep` 里 `beh |
| 0xd5 | t23 | 一致 | 0 | 读 op1 ✓（`ffffffff` = −1 = 不打断，全语料 7/7）；「本条不前进」✓（`c.jump(c.frame.ip)`，与帧状态格写 0 同效）；起表段只在 index == 0 ✓；门 `0x40` ✓（`STAGE_GATE`）；`index < cursor` 的判决 ✓（`shouldWait |
| 0xd9 | t04 | 有缺口 | 1 | 有缺口：结构（无条件清 effectFlags + 派发中时同清 95779）与字段号（174801 / 95779 / 124350）都对齐，但**那条 if 的门读的是永远为 0 的格子** —— emulator 全仓对 `ENGINE_FIELD.dispatchInProgress`（124350）只有这一处 |
| 0xfa | t18 | 有缺口 | 2 | 输入掩码位 0x40 的判据、清 `0x8000000`、消费刷 + 置 bit31 等待门这三项都在 emulator 有对应；但引擎体里**那个 3 槽循环**（`Engine+122505` 的待播语音队列 + `sub_4BB840` 消费）在 emulator 完全没有对应物 —— 这是引擎体里可观测的消费端 |
| 0xfb | t13 | 一致 | 0 | 两格操作数全读、越界 `[0,32)` 校验与抛错、写入位置（索引 = op1 = 掩码位）三条都逐字对上；读端 0x100 也按同一索引查表（input.ts:205 注释与代码 `e.input.joyJump[b]`）。故判一致。 |
| 0xfe | t04 | 一致 | 0 | 六项全对齐：读 op1 一次（int）；unsigned 比较门（emulator 用 `v >>> 0`）；越界 ⇒ 抛且**先于写**；合法 ⇒ 写 `_this[517]`（emulator `ENGINE_FIELD.setKeyTotal = 517`）；错误串逐字一致；无其它字段/副作用。此前的有符号口径缺 |
| 0xff | t14 | 有缺口 | 1 | 引擎 ① 重建掩码 ② 归零 `Engine[cur+122287]` ③ 写 `Engine[cur+122327] = Engine[517]` 三项。emulator 做了 ①②，漏了 ③（`Engine[cur+122327]` 的读取端在反编译体里有实证：`sub_419D20` raw 25096 与 25 |
| 0x100 | t14 | 有缺口 | 1 | argc、掩码两分支、默认键分支、`b >= SetKeyTotal` 门、压返回点的 ±1 差异（掩码分支压本指令 / 默认键分支压下一条）在 emulator 都有对应；但引擎的扫描游标**一旦命中就停在 `b+1` 且跨帧保留**（由 0xFF / `sub_419D20` 复位），emulator 额外加了一条 |
| 0x101 | t14 | 有缺口 | 1 | 掩码侧「读一次挂起事件 + 清零」在 emulator 有对应（`flushPending`）；但引擎另外三条写没有对应物，且都有独立读取端实证：`_this[122370]` 被 ADV 每帧路径 `sub_411900` 读/写（raw 20119-20141）、`_this[174801]` 的 0x800000 |
| 0x106 | t04 | 一致 | 0 | 逐项对齐：不读操作数；只写 op1（int）；读的引擎字段号 550 与 emulator 常量一致（`engineFieldIds.ts:128 engineField550: 550`）；无分支可漏。取值等价性：引擎那格**无任何写点**（raw 里 `_this[550]` 仅 raw 39043 一处）⇒ 恒为 |
| 0x107 | t04 | 一致 | 0 | 逐项对齐：两格 int 读（op1=键、op2=值，与引擎的写入 `_this[op1+551]=op2` 同向）；无符号门（`key >>> 0`）；门通过写同一表基址 551；门不通过不改任何状态。可选顺序差异：引擎先读 op2 再读 op1，emulator 先读 op1 再读 op2 —— 两者都是纯槽读取（` |
| 0x108 | t14 | 一致 | 1 | argc 1 与「只写 op1」一致；位语义（bit0=左、bit1=右）一致；引擎每次重算（先清零）与 emulator 每读即返回当前按住态一致。`pressLatch` 是 emulator 加的「按下至少被读一次」保持位（见下条 finding），不改变位语义与写出位置。 |
| 0x109 | t14 | 一致 | 0 | argc 2、写 op1/op2、哨兵值 (-100000,-100000)、虚拟↔客户区 1:1 三点都有对应：随包 INI 无 `display:VirtualFullScreenType` ⇒ `sub_403500` 的 `a2` 走 `< 2` 分支后缩放为 1（`a9 = (699168 + 2*0)/69 |
| 0x10a | t14 | 有据豁免 | 0 | **据豁免**：代码里读/写侧只剩「映射与 SetCursorPos 都在宿主」这一处不可在 VM 内判定。依据（raw 行）：`display:VirtualFullScreenType` 的默认值是 **0**（`src/configRegistry.ts:62` `{ key: 'display:VirtualF |
| 0x10b | t04 | 一致 | 0 | 逐项对齐「门在 op1（值）上、下标取 op2、表基址 1383」这条易错的不对称语义：emulator 把 op1 叫 `value` 并拿它过无符号门、用 op2 当偏移，与引擎体一致（handler 注释 :182-184 亦已写明同型口径）；越界不改状态、不抛；两格 int 读；下标同样不受门约束（与引擎同为零 |
| 0x10c | t24 | 有缺口 | 3 | 有缺口：白名单/台账的豁免理由「宿主键盘也不进掩码 ⇒ 本指令的写入在 emulator 里一个消费者都没有」**已被 T-0052 推翻**（`tickets/T-0052/ticket.json` status=done；`src/vm/input.ts:421/446` 已把 `keyEdge`/`keysHel |
| 0x10d | t14 | 有缺口 | 1 | handler 体（读-清-写、argc 1、只写 op1）逐字一致；差异只在**累加侧的门**：引擎只有在 `(Engine+699204 & 0x90100000) == 0` 时才累加进 `Engine[1949]`，否则该消息被当作配置键派发、累加器保持 0，于是 0x10D 读到 0。 |
| 0x10f | t04 | 一致 | 0 | 读 op1（int）一次、写 `_this[122369]`、字段号与 emulator 常量逐一相等、无分支可漏、无变换（不 bool 化、不取反）。该字段语义在两侧都未定位（emulator 侧注释 `engineFieldIds.ts:174`「`0x10F` 写的字段（`_this[122369]`）：语义未定 |
| 0x12c | memory | 一致 | 0 | 读位与算术（`op3*op4+op5`）、写目标、步长均一致；语料 10234 处样本（`src/$1$BTANINIT.txt:22` 的 `lookup-array-2d (local-ptr 0) (global-int 6f3420) (global-int 6f341e) 6 (local-int 1)`、` |
| 0x12e | t14 | 一致 | 0 | 逐项对齐：① argc 8；② 操作数读法（op2/op5/op6/op7 取指针、op1/op3/op4/op8 取 int）一致；③ **首项 = op1 + 1**（`let j = start + 1`）对应 raw 39230；④ 两道边界门（`j >= count` 两处写 -1）对应 raw 39231- |
| 0x12f | memory | 一致 | 0 | 键的取法（用 A 里的索引查 B/C）、DEC/ENC 只一层、unsigned 比较、`j<0` 短路与严格大于判据均与体一致；`n<=0` 的提前返回在 `n<=0` 时只少写 `*A=0`（A 为空数组，无消费者可观测）。语料 81 处的 op1 **是 int 目标**（`i12f (local-int 41d |
| 0x130 | t04 | 一致 | 0 | 逐项对齐：不读操作数、只写 op1（int）、字段号 96983 一致、初值与两处置位/清位点都在 emulator 有同值对应物（构造 1 / exit-script 0），无分支可漏。刻意的偏离只有一处且已在 `emulatorOptions.ts:55-57` 自我披露：真机播 LOGO 时 `SYSTEM4`  |
| 0x131 | t04 | 有缺口 | 1 | 有缺口：不读 Engine 字段这条关键口径两边一致（engine-fields.ts:74-79 的专用支正是为此存在），写 op1 一次也一致；但**缺键回退值不同** —— cfg 里没有 `message:MesWinAlpha` 时 emulator 用 `cfgInt(..., 0)` 得 **0**，而引 |
| 0x132 | t02 | 一致 | 0 | 读/写/分支逐条对上：无符号越界判定、错误串分支不动队列、正常分支重建空队（等价 sub_407C50 的 rd/wr/max 全 0）、负数下标走错误串都一致。唯二差别是「错误串只打日志不打宿主消息」与「常驻空数组 vs 析构置 NULL」——后者是同一根因，已在 0x9 的 finding 里报了（exit-scr |
| 0x133 | t02 | 一致 | 1 | 读两格、无符号越界判定、不压分支、FIFO 压入语义（扩容/压实只是引擎缓冲实现细节，脚本可观测的只有长度与出队顺序）逐条对上。已知口径差（teardown 后引擎槽为 NULL ⇒ 空指针崩）在 engine.ts:285-288 与 0x133 note 已披露。 |
| 0x134 | t02 | 一致 | 2 | 读/写/分支逐条对上：无符号越界且两个出参都不写（与引擎一致）、FIFO 取值、成功位、空队成功位=0 都一致。唯一实质差异是空队时 op3 的取值：引擎写未初始化栈残留，emulator 写确定的 0——这是**已披露**口径（见 findings，不算新缺口但按作业书要求保留 finding）。 |
| 0x135 | s03 | 一致 | 0 | 体里每个可观测项都有对应：argc 2、先读 op2（位号）后读 op1（值）、写 op1=op1\|1<<bit、越界不写 op1、越界不抛异常。两处体/实现的表面差异都有据：①体的 `unsigned` 越界口径 ⟷ emulator 的 `(v >>> 0) > 0x1f`（同一口径，−1 判越界）；②体的错误串 |
| 0x136 | s03 | 一致 | 0 | 与 0x135 同形，逐项对上（含「读 op2 后读 op1」的顺序与 `~` 的补码语义：emulator 的 `v & ~(1 << bit)` 与体的 `~(1 << v3) & v4` 等价）；越界分支的「记一条后继续」由 c.log 表达（引擎那侧是调试日志文件写入，返回 void ⇒ 不抛）。既有守卫 te |
| 0x137 | t24 | 有缺口 | 1 | 有缺口：栈本体「删空栈+建空栈」因全库 int 栈家族只有 1 处且无人压栈而确实观测等价（已复核：`src/` 里 `i13[78bcd]` 只有 `CALLBACK_LOAD.txt:108 i137 0` 一处），但体内 **op1 > 0xA 的抛错分支**在 emulator 完全没有对应，豁免文本也没覆盖它 |
| 0x13f | s03 | 一致 | 0 | 位号槽（op3）、待测值槽（op2）、写 op1 的 0/1 结果、越界不写、越界不抛，逐项与体一致；operandPlan 也把 0x13f 声明为 io ['w','r','r']（op1 只写，与 0x135/0x136 的 ['rw','r'] 区分正确）。既有守卫 test/op-141-135-bitops |
| 0x141 | t05 | 有缺口 | 1 | argc（3 槽 ⇒ 1）、op1 的读、**无符号** `> 0x10` 判据、越界不写配置、合法走 SetConfig 而非持久字段五条主线都与体一致；不一致只剩错误分支的**用户可见效果**（引擎把错误串派发成消息，emulator 只打日志），见 findings。 |
| 0x142 | t05 | 有缺口 | 1 | argc（3 槽 ⇒ 1）、op1 的读、写 `_this[174812]`、无分支四条都与体一致；缺口在**消费端**：emulator 只写不读（`scriptEngineFlag` 全仓只有这一处写，`grep` 无读者），引擎虽然内部也零调用，却把它导出成 `sub_4765C0` 查询口（脚本/宿主可读回）， |
| 0x143 | t03 | 一致 | 0 | 逐项对差均成立：① 无操作数（argc 0 与体一致；`test/opcode-arity.test.ts` 已用体里的 N 自动核验）；② 写的 `_this[124350]` 防重入标志在 emulator 有对应门（`e.dispatching`：入队期间不派发、末尾自行派发一次，与 raw 25176/2518 |
| 0x147 | region-hittest | 有缺口 | 2 | 操作数读写（op2/op3 值语义 DEC、op4/op5 地址语义、op6 n）、唯一写回 op1（0/1）、源数组只读、argc 6 都与体一致，且 op4/op5 的『地址语义 + 元素逐个 DEC』在 emulator 由 `plan.ptr` + `readRef(refAt(...))` 精确对应；不一致只 |
| 0x148 | t05 | 有据豁免 | 1 | argc、只写 op1 的方向、读 `_this[97058]`、无分支、初值 0 全部一致。唯一差异是**宿主消费端**：引擎的读者是窗口过程 `sub_4B9240`（点击去抖 ⇒ 决定是否弹系统菜单），emulator 没有这个宿主子系统（`globalSlot97058` 除本条与 0x149 外无读者）⇒ 属 |
| 0x149 | t05 | 有据豁免 | 0 | argc、读 op1、写 `_this[97058]`、无分支全部一致；与 0x148 同因「宿主去抖消费端（窗口过程 sub_4B9240）在 emulator 无对应物」按有据豁免，理由与缺什么材料同 0x148。 |
| 0x14b | s01 | 有缺口 | 2 | argc、op1 的读、FreeLibrary+清 0 的重载语义、FileDB→LoadLibrary 只认 0x5250（既有白名单「只接受 id=0x5250（AGERC.DLL）」复核**仍成立**：语料 `src/SAVE.txt:7 i14b 5250` 是唯一一处，且 0x5250=21072 就是 AG |
| 0x14c | s01 | 有缺口 | 1 | argc 2、op1 是 int 槽号、op2 是**字符串**导出名、`slot > 99` 越界抛引擎同文、成功才写槽表四条都对得上；不一致只有操作数读取时机（op3 的额外读）与错误串里的实参，见 findings。既有白名单「AGERC 导出绑定：需要先加载模块（合成指令里模块未加载）」复核仍成立：`src/S |
| 0x14d | s01 | 有缺口 | 2 | argc 6、op2←返回值、op1 槽、op4 长度、op3 的 DEC→调用→ENC 回写、len<=0 时不动 op3、op5 原样传入（不 DEC）这几条都与体一致；DEC/ENC 公式（`bits.ts` 的 `ror32(key ^ rol32(x,11), 25)` / `rol32(key ^ ror3 |
| 0x191 | s03 | 一致 | 0 | 体与实现一一对应且都无分支：argc 2、读 op2 一格（浮点）、写 op1 一格浮点、语义 = 绝对值。两点口径核对：①体里 `fabs(v3)`（C 的 fabs 取 double）与 emulator 的 `Math.abs` 对 float32 输入给出的幅值在同为 float32 的槽里数值相等（NaN/± |
| 0x192 | strings | 有缺口 | 1 | ①argc/读格数（2 格各一次）/写格（只写 op1）与体一致；②字符串型操作数（语料 17747 处的全部形态：global-string / local-string / local-string-ptr）两条路径等价；③判『有缺口』的唯一依据是体里 `sub_42A420` 对**非字符串型**操作数有强制转换 |
| 0x193 | strings | 有缺口 | 1 | ①argc=3、读格（2/3 各一次）、写格（只写 op1）、拼接顺序（op2 在前）与体一致；②自拼接情形 emulator 也正确（先取值后写回）；③判『有缺口』的依据与 `0x192` 同源：体里 `sub_42A420` 对非字符串型操作数有强制转换分支，emulator 落到 `String(a.raw)`（ |
| 0x194 | config-read | 一致 | 0 | 一致：读位/类型（两个字符串 + 一个 int 结果位）、写位与值域（0/1）、argc（3）逐项对上；`readStringOperand` 覆盖 `local/global string` 与 `local/global string-ptr` 四种实参形态（operand.ts:152-169），与语料 `i19 |
| 0x195 | config-read | 一致 | 0 | 一致：与 0x194 同构的读/写与 argc 全部对上，取反向也一致（不等 ⇒ 1）；`p.setInt(1, …)` 确实是回写而非跳过。语料 24 处/20 文件中最有代表性的是 `i195 (local-int 7d4) (local-string-ptr 0) ""`（SETFATE.txt:16，空名过滤） |
| 0x196 | t18 | 有缺口 | 2 | 外层门（①②路 vs ③路）、MessageSpeed/ADV 的里层条件、三条出口写 489484、第③路的 `\|= 0x10000` 都已接（`tickets/T-0094` + 审计 `op-3-004`）；但两处可观测项缺失：被调返回值的门（②③路都只在非 0 时才写状态）与体里对 **op4** 的读取/其 |
| 0x197 | t18 | 有缺口 | 2 | 读 1 格 + 写「注音字号」这一核心字段在 emulator 有对应，但**引擎体真正做的是把新字号灌进 10 个字体对象（`Font[261..270]` 的 `+200/+204`）并立即重建 GDI 字型与度量**；emulator 只留一个标量、把它当「下一次排版的样式」延迟生效，且没有那 10 个对象的对应 |
| 0x198 | t19 | 有缺口 | 2 | 读格数/顺序/类型、op1=0 的默认窗回退、落点（该窗几何 x/y）都有对应；但引擎的两处分支在 emulator 没有等价物：① 「窗对象不存在 ⇒ 不写」被 `geom()` 的**惰性建窗**抹掉（对任意窗号都返回一份 defaultWinGeom）；② `0x198` 在引擎里**不触发任何重绘**，emul |
| 0x199 | frame | 一致 | 0 | 三分支、写序、写入字段与 emulator 逐条对应；redisplayReturn 的（index+1）与引擎 `((ip-ip_base)>>2)+1` 同值（BinInstruction.index 就是 dword 偏移）；ip 不前进由 jump 承接。 |
| 0x19a | t19 | 一致 | 0 | ① 读格：引擎不读格、emulator 也不读（计划层 `['w']` 声明正确）；② 写的格：两侧都写操作数 1；③ 字段：都取 `_this[97050]`（emulator `msgwin.skipMode`，与 `1415`/`skipMirror` 分开存，恰与引擎「1415 可被 0x19C 单独置 1 而 |
| 0x19b | t19 | 一致 | 0 | 五处字段一一对应：`clearAdv` ↔ 清 `effect_flags` 的 0x8000000；`skipMirror(1415)` ↔ `_this[1415]`；`advEnter(97051)` ↔ `_this[97051]`；`cancelStage(122370)` ↔ `_this[122370]` |
| 0x19c | t19 | 一致 | 1 | 四条分支逐条对上：97050≠0 ⇒ `skipMirror=1` 后置位；122455≠0 ⇒ `showing!==0` 使 `else if` 不成立、落到置位块；122455==0 且 124331==0 ⇒ 清 `skipMirror`(1415) + `clearAdv` 后 return（与引擎同一出口） |
| 0x19d | resource-usage | 有缺口 | 2 | 体里可观测项基本都有对应：argc=2 与计划表 `[0x19d, 2, ['int','int'], ['w','r']]` 一致；两格读写次序/类型一致（指针型操作数由 `writeIntOperand` 写穿所指处，与 `sub_42B4B0` case 6/12 同口径，故语料里 SETMEMOIR 的 `(l |
| 0x19e | save-slot | 有缺口 | 3 | argc、op2 读、op1 写、写序（op1 是唯一输出，恒最后写）、`_this[120*cur+383220]` 的指令长度槽都由计划层/调度器覆盖，写侧返回码 1 的两个语义（打不开 / 写失败）也对得上；判「有缺口」是因为引擎有两处**可观测的宿主交互分支**在 emulator 里完全没有落点（见 find |
| 0x19f | save-slot | 有缺口 | 2 | argc、op2 读、op1 写、`FileA == -1 ⇒ 1` 早退、不转移（a6=0）都对得上；判「有缺口」是因为 emulator 在三条路径上写出了引擎在该指令里**不可能产生**的返回码：**2**（解析失败）与 **0**（帧一条都装不上 / 真槽主体未解析，而引擎那时 op1 只能是 0 或 1，见  |
| 0x1a0 | save-slot | 有缺口 | 1 | argc、op2 读、op1 写、op3..op9 写、**成功分支的写序（op3..op9 先、op1 最后）**、失败分支不碰 op3..op9、三个返回码 0/1/2、指针型操作数写穿（`writeIntOperand` 的 `TYPE_LOCAL_PTR` 分支 raw `src/vm/operand.ts:3 |
| 0x1a1 | save-slot | 一致 | 0 | 引擎的 9 个可观测项逐条对上：① argc=2 与 arity 槽 5；② op2 读一次；③ **op1 不写**（计划声明 `unused`、handler 一个字节都不写，`test/save-slot.test.ts:353` 有专门断言「0x1A1 不写操作数（引擎不调 sub_42B4B0）」）；④ 只读 |
| 0x1a2 | strings | 一致 | 0 | ①argc=1、读格（1 格两种口径）、不写操作数、写序一致；②键构造逐字等价（哨兵 3 + 8 位小写十六进制，语料实测 9 字符键、引擎 Src 也是 12 字节够用）；③下标口径对全仓语料两种形态都成立：`save-int (global-int 1390)` 414 处 ⇒ `a.raw` = 槽号（引擎 ca |
| 0x1a3 | strings | 一致 | 0 | ①argc=1、读槽号、命中/未命中二选一（未命中 0）、**两条路径都写回 op1** 与体一致；②键构造同 0x1A2（逐字同形）；③编码层：引擎存的 `v4` 是 `sub_41BF50` 解出的值、写回时 `sub_42B4B0` 再编码，emulator 取的也是**解码域**的值、写回时 ENC 编码 ⇒  |
| 0x1a4 | t05 | 一致 | 0 | argc（5 槽 ⇒ 2）、两格都是读、op1→21670(dx) / op2→21671(dy) 的**对应关系**、两字段各写一次、消费端（描边渲染）全部与体一致。唯一差异是**同一次调用内两个独立字段的写序**（引擎 21671→21670，emulator 21670→21671）——两目标是不同地址、彼此无依 |
| 0x1a5 | t19 | 有缺口 | 2 | 读格（op1、字符串类型）与「换成下一次排版用的字体面名」这条主语义都有对应，消费端已接（resolveFace → FontSpec.family → 渲染器）。缺口是引擎体里三件 emulator 无对应物的事：① **面名合法性警告**（`sub_428990` + 错误串分支，这是体里唯一的分支）；② 5 处字 |
| 0x1a6 | strings | 有据豁免 | 0 | ①argc=2、读 2 格写 1 格、`>> 1` 的字节口径与体一致（语料 217 处/215 脚本，`test/op-string-len.test.ts:82` 已锁）；②第 2 格的非字符串型操作数确实有缺口（体里 `sub_41B640` 对 int 族走 `_itoa_s` 转十进制串、float 族走 ` |
| 0x1a7 | t03 | 一致 | 0 | 引擎体除 arity 槽外无任何可观测项，emulator 的零参数 handler 与之一致：① 不读任何操作数 —— 计划层用 `unused` 如实声明 op1（`operandPlan.ts:1444`，`unused` 的语义正是「有这一格但没人消费」），且 `test/operand-plan.test.t |
| 0x1a8 | t03 | 一致 | 0 | 体唯一的可观测写是步长槽置 1，而该槽的**唯一读者**是主循环 raw 20165（`_this[30*cur+95782] += 4 * _this[30*cur+95805]`）⇒ 语义 = ip 前进 4 字节 = 1 条指令，这正是 emulator 的默认推进（下标制 `+1`）；emulator 不建模该 |
| 0x1a9 | strings | 一致 | 0 | ①argc=1、读格（1 格两种口径）、不写操作数、哨兵 5 的键构造与 `%c%8.8x` 同形；②下标口径逐 tag 对齐：语料 `save-string (local-string-ptr 0)` 1022 处、`(global-string …)` 15 处，分别落到 emulator 的指针族/串池分支，与引 |
| 0x1aa | strings | 一致 | 0 | ①argc=1、读下标、命中/未命中二选一、两条路径都写回第 1 格，与体一致；②未命中：引擎给静态空串、emulator 给 `''` ⇒ 同义；③下标口径与 `0x1A9` 同（读侧 `sub_418AE0` 只认字符串族 tag，emulator 的 `readStringIndexOperand` 同样对其它  |
| 0x1ab | save-slot | 有缺口 | 1 | argc、op2 连读两次、op1 写、三态映射（`r.dat ? (r.sth ? 0 : 2) : 1` 与 `!DeleteFileA` 口径一致，含「`.STH` 失败盖成 2」）都对得上；判「有缺口」是因为宿主的**覆盖范围**只有 overlay 层（见 finding）。 |
| 0x1ac | save-slot | 有缺口 | 1 | argc、op2/op3 各读两次、op1 写、三态映射（含「`.STH` 失败盖成 2」）都对得上；判「有缺口」是因为宿主把「源读不到」当成 `dat = false`（⇒ op1 = 1）而引擎的 `CopyFileA` 对「源不存在」同样返回 0 ⇒ 引擎报 **0**（成功），并且引擎在**任何** CopyF |
| 0x1ad | t05 | 一致 | 0 | argc 0、无操作数读写、写 166963 = 当前帧号、消费端的负值回退四条都与体一致；emulator 侧「写字段 + 存档退栈消费端」都已存在（既有描述与代码注释已引 T-0061）。体里没有可观测项缺对应，故「一致」。 |
| 0x1ae | save-slot | 有缺口 | 2 | argc、op2/op3 读、op1 写、三态（1 打不开 / 2 写失败 / 0 成功）、`CreateFileA` 用 CREATE_ALWAYS、产物是 BMP 这些都对得上；判「有缺口」是因为引擎的 `Engine[166964] == 1`（DrawMode = D3D）分支在 emulator 里不存在（两 |
| 0x1af | save-slot | 有缺口 | 2 | argc、op2/op3 读、op1 写、三态（1 打不开 / 2 读失败 / 0 成功）、只读 + 非 BMP（BM 魔数校验）默认拒绝、成功后才写目标槽这些都对得上（`tickets/T-0036` 已把 op3 = 纹理槽这件事做对）；判「有缺口」是因为 ① 零长度文件被报成 1（引擎在两条路径上都报 2，因为  |
| 0x1b0 | memory | 一致 | 1 | 字节数口径（4*op3）、源/目标方向（op1→op2）、无类型变换三点与体一致；emulator 额外加的类型/步长一致性断言在语料 255 处上不触发（实测签名全部自洽：105 处 `global-int,global-int`、36 处 `local-ptr,local-ptr`、66+7+6+2… 处含个数的组 |
| 0x1b1 | t05 | 一致 | 0 | argc（3 槽 ⇒ 1）、op1 的读、写 `_this[21672]`、消费端判据 **`== 1`**（emulator 用 `=== 1`，与体 raw 71350 的 `v7 == 1` 同口径 ⇒ 值 2 在两边行为也一致）、非 1 时用窗口/操作数坐标四条都对得上；字段名「字格图标目标分支」也与体 raw |
| 0x1b2 | strings | 有缺口 | 1 | ①argc=1、读 1 格、不写操作数、效果（追加到同一个文本缓冲）与体一致；②判『有缺口』是因为第 1 格的非字符串型操作数：引擎的 `sub_41B9B0(_this, 1)` 与 `sub_41B640` 同为 tag 分派转换器（case 0 立即 int 就走 `_itoa_s(..., 10)`），语料 ` |
| 0x1b3 | strings | 一致 | 0 | ①argc=0、无操作数、追加内容（两字节 CRLF）与体逐字一致（常量定义 raw 4320 已核对）；②`textBuffer` 的追加语义与引擎同一个 `std::string` 累加等价，`test/op-1b2-text-buffer.test.ts:40` 已断言语料级效果（`'HELLO\r\n'`），并 |
| 0x1b4 | strings | 一致 | 0 | ①argc=0、无操作数、『取出整段并清空』的效果与体一致（`test/op-1b2-text-buffer.test.ts:46` 已锁清空）；②引擎把取出的段交给 `sub_4034F0` 的虚调用（宿主输出端），emulator 用一条 `c.log` 留痕 —— 这是**宿主缝**而不是纯数据 opcode，属 |
| 0x1b5 | t19 | 一致 | 0 | ① 读格：引擎读 op1（两次、同一格、同值），emulator 读一次并把同一个值用于两处 —— 引擎那次重复读是体里的冗余，`0x74`（`sub_41F320` raw 28648）同族只读一次作对照，故不构成「漏读格」；② 写：字段 `_this[21668]` ↔ `ENGINE_FIELD.messageS |
| 0x1b6 | t19 | 有缺口 | 2 | 写格与归一（`!= 0` ⇒ 0/1）两侧一致，字段 `97052` 也一致（emulator 放在 `Engine.advFields`）。缺的是**消费端**：引擎的 97052 有两处真消费 —— 消息泵入口 `sub_4090F0`（raw 13699-13707，被 `sub_4764E0` raw 9094 |
| 0x1b7 | t19 | 有缺口 | 1 | 写格（op1 读、值归一为 0/1）与字段 `97052` 两侧一致；与 `0x1b6` 成对（同一条 `advFields` 真源）也没问题。缺口与 `0x1b6` 是**同一条消费端缺口**（引擎的 `sub_4090F0`/`sub_476080` 会在下一次消息泵把 97052 清 0 并按 `set:Coex |
| 0x1b8 | config-read | 有据豁免 | 0 | 有据豁免：键名与 0/1 的映射（0⇒Time0、1⇒Time1）、读 op1 / 写 op2、原值直写、argc 2 全部一致。唯一分歧仍是越界 selector 的错误报告（见本批 0xc5 的 finding，同一引擎通路，不重复开条目），而它在本条**可证不可达**：语料 2 处全是字面量选择器（CONFIG1 |
| 0x1b9 | t19 | 一致 | 0 | ① 两格的读法与类型一致（选择器 + 值），引擎把选择器读了两次是体里的冗余（两次都取同一格同一值，对照 `0x2E7` 同形）；② 写的键与 idx 的映射逐字一致（`message:AutoMessageTime0/1`，注意引擎的符号名反向：`aMessageAutomes_3` 才是 Time0）；③ 唯一的错 |
| 0x1ba | s06 | 一致 | 1 | 四条分支、同现状早退、非法类别不写，逐条都有对应；op2「入口统一读一次 vs 每分支各读一次」对 VM 不可观测（`readIntOperand` 无副作用），是 emulator 注释里已声明的等价改写。唯一没有宿主物的是 `sub_406DF0` 的影片播放器音轨下发（重写侧影片层未建模）—— 属已登记缺口，按「 |
| 0x1bb | text-items | 有缺口 | 1 | 读写格与两个合法分支（0 ⇒ 0x80000000、1 ⇒ 0）逐条对上：`ENGINE_FIELD.textBaseGate = 97055` 与体里 `_this[97055]`/`_this + 388220` 是同一格（388220 = 4*97055），emulator 读的 `plan.int(1)` 与体 |
| 0x1bc | s06 | 一致 | 1 | 引擎清零的 9 个字段（21315..21320、122501、122505..122510）在 emulator 都有对应写点，且顺序一致；释放 3 个语音通道对象对应 `voice-reset`×3（宿主 `AudioEngine.voiceReset` = 停播 + 清 request/deferred/load |
| 0x1bd | s06 | 一致 | 1 | 五个可观测面逐条对上：op1 读数与用途、循环位恒 1（`voicePlayOrDefer(…, loop=true)` 与 `pushVoiceRecord(loop = 1)`）、ADV 寄存/立即起播二分（`advActive`）、`Engine[97055]` 记账门（`textRecordingEnabled |
| 0x1bf | t05 | 一致 | 0 | argc 0、两条 if 的**顺序与「第二个 if 重读被 ① 改写后的字段」**、`& 0x10000` 清成整字段 0、`& 1 == 0` 置 `122503 = 1`、返回值恒 1、消费端（play-bgm 的 `!122503` 让路判据 + 两个配置 `== 1`）全部一致。`122503` 不是脚本全局 |
| 0x1c1 | t20 | 一致 | 0 | ① 读的三格 ② 写目标（窗对象 +36/+40 ⇒ `MsgGeom.wrapRight/wrapBottom`）③ 默认窗回退（`resolveWin` 的 0 ⇒ defaultWin 与引擎 `if (!a2) v4 = _this[307]` 同口径）④ 无遗留分支 ⑤ 消费端存在：`text/layout. |
| 0x1c7 | t20 | 一致 | 0 | 位号、判定方向、写值域（只写 0/1）、写向（只写不读 op1）与引擎逐字一致；操作数类型越权在 emulator 由 `writeIntOperand` 的 default 分支抛错表达，与引擎的 C++ 类型异常同形（不是能力缺口）。 |
| 0x1c8 | strings | 一致 | 0 | ①argc=2、读 2 格（第 1 格写/第 2 格读）、写序一致；②数值口径一致：`%d` 是**有符号** 32 位十进制、`\| 0` + `String()` 也是（负数两边都是 `-1` 形态、无前导零），语料 11 处/6 脚本的用例形态是 `to-string (global-string d5d) (g |
| 0x1c9 | s06 | 有据豁免 | 1 | 判「有据豁免」而不是「有缺口」：引擎的实质副作用集中在两条重写侧**没有对应层**的路径上 —— ① 音频驱动文件的装载（`sub_4559C0`/`sub_455560`/`sub_4B8490`/`sub_4B86E0`）：重写侧用 Web Audio，没有 DirectSound 设备数据/驱动文件这一层；② 窗 |
| 0x1ca | t20 | 一致 | 0 | 引擎的写端（`_this[174405]` 组件 +12 Set）与读端（同对象 +4 Get，见 0x1CB 体）是同一键的往返，emulator 用「运行期覆盖 + 启动配置回落」表达同一往返，写后立即可被 0x1CB 读回；组件 Set 是否顺带落盘属于 vtable thunk（handler 体之外），不作为 |
| 0x1cb | t20 | 一致 | 0 | 读键、读对象、写回第 1 格三者与引擎一致；操作数计划把 op1 声明为 `io:['w']`（operandPlan.ts:1188）与体内「只写不读」吻合。语料 337 处，如 `src/$1$SC0330.txt:443` 的 `i1cb (global-int 139d)`：旧实现当 no-op 会让脚本读到上 |
| 0x1cc | t20 | 一致 | 0 | 字段、写回、值域三项逐字对应；与 0x1C7 一起构成 SN0000 的等待判据（`i1c7 f7ff5` / `i1cc f7ff6` → `or` → `jcc`），两侧都写回操作数，故无静默错值。 |
| 0x1ce | t20 | 有缺口 | 1 | 四条可观测项（读 1 格两次、写 107706、非零分支三笔、零分支的 bit30/bit100000 双门与收尾目标）在 emulator 都有对应；`sub_453A90` 的计时器重启按「不补欠账、按当前节拍重置」近似（handler 注释已登记，与主循环 raw 20887-20894 的消费形态自洽）。唯一未 |
| 0x1cf | t05 | 一致 | 0 | argc（3 槽 ⇒ 1）、op1 的读、写 `_this[122504]`、**不写 122503**、无分支、与 0x1BF 的分工六条都与体一致；语料 44283 处 / 228 文件（`i1cf 0`）与实现路径一致。 |
| 0x1d0 | t24 | 一致 | 0 | 一致：引擎体的每个可观测项（只读 op3 / 写 op1、op2 及其写序 / 页表与记录表只读 / 四条失败路径与 LABEL_22 的 -1/-1+ret=1 / 返回值被忽略 / `sub_459860` 不改游标⇒幂等）在 emulator 都有逐条对应；`test/op-1d0-page-index.test |
| 0x1d2 | text-items | 一致 | 1 | ① 门：`textRecordingEnabled` = `(engineValues.get(97055) ?? 0) === 0`，与体 `if (!_this[97055])` 同（未赋值初值 0 ⇒ 记账开，与引擎装载清零一致）；② 读位/顺序：`plan.int(1)`=key、`plan.int(2)`=v |
| 0x1d3 | text-items | 一致 | 0 | 逐项对上：① argc 11 ↔ 计划 `[0x1d3, 5, ['int'×5], ['w','w','unused','r','r']]`；② 读位 op4/op5 与体一致，op3 两边都「读了不用／根本不读」（emulator 声明为 `unused`、handler 不读；**不接受**把死读搬进 emula |
| 0x1d4 | text-items | 一致 | 0 | ① argc 9 ↔ 计划 `[0x1d4, 4, …]`；② 读位 op4 与体一致，op3 两边都不消费（体读而不用、emulator `unused` 声明且不读）；③ 选择器恒 0 与体 raw 38142 的字面量末参一致；④ 未命中/越界初值 `-1/-1` 与体 `*a2 = -1; *a3 = -1`  |
| 0x1d6 | t23 | 一致 | 0 | 读 op2 ✓、写 op1 ✓（`元素个数 + 1`，与 `((end−begin)>>2)+1` 同式）、消费端 ✓（`resolveBgmResource` 的包外分支 `base[曲号 − 2]` 就是 `sub_48DB80` raw 108738 的等价物）、无分支可漏 ✓、帧状态格写 5 属「本条前进」的等 |
| 0x1d7 | t23 | 一致 | 0 | 三支与返回值逐项对上：`n < 0 ⇒ −1` ✓；`组数 ≥ n ⇒ 截断第 n 组 + 返回 0` ✓（`groups.length` = 组数，因引擎 PCM ctor `sub_48A830` raw 106797-106798 把 `_this[330]/[331]` 都置 0 且组对象无 ctor ⇒ 初值 |
| 0x1d8 | t23 | 一致 | 0 | 读 op2/op3 ✓（取值顺序先 op3 后 op2 也照抄）、写 op1（packed id / 槽下标 / −1）✓、越界 −1 ✓、从下标 1 找 0 槽 ✓、找不到落回追加支 ✓、消费端 ✓（`resolveBgmResource` 的包内分支 `组[(id>>24)−1][id & 0xFFFFFF]` = |
| 0x1f4 | frame | 一致 | 0 | 同一对 dword 字段（107438/107439）、同一 if/else 结构与同一写序；timeGetTime 由 host 时钟 nowMs 近似（帧循环同一帧内多次 i1f4 取同一 nowMs，与引擎同一 tick 内多次 timeGetTime 的差异不可观测）。 |
| 0x1f5 | frame | 有缺口 | 2 | 字段对（107439/107438）与递减/清锁两条路径一致，但引擎解锁分支里的派发判据是**三合一**（停靠标志置位 + 非派发中 + 队列恰剩 1 项），emulator 一个都没实现（注释只提到 dispatch_in_progress 这一条），且第二条的「队列恰剩 1 项」在注释里根本没有出现 ⇒ 派发能力缺 |
| 0x1f6 | t07 | 有缺口 | 1 | 四张表里第三张（`sub_4A9D10(v1 + 270)` = Scene+1080 572B-A，raw 130765）在 emulator 没有等价容器、`scClearDrawContainer` 也不动它 ⇒ 体里的一处副作用缺对应，判有缺口；既有 `tickets/T-0144` 已在票面/`notes.m |
| 0x1f7 | gfx-texture | 一致 | 0 | op1/op2 的读法、两条分支（≤1 单删 / >1 区间删）、区间口径 [handle, handle+count) 与「四张表一起擦」都对上了；引擎该体没有其它可观测项，emulator 也没有多做的动作。 |
| 0x1f8 | gfx-texture | 有缺口 | 2 | argc/操作数、表面记录与「新建空白表面（旧内容一起丢）」都有对应；缺两条：引擎在该体里先销毁同槽的 movie 播放器对象（emulator 无 per-slot movie 模型），以及 mode==3 建的是另一个类（DividedTexture，带子纹理 vector）而 emulator 只存 mode 号 |
| 0x1f9 | gfx-texture | 有缺口 | 2 | argc、三个操作数的读法、颜色归一化（负值落 0 / 强置 A=0xFF）与槽→imgid 记录都有对应；缺两条：颜色在引擎里被真正消费（进表面对象的装载调用并写槽记录 +4），而 emulator 走的那条 native 缝没有任何宿主实现；以及引擎先销毁同槽 movie 对象，emulator 无对应模型。 |
| 0x1fa | gfx-texture | 有缺口 | 3 | 「释放该槽的表面」有对应；缺三条：① 引擎只在该槽 movie 对象释放后才继续（emulator 无 movie 模型）；② sub_49E980 的 `Scene[slot+11676] == 0` 门在 emulator 完全没有；③ 引擎把槽→imgid 记录写成 −1，emulator 的 texSlots  |
| 0x1fb | gfx-texture | 一致 | 0 | 八个操作数的读法、源矩形「left/top + 宽/高」的口径、op1=handle/op2=槽 的分工、目标位置按 float 语义、以及子函数的写格（+4/+8..+0x14/+0x24..）都有对应；emulator 额外补的 `texSlots.set(slot, 0)` 与引擎 bss 初值 0 同值（0x2 |
| 0x1fc | t11 | 有缺口 | 1 | 读的操作数（op1）对上；但引擎体写的是 DrawItem 的三组变换字段（`+104` 与 `[27..42]/[59..74]/[91..106]`，会进 D3D 世界矩阵），emulator 侧只清自己那张诊断记录 map，没有任何渲染消费端 ⇒ 有缺口（缺的是"复位"这个效果本身，不只是某个字段）。 |
| 0x1fd | t07 | 一致 | 0 | argc=4、int/float 的类型分配（op1 int、op2..op4 float）、除数 100（`.lst` 里 `dq 100.0`）、写的引擎侧三格（`+0x68` 世界矩阵位、`+0x6C` work 缩放矩阵、`Scene+46508` 脏位）、项不存在即建项、零分支 ⇒ 逐项对上；emulator |
| 0x1fe | t11 | 有缺口 | 1 | 读的四格 float（不除 100）与 op1 对上；但引擎把 op2/op3/op4 当**轴向量**、op5 当**角度**并生成旋转矩阵写进绘制项（`+104=1`/`+492..+500`/`+516`/`+236`），emulator 只把四个裸值塞进诊断 map（全仓库唯一读者是 `scene/snapsh |
| 0x1ff | t08 | 一致 | 0 | 六项判据逐项对得上：①读 op1..op4 的类型/位置一致 ②本条无写出操作数 ③引擎字段 +0x68 / +0x16C / 脏位 46508 在模型里分别是 `Item.useWorld` / `Item.transWork` / `SceneState.dirty` ④本体无分支，被调的唯一分支（缺 key 即建 |
| 0x201 | t06 | 有缺口 | 1 | handler 形状与引擎一致（单 w、读 `_this[166964]`），但 emulator 侧**该字段没有任何生产端**：`CONFIG_FIELD_BINDINGS`（engineConfig.ts:229-257）里没有 `set:DrawMode` → 166964 的绑定（只有 displayScre |
| 0x202 | t08 | 有缺口 | 1 | 五项判据（读操作数 / 写操作数 / 引擎字段 / 分支 / 消费端）都对得上；唯一缺口在第 ⑥ 项“被调副作用”的下游读侧：引擎逐帧把颜色窗的插值结果**写回 DrawItem `+0x60`**（sub_49AA30 raw 117454-117479，`*((_DWORD *)a2 + 24) = v23;` 与 |
| 0x203 | t08 | 一致 | 0 | 判据①-⑥全中：读值/读序（α→color→blend→handle）一致；无写出操作数；`+0x30`→`Item.blend`、`+0x60`→`Item.from`、脏位→`SceneState.dirty`；三条回退分支（>255 钳、<0 取 FROM 的 α、<0 取整份 FROM）逐条实现且都在写前取；消 |
| 0x204 | t20 | 有缺口 | 2 | 读的 4 格、门③（空串）、门①（槽表面存在 ⇒ 宿主同口径）、描边/非描边两路（宿主按 `outlineMode` 分档）、全局字体/颜色/描边来源都与引擎对应；门②（GDI 可锁定）与「空白字在 BlankExtentMode==1 下按量宽前进」两项在 emulator 无对应物（后者已登记，见 finding） |
| 0x205 | t20 | 有缺口 | 2 | 读的 6 格、格式化的全部位语义、x 前进量三档、全角转换都在 emulator 有对应；`BlankExtentMode == 1` 的 cy 覆盖只做到「门 + 公式已接、度量来源缺 ⇒ 显式回退」（见 finding 2）；**op2 的写回是 emulator 自造的**（见 finding 1）。 |
| 0x207 | t11 | 有据豁免 | 0 | 有据豁免，三条都能对上或已被等价物取代：① 八格读法、两次重复读 op3/op4/op7/op8、src/dst 两组矩形的化开逐字一致（emulator 直接用 op5/op6 当宽高，与引擎重复读的结果同值）；② 夹取：`sub_4A3980` 是"等量平移"，而 `clampScaledBlit` 的按比例跟随在 |
| 0x208 | gfx-texture | 有据豁免 | 1 | 操作数与两个写回、>999 的 0/0 回退、表面不存在时的 0/0、以及「这是会写脚本操作数的 getter」都对上了。唯一的偏离是 **pixi 路径的同步性**：引擎在同一帧内读出真实宽高，emulator 的纹理要经异步载入，未就绪时返回 0×0 —— 这条已在 headlessScene.ts 的注释（tic |
| 0x20a | t20 | 有缺口 | 2 | ① 的重排（宿主通道固有步骤、顺序在贴格之前）与 ② 的门 + 游标取值在 emulator 有对应，op1 读两次不产生可观测差（两次读之间没有写脚本状态的语句）；未建模的是门的选择器 `124350 ? 95779 : 174801`（见 finding 1）与 `set:DrawMode` 分路（见 findin |
| 0x20b | gfx-texture | 有缺口 | 1 | 七个操作数、op4/op5 = 宽/高（而不是右下角）、α 只钳上界、颜色「A 恒 FF + 低 24 位」且**无负值回退**、以及「没有表面就不画」都对上了；缺 callee 里的**矩形夹取 + 空矩形早退**（见 finding）。 |
| 0x20c | frame | 一致 | 0 | 操作数面（0 个）与字段面（107438 门 + 92333/92334 时钟对）一致；`sub_4B4040` 是宿主绘制容器的呈现端（D3D/DDraw 子系统），emulator 由渲染帧循环的 `native.frameTick` 承担呈现，属有据豁免；`_this[168998]=0`（byte 675992 |
| 0x20d | t11 | 有缺口 | 1 | 读的一格（op1）与消费端（`Scene+46456` → 混合门）都在 emulator 有对应，但引擎"槽纹理不存在 ⇒ 不改目标 + 错误串 + return 0"这条分支没有对应：emulator 无论如何都改写目标槽，而目标槽正是混合门的一半条件 ⇒ 有缺口。另外"op1 > 999 归一化成 -1"这一支在 |
| 0x20e | t12 | 有缺口 | 1 | 可观测项对差：argc/操作数（0 读 0 写）一致；`sub_498B60` 的 Clear 与错误串属设备层，emulator 无渲染目标可清（重写侧每帧自绘）⇒ 有据豁免；但分支 ① 的条件（两个渲染目标状态字段）在 emulator 里既没有字段也没有判据，handler 只笼统调一次宿主缝（连注释都没提条件） |
| 0x20f | gfx-misc | 有缺口 | 3 | 三格操作数的读法与 argc 一致（含读序：引擎最后读的是 op3，emulator 的 `p.int(1)/p.int(2)/p.int(3)` 实参求值顺序也是 op3 最后），但引擎体里的三类可观测项在 emulator 全无对应：① 影片对象表与槽纹理表的读写（`_this+4*op2+378688` / `+ |
| 0x212 | t21 | 有缺口 | 1 | 读的两格、写的偏移（+100）、写序（先 op2 后 op1）都对上；差的只有引擎那个「对象表项为空 ⇒ 跳过写」的空指针门：`MsgWindow.object(idx)` 是「取（必要时新建）」，对空表项与越界下标都会**新建一个全 0 对象再写**，与引擎的静默跳过不等价（本批 0x213/0x25d 同一实现同一 |
| 0x213 | t21 | 有缺口 | 1 | 偏移（+104 op2、+108 op3）、读序、argc 全对；差的仍是「对象为空 ⇒ 跳过」的门（同 0x212/0x25d 的 `object()` 自动新建）。注意这对字段不是死写：`styleOfWin` 读 f104 当层序、`itemRangesOf` 用 f104/f108 判「脚本删掉这窗的图元」，所 |
| 0x214 | t08 | 一致 | 0 | 判据①②（无写出操作数；读序 op2→op1）一致；判据③接口是“绘图项表内整块互换 740B”，emulator 用“按 handle 原地交换字段、保留对象身份”等价实现，且明确不碰网格表；判据④四个分支（都缺/单缺/都在）都有对应实现，其中“都缺 ⇒ 只置脏、非失败”与 scSwapItems 返回 false  |
| 0x215 | t08 | 一致 | 0 | 引擎两条 −1 分支（键不存在 / flags bit0 未置）在共享层逐条复刻，返回值写回 op1，类型 int；无错误串、无建项副作用 ⇒ 六项判据全中。 |
| 0x216 | t08 | 一致 | 0 | 读/写操作数、被读的引擎表（槽↔imgid）与“未绑定 = 0”都与体一致，且无分支可漏。唯一理论上不确定的是极界槽号（≥1000）落到相邻字段时的引擎返回值：emulator 按“未绑定 ⇒ 0”处理；`_this[81174 + 5*slot]` 在 slot ≥ 1000 时越出网格/绘制项表域，本轮未确定那块内 |
| 0x217 | t08 | 一致 | 0 | 读操作数/类型、字段落点（`+24/+28/+32` → `Item.pivot*`）、无门控写入、脏位置位、消费端公式（与 0x219 的 `pos` 是两个三元组且都被正确分别消费）逐项一致。handler 的 JSDoc 末句写“转发 `native.setDrawPivot`”而代码就是 setDrawPivo |
| 0x218 | t08 | 一致 | 0 | 两条分支（缺项全 0 / 存在取 `+24/+28/+32`）、三条 float 回写、与 0x217 的读写格严格对应（无 flags 门，与体一致）⇒ 一致。 |
| 0x219 | t08 | 一致 | 0 | 读操作数/类型、字段落点（`+36/+40/+44` → `Item.pos*`，与 0x217 的 pivot 严格区分）、无门控写入、脏位置位、消费端（位置语义 + 与 pivot 的配对公式）逐项一致 ⇒ 一致。 |
| 0x21a | t09 | 一致 | 0 | 读/写的格、读序写序、引擎字段（DrawItem +36/+40/+44）、未命中回退（全 0）与非门控语义在 emulator 逐项有对应；无遗漏分支。 |
| 0x21b | t06 | 一致 | 0 | 读格、写字段、BOOL 归一（含负值 ⇒ 1）、无回写 —— 逐条对上。该字段的复位（raw 17982）与主循环流程读（raw 20746）属引擎整体复位 / 主循环能力，不是本 handler 体内可观测项，且已登记在 `analysis/fields.json` 的 `engine_bool_flag`（evid |
| 0x21c | frame | 一致 | 0 | 字段（174801 = effectFlags）、位（0x400）、动作（\|=，非赋值）都一致；宿主转发只是额外通知，不改变 VM 状态。 |
| 0x21d | t09 | 有缺口 | 1 | 操作数读写、三张表的命中语义、键不动、成功置脏、源全不存在的错误串在 emulator 都有对应；但引擎的另外两条失败路径（顶点缓冲创建失败 / 加锁失败）在 emulator 完全没有对应物，故判有缺口。 |
| 0x21e | t09 | 一致 | 1 | argc、6 格操作数（含三个 float 的 ÷100）、`flags & 1` 门控与「只建项不配窗」、delay/dur/目标矩阵/useWorld/共享起点、窗末 work←target 的推进语义在 emulator 逐项有对应；无遗漏分支。 |
| 0x21f | t09 | 一致 | 0 | argc、7 格操作数（4 float + 3 int、读序）、`flags & 1` 门控、delay(+64)/dur(+84)/轴(+0x1F8..0x200)/止角(+0x208)/目标旋转矩阵/useWorld、窗末 work←target 在 emulator 逐项有对应；无遗漏分支。 |
| 0x220 | t09 | 一致 | 0 | argc、6 格操作数（3 float 不除 + 3 int、读序）、`flags & 1` 门控、delay(+68)/dur(+88)/目标平移矩阵/useWorld、窗内线性插值与窗末 work←target 在 emulator 逐项有对应；无遗漏分支。 |
| 0x223 | t09 | 有缺口 | 1 | 8 格操作数（含 [6]/[7] 交叉）、写序、记录表归属（Scene+1048）、24 格默认记录与 `[4] = -1`、分类格 `[0] = 0`、置脏、类别 0 的消费端在 emulator 都有对应；但引擎在写记录之前的**惰性建层/渲染目标重绑**（`sub_4A2C10`）在 emulator 没有任何对 |
| 0x224 | t12 | 一致 | 0 | 逐条对上：argc 0、0 读 0 写、无分支；引擎「逐节点 delete + 复位头尾 + size=0」的语义在 emulator 是 `transitions.clear()`（真的清空）+ 同步清运行期窗状态，方向一致。唯一差异是引擎删的是 C++ map 节点对象、emulator 清的是纯数据表，属宿主表示 |
| 0x228 | t09 | 一致 | 0 | argc、op2 作为输入 + op1/op3/op4/op5 作为输出（含失败支只写 op1、成功支最后写 op1 的写序）、查表只读不建、量取自 +0x16C 平移 work 矩阵，在 emulator 逐项有对应。体里 D3DXMatrixDecompose 的失败支无实证可达（需要退化矩阵，语料 1097 处都 |
| 0x229 | t12 | 有缺口 | 1 | 操作数侧一致：5 格全读、类型一致（2 int + 3 float）、无回写。差异在写侧：引擎每次都先把 Scene+1120 起的 740 B「DrawItem 模板」整块复位（`sub_49A300` 里连 `+720 = 0`、`+576 = -1`、`+724/=+728/=+732` 的搬运都在），再写 `[ |
| 0x22a | t09 | 一致 | 0 | argc、三条全 float 操作数（÷100）、无 handle 查表、被调体写 Scene 变换块与置脏、以及「只作用于层号 ∈ [20,30)」的消费端范围在 emulator 都有对应；无遗漏分支。 |
| 0x22c | t09 | 一致 | 0 | argc、三条全 float（不除）、无 handle 查表、被调体写 Scene+371 平移块与置脏、消费端只作用于层号 ∈ [20,30) 在 emulator 都有对应；无遗漏分支。 |
| 0x22d | t10 | 有缺口 | 2 | 操作数（5 格读、顺序按值等价）、写的量（axisScale = 三 float ÷100、maskA/maskB = 两个 int）、消费链（只作用层 20..29）都有对应；但 emulator 把 Scene 的四条写端（0x22A/0x22C/0x22D/0x22F）**归并进同一个 kind 互斥的 `Sce |
| 0x22f | t10 | 有缺口 | 2 | 操作数（5 格读）与「写 Scene[297]/[302] + 三 float 不除」都有对应，但**三 float 的引擎语义与 emulator 建模不同**（见 findings，新增的 P1 语义缺口）—— 引擎把 op3/4/5 同时当作 `Scene+387` 平移矩阵分量与旋转轴，并在层 20..29 支 |
| 0x230 | t10 | 一致 | 0 | 逐项对齐：读 1 格 int ✓；清 bit2 ✓；清 5 个周期槽（544/548/552/556/560）✓（emulator 遍历 `it.loops` 全量清零，恰为 5 条通道）；清 W_FLIPBOOK 起点槽 +540 ✓；不清 +524/+528/+532/+536 ✓（`LoopWin.start`  |
| 0x231 | t10 | 一致 | 0 | 4 格 int 读 ✓；置 bit2 ✓；`+540 = 0` ✓；`+560/+568/+572` 三格写入 ✓（`+568/+572` 与 A 层 0x239 共用同一对 `fbFrames/fbCols`，与引擎一致）；置脏 ✓；无门控、缺失即建项 ✓；消费端分帧公式（除法 + 取模，单调递增而非三角波/保持末帧 |
| 0x232 | t10 | 一致 | 0 | 4 格 int ✓；op3>255 钳 255 ✓；op3<0 取当前色 alpha ✓；op4<0 取当前色 rgb ✓；ARGB 组装顺序 A/R/G/B ✓；置 bit2 ✓、`+524 = 0` ✓、`+544 = period` ✓、`+576 = loopTo` ✓、置脏 ✓；缺失即建项 ✓。回退源：引擎从 |
| 0x233 | t10 | 一致 | 0 | 5 格读（三 float ÷100、两 int）✓；置 bit2 ✓；`+528 = 0` ✓；`+548 = op2` ✓；`+592` 缩放阵三分量 = op3/4/5 ÷100 ✓；置脏 ✓；缺失即建项、无 `flags & 1` 门控 ✓；消费端「三角波 + 逐格 lerp(单位阵, L, tri/period |
| 0x234 | t10 | 有缺口 | 2 | 5 格读 ✓、置 bit2 ✓、`+532 = 0` ✓、`+552 = period` ✓、`+580/584/588` = 三 float 原样 ✓、锯齿波形与 `%period` ✓、置脏 ✓、缺失即建项无门控 ✓。缺口在**旋转语义的降维**：引擎是 `D3DXMatrixRotationAxis(轴 = 三  |
| 0x235 | t10 | 一致 | 0 | 5 格读 ✓、置 bit2 ✓、`+536 = 0` ✓、`+556 = period` ✓、`+656` 三分量 = op3/4/5 原样 ✓、置脏 ✓、缺失即建项无门控 ✓；消费端三角波 + `(tri·L + (period−tri)·I)/period` 与「基线平移 + L·r」在数值上一致（`L·(tri/ |
| 0x238 | t12 | 一致 | 0 | 逐条对上：argc=1、读 1 格 int、不回写；写的两个字段（92338/92339）与引擎同偏移同序（先 0 后 op1），并有真消费端（`gatePending`）。引擎 handler 体无分支，故无遗漏分支。 |
| 0x239 | t10 | 有缺口 | 1 | 6 格 int 读 ✓；写入集（bit1、`+52`、`+72`、`+92`、`+568`、`+572`、`+564`）全部有对应字段 ✓；`flags & 1` 门控**已建模**（`scSetFlipbook` 的 `created-gated` 早退）✓；消费端分帧公式、列/行换算、源矩形平移（不是 UV）、窗末 |
| 0x23b | t07 | 一致 | 0 | 读序（op2 先判门、其余操作数只在门内读）、门条件（`v2 <= 0xA && *v3`）、错误串分支（emulator 静默返回并打日志）、三档 x 计算、前导零判据（`flags&1 \|\| k==last \|\| rem!=0`）、区间删除 `[id, id+digits)` 与「op6≤0 ⇒ 不画」都能 |
| 0x23c | frame | 一致 | 0 | 字段对与写序一致，且同样无条件；不渲染这一点与体一致（体无 sub_4B4040 调用）。 |
| 0x23d | gfx-misc | 有缺口 | 1 | 引擎侧有可观测效果（958 个影片对象析构 + Scene 侧解绑 `Scene[5*slot+466] = -1` 与槽对象析构），emulator 侧没有任何对应写入 ⇒ 不是「有据 no-op」而是缺实现；该缝虽已在 nativeTap 登记（不是静默），但引擎语义的一半（Scene 解绑 `Scene+1864 |
| 0x23f | gfx-texture | 有缺口 | 2 | 操作数与写回、`×1000` 的取整、缺项 ⇒ −1 的形状都对，但**取值来源接错了表**：引擎读 `Engine[op2+94672]`（该槽的**对象**，由 0x236 sub_4246B0 / 0x20F play-movie 惰性建、被 0x1F8/0x1F9/0x1FA 置 0，尺寸由 sub_4080B |
| 0x242 | t12 | 一致 | 0 | 逐条对上：argc=2、两格 int 全读（读序 op2→op1 不影响结果）、不回写；写的两个落点（DrawItem `+720` 与相邻表项 `+504`）都有对应；「缺失即建项」语义一致；`+720` bit0 有真消费端（池挂起判据 `ops.ts:930`）。引擎另写脏标记（`sub_4AAA50`/`sub |
| 0x243 | t12 | 一致 | 0 | 逐条对上：argc=0、0 读 0 写；唯一分支的条件（`Engine[92340]` bit1）在 emulator 有对应字段（`0x24e` 写、`ENGINE_FIELD.msgField92340`）且判据同形；两个真消费字段（92338/92339）清 0 与引擎同序。豁免项：`Engine[92336]` |
| 0x244 | t11 | 有缺口 | 1 | 绘制项那一支（`Scene+1036` → `record+52`）逐字对上（emulator 的 `Item.animStart` 就是 +0x34 = +52，见 engineDrawItem.ts:27）；但引擎体的另外两张 572B 表（`Scene+1084`/`+1100` 的 `node+24`）在 em |
| 0x245 | gfx-texture | 有缺口 | 3 | operand 计划里 argc=2、op1/op2 的位置对；缺三条：① 引擎的对象存在门（不存在就完全不动、连 op2 都不读）；② 引擎写进对象前要 ÷1000（dbl_51FB50）；③ 交付侧没有任何消费者（宿主三实现都没有该方法）。语料 0 处 ⇒ 目前不可见，但这是「对象属性面」的一部分，重实现对不上会在 |
| 0x246 | gfx-texture | 有缺口 | 4 | argc 与 op1/op2 位置对；缺四条：对象存在门、类型标记门（52839C）、÷100 的单位、以及交付侧没有消费者。另注意该缝与 0x1F9/0x249 的颜色共用，一个宿主实现无法同时满足两种语义（颜色 vs 参数值）。 |
| 0x247 | t06 | 一致 | 0 | 与写入端 0x21b 同一字段（166965）、同一 0/1 归一；字段缺省取 0 恰等于引擎的初始值/复位值（raw 17982 `*(_DWORD *)(_this + 667860) = 0;`）⇒ 一致。 |
| 0x248 | gfx-misc | 有缺口 | 1 | op1 的读法与 argc 一致，但引擎侧该全局**有消费者**（多个 blit 函数按它分块），emulator 侧 `engineValues[-248]` 是纯死写：grep `-248` 只得 gfx-misc.ts:59 一处写入 + 两处注释，无任何 `get(-248)`，也不在 dead-writes  |
| 0x249 | gfx-texture | 有缺口 | 3 | 操作数、颜色归一化（T-0086 已修）、按 id 开文件的「已使用」标记、异步失败归宿主都有对应；缺三条：① 引擎在本条路径把槽→imgid 记录写成 **−1**，emulator 写成 imgid（方向相反，0x216 可观测）；② 引擎先销毁同槽 movie 对象；③ 颜色交给的宿主缝没有消费者。 |
| 0x24e | t06 | 一致 | 0 | 写入语义逐条对上（读 op1 原样写 92340，无回写、无变换）。消费端：bit1 那条门已在 emulator 建模（gfx-state.ts:97 与 raw 13910 同形）；bit0 的消费端（raw 21114）属 `system:EffectSkipOnClick` 特效跳过路径，该路径在 emulat |
| 0x24f | t12 | 有缺口 | 1 | 操作数侧与两条边界都对上（10 格全读、无回写；`split < 1` 改值后照写、type 无符号越界直接 return 不写；`[9..12]` 由默认记录 `sub_49A640` 的 0 提供）。写侧 12 格的下标/取值也逐格核对一致。缺口在**工作纹理对象的建/换**：引擎每次都对 op2 槽做「槽空或对象 |
| 0x250 | t12 | 一致 | 0 | 逐格核对：引擎显式写的 16 格与 emulator 的 16 条 writes 下标/取值一一对应（含 `[17]/[18]/[21]/[22]` 的显式清零），延迟/时长/纹理槽/起始项/跨度的落位一致，脏标记由 `dirty = true` 覆盖，`[9..12]` 由默认记录提供（与引擎 `sub_49A640 |
| 0x251 | t12 | 一致 | 0 | 逐格核对：引擎 16 格显式写与 emulator 16 条 writes 一一对应（`[19]/[23]` 显式清零，`[20]` 收 op8、`[21]` 收 op9、`[22]` 收 op10 的错位关系也一致），延迟/时长取自最后两格 op11/op12，`[13]=1` 与 `0x250` 的 `[13]=0 |
| 0x252 | t06 | 一致 | 0 | 读格与写入字段（92323）原样对上；引擎该字段无读者（`92323]` 1 命中 = raw 33064 写；字节 369292 0 命中）⇒ emulator「存而不消费」与引擎观测等价，不能按 noop-claim-unjustified 记 ⇒ 一致。 |
| 0x256 | t13 | 一致 | 0 | 五个操作数全读、读写语义与分支全部对上：区间 `[op1, op1+op2)`、只碰已存在项、逐项写平移、置脏，emulator 侧都有对应且注释（gfx-state.ts:377-384 / scene/ops.ts:1195-1208）与引擎体一致。宿主端 3D 不渲染属渲染后端能力（本次只核指令层），不记缺口。 |
| 0x258 | t13 | 一致 | 2 | ① 两格操作数读写一致（op2 引擎截 char、emulator 因按位取低两位而等价）；② 两张镜像表 emulator 合成一张 `texSlotFlags`（值 = bit0 \| bit1<<1），命名与 `& 0x3` 都是**单一表示**的写法，两位语义对得上；③ 这四格引擎字段全工程**只在 raw 3 |
| 0x259 | gfx-misc | 有缺口 | 1 | 清标志位这一步与引擎一致（0x258 同时写主/镜像两表、此处整表归零，一个 Map 足以覆盖），保留 imgid 与槽对象也与引擎逐位复核一致（T-0102）。缺口在**消费者**：`Engine.texSlotFlags` 全仓只有声明、写入与清空（grep 仅命中 engine.ts:518、gfx-state. |
| 0x25a | t06 | 有据豁免 | 1 | 两个字段的写入与取值和引擎完全对上；两条 `sub_4A5470` 下发没有对应物 —— 它属项目明确排除的「播放影片/媒体图层」子系统（`analysis/opcodes.json` 的 0x25A 语义条目即自称『已登记缺口』，`host` 侧无影片层可挂）⇒ 判「有据豁免」，同时按契约保留一条 P3 findin |
| 0x25b | t06 | 有缺口 | 2 | 读格与 92381 写入对得上，但有两处缺口：① 引擎体内还写 `_this[92379] = 2`，emulator 这一格根本没写（与同族 0x25A 写 mediaMode=1 不对称）；② `!167990` 时的第二次读 + `sub_408440` 图像加载与失败抛异常完全未建模 ⇒ 判「有缺口」（两条都已 |
| 0x25c | t21 | 有缺口 | 1 | 13 个 dword 的布局、加法对（op7+op5 / op8+op6）、哨兵 -1、清零段、argc=17 全部逐格对上；差的是消费端：`block224` 在 emulator 全仓**没有任何读者**（只有接口声明与初始化，见 finding），而引擎这 13 dword 是写进被所有绘制路径共用的窗对象缓冲头 |
| 0x25d | t21 | 有缺口 | 1 | 偏移（+276 op2、+280 op3）、读序、argc 一致；差的仍是对象为空的分支（`object()` 自动新建）。`f276/f280` 有真消费者（`itemRangesOf` 的第二组区间），所以伪造对象同样会把假区间送给宿主。 |
| 0x25e | t21 | 一致 | 0 | 读的 5 格、写的 3 个偏移、窗号 0 ⇒ 默认窗的回退、alpha>255 截断、RGB 取 op5 低 3 字节且不移位，全部对上；`packArgb` 与引擎位运算逐位等价（负 alpha 时 `a & 0xff` 与引擎有符号右移的差别属不可达输入）。无额外能力被发明。唯一可记的读数差：体把记录长度写成 11 |
| 0x25f | t21 | 一致 | 0 | 读的 4 格、写的两个偏移（+264 op2、+268 ARGB）、alpha 钳位 255、RGB = op4 低 3 字节、窗号 0 回退，全部对上；与引擎位运算逐位等价，无发明的能力。唯一可记的读数差：体把记录长度写成 9，而按 `N = 2*argc+1` 应为 9（argc=4）——一致，无分歧。 |
| 0x260 | t21 | 有缺口 | 2 | 4 格读、op→字段的映射（80102=op1/80103=op2/80104=op3/80105=op4）与引擎一致；但① 存储位置不对：引擎是 Font 级**全局** 4 格，emulator 挂到「**写入那一刻的 defaultWin** 的 geom」上，脚本若在此后切默认窗，这 4 个值就跟着错窗（全局语 |
| 0x261 | t06 | 一致 | 0 | 读格与写入字段对上（80101），消费端也按引擎同口径只取 bit0（emulator 存整值 + `& 1`，与引擎 `*(_BYTE *)(… + 235108) & 1` 一致）；语料 1048 处、值只有 1（×878）与 0（×170）两档，bit0 两侧都有真实用例 ⇒ 一致。 |
| 0x2bd | t21 | 有缺口 | 2 | 主字段（`_this[75953]` = Font+218516，引擎的权威字重格）与真假两支的 700/0 取值都对，`font.mainBold` 也确实进了排版快照；缺的是引擎那对**同步写的另一个成员** `Font+1248`（emulator 不建模，见 finding）与 `sub_459F40` 的 ` |
| 0x2be | t21 | 有缺口 | 2 | 主字段（`_this[75971]` = Font+218588，与 `sub_418680` 写注音字号的 `_this[54646]`=+218584 相邻，故可认定为注音那一格）与真假取值都对，`font.rubyBold` 进了注音样式快照；缺的是① 同步写的另一格 `Font+1308`（emulator 不 |
| 0x2bf | s06 | 一致 | 1 | ① 三个操作数的读取与用途逐条对应（op1=通道、op2=循环、op3=延迟）；② 引擎写 5 个字段的语义在 emulator 收敛成一个 `delay` 记录（武装/起始时刻/延迟/循环 + 全局有待处理位），是同一状态机的等价物；③ 越界通道双侧都是「不武装」（引擎报错串 + return 0，emulator  |
| 0x2c0 | s06 | 有缺口 | 2 | 读操作数（op1..op3）与「武装 + 按延迟起播」的状态机 emulator 都有，但引擎起播时还消费了两个 emulator 丢掉的东西：**循环位**（来自通道状态 `Voice[...+122508/12]` 的第 0 位，不是操作数）与 **op2 原值（pan）**；emulator 把 `loop` 硬 |
| 0x2c5 | strings | 一致 | 0 | ①argc=2、读 2 格写 1 格、字节口径与体一致（`test/op-string-len.test.ts` 锁了 0x2C5 的字节口径订正，`tickets/T-0076` 的 B3 记录）；②语料 8 处 op2 全是字符串型（`local-string-ptr 0` / `local-string 0`）⇒ |
| 0x2c6 | strings | 一致 | 0 | ①argc=2、读 2 格写 1 格；②口径一致：引擎 `_mbstrlen` 数的是**字符**（SJIS 双字节字算 1），emulator 的 `.length` 对 BMP 字符同样是 1（语料 8 处的 op2 是 `local-string-ptr`/`global-string`，内容为 SJIS 范围内 |
| 0x2c7 | strings | 有缺口 | 1 | ①argc=4、读格与类型（2=串/3=int/4=int）、写格（只写 op1）、写序一致；②越界与 `len <= 0` ⇒ 空串、两种『切在全角中间』修正、以及修正后的 `n <= 0` 再取空串都与体对应（`config-version-substr.test.ts:110-134` 锁了 TITLE 的真实三 |
| 0x2c8 | strings | 有缺口 | 1 | ①argc=4、读格与类型、写格（只写 op1）、字符口径与『双字节只看起点、单字节还看钳位前终点』的不对称逐条复刻（`test/op-1cb-2c8-2c9.test.ts:113-129` 锁）；②判『有缺口』是因为体的**256 字节截断**在 emulator 侧没有对应（`strcpy_s(Destinati |
| 0x2c9 | memory | 一致 | 0 | 按 tag 分派、负索引错误串、扩容式 ENC(0) 初始化、写 op1 引用（非元素值）四点与体一致；差异仅在“引擎只补 [旧长, idx] 的新槽”与“emulator 只补缺失槽”的不同表达——两者都是“已有元素绝不被覆盖”，语义等价。语料 0 处调用 ⇒ 无实测可对照，但逐支对应完备。 |
| 0x2cc | config-read | 一致 | 0 | 一致：键名相同、无读位、只写 op1、原值直写、argc 1；缺配置时引擎的 GetConfig 缺省与 emulator 的 `cfg(c,key)` 缺省 0 同口径。语料 4 处（CONFIG1.txt:1570/1577 等 `i2cc (local-int 7ef)`）只要求写回 op1，无更细分歧。 |
| 0x2cd | t22 | 一致 | 0 | 读的格子、写的键、无分支三点全部对上：引擎只读 op1 并写配置表同名键，emulator 同样只写该配置键，没有多余字段写、没有自行回退分支。 |
| 0x2ce | t06 | 一致 | 0 | 逐条对上：同一字段（167990 = 字节 671960）、同一配置来源（引擎 raw 11980 `GetConfig("display:ScreenMode") != 0` 对应 engineConfig.ts:235）、同一 0/1 归一、同样只写 op1 不回读 ⇒ 一致。附注：`app/amayui-emul |
| 0x2d0 | s03 | 一致 | 0 | 读的两格（op2、op3）、写的 op1、运算（float 加）、argc 3 全部对上，且两侧都无分支 ⇒ 无可漏分支。语料形状 `i2d0 (local-float 4) 72 (local-int 246)`（12 处 / 4 文件）里第二格是 int 立即数：体侧 sub_41C300 的 case 0 把它转 |
| 0x2d1 | s03 | 一致 | 0 | 减法方向、读的两格、写的一格、argc 3 全对上，两侧均无分支。语料 `i2d1 (local-float 0) 0 1`（13 处 / 4 文件）里两格都是立即 int，走与 0x2d0 同一套 int→float 语义；对照另一侧的 0x2d3/0x2d4（fdiv/fmod）不在本批，未一并判定。 |
| 0x2d2 | s03 | 一致 | 0 | 读 op2/op3、写 op1、float 乘法、argc 3 逐项对上，两侧都无分支。语料 **0 处 / 0 文件**（全库无 `i2d2`）⇒ 该指令目前不可见，但口径与体一致；它没有独立单测（只有 test/operand-plan.test.ts:130 的逐条计划核验覆盖），这属于测试覆盖问题而不是实现缺口 |
| 0x2d3 | s04 | 一致 | 0 | ①②③④ 四项都对上：读 op2/op3 ⟷ `p.float(2)/p.float(3)`（计划 kinds 两位 float）；写 op1 ⟷ `p.setFloat(1,…)`；除法语义与「无除零分支」一致（JS `x/0` 同样得 ±Infinity/NaN，不抛）；float 截断由计划层的 float 槽写 |
| 0x2d4 | s04 | 有缺口 | 1 | 读/写位数与顺序、被调 tag switch 都一致；唯一差是引擎 `fmod(l, 0)` 产生 NaN、而 emulator 用 `r === 0 ? 0 : …` 把除零改成静默 0（多了一条引擎没有的分支 + 不同的值）。该 opcode 语料 0 处，故缺口当前不可见，按 approximation 记 P3 |
| 0x2d5 | s04 | 一致 | 0 | argc 2 ⟷ 计划 `argc: 2`（`test/opcode-arity.test.ts` 同源核验 N=5）；读 op2 ⟷ `p.float(2)`；写 op1 ⟷ `p.setFloat(1,…)`；无分支 ⟷ 无分支。语料 13595 处全是 `float-mov <float 槽> <int/floa |
| 0x2d6 | s04 | 一致 | 0 | argc 2 ⟷ 计划 `argc: 2`；读 op2 的「整数语义」逐 tag 对上（引擎 `sub_41BF50` 的 tag 1 是 `(int)float` 截断，emulator `readIntOperand` 的 `\| 0` 同为截断；字符串 atoi、指针解引用同样对齐）；写 op1 ⟷ `p.set |
| 0x2d8 | memory | 一致 | 0 | 填充值是脚本值（与 0x6c 的固定 ENC(0) 区分）、个数语义、`count>0` 短路三点与体一致；语料 69 处形态（`set-array-to (global-int ea732) 1 64`、以及 `local-ptr`/`local-int` 目标）都不分叉。 |
| 0x2da | t07 | 有缺口 | 1 | 读的 8 格操作数、写的 7 个 dword（同键记录整体覆盖）、错误串分支、门内才读 op2..op8 都一致；唯一偏差是门比较的符号性：`v2` 在引擎是 `unsigned int`，`n = -1` 在体里走越界支（`-1 > 0xA` 按无符号为真），emulator 的 `n < 0` 也判越界 ⇒ 两者对 |
| 0x2db | t07 | 一致 | 0 | 引擎侧全部可观测项（argc、读 op1、写 71744 的值、71744 的消费端二选一）在 emulator 都有对应，且消费端判据与引擎同一格、同一三分支形状；`sub_459F40` 的 GDI 字体对象重建属宿主侧，emulator 的等价物就是它 1709 行按 71744 现算度量 ⇒ 判一致。 |
| 0x2dc | t22 | 一致 | 1 | 写的格子（int op1）与「空表 ⇒ -1」这条钳位都对上；读的不是操作数而是宿主字体表，emulator 用固定表 `ENGINE_FONT_LIST` 表达，属已建模的等价物（同一张表被 0x2DD/0x2DE 共用，三处口径一致）。 |
| 0x2dd | t22 | 一致 | 0 | 读格（int op2）、写格（字符串 op1）、越界两侧（`<0` 与 `>= count` ⇒ 空串）都对上；操作数类型（op1 需字符串型、非字符串型在引擎抛异常）也有既有豁免并在 test/opcode-operands.test.ts:117 登记。 |
| 0x2de | t22 | 一致 | 0 | 读格（字符串 op2）、写格（int op1）、剥 '@'（raw 35149）、未命中 -1 三条都对上；`sub_428990` 的比较用 `sub_401000` 逐字节，emulator 用 normalizeFace（大小写/空格不敏感）属更宽的近似，但候选表本身是 emulator 自定的等价物，库内面名不 |
| 0x2e5 | t14 | 有缺口 | 1 | handler 体（读-清-写、argc 1、只写 op1、与 `_this[1949]` 分开的两个累加器）逐字一致 —— 这也是本批里最容易做错的一处（旧状态是「未实现」），现已实现且守卫齐。唯一差异是累加侧的同一个 `0x90100000` 门未建模。 |
| 0x2e6 | config-read | 有据豁免 | 0 | 有据豁免：键名注意点已复核 —— 引擎的常量命名有陷阱（`aMessageAutomes_2` 是 Pitch0、无后缀的 `aMessageAutomes` 是 Pitch1，raw 4310/4313），emulator 的 `{0: messageAutoMessagePitch0, 1: messageAuto |
| 0x2e7 | t22 | 一致 | 0 | 三分支与键名逐一对上（op1 非零且 op2≠1 ⇒ 不写任何键；op1==0 ⇒ 不校验 op2 直接写 Pitch0），读格顺序（先 op1 再 op2）也一致。既有审计 P2 `op-10-001` 的静默回退已修，且 test/config-version-substr.test.ts:201-205 已钉住「 |
| 0x2e8 | t22 | 一致 | 0 | 与 0x2CD 同形：读 op1、写同名配置键、无分支，逐项对上。 |
| 0x2e9 | t07 | 有缺口 | 1 | 指令侧的 ①②③④（读 op1 / 写 122464 / 无分支）逐项一致；缺的是⑤消费端——引擎里靠这格计算自动翻页停留时长，emulator 无对应读者（既有 `tickets/T-0076` 已把「消费端（自动翻页）仍是缺口」写明）。故判有缺口并保留 finding 复核。 |
| 0x2ea | config-read | 一致 | 0 | 一致：键名（`aMessageAutomes_1` 是 Option 而非 Pitch1/Time1，这是本条唯一的命名陷阱，emulator 取对了）、无读位、只写 op1、原值直写、argc 1 全部吻合。语料 1 处（CONFIG1.txt:1606 `i2ea (local-int 7f4)`）随后 `and  |
| 0x2eb | config-read | 有缺口 | 2 | 有缺口：读法（字符串查询 vtable+8 的对应物）、写位（op1 字符串）、argc 1、消费端（TITLE 三段切片）都对，但**取值口径**有两处分歧且其中一处在本机可复现：(a) 引擎内建缺省是 `1.00`、emulator 是 `1.07.0019`，且引擎第三层（`set:VerRegPos` → 注册 |
| 0x2ec | strings | 一致 | 0 | ①argc=2、读 2 格写 1 格、写序一致；②数值口径一致：C `atoi` 与 emulator 的『跳空白 + 符号 + 十进制前缀 + 无数字返 0』在语料形态（`TITLE.txt:599/602/605 i2ec (local-int 3ed) (local-string 1)`，源串是版本号切出的 `" |
| 0x2ed | config-read | 一致 | 0 | 一致：键名、写位、原值口径、argc 1 全部对上，且它由 `CFG_READ` 表导出（`cfgReadPlanOps()` 会机械核验结果位与 argc），T-0098 已把读侧补上并与写侧 0x2EE 做成闭环。语料 0 处 ⇒ 无脚本可见差异，也不存在未注册风险（已在表内）。 |
| 0x2ee | t07 | 一致 | 0 | 读的操作数（同一格两次读 ⇒ 一次读等价，已有清单 `.lst` 61030/61034 双 `push 1` 佐证）、两处写（字段 80106 与 SetConfig 常量键）在 emulator 都逐项对应，无遗漏分支；读侧 `0x2ED`（sub_431230：`_this[30 * _this[95776] + |
| 0x2f2 | region-hittest | 一致 | 0 | 逐项对上：op2/op3 值语义、op4 地址语义（4 个连续 dword）、op5/op6 为偏移且『偏移加在 DEC 之后』（40705-40708 对 emulator 358-361）、唯一写回 op1（0/1）、argc 6（13 = 1+2*6，且 emulator 计划 argc 6 / kinds [i |
| 0x2f3 | text-items | 一致 | 0 | ① argc 13 ↔ 计划 `[0x2f3, 6, …]`；② 读位 op5/op6 与体一致，op4 两边都不消费（体读而不用、emulator `unused` 且不读）；③ 写位 op1/op2/op3 = +20/+24/+28、顺序 1→2→3 与体一致，命中失败时 `-1/-1/0`；④ 扫描语义（掩码  |
| 0x2f4 | s06 | 有缺口 | 2 | 结构（分支、登记、状态位意图）基本对上，但有四处差异：① **状态位**（`21315`/`21318` 的读写、`122505`/`122508` 的置值）emulator 一律不写；② op2 在引擎里是**原值**参与起播（`a4 & 1`）与 ADV 寄存，emulator 归一成 `!== 0`；③ 记录里的 |
| 0x2f5 | s06 | 有缺口 | 2 | 读操作数齐（op1..op4，且 op4 当通道），武装 + 延迟起播在 emulator 有对应；缺口同 `0x2C0`：op2（引擎当 pan 消费）在宿主被丢，循环位硬编码 false。verdict = 有缺口，理由与 `0x2C0` 同构。 |
| 0x2f6 | t01 | 有缺口 | 3 | 操作数读、分支结构、宿主消费端（停播/清通道状态）都对得上；但引擎在 0x2F6 里**必然写**的 5 处引擎字段（`[21315+ch]`/`[21318+ch]`/`[122505+ch]`/`[122508+ch]`/`[122501]`）emulator 一处都不写（同族 0x1BC 却照写 ⇒ 同一批字段两 |
| 0x2f7 | t01 | 有缺口 | 2 | 唯一的引擎可观测写（`Engine[21315+ch]=1`）在 emulator 只映射成宿主内部布尔 `flag`，不落 `engineValues`；而同一槽位的 0x1BC 却会写 `voiceChannelStateBase+ch` ⇒ 同族两套口径，故判有缺口。 |
| 0x2f8 | t01 | 有缺口 | 1 | 操作数读序、对称钳位、宿主下发（SetPan ↔ Web Audio setPan）三项对齐；差异只剩「设备通道号上界」这一条边界（引擎 ch<15 即可写字段，emulator 只认 0..2，ch=3..14 时引擎改设备字段并下发 pan、emulator 静默丢弃），故判有缺口。 |
| 0x2fa | t24 | 有据豁免 | 0 | 有据豁免：本次复核确认写入目标是**只写不读**的 `Engine[1951]`（`grep 1951` 全文件只有 raw 33724 这一处写、零读取点），emulator 不建模它不会丢掉任何消费者；读 op1 本身无副作用 ⇒ 跳过与「读了再丢」对 VM 观测等价（「读了再丢」还会引入死读，与 0x1D3/0x |
| 0x2fc | t15 | 有据豁免 | 1 | 复核后**该豁免仍成立**：我在体里确认 `sub_477980` 的**唯一**返回 1 的点就是 (v9[4]&4)==0 且 (v9[4]&0x10)!=0 的命中项（raw 92144-92155），桌面鼠标（无触摸项）必然返回 0 ⇒ 走 raw 40799 的 op1=0 分支，与 emulator 逐字一 |
| 0x2fe | t22 | 有缺口 | 2 | 读格（字符串 op1）对上，但被调副作用里可观测的部分没有对应物：引擎的「不在可选字体表且非 AGE Extend ⇒ 打警告」这一支在 emulator 完全不存在（任何面名静默接受），且引擎同时写死的**注音字形布局字段**（`Font+1292/+1296/+102032/+102036` 与字号挂钩）与 `@` |
| 0x2ff | t01 | 有缺口 | 2 | 操作数读齐、语义（「预备、不生效」）方向一致；但引擎本条的两个字段写（`[21318+ch]=1`、`[21321+ch]=op2`）emulator 一个都不落，且把原值钳到 0..10000（引擎保留原值 —— `[21321+ch]` 在 0x302 里会被置成 0x10000，说明该槽允许 >10000 的值） |
| 0x300 | t22 | 一致 | 0 | 读的 3 格、写的两个字段与偏移（122466 保留 bit16 的合并口径、122476 直写）逐项对上；额外写的 `gateOf` 只是 emulator 对「122466/122476 这两个字段的消费端」自建的等价结构，不改变字段值。 |
| 0x301 | t22 | 一致 | 1 | 读格、写的字段（122486、对象 +132）与两段绘制项删除的等价物（`msgWinClear`）都对上；`sub_404F80` 里唯一被 handler 改写/漏掉的是「窗号 0 ⇒ 默认窗」这一重定向（见 finding，语料 0 处、且 emulator 对 win 0 有独立对象槽）。 |
| 0x302 | t01 | 有缺口 | 2 | 设备侧「因子生效并下发」的链路（`sub_4BBC30`→`sub_4B6210`→宿主增益）在 emulator 有等价物；但引擎本条**必然写**的两处字段（`[21318+ch] = 0x10000`、`[21321+ch] = op2`）emulator 一处不写，且额外做了引擎没有的动作（应用后清 `prep |
| 0x303 | t23 | 一致 | 1 | 引擎可观测项逐项对上：读 op1/2/3 ✓；op1=0 回退默认窗 ✓（`resolveWin`）；+288/+292 两格 ✓（`align`/`alignWidth`）；无分支/钳位/错误串 ✓；被调 `sub_456600` 的副作用只有那两格写 ✓；写侧无回写 ✓。唯一超出引擎的是把 op2 归一成 0/1/ |
| 0x304 | t23 | 一致 | 0 | 读/写逐项对上：`flags=1`（赋值，注释也点明不是 \|=）✓；槽回退默认窗 ✓（`resolveWin` = `i === 0 ? this.defaultWin : i`，与 `if (!v1)` 同形）；`+296 ← +132` ✓（`lineCursorSave ← reveal.shown`）。引擎在 |
| 0x305 | t23 | 有缺口 | 2 | 清 `flags` 与取回行游标已对上；但引擎的**总门** `(flags & 0x10001) == 65537` 在 emulator 里没有对应分支：emulator 无条件把余下行贴出/结束逐字显现。该门不是恒真——bit16 只由 `0x6E show-text` 在 bit0 已置时补写（raw 2910 |
| 0x306 | t07 | 一致 | 0 | 读配置键、写的操作数序号（op1）、缺省值来源（注册表默认 0，raw 111578-111579）三项都一致；与同名写入端 `0x307` 共享同一注册表，读回闭环成立。 |
| 0x307 | t07 | 一致 | 0 | 读 op1、写到同一配置键、无门控三处一致（`setConfigValue` 也不做值域裁剪 ⇒ 与体的「照写」同形）；写入端与 `0x306` getter 构成闭环，`tickets/T-0076` 记的实现已落地。 |
| 0x308 | t24 | 有缺口 | 2 | 有缺口：引擎侧有真实宿主副作用（按 `op1 \|\| GetConfig("system:limitTouch")&1` 选择注册还是注销触摸，经 LoadLibrary/GetProcAddress 调外部导出）加一个字段写，emulator 侧只有一行日志 —— 白名单把它当「unhandled 桩」豁免，但 ` |
| 0x30a | t24 | 有缺口 | 1 | 有缺口：表本体只写不读（跳过它确实不可观测，这部分豁免成立），但 `op1 > 0x1F \|\| op2 > 7` 的**越界抛错分支**在 emulator 没有任何对应，白名单文本（「键位注册；emulator 无按键表」）也没覆盖它 —— 且 op2 为负/大值时引擎会先抛错、emulator 会静默放行。 |
| 0x320 | t11 | 有缺口 | 3 | 操作数格（10 格读、0 格写）、DEC 解码、颜色合成 `(a<<24)\|(rgb&0xffffff)`、`vcount<=0` 不建几何这三项都对上；缺的是几何侧的 −0.5 半像素偏移（引擎 `dbl_51D7F8 = 0.5`，raw 4197 + 122424-122425）、新建 mesh 记录的默认态色 |
| 0x321 | t13 | 一致 | 2 | 三格操作数全读、写位置 `[op2 + 7]`、置脏三项都对上；find-or-create（引擎自动建零值项）与 emulator 的 Map 懒创建语义等价，故判一致。另有两条备注性 finding（自动建项未显式建模、op2 无范围校验）。 |
| 0x322 | t11 | 一致 | 0 | 引擎两条可观测项都对上：α 的 clamp/回退（`>255⇒255`、`<0⇒state0 高字节`）、rgb 的负值回退（`<0⇒state0`，且只取低 24 位），写入字段 `entry[9]=op2`/`entry[13]=color` 与 emulator 的 `m.blend`/`m.state0` 一一 |
| 0x323 | t11 | 一致 | 0 | 五格读序/透传语义、`flags \|= 2`、窗三格（`start=0`/`delay=op2`/`dur=op3`）、`state1 = argb`、负值回退取 state0 全部对上；buffer 置脏（`Scene[11627]`）由 `s.dirty = true` 承接。与 0x322 同源的"未设色记录  |
| 0x324 | t24 | 有据豁免 | 0 | 有据豁免：体只写 arity 槽（emulator 由 stepOnce 统一写 2*argc+1 = 1）并释放 emulator 里**根本不存在**的对象 —— 无对象可析构、无旗标可清，对 VM（不回写操作数、不改 ip/cur）与画面都不可观测；同族 0x325/0x326/0x327 同一登记口径（stub |
| 0x325 | t24 | 有据豁免 | 0 | 有据豁免：emulator 没有 Effect3D 子系统（管理器 + 三效果槽 + `sub_4535F0` 帧推进都不存在），而这两格在引擎里**只被 `sub_4535F0` 读**，本指令又不回写操作数、不改 ip/cur ⇒ 跳过对 VM 与画面均不可观测；扩展点 = 先建 `Scene+50704` 管理器 |
| 0x326 | t25 | 有据豁免 | 2 | 有据豁免成立：① 体内**零操作数写原语**（无 `sub_42B4B0`/`sub_42BA00`/`sub_418B90`/`sub_418CC0`），4 格全是值读 ⇒ 对 VM 不可观测；② 消费端是 Effect3D 管理器 + D3DX effect + Snow 对象，emulator 确实没有该子系统（ |
| 0x327 | t25 | 有据豁免 | 1 | 有据豁免成立：体内零操作数写原语、不改 ip/cur（只写 arity 槽），1 格全是值读；消费端是 Effect3D 管理器的 Rain 槽，emulator 确认没有该子系统（全 `src/` 无 Effect3D/Rain 模型；`scene.meshes` 只由 `0x32A`/`0x32B` 释放/清空，无 |
| 0x328 | t25 | 有据豁免 | 2 | 有据豁免成立：零操作数写原语（数组被**读**并解码到临时堆缓冲，不写回脚本内存）、不改 ip/cur；消费端是 Effect3D 管理器的 Leaf 槽，emulator 没有。★但豁免理由里「emulator 无 3D 网格表」不准确：网格槽表**已建模**（`renderer/scene/ops.ts` 的 `s |
| 0x329 | t25 | 有据豁免 | 1 | 有据豁免成立：零操作数写原语、不改 ip/cur；渲染消费端（`sub_47A190(Scene+50708+4*槽, 矩阵)` 等绘制路径）在 emulator 没有 3D 网格对象可建。副作用披露（装载失败的异常路径不发生）已在作业书与台账 note 里明写，本次复核确认属实且**不止异常**（见 findings |
| 0x32a | t13 | 一致 | 0 | 唯一操作数 op1 读到位、语义（释放该槽 + 清零）对上，且 emulator 的 `delete` 对不存在键本来就是 no-op（与引擎判空分支等价）；置脏、记录释放都做了。宿主侧 3D 模型对象不渲染、`released3D` 无消费端属渲染后端能力，不记指令层缺口。 |
| 0x32b | gfx-misc | 一致 | 0 | 引擎体只有『逐项析构并写 0』一条能力，emulator 的整表 clear 与之等价（同样只清网格、不碰绘制项/纹理/记录表；`scene.meshes` 的键就是 create-mesh 的槽号 ⇒ 覆盖 0..999 的语义一致）。操作数（argc 0）、引擎字段（仅该槽表）、分支（空槽跳过 vs 清空现存项）三 |
| 0x32c | t25 | 有据豁免 | 1 | 有据豁免成立：零操作数写原语、不改 ip/cur、6 格全是值读；消费端 = D3D 设备 `SetTransform(view)` + Effect3D 管理器三矩阵转发，emulator 两者都没有（`Engine[93384]`/`Scene+50704` 管理器、`Scene+41928..` 矩阵面在 `sr |
| 0x32d | t13 | 有缺口 | 1 | 写序/通道映射错位：引擎 R = `BYTE2(v3)`（第三字节）、G = `BYTE1(v3)`、B = `BYTE0(v3)`（最低字节），emulator 却把**最低字节当 R、第三字节当 B**，颜色被交换（`rgb = 0xFFFFFF` 时引擎得白 (1,1,1)，emulator 得 (1,0,0)  |
| 0x32e | t25 | 有据豁免 | 2 | **有据豁免仍成立**，但豁免理由的**定性写错了**：0x32E 是 **D3D 光源配置**（`SetLight`+`LightEnable`），与已实现的 `0x32F`（`sub_4272B0` → `sub_49A150` → 同一个设备 vtable+212 = `LightEnable`，见 `handl |
| 0x32f | gfx-misc | 有缺口 | 1 | 引擎体有两个可观测项（Scene 灯光 enabled 标志位写 0、设备 `LightEnable(op1,0)`），emulator 既不写该标志位也没有宿主实现 ⇒ 效果不出现（不报错）。属已在 nativeTap 登记的宿主能力缺口，故保留 finding 并在 alreadyKnown 注明登记处；engin |
| 0x33f | t13 | 有据豁免 | 2 | 豁免依据（本次复核成立）：① 引擎确实三格全读（op2 经 `v2`、op3 经 `v3`、op1 三处 `sub_41BF50(_this, 1)`）；② `Scene+1264` 的消费端是效果通路的 shader 混合常量（raw 65904-65907 一带），emulator 既不建模效果常量通路、也不建模绘 |
| 0x340 | gfx-misc | 有缺口 | 1 | 引擎体有两个可观测项（写 `Scene+13948` 状态槽、设备状态 #22 下发），emulator 既不建模状态槽也没有宿主实现 ⇒ 效果不出现。属已登记的宿主能力缺口（nativeTap），语料 0 处，故 severity P3。 |
| 0x341 | t15 | 有缺口 | 2 | 操作数（op1 文件 id、op2 槽号）、argc、以及读文件然后构造对象写入同一槽数组这一串都对上了；判有缺口是因为体里两处可观测语义在 emulator 侧无对应：① 失败 ⇒ **抛异常**（raw 34488-34491），emulator 只 log 一条并把槽留空（assetLoader.ts:55/61 |
| 0x342 | t15 | 一致 | 0 | 逐项对齐：读 op1 ✓（`optInt(1)`）；槽号不做范围检查（引擎 `_this[op1+13953]` 越界即读到别的字段，emulator 的 `slotOf` 原值透传，runtime.ts:112-115 已声明同口径）；槽为空则无操作 ✓（Map.delete 对不存在的键是 no-op）；非空则移除 |
| 0x344 | t15 | 一致 | 0 | 逐项对齐：读 op1/op2 ✓；`record[0] \|= 1` ✓（`node.flags \|= 1`）；`record[1] = op2` ✓（`node.slot = slot`，即消费端 raw 134320 查的那个槽号）；取不到就建 ✓（`ensureNode`/`makeNode` 缺省 flags |
| 0x345 | t15 | 一致 | 0 | 三项参数按体的口径对上：op1 = 纹理文件 id（进 `sub_4559C0`）✓、op2 = **槽（对象）选择**（体里 a6=op2 决定 `_this[a6+13953]` 与 `sub_478370` 的目标对象；`sub_478370` 内部把 `this` 的 +36 当 10 个纹理指针数组，越界与  |
| 0x346 | t15 | 有缺口 | 1 | 三块 windows-from 矩阵（缩放 `+80..143`、旋转 `+208..271`、平移 `+336..399`）与基础矩阵 `+508..571` 的复位 ✓、`+76 = 0` ✓、`+4`/`+8..16`/`flags`/`+24`/delay·dur/颜色/`to` 矩阵不被碰 ✓。但体里**没有 |
| 0x347 | t15 | 一致 | 0 | 逐项对齐：读 op1(int) / op2..op4(float) ✓；`÷100`（`dbl_5201F0` = 100.0 已在 `.lst` 的 `.data:005201F0` 实证）✓；写 `+80` 三分量 ✓（`node.scale`）；`+76 = 1` ✓（`matrixDirty = true`）； |
| 0x348 | t15 | 一致 | 0 | 逐项对齐：读 op1(int) / op2..op4(轴 float，不除 100) / op5(角 float) ✓；轴写 `+464..472`、角写 `+488`（**存度**）✓；`+76 = 1` ✓；度→弧度用 `π/180` ✓（我先核了 `.lst`：`.data:00526C98 dq 3.14159 |
| 0x349 | t15 | 一致 | 0 | 逐项对齐：读 op1(int) / op2..op4(float，像素单位不除 100) ✓；写 `+336..399` ✓（`node.translate`）；`+76 = 1` ✓；无门、无钳位 ✓。语料 7 处（全在 BTL.txt：`i349 (local-int fff) (local-int 1000) 0 |
| 0x34a | t16 | 有缺口 | 1 | 读/写操作数口径、落点（+8/+12/+16）与子调用链都一致；但引擎最后那句置脏 `_this[11627] = 1`（Scene+46508）在 emulator 没有对应（L2dNode 既无 matrixDirty 也无宿主置脏缝），按可观测项 ③/⑥ 判有缺口。 |
| 0x34b | t16 | 一致 | 0 | 操作数口径（op2/op3 为 int、op4..op6 为 float ÷100）、delay/dur 落点（+32/+52）、门、`\|= 2`、`+24 = 0`、`+76 = 1` 都有对应；矩阵 `rec+144` 由 live2d/nodeMatrix.ts 合成时按窗重建（`affineScaling`/ |
| 0x34c | t16 | 有缺口 | 1 | 落点（+36/+56/+476..484/+492）、门、°→弧度（nodeMatrix.ts 的 affineRotation 用 PI*deg/180）都对；但 handler 给 op6 兜了 `?? 1` 的缺省 —— 引擎体对 op6 是无条件 sub_41C300 读，没有任何缺省值 ⇒ 记一条 host- |
| 0x34d | t16 | 一致 | 0 | delay/dur 与三分量的口径（op2/op3 int、op4..op6 float）、落点（+40/+60/+400）、门、`\|= 2`、`+24 = 0`、`+76 = 1` 都在 emulator 有对应；矩阵 +400 由 nodeMatrix 合成器按窗重建。★作业书里标的"已按体改对"与本次读体一致（ |
| 0x34e | t16 | 有缺口 | 3 | 操作数口径（4 个 int）与"装载即入队"都有对应，但三处可观测项缺失：错误路径不抛异常（只是 log + return null）、pending 纹理/动作号没有被搬到动作记录上（只被清空）、op2 != 0 支线的循环位在引擎里不被消费却在 emulator 里生效。 |
| 0x34f | t16 | 有缺口 | 1 | 操作数与"写哪条记录"（实例的纹理色项）对上了，但引擎的"整数 → 三个 0..1 分量（/255）"解码与 `op2 < 0 ⇒ 查纹理色记录` 回退在 emulator 完全没有对应（emulator 直接把 int 存进表里，且消费端只把它透传、未使用）。 |
| 0x350 | t16 | 有缺口 | 1 | 实例存在性门与"复位动作队列"对上了，但引擎同一处还清实例的 `+21/+22`（装载标志）与 `+20`（播放/循环位），emulator 的实例模型没有对应字段 ⇒ 记 missing-behavior（子槽逐部件乘色之外的状态清空缺失）。 |
| 0x351 | t16 | 有缺口 | 2 | `op3/255` 的换算与"按名字设参数"对上了，但引擎写明的 [0,255] 钳位在 emulator 完全没有（raw 原值直接除以 255）；另外实例不存在时引擎静默不做事、emulator 会 `ensureSlot` 建出一个空槽。 |
| 0x352 | t16 | 有缺口 | 1 | op2 的零/非零分支与落点（待纹理号 vs 待动作号）一致，但引擎的两条子函数都有 `if (*(_DWORD*)_this)` 实例存在性门（不存在 ⇒ 静默不做事），emulator 用 ensureSlot 反而**把不存在的实例建出来**并写入值 ⇒ 记 missing-branch。 |