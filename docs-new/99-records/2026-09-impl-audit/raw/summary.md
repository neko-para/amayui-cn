# 汇总（机器生成骨架；最终叙述见 records 报告）

- 已校验批次：指令 43/32，能力 7/7
- 覆盖 opcode：364/364；能力条目：54/54
- findings 合计 **383**（新增 280 / 已知 103）
- 严重度：{"P3":194,"P1":48,"P2":140,"P0":1}（新增：{"P1":29,"P2":94,"P3":156,"P0":1}）
- kind：{"missing-branch":74,"approximation":57,"missing-operand-io":37,"missing-consumer":49,"host-invented":13,"missing-behavior":70,"unclear":7,"stale-ledger":42,"overreach":33,"noop-claim-unjustified":1}
- 分类：{"tracked-done":79,"new":139,"unclassified":25,"audited-2026-09":129,"tracked-open":11}（每类严重度：{"new":{"P1":18,"P3":73,"P2":47,"P0":1},"audited-2026-09":{"P3":78,"P2":48,"P1":3},"tracked-open":{"P3":2,"P2":4,"P1":5},"tracked-done":{"P3":31,"P2":31,"P1":17},"unclassified":{"P3":10,"P2":10,"P1":5}}）
- 裁决分布：{"有据豁免":22,"有缺口":159,"一致":210,"描述过期":27}

## 新增 findings（按严重度）

| # | 对象 | kind | sev | 一句话 | 引擎锚点 | emulator 锚点 | 分类 |
|---|---|---|---|---|---|---|---|
| 1 | `0x108` | host-invented | P3 | `readButtons()` 把 `pressLatch`（mousedown 时置位、**首次读取才清**）或进返回值 —— 引擎的 `sub_477220` 每次都从清零的局部量重算、**没有任何保持位**；若一次 down+up 全落在两次 0x108 之间，引擎两次都读到 0，emulator 会在紧接着的那次读取里读到「左键按下」。 | `39051-39054` | `src/vm/input.ts:361` | unclassified |
| 2 | `0x10d` | approximation | P3 | 滚轮累加侧漏了引擎的「滚轮当按键」门：引擎在 `(Engine+699204 & 0x90100000) != 0` 时**不累加** `Engine[7796]`（改派发 `set:WheelKeyUp/Down` 掩码位），emulator 的 `addWheel()` 无条件累加 ⇒ 配置把滚轮当按键时，`0x10D` 会读到真机读不到的增量。 | `141580-141583` | `src/vm/input.ts:334` | new |
| 3 | `0x142` | missing-consumer | P3 | 引擎把 `_this[174812]` 导出成查询口 `sub_4765C0(){ return _this[699248] != 0; }`（脚本/宿主可读回该开关），emulator 的 `ENGINE_FIELD.scriptEngineFlag` 只有 0x142 这一处写、全仓**没有任何读者**（无 getter、无导出查询、无宿主消费）⇒ 设置页挂起/恢复的状态在 emulator 里写进去即断链。 | `31024-31027` | `src/vm/handlers/engine-fields.ts:378` | new |
| 4 | `0x148` | missing-consumer | P3 | 引擎该槽的读者是窗口过程 `sub_4B9240`（`timeGetTime() - dword_55E1D8 > *(_DWORD *)(dword_55E1BC + 388232)` 才继续走弹系统对话框的分支），emulator 没有对应的「点击去抖/系统对话框」宿主子系统 ⇒ `Engine.globalSlot97058` 写进/读出都不被任何东西消费。 | `141038-141043` | `src/vm/handlers/engine-fields.ts:43` | new |
| 5 | `0x14c` | missing-operand-io | P2 | 引擎对 **op3 的读取是条件性的**：只有 `slot > 0x63` 的越界分支里才 `sub_41B640(_this, 3)` 取第三个操作数（当错误串的 `%s` 实参）；emulator 却在任何路径都无条件 `plan.str(2)` 之后再无条件读 op3（`op_agerc_bind_export` 里没有 `plan.str(3)`，但 0x14C 的第三格在 real 语料里根本不存在）。后果：真机上 slot 合法时**不存在**的 op3 完全不碰；emulator 的 handler 一旦被改成读 op3 就会在合法槽路径上凭空要求第三个操作数（或按引擎写就只在越界分支读）。 | `31117-31120` | `src/vm/handlers/agerc.ts:96` | new |
| 6 | `0x192` | missing-operand-io | P2 | 第 2 格操作数的**强制转换能力**缺失：引擎 `sub_42A420` 对 int 族 tag 走 `_itoa_s(值, …, 10)`（取槽里的**值**转十进制串）、float 族走 `%lf`、指针族解引用后转换；emulator 的 `readStringOperand` 对 `TYPE_GLOBAL_INT`/`TYPE_LOCAL_INT` 等走到 `default: return String(a.raw)`，产出的是**槽号字面量**（如 global-int 0x18e3e ⇒ 字符串 "18e3e"... 实际按十进制 "102462"）而不是槽里的值，且不抛错 ⇒ 目标串内容静默错误。 | `36450-36458` | `src/vm/operand.ts:152` | unclassified |
| 7 | `0x193` | missing-operand-io | P1 | 第 2/3 格操作数的**指针解引用与整数/浮点转串**能力缺失：引擎 `sub_42A420` 对 `local-ptr`/`global-ptr` 族取**所指处的值**再转换（int ⇒ `_itoa_s` 十进制、float ⇒ `%lf`），emulator 的 `readStringOperand` 对这些 tag 落到 `default: return String(a.raw)` ⇒ 拼进去的是**槽号字面量**而不是元素值，数值消息会显示成十六进制槽号，且不报错。 | `36470-36475` | `src/vm/handlers/strings.ts:120` | unclassified |
| 8 | `0x19c` | approximation | P3 | 引擎 `0x19C` 在置 ADV 位的同一块里顺手清 `0x20000000`（= `sub_453A60` 逐字计时器「正在跑」标志，「进入 ADV 就把在跑的计时器作废」）；emulator 的 `op_adv_enter` 只做 `setAdv` + `m.skipping = 1`，没有对应地清自己的计时器态（`SLEEP_GATE` / `msgwin.beginReveal` 的 `revealUntil`）。 | `24567-24574` | `src/vm/handlers/msgwin.ts:1000` | new |
| 9 | `0x19d` | approximation | P3 | 查询口径被简化：体判的是「16 位哈希槽值等于 `(u16)(28569*id − 20304)`」（表由 `sub_454960` 写 `87912345*id − 1330597712`，取低 16 位），emulator 判的是「Set 里有完整 id」——两者都是 16 位/精确键的区分，理论差异只在「从未打开过但低 16 位哈希撞上已打开 id」的 id 上出现（概率约 1/65536，可忽略，但语义不是同一件事）。 | `38266-38286` | `src/vm/handlers/resource-usage.ts:73` | new |
| 10 | `0x1b0` | host-invented | P3 | 引擎体是裸 `memcpy`，对源/目标操作数的种类与元素宽度一无所知（string 族按 28 字节/元素排在池里也只是裸字节搬运）；emulator 额外要求 `dest.kind === src.kind && dest.stride === src.stride`，不符即抛 ⇒ 引擎会做的“异构操作数之间的按字节拷贝”在 emulator 直接失败。 | `37992-37995` | `src/vm/handlers/memory.ts:57` | new |
| 11 | `0x1b2` | missing-operand-io | P2 | 第 1 格的**整数转串**能力缺失：引擎 `sub_41B9B0(_this, 1)` 按 tag 分派并对 int 族走 `_itoa_s(值, …, 10)`（把槽里的**值**转十进制串）再追加进文本缓冲；emulator 的 `readStringOperand` 对 `TYPE_GLOBAL_INT` 落到 `default: return String(a.raw)` ⇒ 追加的是**槽号字面量**（`global-int 0` 时恰好也是 "0"，但其它槽号就是槽号），且计划层 `0x1b2` 把该位声明为 `kinds: ['str']`，实际是按 str 口径读。 | `26398-26402` | `src/vm/operand.ts:152` | unclassified |
| 12 | `0x1b6` | missing-consumer | P1 | 引擎有一条**「消费即清零」**的帧循环：消息泵入口 `sub_4090F0` 一进来 `if (a1[97052]) { a1[97052] = 0; return; }`（`sub_476080` raw 90961-90965 同形，改为按 `set:CoexistMesSkip` 决定 `97050 = 0`），即 `0x1b7` 置的共存态**只活到下一次消息泵调用**；emulator 把 97052 存进 `Engine.advFields` 后再没有任何读取/清零点 ⇒ 一旦 `i1b7 1`，此后所有 `0x1b6` 永远读回 1，计数永不归零。 | `13699-13707` | `src/vm/handlers/msgwin.ts:1084` | new |
| 13 | `0x1b6` | stale-ledger | P3 | emulator 的注释把引擎的消费端写对了、却与代码相反：注释说「引擎帧循环（raw 13699-13705）每帧看到它就 `97052 = 0` 并提前 return，同时按 `set:CoexistMess` 决定 `97050 = 0`」，但同一文件里没有任何实现这一句的代码，且把配置键名写成 `set:CoexistMess`（引擎表 raw 4276 的键串是 `"set:CoexistMesSkip"`，emulator 注册表 `configRegistry.ts:79` 也是 `set:CoexistMesSkip`）。 | `4270-4280` | `src/vm/handlers/msgwin.ts:1074` | new |
| 14 | `0x1ba` | missing-consumer | P2 | 四条分支末尾（以及 `0xBB`/`0xBC`）都会调 `sub_406DF0(Engine, 类别, a2)` 把该类声音的开关下发给正在播放的影片播放器音轨；emulator 的 `switchMovieEnable` 只写配置 + 打日志，`switchSeEnable`/`switchVoiceEnable`/`switchMusicEnable` 也只发各自的 `enable`/`bgm-mode` 意图，影片播放器音轨无宿主对应物 ⇒ 关掉 SE/语音/音乐时，影片里的对应音轨不会跟着静音。 | `12095-12103` | `src/vm/handlers/audio.ts:346` | unclassified |
| 15 | `0x1f8` | missing-branch | P3 | 引擎 create-texture 在建设定表面之前会先销毁**该槽已有的对象**（`_this[slot+94672]`，与 0x20F play-movie、0x236 sub_4246B0 建 entry 的是同一张 1000 槽表：sub_488FB0 + vtable 析构 + 置 0）；emulator 的 createTexture 只建空白表面，没有 per-slot 对象记录可清（pixiBackend.playMovie 仅 pushLog，0x236 亦未建模）。 | `31171-31188` | `src/vm/handlers/gfx-texture.ts:143` | new |
| 16 | `0x1f8` | missing-branch | P3 | `mode == 3` 时引擎建的是另一个类（`operator new(0x460)` + sub_43A5C0 = DividedTexture，vtable 不同且多出 +1104/+1108/+1112 的子纹理 vector），mode != 3 才是 NormalTexture（`operator new(0x450)` + sub_48AB20）；emulator 只把 mode 存进槽记录（scSetSlotMode 供混合门控）+ 打印日志，不建任何「分块纹理」模型。 | `122855-122872` | `src/renderer/pixi/textureCache.ts:349` | new |
| 17 | `0x1fa` | missing-operand-io | P2 | 引擎 release-texture 经 sub_49E980 把槽→imgid 记录写成 **−1**（`Scene[5*slot+466] = -1`，0x216 读的就是这一格）；emulator 的 op_release_texture 只删 texSizes，**从不清 `Engine.texSlots`**，于是释放过的槽仍返回旧 imgid。 | `119585-119603` | `src/vm/handlers/gfx-texture.ts:173` | new |
| 18 | `0x1fa` | missing-branch | P3 | sub_49E980 的整个清理块被 `if ( !_this[a2 + 11676] )` 门住：该格非 0 时引擎**不写槽记录 −1、也不销毁表面对象**；emulator 无条件删 texSizes + releaseTexture，没有这条门。 | `119585-119603` | `src/vm/handlers/gfx-texture.ts:174` | new |
| 19 | `0x1fa` | missing-branch | P3 | 引擎先销毁**该槽的 movie 播放器对象**（`_this[op1 + 94672]`，与 0x20F play-movie 同一张 1000 槽表：sub_488FB0 + vtable 析构 + 置 0）再释放槽；emulator 无 per-slot movie 模型可清。 | `31245-31269` | `src/vm/handlers/gfx-texture.ts:172` | new |
| 20 | `0x1fc` | missing-consumer | P3 | `0x1FC` 的语义是"把该 DrawItem 的变换字段清零/复位到单位"（`+104 = 0` 关掉用矩阵那条路，三组矩阵格复位），emulator 只把 handle 记进 `render4.primReset` 并删掉 `render4.primTransform` 里的记录 —— 既没写绘制项，也没有任何渲染路径读这两个字段。 | `131275-131276` | `src/renderer/scene/ops.ts:1083` | new |
| 21 | `0x1fe` | missing-consumer | P2 | `0x1FE` 的四个值在引擎里是「旋转轴 (op2,op3,op4) + 角度(度, op5)」，会组成 `D3DXMatrixRotationAxis` 写进该绘制项的变换块并置 `+104 = 1`；emulator 只把 `[a,b,c,d]` 记进 `render4.primTransform`，没有任何渲染代码读它 ⇒ `i1fe` 的旋转完全不生效。 | `131387-131390` | `src/renderer/scene/ops.ts:1090` | new |
| 22 | `0x20d` | missing-branch | P3 | 引擎在该槽纹理未创建时打「SetTargetTexture エラー：テクスチャが作成されていません． TEXTURE=%d」并**保持 `Scene+46456`（当前渲染目标）不变**，emulator 无条件把 `render4.renderTargetSlot` 改成新槽 ⇒ 混合门（0x203/0x322 选择子 2 的 `(ONE,ZERO)` 覆盖）会按错误的槽重算。 | `124872-124873` | `src/renderer/scene/ops.ts:1264` | new |
| 23 | `0x212` | missing-branch | P3 | 引擎 `if ( v4 )` 的空表项门在 emulator 被换成「必要时新建对象」：op1 指向未创建的对象槽时，引擎什么都不做，emulator 会凭空建一个对象并把 +100 写进去（该伪造对象此后还会被 0x213/0x25d 的同族写与 `itemRangesOf` 读到）。 | `31748-31754` | `src/vm/handlers/msgwin.ts:1139` | new |
| 24 | `0x21d` | missing-branch | P2 | CopyScene 的网格顶点缓冲失败两条错误路径未建模：引擎在「源网格命中」后还有顶点缓冲创建失败与加锁失败两个 `return 0` 分支（各自打一条独立错误串），emulator 的 `copyScene` 只有布尔 `copied`（源是否存在）一种失败表达，无法表示「源存在但缓冲失败」⇒ 该情形下 emulator 会静默当作成功、照常置脏并复制网格。 | `131184-131206` | `src/renderer/pixiBackend.ts:967` | new |
| 25 | `0x22f` | approximation | P2 | 引擎 0x22D 写的 `Scene+323` 缩放阵与 0x22F 写的 `Scene+387` 平移阵在收尾**连续两次 D3DXMatrixMultiply 叠加**（`v90 ← v90·v118`，`v73 ← v73·v120`）⇒ 先 i22d 后 i22f = 「先按 S 缩放、再平移 T」；emulator 只有一份 `sceneXform` 且取值按 `kind` 互斥，0x22F 把 `kind` 设成 'axis-scale' 后 `axisScale` 仍是 `sceneXformOf` 建锚时的 (1,1,1) ⇒ **0x22D 的缩放效果被整体丢弃**（引擎两个矩阵同时在）。 | `117927-117931` | `src/renderer/scene/ops.ts:1407` | new |
| 26 | `0x239` | missing-branch | P3 | 引擎的门是读**已存在元素**的 `flags` bit0（`if ((*(_BYTE *)result & 1) != 0)`）—— 一个 `flags = 0` 的既有项（例：早前 `set-draw-color-alpha`/`set-draw-pos` 建过但从未 `draw-texture`）同样会被跳过；emulator 用 `scEnsureItem().created` 近似（`(item.flags & 1) === 0` 只在**本次新建**时为真），无法表达「既有但 bit0 = 0」的项 ⇒ 这类项上 emulator 会写入 flipbook，引擎不会。 | `132128-132133` | `src/renderer/scene/ops.ts:514` | new |
| 27 | `0x245` | missing-branch | P2 | 引擎有 `if ( _this[result + 94672] )` 门：该槽没有对象时**不读 op2、不做任何事**（返回值就是 op1）；emulator 无条件读 op2 并调用宿主缝 —— 对没有对象的槽也产生一次调用（在 DropRecorder 里留下丢弃记录）。 | `32660-32678` | `src/vm/handlers/gfx-texture.ts:112` | new |
| 28 | `0x245` | missing-operand-io | P2 | 引擎把 op2 **除以 dbl_51FB50（= 1000.0）** 后才写进对象（sub_4081B0 的第 2 实参是 double）；emulator 把 op2 原值直接交给宿主缝 ⇒ 单位差 1000 倍（同一族的 0x246 是 ÷100，两者不是同一个常量）。 | `32660-32678` | `src/vm/handlers/gfx-texture.ts:113` | new |
| 29 | `0x245` | missing-consumer | P3 | emulator 唯一的落点是 `native.setTextureObjectFloat`，而 PixiBackend / HeadlessScene / StubNative 都没有实现它（native.ts 只声明为可选缝）⇒ 引擎里真实写进对象字段的浮点参数在 emulator 完全消失（nativeTap 记一条丢弃记录）。 | `32660-32678` | `src/vm/native.ts:273` | unclassified |
| 30 | `0x246` | missing-branch | P2 | 引擎有两道门：`if ( _this[result + 94672] )`（该槽没有对象 ⇒ 不读 op2、不调用）与 `if ( *(_DWORD *)(v4 + 1084) == dword_52839C )`（对象类型标记不符 ⇒ 不调用）；emulator 两道都没有，无条件读 op2 并把调用送给宿主缝。 | `32679-32703` | `src/vm/handlers/gfx-texture.ts:126` | new |
| 31 | `0x246` | missing-branch | P2 | 类型标记门 `*(obj+1084) == dword_52839C` 是引擎体的第二道条件（只有该类型才调 `*(obj+1044)` 子对象的 vtable+56）；emulator 完全没有类型概念（也没有对象模型）。 | `32679-32703` | `src/vm/handlers/gfx-texture.ts:127` | new |
| 32 | `0x246` | missing-operand-io | P3 | 引擎下发的实参是 `(double)op2 / dbl_5201F0`（÷100），emulator 把 op2 原值交给宿主 ⇒ 单位差 100 倍。 | `32679-32703` | `src/vm/handlers/gfx-texture.ts:127` | new |
| 33 | `0x246` | missing-consumer | P3 | 宿主三实现都没有 `setTextureObjectParam`（native.ts 只声明为可选缝）⇒ 引擎里真实的 vtable+56 调用在 emulator 消失；且该缝被 0x1F9/0x249 复用来传**颜色**（0xFFRRGGBB），与这里的「op2÷100 的参数值」语义冲突，任何单一实现都无法同时正确。 | `32679-32703` | `src/vm/native.ts:278` | unclassified |
| 34 | `0x248` | missing-consumer | P3 | 引擎全局 `dword_55052C`（默认 256，blit 分块/格宽除数）在 emulator 里没有消费者：写进 `engineValues[-248]` 后无人读 ⇒ 脚本改这个值不会让 emulator 的任何绘制/换算随之改变（真机的 blit 分块口径会变）。 | `45886-45888` | `src/vm/handlers/gfx-misc.ts:59` | unclassified |
| 35 | `0x249` | missing-branch | P3 | 引擎 0x249 在按 op1 开文件前先销毁**该槽的 movie 播放器对象**（`_this[op2 + 94672]`，与 0x20F play-movie 同一张 1000 槽表：sub_488FB0 + vtable 析构 + 置 0）；emulator 没有 per-slot movie 记录可清。 | `32716-32769` | `src/vm/handlers/gfx-texture.ts:97` | new |
| 36 | `0x24f` | missing-consumer | P3 | 引擎在写记录**之前**会按 op2（工作纹理槽）检查 `Scene+42456` 槽表：槽不存在、或该纹理对象的 `[262]`（字节 +1048）与当前渲染目标 id 不符 ⇒ 调 `sub_4A2C10(Scene, op2, 宽, 高, 当前 id)` 重建/换绑工作纹理；emulator 没有纹理对象层，op2 只是被写进记录 `[4]`，这条建/换行为与「槽无效」这一分支完全缺失。 | `133744-133782` | `src/vm/handlers/gfx-state.ts:239` | new |
| 37 | `0x25a` | missing-consumer | P3 | `sub_4A5470(Scene, id)` 的两条媒体下发分支（模式变化门 raw 33198-33199、非 display:ScreenMode 门 raw 33200-33201）在 emulator 完全没有对应：`op_set_media_movie` 只记 `mediaMode/mediaId` 两个字段，两个门的输入（`92377` 模式镜像、`167990`）在 emulator 也没有维护者 ⇒ 影片/媒体层永远不会出现在宿主画面上。 | `33198-33201` | `src/vm/handlers/engine-fields.ts:423` | unclassified |
| 38 | `0x25c` | missing-consumer | P3 | `block224` 写完即弃：emulator 里没有任何排版/渲染代码读它（引擎那 13 dword 是文本项缓冲头，被绘制路径消费）。 | `68358-68370` | `src/vm/handlers/msgwin.ts:1211` | new |
| 39 | `0x25d` | missing-branch | P3 | 与 0x212/0x213 同一处缺口：对象表项为空时引擎不写 +276/+280，emulator 会新建对象写入，并让该区间进入 `itemRanges`（第二组 DrawItem 区间）。 | `33259-33264` | `src/vm/handlers/msgwin.ts:1160` | new |
| 40 | `0x2bf` | missing-branch | P3 | 引擎的越界分支会**报错**（`aSetdelaySound` 错误串 + `sub_4034C0`），emulator 的越界只在宿主 `#seChannel` 里记一条日志、不发任何意图；两边都不武装，但「引擎会弹错误」这一可观测串在 emulator 侧丢失。 | `137734-137739` | `src/audio/audioEngine.ts:350` | new |
| 41 | `0x2c0` | missing-operand-io | P1 | 0x2C0/0x2F5 的 **op2 = 附带值** 在引擎里是**要消费的 pan**（起播时 `sub_4BB840` 的第 5 实参 = `Voice[5053+ch]`，而 `Voice[5053+ch]` 就是排入时写下的 op2）；emulator 的 handler 把它当 `aux` 传下去，宿主 `voiceQueue(ch, id, _aux, delayMs)` 又**整个丢弃** ⇒ 排队语音起播时 pan 恒为当前通道的 `v.pan`（默认 0），op2 的定位信息丢失。 | `142661-142666` | `src/audio/audioEngine.ts:390` | new |
| 42 | `0x2c0` | missing-behavior | P2 | 引擎排队起播的循环位来自 `(unsigned int)v3[12] & 1`（通道状态里的位，`0x243`/`sub_426360` 一类指令写它），emulator 在 `voiceQueue` 里把 `loop` **硬编码为 false** ⇒ 先置了循环位的通道，经 `i2c0`/`i2f5` 排队起播时引擎会循环、emulator 只播一次。 | `142661-142667` | `src/audio/audioEngine.ts:650` | new |
| 43 | `0x2d4` | approximation | P3 | 引擎 `fmod(被除数, 0)` 得 NaN（无分支、无错误串），emulator 用 `r === 0 ? 0 : l % r` 把除零改成静默 0 ⇒ 浮点取余的除零结果与真机不同（多一条引擎没有的分支）。 | `40161-40174` | `src/vm/handlers/arithmetic.ts:201` | new |
| 44 | `0x2da` | approximation | P3 | 门的符号口径与体不同但结论等价：体是 `unsigned int v2` 与 `0xA` 比较（负数按 2^32 巨值 ⇒ 越界），emulator 写 `n < 0 \|\| n > 0xa`（显式把负数判越界）。两者对 `n = -1` 都返回、都不写记录 ⇒ 本轮**未发现**行为差异，登记为口径近似（若将来有人把 `n < 0` 改掉或按无符号归一化，就会与体分叉）。 | `33503-33509` | `src/vm/handlers/gfx-cg.ts:33` | new |
| 45 | `0x2dc` | approximation | P3 | 引擎的字体表是被枚举填充的运行时表（`EnumFontFamiliesExA` 写入 `Font+201664`），未枚举时 `v1` 为 0 ⇒ 返回 **-1**；emulator 用编译期常量 `ENGINE_FONT_LIST`（9 项，恒非空）⇒ **永远不可能返回 -1**，即引擎「字体枚举失败/为空」这一支在 emulator 不可达。 | `40244-40248` | `src/vm/handlers/msgwin.ts:1379` | new |
| 46 | `0x2e5` | approximation | P3 | 水平滚轮累加侧漏了引擎的 `(Engine+699204 & 0x90100000) != 0` 门：真机在该门成立时把 `WM_MOUSEHWHEEL` 当作 `set:HWheelKeyUp/Down` 配置键派发、**不写** `Engine[7800]`；emulator 无条件累加 ⇒ `0x2E5` 会读到真机读不到的横滚增量。 | `141608-141611` | `src/vm/input.ts:342` | new |
| 47 | `0x2eb` | approximation | P3 | 取值口径近似：引擎是「内建 `1.00` → INI `[set] GameVersion` 覆盖 → `set:VerRegPos` 非空时用注册表 `DisplayVersion` 覆盖」三层，emulator 是「INI → DEFAULT_GAME_VERSION(`1.07.0019`)」两层，注册表层整体不实现（跨平台/免安装取舍），且缺省值与引擎内建的 `1.00` 不同。 | `42575-42593` | `src/vm/handlers/config-read.ts:138` | unclassified |
| 48 | `0x2f5` | missing-operand-io | P1 | 0x2F5 的 **op2** 是引擎起播时消费的 pan（`sub_4BB840` 第 5 实参 = `Voice[5053+op4]` = 排入时写的 op2），emulator 传成 `aux` 后被宿主 `voiceQueue` 丢弃 ⇒ 指定通道排队起的语音少了定位信息。 | `142565-142569` | `src/vm/handlers/audio.ts:545` | new |
| 49 | `0x2f5` | missing-behavior | P1 | 排队起播的循环位：引擎取通道状态的 `(unsigned int)v3[12] & 1`，emulator 在 `voiceQueue` 里硬编码 `loop: false` ⇒ 通道已置循环位（`0x243`/`sub_426360` 一类写 `Engine[5037+ch]`）时，`i2f5` 排队的语音引擎会循环、emulator 只播一次（`#voiceBusy` 判单次时长，播完即停）。 | `142661-142667` | `src/audio/audioEngine.ts:393` | new |
| 50 | `0x2f7` | missing-operand-io | P3 | 0x2F7 写 `Engine[21315+ch] = 1` 这一格在 emulator 不落地（只写宿主 `v.flag`）；0x1BC 清同族三格、0x2F7 置位这一半缺失，engineValues 里该槽永远看不到 0x2F7 的效果。 | `33694-33704` | `src/vm/handlers/audio.ts:562` | new |
| 51 | `0x2f7` | approximation | P3 | 状态位是**位域**（0x2F4 一族读 `[21315+ch] & 0x10000` / `& 1` 并在两态间翻转），emulator 的 `v.flag` 是 0/1 布尔，无法表达 0x10000 位；0x1BC 的一次性清 0 也与 0x2F4 的翻转口径不同。 | `33620-33628` | `src/audio/audioEngine.ts:417` | new |
| 52 | `0x2ff` | missing-operand-io | P2 | 0x2FF 的两个引擎字段写（`Engine[21318+ch] = 1` 的「有因子值」位、`Engine[21321+ch] = op2` 的原始因子）emulator 全缺 ⇒ 同族的 0x302（读这两位决定是否把因子写进设备）与 0x2F4 一族（按 0x10000 位翻转）在 emulator 里都失去「预备」这一半的输入。 | `33728-33740` | `src/vm/handlers/audio.ts:578` | new |
| 53 | `0x2ff` | approximation | P3 | emulator 对因子做 `clampVolume`（0..10000 + `Math.round`），引擎对 0x2FF 的 op2 是**原值直写**（`[21321+ch]` 随后可被 0x302 置成 0x10000，说明该槽不是 0..10000 域）。 | `33728-33740` | `src/audio/audioEngine.ts:423` | new |
| 54 | `0x3` | missing-behavior | P2 | 深度越界在引擎走 ShowMessage 报错串（`_TI1_AVCommand_ShowMessage_Exception__`，与装载失败抛的 Command_Exit 是**两种**异常），emulator 把它和装载失败统一成 `throw new Error`：既没有报错串文本，也没有「可恢复报错 vs 程序中止」的区分。 | `26776-26783` | `src/vm/handlers/control.ts:288` | new |
| 55 | `0x3` | missing-operand-io | P3 | 引擎在**装载成功之后**才写被调帧记录的「调用点」格 `v4[95804] = 当前 ip 的 dword 偏移`（= 本条指令下标）；emulator 在 `await readScript` 之前就 `c.frame.ip += 1`，装载失败时调用方的 ip 已被改（引擎那时 `v4[95804]` 还没写、且随后抛 Exit 中止，无后续读取）。 | `26784-26788` | `src/vm/handlers/control.ts:290` | new |
| 56 | `0x301` | missing-branch | P3 | 引擎 `sub_404F80` 对传入窗号做 0⇒默认窗 的重定向（`if ( !a2 ) v2 = *(_DWORD *)(_this + 1228);`，raw 10748-10749），emulator 的 `op_msgwin_slot_clear` 把 op1 直接当窗号用（`e.msgwin.object(v)`），没有把 0 映射到 `defaultWin`。 | `10747-10750` | `src/vm/handlers/msgwin.ts:1606` | new |
| 57 | `0x302` | missing-operand-io | P3 | 0x302 的 `Engine[21318+ch] = 0x10000` 与 `Engine[21321+ch] = op2` 两处字段写在 emulator 全缺 ⇒ 与 0x2FF 一样，`[21321+ch]` 这个「本通道的因子」在 VM 字段面永远是 0，engineValues 无法复核引擎「0x2FF 预备 → 0x302 生效」的两次写。 | `33767-33777` | `src/vm/handlers/audio.ts:642` | new |
| 58 | `0x302` | host-invented | P3 | `voiceFactorApply` 里 `v.preparedFactor = null` 是宿主自创：引擎 0x302 只**写** `[21321+ch]`（保留因子值），并不清 0x2FF 预备的那个值；`sub_4BBC30` 之后 `[21321+ch]` 仍在，后续 0x302/0x2F4 还能再读它。 | `142680-142695` | `src/audio/audioEngine.ts:429` | new |
| 59 | `0x32f` | missing-consumer | P3 | 灯光开关整条能力缺失：引擎写 `Scene[4*op1+54708] = 0`（灯光 enabled 位，设备重建时被读来回放 SetLight/LightEnable，raw 122112-122118）并下发设备 LightEnable；emulator 既不写这个标志位也没有宿主实现（gfx-misc.ts:32 的 `setLight?.()` 落在空处）⇒ 关灯意图完全消失。 | `116741-116744` | `src/vm/handlers/gfx-misc.ts:32` | unclassified |
| 60 | `0x340` | missing-consumer | P3 | 渲染状态整条能力缺失：引擎把 op1 存进 `Scene[13948]` 并在设备重建时重放（raw 122124），emulator 既不写这个状态槽也没有宿主实现（gfx-misc.ts:98 的 `setRenderState?.()` 落在空处）⇒ 脚本设过的渲染状态 #22 在 emulator 完全不可见。 | `116874-116875` | `src/vm/handlers/gfx-misc.ts:98` | unclassified |
| 61 | `0x34c` | host-invented | P3 | emulator 在 op6 缺失时把轴 z 分量当 1（`p.float(6) ?? 1`），引擎体对 op6 是无条件读（无任何默认）；若真缺 op6，引擎读到的是 0/未定义位模式而非 1 ⇒ 旋转轴不同。 | `34666-34668` | `src/vm/handlers/live2d.ts:224` | new |
| 62 | `0x350` | missing-behavior | P2 | 引擎 0x350 除复位队列外还清实例的 `+21/+22`（本帧已装载标志）与 `+20`（播放/循环位）；emulator 只把 `inst.current` 置 null，动作装载标志与播放位状态没有被清（子槽+动作记录上的循环位也没清）。 | `92653-92663` | `src/live2d/mtn.ts:273` | new |
| 63 | `0x351` | missing-branch | P2 | 引擎把 op3 钳到 [0,255] 后再 /255（负数 ⇒ 用 0，>255 ⇒ 用 255，且钳位值会覆盖除法用的局部量）；emulator 无钳位，直接把原值 /255 ⇒ 越界实参会得到范围外的参数值。 | `34756-34772` | `src/live2d/runtime.ts:248` | new |
| 64 | `0x351` | host-invented | P3 | 引擎在实例/模型不存在时静默不做事（sub_478520 的 `if (v3)`），emulator 走 `ensureSlot` 会把该槽**建出来**并写入参数 ⇒ 多出一个空实例槽（引擎那边没有任何表项产生）。 | `92667-92674` | `src/live2d/runtime.ts:249` | new |
| 65 | `0x5` | missing-branch | P3 | 引擎在返回点 `== -1` 时**不跳**、步长槽仍为 1 ⇒ 落到下一句（哨兵 = no-op）；emulator 只看 `undefined`，`-1` 会走 `dwordToInstr[-1]`，多数情况下取不到就抛错（如果映像里恰好有 -1 键则落错指令）。 | `25713-25717` | `src/vm/handlers/control.ts:115` | new |
| 66 | `0x5` | unclear | P3 | 引擎 ret 在跳转后还会弹一层全局「末层栈」（`_this[107437]` / 基址 `_this[107436]`）并把弹出的值写进全局标志字 `_this[174801]`；emulator 两者都不建模。 | `25718-25724` | `src/vm/handlers/control.ts:109` | new |
| 67 | `0x6` | approximation | P3 | 越界分支形态不同：引擎 op2>=40 是「组错误串 + 抛 ShowMessage，未出栈（cur 已被写成 op2）」，emulator 是直接抛 JS Error（无消息语义、且把越界当成解释器错误而非宿主提示）。 | `26847-26854` | `src/vm/handlers/control.ts:412` | new |
| 68 | `0x6` | approximation | P3 | emulator 额外加了 frameIdx<0 的拒绝分支；引擎只比较 v4 >= 40（有符号 int），负数帧号在引擎里会以 _this+120*v4+… 继续算地址、不会抛错。 | `26846-26848` | `src/vm/handlers/control.ts:412` | new |
| 69 | `0x6` | missing-operand-io | P3 | load-frame 装载 op2 帧时，引擎经 sub_40ED40 先把该帧的 caller（帧 +76）重写成「装载时的 cur」（sub_40EA00 先置 -1，随后 raw 18637 再写 _this[383108]）；emulator 的 loadScriptIntoFrame 不碰 frame.caller，预装帧的 caller 保持旧值（首次为 -1）。 | `18635-18637` | `src/vm/handlers/control.ts:417` | new |
| 70 | `0x61` | missing-operand-io | P3 | 引擎体对 op1 是“按操作数自身 tag 写”的 —— op1 为直接型（int/float/string）时它把地址写进同类型的**直接槽**、为指针型时写指针池条目；emulator 的 `setRefOperand` 只接受 6 种指针型，遇到直接型 op1 直接抛错 ⇒ 若脚本给 int 目标，引擎静默写入而 emulator 抛。 | `37747-37750` | `src/vm/operand.ts:147` | unclassified |
| 71 | `0x64` | missing-operand-io | P0 | 字面数组的每一项在引擎体里被显式加密一次（`ROL4(key ^ ROR4(池原始值,7),21)`）后存**裸位模式**；emulator 把池原始值交给 `writeRef`，int 槽又被 `ENC` 一次 ⇒ 存进去的是“池原始值的 ENC”，任何 int 解引用读回都变成 `DEC(原始值)` 而非原始值（key≠0 即错，语料里 7259 处 0x64 全部受影响）。 | `37793-37795` | `src/vm/handlers/memory.ts:69` | new |
| 72 | `0x64` | missing-operand-io | P3 | 长度 ≤ 0 时引擎体内不写任何槽（`if (result > 0)`）；emulator 无该分支，`data.length === 0` 时表现相同、但 `data` 缺失时直接抛错而不是空操作 —— 属“分支缺失但当前等价”的记账项。 | `37787-37788` | `src/vm/handlers/memory.ts:68` | new |
| 73 | `0x73` | approximation | P2 | `sub_456430` 写进窗对象的 40 字节里，`win+36` 是 **op2**（`v13[5] = a3`）、`win+40` 是 **op3**（`v13[6] = a4`），而 emulator 存的是 `srcSurface ← op4` / `textY ← op3` / `textX ← op2`：`win+36/+40` 在 emulator 由 `srcSurface/textY/cellW/cellH/originX/originY` 六个槽承担，其中「`+36` = op2」这一格没有对应（既不是 `srcSurface` 也不是 `cellW` 的自证）。语料两例可判别：`NOVEL.txt:11` 的 `i073 8 0 (local-int 0) c 0 (local-int 1) 38 38 8 64` —— 若按「`+36` = op2」读则 op2 恒 0（该例）而 `i073 1 37a 6e c 0 0 23 23 a 64` 的 `+36` 应是 0x37a，与 emulator 的 `srcSurface = 0xc`（op4）分属两格。 | `68292-68303` | `src/vm/handlers/msgwin.ts:776` | new |
| 74 | `0x73` | missing-consumer | P3 | `if (!_this[166964])`（GDI 路径）时引擎会 `(*(vtable+4))(Engine+1978, 33, 2*op7, op8, 2)` 建/改绘制容器（携带 `2*op7` 宽、`op8` 高、类型 2）；emulator 只写 `CharGrid` 结构，没有任何「按字格尺寸重建绘制容器」的对应物。 | `28631-28636` | `src/vm/handlers/msgwin.ts:772` | new |
| 75 | `0x7a` | approximation | P3 | 引擎的 `buf[-20]` 是**文本项缓冲头里的活游标**：排版例程每次写新记录时用 `*(_DWORD *)(v5[12] - 20) = v5[7]` 把它复位成对象自带的 `obj[28]`（0x79 的文字起点）；emulator 的 `o.pre48Set/pre48a/pre48b` 是**常驻覆盖**，一旦置上就再也不会回落到 `g.originX/originY`，故「0x7A 覆盖 → 后续排版复位 → 再 0x79 之外的重排」这条时序在 emulator 里表现不同。 | `82684-82685` | `src/vm/handlers/msgwin.ts:214` | new |
| 76 | `0x8` | approximation | P3 | emulator 对 frameIdx<0\|\|>=40 抛错；引擎在本体里没有任何 op1 值域校验，只用 _this + 120*v2 直接算帧地址（越界即越界访问，不会报错）。 | `26888-26891` | `src/vm/handlers/control.ts:68` | new |
| 77 | `0x8` | missing-operand-io | P3 | 引擎把「调用方恢复位置」编码成调用方帧的步长槽=3（由主循环 `ip += 4*step` 消费）；emulator 不建模该槽，改在切换时直接 `ip += 1`。两者净效果相同，但引擎若在返回前又执行别的指令会以 3 为准——重写侧没有这一层，属实现口径差。 | `26886-26887` | `src/vm/handlers/control.ts:72` | new |
| 78 | `0x8c` | approximation | P3 | emulator 要求 op1 必须命中 labelMap（否则抛 `jmp: unknown label`）；引擎不校验——`ip = base + 4*op1` 可以指向任意 dword 偏移（包括非指令边界或脚本外），不会报错。 | `29395-29399` | `src/vm/handlers/control.ts:40` | new |
| 79 | `0x8f` | approximation | P3 | emulator 要求跳转目标命中 labelMap（否则抛 `call: unknown label`）；引擎直接 `ip = base + 4*op1`，不校验目标是否为指令边界。 | `29469-29471` | `src/vm/handlers/control.ts:58` | new |
| 80 | `0xb8` | missing-behavior | P2 | `0xB8` 在 emulator 里只落成宿主的纯停播（`bgmStop()` → `#stopBgm()`），但引擎体**不碰**任何 BGM 模式/开关字段；若宿主的 `#bgm.mode` 被置 0，后续 `0xB7`/`0xBF` 起播会被 `bgmPlay` 的 `this.#bgm.mode === 0` 门静默挡掉，直至某条 `0xBC`/`0x1BA` 再把它设回非 0。 | `24821-24828` | `src/vm/handlers/audio.ts:112` | new |
| 81 | `0xbb` | approximation | P3 | 配置 `sound:SE` 的写回被归一成 0/1，而引擎把「非 0」原样搬 ±3：`sound:SE = 2` 时引擎写 5、emulator 写 1；后续 `sub_408D90` 的幂等判据 `(a2 != 0) == v3` 在两边仍一致，所以只在配置被手工置成 0/1 以外的整数时才有差异。 | `13551-13553` | `src/vm/handlers/audio.ts:315` | new |
| 82 | `0xc2` | missing-branch | P2 | 非 ADV 路径里 `effect_flags & 0x200` 置位时的 `set:TransferMusicVolume` 分支（`sub_418580`：1 = 按淡变进度把当前音量插值到目标、2 = 直接跳到目标）完全未建模，节流值 `sub_453A60(_this + 107503, op2/10 或 op2/1000)` 也没有对应物；宿主用 `bgm-fade{value, step}` 的每帧相对增益插值近似。 | `24041-24053` | `src/vm/handlers/audio.ts:491` | new |
| 83 | `0xc2` | approximation | P2 | `sub_489D10` 的起播前提是「`Music[264]`（当前音量运行态）== 0 或后端未在播」，emulator 只判 `Music[259] != 0`：跳过了 `Music[264] == 0` 这一半，等于假设「当前音量为 0 ⇔ 后端没在播」。这个假设在「后端在播但音量被置 0」时（`0xC6` 设 BGM 音量 0，或 `0xC2` 内部把 `Music[264]` 走完）不成立。 | `106287-106291` | `src/vm/handlers/audio.ts:489` | new |
| 84 | `0xc6` | approximation | P3 | emulator 把 op2（音量值）**钳位后**写进配置（`AudioEngine.setVolume` 的 `clampVolume(value)`），而引擎 `sub_4071D0`/`sub_489B80`/`sub_4B68A0` 是**原值直用**：`sound:Volume0..4` 与影片 `_this[490000]` 存的是脚本给的原始值（`sub_4092C0` 末尾 `sub_4B68A0(_this + 4666, a2)`）。用 >10000 或负值调 `i0c6` 时，两侧配置与持久化到 `SYS4REG.INI` 的内容会不同。 | `29978-29981` | `src/audio/audioEngine.ts:572` | new |
| 85 | `0xc6` | stale-ledger | P3 | 既有语义/代码注释把越界错误串写成 `sprintf("setVolume")`，引擎里的实际串常量是 `SetVolumeの引数が不正です．\r\n`（raw 4413），且引擎除了 sprintf 还会 `sub_4034D0` 抛 ShowMessage。emulator 既不显示也不抛，只静默 return（宿主越界日志在 `AudioEngine.setVolume` 的 default 分支，本函数提前 return 时根本走不到）。 | `29969-29972` | `src/vm/handlers/audio.ts:659` | new |
| 86 | `0xc7` | missing-branch | P1 | 引擎 `sound:Music` 支写的是 `(v2 >= 0)`（只有负值算关），SE/Voice/Movie 才是 `!= 0`；emulator 用同一个 `bool: true` 把四条都按「非 0 ⇒ 1」处理 ⇒ selector=1 时取值口径相反：`sound:Music == 0` 引擎写 1、emulator 写 0；`sound:Music < 0` 引擎写 0、emulator 写 1（仅 >0 时一致）。 | `38679-38713` | `src/vm/handlers/config-read.ts:63` | new |
| 87 | `0xcd` | missing-behavior | P1 | 引擎每次执行 0xCD 都写本帧的 `Engine[120*cur + 383220]`：进入时置 1、真正推进后落回 0、未注册目标时保持 1；这一格被主循环**当作 ip 步进量消费**（`ip += 4 * gate`）⇒ `0` = 按正常顺序走到下一条、`1` = 跳过一条（`mouse-callback` 注册后紧跟的 `get-input-type` 就是这种布局）。emulator 完全没有这一格的状态与它的消费点，一律按下一条推进。 | `25838-25844` | `src/vm/handlers/input.ts:242` | new |
| 88 | `cap:adv-advance-route-table` | missing-behavior | P1 | 鼠标右键的『取消/跳读』通路（raw 20365-20374：Engine[489488] 的 label 偏移 + 清 ADV 掩码 0x6000000 + 改写帧 ip）与 20341-20360 的逐字中滚轮上键整段收尾分支，emulator 完全没有实现；条目自己把它登记为缺口 (d) | `20365-20370` | `src/vm/engine.ts:1324` | unclassified |
| 89 | `cap:adv-advance-route-table` | overreach | P3 | reads 把表内 dword 下标写成 Engine 绝对偏移：游标 `panelA[7468]` 的绝对字节是 51848（+推进标志 51840），条目写 Engine+29872/+29864；与同列表里按字节写的 Engine+21976(0x55D8) 量纲不一致 | `20284-20287` | `src/vm/route.ts:108` | new |
| 90 | `cap:adv-flag-lifecycle` | stale-ledger | P2 | note 同一条里自相矛盾：前半说 ADV 位由 Engine.serviceAdv()（每帧）清除，末句又说『仍未建模：sub_411900 的 cancel-message 三态机』——三态机就在 serviceAdv 里且已有 E2 断言 | `20115-20122` | `src/vm/engine.ts:764` | new |
| 91 | `cap:adv-flag-lifecycle` | missing-behavior | P2 | 0x72 尾段的 AutoMessage 自动放行分支（_this[97052] 非 0 时按 message:AutoMessage* 起 sub_453A60(_this + 107545, v10) 计时器）在 emulator 里没有等价物 ⇒ ADV 位在自动播放模式下不会被自动清 | `28583-28585` | `src/vm/handlers/msgwin.ts:620` | unclassified |
| 92 | `cap:adv-text-reveal-progress` | stale-ledger | P2 | note 说『仍未建模引擎真正的逐字/逐行推进泵 —— 见 text-reveal-pump-409400』，但该条在台账里是 modeled-verified/E3，实现也在 src/vm/msgwin.ts:467-528 | `28345-28350` | `src/vm/msgwin.ts:514` | new |
| 93 | `cap:adv-text-reveal-progress` | approximation | P2 | 引擎的『显示完了吗』由 sub_48F000 查脚本分段表得出（miss/越界返回 0），emulator 用 `readTextSkipOf(e) !== 0 ? 1 : 0` 伪造返回值，再用 serviceAdv 在下一帧收尾——只有『门开着且非跳读』时强行显示中一帧 | `28350-28355` | `src/vm/handlers/msgwin.ts:440` | unclassified |
| 94 | `cap:audio-device-init` | missing-behavior | P2 | 引擎的声音初始化是「DSOUND.DLL 惰性装载 → `DirectSoundCreate` 建对象 → 建 10 路 SE 缓冲/3D 缓冲」，失败时打串 + **弹 MessageBox** + 清 `Engine[174801] & 0x1000000`（此后所有通道函数早退 = 静默降级）；emulator 只有 WebAudio 宿主（`AudioContext` 懒创建 + resume），没有「设备不可用」这一档，也没有对应的静默降级态。 | `12026-12033` | `src/renderer/audio/webAudioHost.ts:157` | new |
| 95 | `cap:clock-write-clock-freeze` | missing-behavior | P1 | 时钟冻结门未建模：`Engine+107438`（字节 429752）非零时主循环/opcode 侧的时钟刷新语义（`sub_41A090` 只递增帧计数 107439）在 emulator 的帧驱动里没有对应判据。 | `25198-25203` | `src/vm/engineFieldIds.ts:33` | new |
| 96 | `cap:drawitem-loop-anim-frame-drive` | missing-behavior | P3 | B 层五条通道在体内**每条**都置 `Scene+46508 = 1`（强制本帧重画），emulator 的循环项只经 `itemLoopAnimationsPending` 进「合成判据」，没有把这一位置起来 ⇒ 「只挂 B 层动画、别的都没动」时引擎每帧都重画，emulator 靠 needsRender 的另一半（scAnimationsPending）恰好也每帧为真，故当前无可见差；一旦宿主把 needsRender 改成纯脏位驱动，动画就会卡住不刷。 | `118339-118341` | `src/renderer/drawitem/eval.ts:55` | new |
| 97 | `cap:frame-pump-input-refresh` | overreach | P3 | 条目 whySilent 写「输入设备缺失时函数返回 0，主循环只是不进后续处理分支」——体里 `sub_4BBAB0` 的返回 `v9` 是「本帧有语音槽真的被装/起播」的聚合位（raw 142669 `v9 = 1`、raw 142677 `return v9`），与输入设备无关；而调用方 raw 20647-20655 在它为真时做的是 `sub_407120(_this)`（唤醒/重画类动作），不是「跳过输入处理」。 | `142661-142670` | `src/audio/audioEngine.ts:645` | new |
| 98 | `cap:frame-render-gate-mainloop` | missing-behavior | P1 | 引擎的帧提交门 `Engine+667856 == 1`（DrawMode）+ `(667860 != 0 \|\| effect_flags & 0x2400)` 在 emulator 里完全不存在：驱动每帧无条件 `e.nowMs = host.now()` 并在帧末按 needsRender 调 present。 | `20740-20748` | `src/frame/loop.ts:274` | new |
| 99 | `cap:frame-render-gate-mainloop` | missing-behavior | P1 | `Engine+675968`（`aSetIsreggist`，外部暂停）这道门没有等价物：`v92 = 0` 使整段（含 raw 20750-20751 的时钟写入与 present）被跳过。 | `20742-20745` | `src/frame/loop.ts:274` | new |
| 100 | `cap:frame-render-gate-mainloop` | missing-behavior | P2 | 引擎 present 块在体里位于 `(v30 & 0x2000)`（电影对象生命周期）分支**之后**，而 emulator 的帧末 present 与 movie 桩（0x20F 只记录）互不相干，次序语义丢失。 | `20896-20904` | `src/frame/loop.ts:430` | new |
| 101 | `cap:lazy-572b-node-map` | missing-behavior | P2 | Scene+1080 系的 572 B 精灵/特效节点表既无模型也无消费者：引擎里它与立绘表同构、由 opcode 取/建并在帧函数里提交出画（raw 136928 的 sub_4AAEC0(Scene+1080) + sub_4AF560），emulator 只建了 l2dNodes（Scene+1096）那张。 | `136924-136930` | `src/vm/engine.ts:1382` | new |
| 102 | `cap:lazy-572b-node-map` | overreach | P2 | name 的「表头 eager 130366/130384」与 reads 的 +1080/+1084 都落到错的东西上：130366/130384 是 Scene+1084/+1100（另一张表）的 `operator new(0x250u)` 构造点，不是 1080 表头的证据；reads 列的 +1080/+1084 是表对象指针，真正被读的字段是表内计数 1088/1104。 | `130364-130380` | `src/live2d/runtime.ts:147` | new |
| 103 | `cap:lazy-gdi-font-set` | stale-ledger | P1 | 台账说字体系 status=absent、note 说『面名/字号/粗体/竖排 → HFONT 完全未建模』，实际面名映射（fontSet.ts）与字号/字重/竖排参数面（MsgWindow.font + 6 条 op + textLayer.ensureFont）都已建模并有渲染消费者 | `70984-70990` | `src/text/fontSet.ts:156` | new |
| 104 | `cap:lazy-gdi-font-set` | missing-behavior | P2 | 引擎在 sub_459F40 里重建的不只是 HFONT，还有字形度量/位图缓冲与竖排两面（lfEscapement=1800）；重写侧只做 (family,weight,size)→文件的选择，量宽与缩放修正整块缺失 | `71156-71186` | `src/text/fontSet.ts:177` | new |
| 105 | `cap:lazy-gdi-font-set` | overreach | P3 | reads/name 把主套的 CreateFontIndirectA 记成 ×8，体里主套共 9~10 次（含字形度量面 +101852 与 Aspect 修正面 +1100/+218624），漏记的正是『量宽用哪个句柄』这一关键格 | `71042-71046` | `src/vm/msgwin.ts:302` | new |
| 106 | `cap:lazy-mesh-map-node` | stale-ledger | P2 | note『已实现，但无专门测试』与仓库不符：0x322/0x323 的『缺失即建项（flags=0，不画）』既有专门测试（test/mesh-vertex-quad.test.ts 是 0x320/0x322/0x323 的回归套件），又有端到端断言（test/blend-mode.test.ts:191/197 对从未建过的 handle 0x2000 发 0x322，断言 mesh 项被建出来且 blend 落进模型）⇒ status 应是 modeled-verified、guard 应指向它们。 | `17764-17806` | `test/mesh-vertex-quad.test.ts:48` | unclassified |
| 107 | `cap:lazy-mesh-map-node` | overreach | P3 | note 把这条路径写成『sub_4AAB80 等价：scSetVertexColor 走 scCreateMesh』—— 实际走 scEnsureMesh（ops.ts:707/724 → 401），而 scCreateMesh（ops.ts:382）在 vcount>0 时置 bit0=1（可画）⇒ 两者语义相反，代码里还专门注明必须分开。 | `17800-17805` | `src/renderer/scene/ops.ts:707` | new |
| 108 | `cap:lazy-movie-dll` | stale-ledger | P1 | 台账 lazy-movie-dll 声明 status=absent/E0/no-guard，但 0x14B（sub_4229D0，含 FreeLibrary 先释放再 LoadLibraryA 的重载语义）与 0x14C（sub_422AB0，100 槽导出绑定）在 src/vm/handlers/agerc.ts 里已有实现并且挂在 OPS 上。 | `31056-31110` | `src/vm/handlers/agerc.ts:71` | unclassified |
| 109 | `cap:lazy-movie-dll` | overreach | P2 | 条目名『电影解码 DLL 重载』与 whySilent 的『影片』框定在体里没有支持：31073-31076 是 op1 → FileDB 文件名 → LoadLibraryA 的**通用**模块加载，本作唯一调用点是 AGERC.DLL。 | `31073-31076` | `src/vm/handlers/index.ts:55` | new |
| 110 | `cap:lazy-movie-texture-slot` | missing-behavior | P2 | 0x20F（及 32245/32528/32600/32877 四个同型点）对 Engine+4*i+378688 的 CMovieToTexture 槽做惰性创建并绑到 CTexture 槽；emulator 该表/对象不存在，play-movie 只 pushLog。 | `31627-31630` | `src/renderer/pixiBackend.ts:990` | unclassified |
| 111 | `cap:lazy-movie-texture-slot` | overreach | P3 | 条目把被守护的 Engine+378688 槽称作『电影纹理槽』并把 fns 写成 ctor sub_489040——体里 378688 是 CMovieToTexture **对象**表（sub_489040 = 其 ctor），惰性创建在 sub_4237B0（0x20F）体内；真正的纹理槽 365288 空时是抛异常而不是惰性创建。 | `31644-31653` | `src/vm/handlers/gfx-misc.ts:101` | new |
| 112 | `cap:lazy-script-operand-hashmap-node` | overreach | P2 | reads 写 `Engine+378688, Engine+365288`，但这两个字段在声明的 raw 区间（36830-37477）里一次都不出现；它们分别是 CMovieToTexture 对象表与 Scene+42456 的 CTexture 槽表，与操作数节点分配无关。 | `36996-37019` | `src/vm/engine.ts:47` | new |
| 113 | `cap:lazy-script-operand-hashmap-node` | stale-ledger | P2 | emulator.guard=test/xval.test.ts 与本条目无关：该文件断言的是 BIN 解析器与反汇编文本的**指令名顺序**一致，删掉整个操作数池层它照样全绿。 | `37002-37023` | `test/xval.test.ts:64` | new |
| 114 | `cap:lazy-texture-slot` | stale-ledger | P3 | emulator.guard=test/texture-slot-resolve.test.ts 与该条目声称的机制名不副实：0x1F8 的『旧表面释放 + 新表面重建（旧纹理延迟销毁）』没有任何断言，文件只测 draw-item→槽/ imgid 解析与 0x1FA release。 | `122837-122870` | `test/texture-slot-resolve.test.ts:81` | new |
| 115 | `cap:lazy-texture-slot` | approximation | P2 | 0x1F8 的 op4==3 ⇒ DividedTexture、其余 ⇒ NormalTexture（两套 CTexture 类）；emulator 完全忽略 mode，两者都建同一种 canvas 表面，DividedTexture 的分割语义（该槽作为分割/离屏用途）不存在。 | `122855-122872` | `src/renderer/pixi/textureCache.ts:309` | new |
| 116 | `cap:lazy-texture-slot` | overreach | P3 | whySilent『失败仅置空槽』在体里找不到支持：体在纹理创建失败时**保留**刚 new 出的对象 v8 且只写日志（sprintf_s + sub_4034C0 → sub_497620 → sub_438CC0），只有 operator new 失败才是空槽。 | `122876-122887` | `src/renderer/pixi/textureCache.ts:349` | new |
| 117 | `cap:lazy-transition-map-node` | overreach | P3 | `engine.fns` 把 `sub_4AA190` 与 `sub_4AAE10` 并列成「首次写入某 key ⇒ 惰性插入」的同一机制，但 `sub_4AA190` 的体（raw 129585-129596）在 key 缺失时 `return result`（end 迭代器）**不插入**；只有 `sub_4AAE10`（raw 130232-130236 未命中后建节点）才做 find-or-insert，而 `sub_4AA190` 的调用点是另一张表（raw 31973 `sub_4AA190((int)(_this + 80708), v5)`）。 | `129591-129596` | `src/renderer/scene/transition.ts:622` | new |
| 118 | `cap:msgwin-attr-font-opcodes` | stale-ledger | P1 | note 的 P0 no-op 名单（0x75 字号 / 0x76 填充色 / 0x77 描边色 / 0x78 描边档位 / 0x1A4 描边偏移 / 0x8B 第三色 / 0x197 注音字号 / 0x2BD/0x2BE 加粗 / 0x1A5/0x2FE 面名）**全部**已实现且被渲染消费；『仍为 no-op 且属 P0』整句与代码相反 | `28668-28670` | `src/vm/handlers/engine-fields.ts:137` | new |
| 119 | `cap:msgwin-attr-font-opcodes` | stale-ledger | P2 | guard 名不副实：声称的 `test/adv-msgwin.test.ts#0x2DE = 字体名 → 字体表下标` 测的是字体选择器的查表（fontListIndex），与『属性/描边/字号/面名参数面』没有任何断言关系 | `28657-28659` | `test/adv-msgwin.test.ts:113` | new |
| 120 | `cap:msgwin-attr-font-opcodes` | approximation | P2 | 引擎每次写颜色/档位/字号后**立即 sub_459F40 重建 GDI 句柄与度量**（0x75 经 sub_4185F0 raw 24081 同样重建），emulator 只写字段、等下一次光栅化时再读 ⇒ 已排版内容不回溯这一点与引擎一致，但『重建』这一步完全缺失 | `28670-28671` | `src/vm/handlers/msgwin.ts:1497` | new |
| 121 | `cap:msgwin-cancel-key-state` | stale-ledger | P2 | note 说门控是 GetConfig("set:CancelMessageKey")、且『随包 SYS4REG.INI 没有该键』；实际门控键是 set:CancelMesSkipOnClick，它既在引擎内建注册表（缺省 0）也在工程证据 INI 里（=0）——休眠是因为值 0，不是因为缺键 | `4346-4346` | `src/configRegistry.ts:77` | new |
| 122 | `cap:msgwin-cancel-key-state` | overreach | P3 | trigger 写『输入掩码 0x10（取消键按下沿）驱动 122370 在 0→1→2 之间迁移』，体里 0→1 发生在该位**为 0** 的那一帧（else 分支），按下只驱动 1→2；清 ADV/复位 ReadTextSkip 在 2→0 那一帧 | `20119-20126` | `src/vm/engine.ts:776` | new |
| 123 | `cap:msgwin-config-gates` | stale-ledger | P1 | note (c) 把 set:ReDrawTextOnKey / set:WheelKeyUp\|Down 判成『随包 INI 缺失 ⇒ 分支走不到』并说滚轮/重绘未建模；实际引擎内建默认是 1/3/1（可走到），且 emulator 已实现滚轮推进与悬停重绘门 | `111546-111548` | `src/vm/engine.ts:1333` | new |
| 124 | `cap:msgwin-config-gates` | missing-consumer | P1 | message:AutoMessageTime{0,1} / AutoMessagePitch{0,1} / AutoMessageOption 有写入端（0x1B9/0x2E7/0x2E8）但没有任何消费者；引擎的消费者是 0x72 尾段的自动翻页计时器 | `28556-28561` | `src/vm/handlers/msgwin.ts:1475` | unclassified |
| 125 | `cap:msgwin-config-gates` | stale-ledger | P2 | note (b) 写『message:MesWinAlpha=8 ⇒ 每段文本 8ms 节流（已实现）』；引擎那段节流读的是 Engine[21668] = message:MessageSpeed，MesWinAlpha 从不进字段 | `28361-28362` | `src/vm/handlers/msgwin.ts:411` | new |
| 126 | `cap:msgwin-line-fade-window` | missing-behavior | P2 | 消息窗每行的淡入色窗从未被建立：`MessageSpeed × MessageFade / 100` 这个时长在 emulator 里只存在于配置绑定与注释，文字仍然瞬现。 | `72336-72348` | `src/engineConfig.ts:240` | new |
| 127 | `cap:msgwin-object-table` | stale-ledger | P2 | note 说『仍未建模：sub_404F80 的布局重算（移除图元区间）』，实际 0x301 的 handler 已经逐步照抄了 sub_404F80（清 +122486、清对象 +132、msgWinClear 删绘制项、闸门开着时重新武装显现）并有测试断言 | `10750-10756` | `src/vm/handlers/msgwin.ts:1608` | new |
| 128 | `cap:msgwin-object-table` | overreach | P3 | trigger 写『0x300/0x301 写旗标/值并触发 sub_404F80 布局重算』——0x300 体里没有 sub_404F80 调用，只有 0x301 调它 | `33749-33752` | `src/vm/handlers/msgwin.ts:1590` | new |
| 129 | `cap:msgwin-text-method-opcodes` | stale-ledger | P1 | note 说『其余约 22 条纯转发器（0x70/0x73/0x74/0x75/0x197/0x198/0x1B5/0x1BB/0x1C1/0x1C9/0x1CE/0x324…）仍是 no-op —— 它们转发到不存在的文本布局子系统』，实际这批里 0x70/0x73/0x74/0x75/0x197/0x198/0x1B5/0x1C1/0x1CE 九条都已注册真实现 | `28409-28415` | `src/vm/handlers/msgwin.ts:1263` | new |
| 130 | `cap:msgwin-text-method-opcodes` | overreach | P2 | 名单把 0x1BB 算作『转发到文本布局子系统的纯转发器』，体里它是记账门 SetTB（写 Engine+388220 = 0 / 0x80000000，非法值打错误串），emulator 也已真实现；同类误列还有 0x1C9（音频设备初始化）与 0x324（Effect3D 销毁） | `29226-29230` | `src/vm/handlers/text-items.ts:188` | new |
| 131 | `cap:msgwin-text-object` | missing-behavior | P2 | 0x7D（十六进制串入队，sub_41F580）与 0x6E 同形地走 sub_46CBF0/sub_46BE30，但 emulator 的 MSGWIN_OPS 与操作数计划里都没有它，条目 also 没把它列进『仍未建模』清单 | `28746-28752` | `src/vm/msgwin.ts:681` | new |
| 132 | `cap:msgwin-text-object` | overreach | P3 | reads 把 `Font+1032/+1036/+1040` 与 `Engine+667856` 当作『文本对象』的读点，但 0x6E/0x71/0x72 体里没有这些读；+1040 是绘制容器、只在 sub_404F80（0x301 的落点）被读 | `10750-10756` | `src/vm/handlers/msgwin.ts:1590` | new |
| 133 | `cap:render-3d-layer-dual-commit` | overreach | P3 | `reads` 列了 `Scene+46512`，但 sub_4B4460 体内（136969-137285）一次都没有它：46512 的读者全在别的函数（136691/136718 在 sub_4B06D0、136842 在 sub_4B4040、133508 在 sub_49AA30）⇒ 该格属于『提交冻结/跳过』机制，不是本条目对象。 | `136688-136695` | `src/renderer/pixi/presenter.ts:191` | new |
| 134 | `cap:scene-3d-effect-level-writer` | missing-behavior | P1 | 等级（Scene+46668）的持续后果 emulator 一条都没有：(a) 126563-126572 按 `>=2` 把 36/37 两个 scratch 槽以 mode 1、否则以 mode 2 建（sub_4A2C10 的第 5 实参）；(b) 134822-134852 按等级惰性建不同的 ID3DXEffect（≥2→资源 201、≥1→200、>1→203）并据此在 2D 绘制循环里走不同支（135518/136024/136517 的 `<2`、136199 的 `>=1`）；(c) Set3DEffectSnow 另有 `>=1` 门（23917）。emulator 既无等级字段，也无 36/37 的等级化 mode，presenter.ts:420 只把『scratch 层 36/37』当固定（无等级输入）机制使用。 | `126552-126572` | `src/renderer/pixi/presenter.ts:420` | new |
| 135 | `cap:scene-3d-effect-level-writer` | stale-ledger | P2 | emulator.status=`partial` 与同一条目的 note 首句『emulator 完全未建模』互相矛盾：代码里 `46668` 只出现在一条注释（stubs.ts:107，说明 0x326 自带该门槛），没有任何读者或字段 ⇒ 按 statusEnum 现状应是 `absent`。 | `126552-126557` | `src/vm/handlers/stubs.ts:107` | unclassified |
| 136 | `cap:scene-3d-effect-level-writer` | overreach | P3 | note 的落点『presenter.ts:157 只实现 bit0 门』指错了地方：157 行是 `present()` 的签名收尾 `): number {`；真正的 bit0 绘制门在 presenter.ts:425-428，且它与 Scene+46668 无关。 | `126552-126561` | `src/renderer/pixi/presenter.ts:428` | new |
| 137 | `cap:scene-3d-weather-effects-rain-snow-leaf` | stale-ledger | P2 | `emulator.guard` 写『（无）』，但 test/op-327-32e-setweather-noop.test.ts 就是这条『0x327/0x328/0x329/0x32C/0x32E 已登记为 engine-internal』订正的守卫（断言 handlerKind、ip 前进、台账 disposition 与带票）；同时 src/vm/handlers/stubs.ts:213-215 的注释仍写着『0x327…与 0x328…目前根本没注册 ⇒ 命中即 NotImplementedOp。故意不上桩』，与同文件 156-157 行的注册矛盾。 | `136826-136830` | `test/op-327-32e-setweather-noop.test.ts:84` | unclassified |
| 138 | `cap:scene-3d-weather-effects-rain-snow-leaf` | overreach | P3 | `engine.fns` 列了 8 个函数却没有 sub_4531B0 —— 它是 `Manager[262..309]` 三组 16-dword 参数块的**唯一写入端**（`qmemcpy(_this + 262, &a2, 0x40u); qmemcpy(_this + 278, &a18, 0x40u); qmemcpy(_this + 294, &a34, 0x40u);`），而条目又把这三组字段写进 `reads`；其调用点 116499 与 122055 传的接收者都正是 `Scene+50704`。 | `122049-122056` | `src/vm/handlers/stubs.ts:152` | new |
| 139 | `cap:scene-draw-total-gate-1056` | overreach | P3 | reads 只列 Scene+1056/Scene+46456，读起来像「sub_4B06D0 只碰这两格」；实测该函数还从 Scene+1052 取链头并整表遍历，并读 Scene+1860/1112/1116。 | `134856-134874` | `src/renderer/scene/transition.ts:94` | new |
| 140 | `cap:scene-draw-total-gate-1056` | approximation | P3 | 「本文件内 1056 只有一个 if 判断」不完整：1056 有 6 个写点（raw 78785 是一次整段字段复位、115310 是置 1），它们是这个「转场记录条数」的生命周期来源；emulator 用容器长度隐式表达该条数，没有这格，也没有「条数 = 0 ⇒ 整遍不执行」的显式门。 | `115305-115312` | `src/renderer/scene/ops.ts:981` | new |
| 141 | `cap:scene-frame-commit` | overreach | P2 | 等键优先级说反了：note 断定「节点输给 item 与 mesh（raw 135586-135614 的三个分支）⇒ order=3」，而 sub_4B06D0 的等键判定是 `if ( v67 > v66 && v66 >= 0 )` ⇒ 键相等时**节点支先画**（L2D 在 mesh 之下），emulator 的 order=2/3 正好相反；且“item/mesh 参与同一归并”也无体支持（raw 136618-136700 是在 136713 归并循环**之前**独立走完的那趟）。 | `135586-135598` | `src/renderer/pixi/presenter.ts:386` | new |
| 142 | `cap:scene-frame-commit` | stale-ledger | P3 | note ②「跨路的提交次序…只有源码注释，没有守卫」已过期：test/render-draw-order.test.ts（T-0124 的 Z1 零覆盖点补洞）已经用 presenter.present 的可见次序把 layer 升序 + 同层 handle 升序钉住，并做了反向自检。 | `134417-136966` | `test/render-draw-order.test.ts:55` | new |
| 143 | `cap:scene-norender-mode` | stale-ledger | P1 | note 的「绑定可能错位」是假的：引擎自己就是用 display:ScreenMode 填 Engine+167990，emulator 的绑定与 0x2CE getter 都正确；错的是条目给这个字段起的名字（无渲染/隐藏窗口模式）。 | `11980-11982` | `src/engineConfig.ts:235` | new |
| 144 | `cap:scene-norender-mode` | overreach | P2 | fns/raw 覆盖面与「19 处门」不匹配：fns 里 5 个函数在本区间内没有任何 167990 读点，19 处引用有 11 处落在 engine.raw=11120-40120 之外（sub_40A4C0@14778、sub_425DB0@33200、sub_42EE10@39090、sub_42FBC0@39592、sub_431BA0@40804、sub_431CF0@40903、sub_432000@40986、sub_433AB0@42089/42142 直到 sub_4B92F0@141165）。 | `14778-14778` | `src/vm/handlers/input.ts:366` | new |
| 145 | `cap:scene-render-freeze-46676` | missing-behavior | P1 | 引擎的冻结总闸是**帧级门**：Scene+46676 非零时 sub_4B4040 整趟帧提交（含 2D 项/文字/转场遍/mesh 表）与 sub_49FCD0 的 3D 帧都被跳过；emulator 只把 46512 当成「立即收尾窗」的模型内冻结（scAdvance 的 freeze 形参），并没有任何以 46676 为条件的帧级跳过。 | `136786-136796` | `src/renderer/scene/ops.ts:816` | new |
| 146 | `cap:scene-render-freeze-46676` | missing-consumer | P2 | blend.ts 的 sceneFrozen 只有「声明 + 一个纯函数里的判据 + 一条单测」，生产侧从不置位：presenter.ts 组 BlendEnv 时只填 renderTargetSlot/slotMode（src/renderer/pixi/presenter.ts:387-390），全仓 `sceneFrozen` 只有 3 处命中（声明、判据、测试），⇒ 该判据在生产里恒走 !sceneFrozen 分支。 | `133547-133550` | `src/renderer/scene/blend.ts:89` | new |
| 147 | `cap:scene-render-freeze-46676` | overreach | P2 | raw 117239-137597 与 fns 12 条覆盖不到该字段的实际引用面：105 处读点里约 90 处落在区间外（最早 raw 50804 @sub_492810 一系、51070/57203/57274、最晚 raw 141165 @sub_4B92F0），fns 也未列 sub_492810/sub_458230/sub_498720/sub_4B92F0 这些真读者。 | `50804-50804` | `src/renderer/pixi/presenter.ts:386` | new |
| 148 | `cap:scene-slot-release-1000` | missing-behavior | P2 | 1000 槽场景对象表的逐帧老化释放完全缺失：引擎每帧在 Engine+675972 门下扫 1000 个槽，超时对象释放纹理、释放网格、析构并置 0；emulator 用 Map 保存绘制项/网格，永不老化，也没有 1000 槽上限。 | `20660-20670` | `src/renderer/scene/state.ts:28` | new |
| 149 | `cap:scene-slot-release-1000` | stale-ledger | P3 | status=partial 与 note 自述矛盾：note 明说「emulator 的 drawItems/meshes 是 Map（无 1000 槽上限与逐帧老化释放）；纹理槽表按需建，未做老化」——按 schema 这描述的是「完全没有」，应是 absent；同时 evidence=E1 却 guard 为空，没有可核的用例。 | `20725-20726` | `src/renderer/scene/ops.ts:1246` | new |
| 150 | `cap:scene-teardown-on-load-point` | stale-ledger | P1 | note 末句「engineSlot.ts 早把清单解析出来了（imageReload → 应改名 drawItems）却只留 handle、无消费者」已过期：字段已改名 drawItems、解析带 740 B 记录体、并且有真消费者 native.restoreDrawItems（+ 场景层 scRestoreDrawItems）与真守卫。 | `19820-19829` | `src/vm/engineSlot.ts:196` | new |
| 151 | `cap:scene-teardown-on-load-point` | stale-ledger | P2 | note 的「正解 = 恢复 clearDrawContainer/clearMeshSlots + 新增 body 绘制项清单的解析与还原」与实际实现相反：emulator 刻意不调 clearDrawContainer（save-slot.ts:288-291 写明那个缝会把网格一起清，与体不符），只用 restoreDrawItems「先清绘制项、不动网格」；引擎的清场也确实只走 Scene+1032 一棵树（本条 raw 19799-19814 里没有任何 Scene+1064/1096 的操作）。 | `19811-19814` | `src/vm/handlers/save-slot.ts:291` | new |
| 152 | `cap:sprite-2d-draw-layer` | missing-behavior | P2 | Scene+46700 的额外渲染遍在 emulator 里完全缺失：引擎在 39 档、三张迭代表走空后按该字段再调一次 sub_49FCD0（整趟 3D/文字渲染），emulator 只有一条合成路径。 | `136831-136833` | `src/renderer/pixi/presenter.ts:181` | new |
| 153 | `cap:sprite-2d-draw-layer` | overreach | P3 | trigger 漏掉外层门且语义标错：该分支只在 `Scene+46456 == 39` 时可达（raw 136798 `if ( v3 == 39 )` + 136826 三条迭代表走空），且它调的是 3D/文字整趟渲染（sub_49FCD0），不是「无脚本/标题态额外 2D 层」。 | `136797-136798` | `src/renderer/pixi/presenter.ts:382` | new |
| 154 | `cap:text-font-rebuild-cascade` | stale-ledger | P2 | guard 与 note 的行号锚点指向别的东西：note 的 `msgwin.ts:1146-1189` 是窗对象 range/`0x25D`/`pre48` 一族，`test/text-layout.test.ts:248-264` 是逐字显现游标用例 —— 真正的 op 体/派发/断言在 :1493-1554、:1796-1850、config1-chain.test.ts:92-94。 | `70984-70990` | `src/vm/handlers/msgwin.ts:1527` | unclassified |
| 155 | `cap:text-font-rebuild-cascade` | overreach | P3 | engine.fns 把 `sub_45A6E0` 与 `sub_4185F0`/`sub_418680`/`sub_4328F0`/`sub_432DD0`/`sub_428990` 并列为“字体参数 → 句柄重建级联”，但 raw 70940-71273 区间里只有 sub_459F40（70940）与 sub_45A6E0（71192）；其余四个函数的体不在该区间，条目没有给出它们与级联的调用关系证据。 | `71190-71195` | `src/text/fontSet.ts:99` | new |
| 156 | `cap:text-layout-wrap-ruby` | stale-ledger | P1 | 台账把文本排版记成 `absent`/`E0`/无守卫，实际 emulator 已实现等宽网格 + 边界硬断 + 注音配对 + 对齐并有 7 条守卫断言 —— 后续审计会重复去“补”已存在的实现。 | `83363-83997` | `src/text/layout.ts:412` | unclassified |
| 157 | `cap:text-layout-wrap-ruby` | overreach | P3 | reads 里的「FontVWindow+36/+40(右/下边界)」「Font+1236(字宽)」「Font+218592/+218596(缩放)」在 sub_46BE30 体里不是排版度量来源：体只用 psizl.cx/cy + 24B 行记录，缩放只在绘制落点换算出现。 | `83601-83607` | `src/vm/handlers/msgwin.ts:203` | new |
| 158 | `cap:texture-bind-synchronous-then-query` | approximation | P2 | 0x249 之后槽的 imgid 语义与引擎相反：引擎传 a6=1 ⇒ Scene[5*slot+466] = −1，emulator 写 op1（图像 id）⇒ 0x216 查询结果两边不同。 | `123369-123382` | `src/vm/handlers/gfx-texture.ts:93` | new |
| 159 | `cap:texture-bind-synchronous-then-query` | unclear | P2 | 屏障②的『端到端证据』不成立：E2 守卫文件不含该调用点，note 指向的 gs2 运行其自带日志把两张 SN0000 图都判为『★几乎全黑：可能有渲染缺陷』，无法据此判定该屏障已生效。 | `39866-39877` | `test/texture-frame-barrier.test.ts:39` | new |
| 160 | `cap:vertex-buffer-lock-scale` | missing-behavior | P2 | 视口缩放改写完全没有：引擎在 Lock 成功后按 Scene+46456 分档给每个顶点乘 sub_418290/sub_4182B0 的返回因子（raw 133571-133598），emulator 的 mesh 直接用 create-mesh 给的坐标画。 | `133571-133572` | `src/renderer/pixi/presenter.ts:217` | new |
| 161 | `cap:vertex-buffer-lock-scale` | overreach | P3 | trigger 写「Scene+46676 == 0 且脚本深度非 38/负值时才改写顶点」把 sub_4AF1C0 的门当成了整个 fns 的门：sub_4A1F00 在冻结时照样写顶点（只是不乘因子），fns 里列它却无法用这条 trigger 解释。 | `122223-122231` | `src/renderer/pixi/presenter.ts:218` | new |
| 162 | `cap:world-matrix-identity-refresh` | stale-ledger | P1 | note 的『但没有 4×4 世界矩阵链与投影矩阵复位』与代码相反：Scene 世界矩阵的『每帧复位成单位阵 + 链乘进项 work 矩阵』这一级 2026-09 已实现（state.ts:150-189 / ops.ts 的 sceneLayerXform / presenter.ts:261,505），守卫是 test/op-22a-22f-scene-world.test.ts（含 `assert.equal(sceneLayerAffected(19), false, …)` 这类层区间断言）；台账引用的 test/draw-item-anim-window.test.ts 只断言项级动画窗。 | `122150-122154` | `src/renderer/scene/state.ts:158` | new |
| 163 | `cap:world-matrix-identity-refresh` | approximation | P3 | 引擎对**所有**层都把完整 Scene 世界矩阵乘进项 work 矩阵（133403-133407 门为真的支），只有层号 ∈[20,30) 走 `else` 的 decompose→『压成 2D』重建（133411-133438）；emulator 只实现后者（state.ts:185-188 + sceneLayerAffected 的 19/30 边界），且四组矩阵格只保留『最近一次 kind』（state.ts:202-209）。 | `133403-133407` | `src/renderer/scene/state.ts:185` | unclassified |
| 164 | `cap:world-matrix-identity-refresh` | overreach | P3 | name/trigger 把『投影矩阵复位』算进本条，但 engine.fns（sub_4A1E90/sub_4AEEA0/sub_49AA30）与 raw 122130-122157 里没有任何投影操作：全文件唯一的 `j_D3DXMatrixOrthoLH` 在 134354（sub_4B0360 = 3D 层提交函数，由 137253 调用），属于另一条机制。 | `134350-134356` | `src/renderer/drawitem/model.ts:187` | new |

## 已知 findings（2026-09 审计提过 / 已有票）

| # | 对象 | kind | sev | 一句话 | 分类 | 回链 |
|---|---|---|---|---|---|---|
| 1 | `0x100` | missing-behavior | P2 | 引擎 0x100 自己压返回点 = **本指令**（无 +1）⇒ handler 的 ret 回到 0x100 再扫下一个置位，**一次执行把掩码里所有位逐个派发**（`Engine[cur+122287] = v6+1` 是跨帧保留的游标）；emulator 因缺少「handler 返回后重跑本指令」的闭环，又用 `keyScanLastMask !== mask ⇒ b = 0` 近似每帧复位，于是同时按住多个键时只会**重复派发最低位**、其余位在同一帧里永远不派发。 | audited-2026-09 | op-1/0x100-push-return-point op9-op170-doc-createfile-constants-wrong op-4-02 |
| 2 | `0x101` | missing-behavior | P2 | 0x101 漏了三条引擎写：① `_this[174801] &= ~0x8000000u`（清 ADV 运行位 ⇒ 下一次 ADV 泵动作前必须先由脚本重新 arm）；② `_this[122370] = 0`（取消消息键三态机复位，raw 20119-20141 的三态机只读它）；③ `_this[122367] = 1`。emulator 的 `op_poll_input` 一件都没做。 | audited-2026-09 | adv-flag-lifecycle |
| 3 | `0x10c` | stale-ledger | P1 | 0x10c 的豁免理由（「宿主键盘也不进掩码（`InputManager.keyEdge` 只登记，`flushHeld/flushPending` 不并 0..6）⇒ 写入无消费者」）在 T-0052 落地后已不成立：键盘按下沿/按住态现在真的进掩码位 0..6 ⇒ 本指令写入的 VK→掩码位表**已有消费者**，disposition（engine-internal）必须重判，原注的「T-0052 落地后必须转真实现」已到期。 | tracked-done | T-0077(done) T-0052(done) T-0052(done) docs-new/03-engine/scene-start-flow.md:96 op-4-03 op-4-09 op-4-02 |
| 4 | `0x10c` | missing-consumer | P2 | 引擎侧消费者（每帧 `sub_4770A0` 用 `Input[1176+VK]` 当位号并进掩码）在 emulator 里被替换成**冻结常量** `DEFAULT_VK_TO_BIT` + `pressKey`：脚本无法经 0x10c 改写它，键码表 `Engine[1690+op2]` 也不存在 ⇒ 键位重映射（本指令的唯一语义）整体缺失。 | tracked-done | T-0077(done) docs-new/03-engine/scene-start-flow.md:96 op-4-03 op-4-09 op-4-02 |
| 5 | `0x10c` | missing-branch | P3 | `op1 > 0x1F`（unsigned，含负值）时引擎先抛 `ShowMessage("SetKeyMultiの引数が不正です．")` 且**不写表**；emulator 的 no-op 既不抛也不写 ⇒ 越界实参被静默吞掉（没有错误串、没有停）。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 op-4-03 op-4-09 op-4-02 |
| 6 | `0x131` | missing-behavior | P3 | 配置键 `message:MesWinAlpha` 缺失时（SYS4REG.INI 无 `[message]` 段、或根本没读到 INI ⇒ `e.config` 为空），0x131 用 `cfgInt(cfg, CFG.messageMesWinAlpha, 0)` 的回退 0 写给 op1；引擎的配置注册表在构造时就把该键的缺省注入为 8（raw 111471-111472），`GetConfig` 对缺键返回 8 ⇒ 同一场景下引擎写 8、emulator 写 0。 | tracked-done | T-0065(done) docs-new/03-engine/message-config-gates.md#§1 op3-008 |
| 7 | `0x133` | missing-behavior | P3 | 越界（op1>0xA）分支里引擎是把「ADDQ」错误串送进宿主消息通道（sub_408050 组串 + sub_4034D0 打印，会画到引擎的错误提示层）；emulator 只写一行 c.log，宿主侧不可见。 | audited-2026-09 | op-7-0x20b-doc-semantics-but-unimplemented |
| 8 | `0x134` | approximation | P3 | 空队分支里引擎的取值变量是未初始化局部 v8（把一段栈残留写进 op3，值不确定）；emulator 取确定口径「空队 ⇒ op3 = 0」，在真实硬件上可能与引擎写出不同的 op3。 | audited-2026-09 | op-5-003 |
| 9 | `0x134` | missing-behavior | P3 | 越界（op1>0xA）分支里引擎把「GETQ」错误串送进宿主消息通道（sub_408050 + sub_4034D0）；emulator 只写一行 c.log。 | audited-2026-09 | op-5-003 |
| 10 | `0x137` | missing-branch | P3 | `op1 > 0xA`（unsigned，负值也算越界）时引擎写错误缓冲并抛 `ResetStackの引数が不正です．`，**不碰栈槽**；emulator 的 no-op 静默接受任意实参。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/stub-reaudit-2026-09.md:311 op-4-03 op-4-09 op-4-13 op9-op137-index-emulator-points-at-png-signature-byte |
| 11 | `0x141` | missing-behavior | P3 | 越界（op1 无符号 > 0x10）时引擎会 `sub_408050(Engine+8, 1024, "GetMesWinAの引数が不正です．\r\n")` 再 `sub_4034D0` 把该串当消息派发（= 玩家可见的错误提示），emulator 只 `c.log(...)` 写宿主日志、**不产生任何消息/错误窗口**，脚本侧看不到这条提示。 | audited-2026-09 | docs-new/03-engine/message-config-gates.md#§1 op-1/0x141-unsigned-compare |
| 12 | `0x147` | approximation | P3 | n ≤ 1 时引擎仍然调 `CreatePolygonRgn(pts, n≤1, 2)`：n = 0 时点数组一格未写；n = 1 时恰好写了 pts[0] 而 count < 2 ⇒ GDI 无法成区 ⇒ 走 else 分支打印错误串并写 op1 = 0（结果恰好与 emulator 一致）；但 emulator 把它静态化成『n < 2 ⇒ 恒 false』（`if (n < 2) return false;`），既没有『点数组未初始化』这一状态，也没有那句错误串。故『建 region 失败分支不可达』的说法对 n ≤ 1 不成立，退化输入的失败路径是确定可达的（只是可观测结果同为 0）。 | tracked-done | T-0093(done) op-5-003 |
| 13 | `0x147` | host-invented | P3 | emulator 设了引擎体里没有的输入上限：n > MAX_POINTS(2^20) 直接 throw（把整步执行打断成异常），而引擎对任何 n（包括负值与极大值）都只做 `operator new[](8*n)` + `CreatePolygonRgn`，失败时**正常写 op1 = 0** 并继续执行 ⇒ emulator 把一个可观测为『op1 = 0』的路径变成了硬停。 | audited-2026-09 | op-5-003 |
| 14 | `0x14b` | missing-behavior | P2 | 引擎在**第二次 i14b（重载）时必先 FreeLibrary 并把句柄槽清 0，之后 LoadLibraryA 失败也照样把失败结果（NULL）存回该槽** ⇒ 第二次加载失败后模块句柄是 NULL 且旧库已卸载；emulator 的 `if (e.agerc.loaded)` 守卫却在 id 非法时**提前抛错**，把旧状态原样留下（loaded 仍 true、旧 exports 仍可绑定），映射到的是「旧模块还活着」。于是随后的 0x14C 拿到的是「成功绑定的旧导出」，而引擎那里 `*(HMODULE *)(_this + 490072)` 已经是 0。 | audited-2026-09 | op-5-006 op-5-007 |
| 15 | `0x14b` | approximation | P3 | 失败错误串的**实参**不同：引擎先 `GetLastError()`，再按同一个 op1 解析文件名，格式化「%sを読み込み出来ません．\r\n\r\nERRORCODE = %d」；emulator 抛的是自造消息「id=0x… を読み込み出来ません．（本作唯一可加载的模块是 AGERC.DLL…）」，既没有解析出的文件名也没有 ERRORCODE。 | tracked-done | T-0075(done) op-5-006 op-5-007 |
| 16 | `0x14d` | approximation | P2 | 引擎对槽表函数指针**不做任何校验**：`v14 = (…)&_this[sub_41BF50(_this, 1) + 122519]` 之后直接 `(*v14)(...)`（raw 39839/39843），槽未绑定（该项 = 0）或 op1 越界（0..99 之外）就是**跳 NULL / 跳到表外**（真机 = 访问违例崩溃）；emulator 改成 `if (!name) throw …（引擎此处会跳到空函数指针）`。另外 op1 越界 emulator 也不拦（只落到「未绑定」），而引擎是越界读 `_this[op1+122519]` 取到无关字段当函数指针。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:126 |
| 17 | `0x14d` | unclear | P3 | `len <= 0` 时引擎把**临时缓冲指针保持为 0（NULL）**并把这个 NULL 当第 2 实参传给导出函数（raw 39828 `v3 = 0` + `if ( v2 > 0 )` 才 `new[]` + raw 39843 传 `v3`）；emulator 传的是空数组 `buf[]`（`readRef` 语义上没有 NULL 概念）。对已建模的唯一导出 `_SetNameLenMax@20` 不可观测（它只读 `*a2`，即 `buf[0]`），但「导出收到 NULL 还是空数组」这件事我无法在反编译体里判出对所有 21 个导出的可见性。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:126 |
| 18 | `0x196` | missing-operand-io | P2 | 引擎体在三条路上各读了一次 **第 4 格操作数**（`sub_41B640((_DWORD)_this, 4)`，raw 29078/29085/29101），并把它当 `a5` 交给 `sub_46BE30`/`sub_46CBF0`；`sub_46BE30` 内 raw 83941 `if ( a5 >= 0 ) sub_4691A0(..., a5, ...)` 会**额外画一段内联注记**。emulator 只读 op1/op2/op3（`argc = 3` 的计划层），第 4 格既未读也未建模其效果。注意被调里 `a5` 还传给 `sub_45E870(win, a5, …)`（raw 83962-83964），故它同时是「注音记录的类别/来源」参数。 | audited-2026-09 | op-3-004 |
| 19 | `0x196` | approximation | P3 | 第③路（`msgwin.flags` bit0 已置）在引擎里只有 `sub_46BE30` **返回非 0**（= 本行有内容，`*a3 != 0`）时才写 `_this[489988] \|= 0x10000`；emulator 无条件置 bit16（只看 bit0）。 | audited-2026-09 | op-3-004 |
| 20 | `0x197` | missing-consumer | P2 | `sub_418680` 把新字号写进 **10 个字体对象**的 `+200`（字号）与 `+204`（`_this[327]`）并逐个生效；emulator 只有一个全局标量 `MsgWindow.font.rubySize`，没有这 10 个对象的对应物 ⇒ 任何「按字体对象取注音字号」的路径（含被调体里按对象重建/度量）在 emulator 无对应实现。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 21 | `0x197` | approximation | P3 | 引擎在结果处**额外**写 `_this[324] = _this[25509] = a2 / -2`、`_this[323] = _this[25508] = -a2`（注音字型的描边/基线偏移派生态，且是**整数除法**：奇数尺寸会向零截断），emulator 完全不记这四个派生格。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 22 | `0x198` | missing-branch | P3 | 引擎 `sub_456400` 在 `_this[v4 + 261] == 0`（该窗对象不存在 / 窗号未被创建）时**一格都不写、静默跳过**；emulator 的 `MsgWindow.geom()` 是惰性建窗（`if (!g) { g = defaultWinGeom(); this.wins.set(win, g); }`），于是对任何窗号都凭空得到一份新几何并照常写入 ⇒ 「未建窗 ⇒ 写不进去」这条能力缺失，且宿主会因此多出一份本来不存在的窗几何。 | audited-2026-09 | docs-new/03-engine/adv-text-rendering.md:488 |
| 23 | `0x198` | host-invented | P3 | 引擎的 `0x198` 只写窗对象 `+12/+16`，**不置任何脏位、不重画**（置脏/重画是另一族指令的事）；emulator 在这里无条件调 `emitWin(e, win)`，把该窗的 style/segments/revealed/itemRanges 整包重新发布给宿主 ⇒ 凭空多出一次「发布/重绘」这个引擎侧不存在的副作用。 | audited-2026-09 | docs-new/03-engine/adv-text-rendering.md:488 |
| 24 | `0x19d` | missing-branch | P2 | 配置缺键（`set:SaveVersion1` 不存在）时的缺省不是引擎内建默认：emulator 用 `cfgInt(..., 0)` ⇒ v1=0 必进 `v1<3` 提前返回；引擎的注册表在缺键时返回内建默认 `set:SaveVersion1 = 1`（仍 <3，故在本机 INI 上结论同）——但在「引擎默认 ≥3」的键上选支会反，且同仓 `control.ts`/`frame.ts` 都写 `registryDefault(...)`，只有本条写死 0（T-0065 明确点名 `set:SaveVersion1` 默认是 1），`test/gallery-bgm-list.test.ts:87` 还把「缺席 = 0 < 3」固化成断言。 | tracked-done | T-0065(done) |
| 25 | `0x19e` | missing-branch | P2 | 引擎在「只读打开打不开（文件不存在）**或**读不出 292 B 合法头」时会弹 `MB_YESNO\|MB_ICONQUESTION` 覆盖确认框，玩家选「否」⇒ `op1 = 1` 中止覆盖、原文件保持不动；emulator 无宿主对话框，恒按「玩家点了是」直接覆盖 —— 于是「覆盖一个空槽/坏档」在两边返回码相同（0），但「玩家拒绝覆盖」这条路径不存在（emulator 里无法表达 op1 = 1 的这个来源）。 | audited-2026-09 | save-slot-chain:raw |
| 26 | `0x19e` | missing-branch | P3 | 写打开失败时引擎先 `sub_40A4C0(..., aE, 5)` 弹一条错误串（`aE` = 错误提示），再 `op1 = 1`；emulator 只有返回值 1（`saveSlotFromEngine` 的多个 `return 1`），宿主侧没有任何错误提示落点 ⇒ 「存档失败但玩家看不到提示」。 | audited-2026-09 | save-slot-chain:raw |
| 27 | `0x19e` | approximation | P3 | 既有的 `ALLOW_UNDERRUN['0x19e']` 条目（「合成指令里没有 fileSource/slot 数据 ⇒ 提前返回（真实路径读 op1..）」）**措辞与实测不符**：实测（`.tmp/audit/probe-saveslot-touch.mts`）8 条都**碰了 op2..op3**、缺的只是写侧输出格；也就是说「提前返回」发生在**读操作数之后**，条目的理由描述的不是真实机制（且该条目在本测试里其实从未被触发，靠它维持的「存量」是假象）。 | audited-2026-09 | save-slot-chain:raw |
| 28 | `0x19f` | approximation | P2 | 返回码口径偏离：引擎这条指令的 op1 只有两个来源 —— 打不开 ⇒ 常量 1，或 `sub_410160` 的返回值（该函数主出口恒 `return 0`）⇒ 成功装载一律 0；emulator 在「宿主读到的槽字节解析不出头/容器」时写 **2**，在引擎里对应的是「打开成功但 292 B 头读不出」，此时 `sub_410160` 返回 -1 ⇒ 引擎写 -1（非 2）⇒ 脚本若按 `op1 == 0` 判成功，两边都是「非 0」；但脚本若按 `op1 == 1`（打不开）或 `op1 == 2` 分支，emulator 会把「坏档」报成与「文件不存在」不同的码。 | audited-2026-09 | save-slot-chain:raw op9-op415-doc-overstates-modeling |
| 29 | `0x19f` | approximation | P3 | 「帧记录一条都装不上（脚本资源缺失）」时 emulator 走 fallback；在 `full: false`（本指令）下它返回 `code: 0`（`if (full) { ... } return { code: 0, transferredTo: null }`）⇒ op1 = 0 报「装载成功」，而池/帧其实一个都没恢复；引擎在这种情形下同样是 0（打开成功即 0），所以**返回码一致**，差别是 emulator 少了一层可观测性（引擎此时也没有额外的错误码，只有装载内核内部的错误串路径）。 | audited-2026-09 | save-slot-chain:raw op9-op415-doc-overstates-modeling |
| 30 | `0x1a0` | approximation | P3 | 读侧字节口径：引擎只 `ReadFile(hFile, Buffer, 0x124u, &NumberOfBytesRead, 0)` 读 **292 字节**（且要求 `NumberOfBytesRead == 292` 才算成功）；emulator 的 `fs.readSaveSlot` 把整份文件读进内存再 `parseSlotHeader`（只校验 `bytes.length >= 292`）⇒ 「文件长度恰好 292（无状态主体）」在引擎算成功、emulator 也算成功；「> 292 但状态主体损坏」两边都成功（相同）；差异只在 emulator 多读的那部分会被后续 `parseSlotFile`/`loadSlotIntoEngine` 用到（本条指令不用）⇒ 本条的可观测返回码一致。 | audited-2026-09 | save-slot-chain:raw docs-new/03-engine/save-data.md:258 op-2-06 |
| 31 | `0x1a5` | missing-branch | P3 | 引擎 `sub_4328F0` 的第一件事是**面名合法性检查**：`sub_428990`（在「可选字体一览」`Font+50416` 的 32 B/条表里按名字查）返回 < 0 且面名不是 `AgeExtend` ⇒ 打印 `警告：[%s]は選択可能フォントの一覧に含まれていません。`；emulator 的 `op_set_main_face` 不做任何校验，任意字符串都直接存进 `font.mainFace`。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 |
| 32 | `0x1a5` | missing-consumer | P3 | 引擎把每个用过的面名**去重后追加进两张表**（`Font+101832` 与 `Font+200492`，各 28 B/条：`sub_4327A0` 扩容 + `sub_432860` 追加），并把面名/'@' 竖排变体分别存进 `Font+1260`/`Font+102000`；emulator 只保留 `mainFace` 一个字符串，既不维护「已用面名表」，也没有 '@' 竖排变体这一份 —— 这两张表在引擎里被绘制期遍历消费（raw 86600-86625 / 87342-87352 / 87940-87965 / 88698-88708）。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 |
| 33 | `0x1ab` | missing-branch | P2 | 引擎的 `DeleteFileA` 作用于**真实存档目录里的那一份文件**（`sub_408A40` 给的目录），删成功即返回 0；emulator 只从 overlay 目录 `unlink`，overlay 里没有这份文件时 `#removeOverlayFile` 返回 false ⇒ `op1 = 1` 报「`.DAT` 删失败」，而引擎在同情形下删的是 base 里的那份、返回 0 ⇒ **返回码相反**；同理 `.STH` 在 base 里时引擎可能报 2、emulator 报 0。 | audited-2026-09 | save-slot-chain:raw |
| 34 | `0x1ac` | host-invented | P2 | 「源槽不存在」时 emulator 报 op1 = 1（失败），引擎报 **0**（成功）：引擎的 `v5 = !CopyFileA(...)` 只看 `CopyFileA` 的返回值，而 `CopyFileA` 返回非 0 的场合包含「源不存在」⇒ 引擎把「从空槽复制」判成成功（并且这次调用已经把目标 `.DAT` 截断/覆盖）；emulator 的 `r.dat = d !== null` 把它判成失败，于是 `plan.setInt(1, 1)`。 | audited-2026-09 | save-slot-chain:raw |
| 35 | `0x1ae` | missing-branch | P2 | `Engine[166964]`（DrawMode）的 D3D 分支没有建模：引擎在 DrawMode = 1 时走 `CloseHandle(FileA)` + `sub_4A5260(Scene, FileName, op3)`（纹理截图，产物 BMP 由截图函数自己写），非 D3D 才走 `sub_43BF20(Engine+7912, op3, hFile)`；emulator 只有一条「读宿主画布像素 → encodeBmp」的路，任何宿主模式下都走它 ⇒ 在 D3D 模式下 emulator 走的是引擎的**另一条**实现（D3D 模式下的截图几何/像素格式差异不在 emulator 覆盖内）。 | audited-2026-09 | save-slot-chain:raw op8-F7 |
| 36 | `0x1ae` | host-invented | P3 | 取不到纹理槽像素时（headless、或该槽不是 `create-texture` 出来的画布槽）emulator 写一个 4 字节长度前缀 + `AMYTH1\n{...}` JSON 的**非 BMP 块**并报 op1 = 0（成功）；引擎在同一情形下走 `sub_43BF20` 写的是真 BMP（槽为空时 `sub_43BF20` 内部返回 0 ⇒ 引擎 op1 = 2 失败），或 D3D 下 `sub_4A5260` 打 `テクスチャが作成されていません` 错误串后返回 0 ⇒ op1 = 2。⇒ emulator 会产出**引擎读不回来**的 .STH（引擎的 `0x1AF` 用 `sub_43E9F0`/`sub_49E9D0` 校验魔数 `BM`，raw 49987 的 `*((_WORD *)v7 + 2) == 19778`）却报成功。 | audited-2026-09 | save-slot-chain:raw op8-F7 |
| 37 | `0x1af` | approximation | P2 | 返回码口径偏离 + 判据收窄：① 读到 0 字节的 `.STH` 时 emulator 早退报 op1 = 1（「打不开」），引擎在两条路径上都是「打开成功、读入失败」⇒ op1 = 2（D3D：`sub_49E9D0` 的 `sub_4036B0` 读 14 B 失败返回 0；非 D3D：`sub_40BF20` → `sub_43E9F0` 的 `sub_4036B0` 同样失败）；② emulator 只认 BMP（`decodeBmp`）加一个自造的「4 字节长度前缀」兜底，引擎的 D3D 路径（`sub_49E9D0`）在 `BM` 魔数不符时还会按 `v10 = *((_DWORD *)v8 + 2)` ∈ {1,2} 且 `*((_DWORD *)v8 + 3) == 0` 的第二格式（AGF/dd 系）解入 ⇒ 引擎写的非 BMP 缩略图在真机能读、在 emulator 报 2。 | audited-2026-09 | save-slot-chain:raw op8-F7 |
| 38 | `0x1af` | missing-consumer | P3 | op1 = 0 报「成功」但目标纹理槽不存在时，宿主 `setSlotPixels` 会**静默忽略**（`TextureCache.setSlotPixels` 里 `#canvasSlots` 没有该槽就直接 log 后 return）⇒ 引擎在同情形下 `sub_40BF20`/`sub_49E9D0` 已经失败（`sub_40BEB0` 清槽 + 读入失败 ⇒ 返回 0）并让 op1 = 2，emulator 却报成功而无任何图像落地（脚本后续 `draw-texture` 画到空槽）。 | audited-2026-09 | save-slot-chain:raw op8-F7 |
| 39 | `0x1b7` | missing-consumer | P1 | `0x1b7` 写进 97052 的值在引擎里**有到期机制**（消息泵入口 `sub_4090F0` raw 13699-13703：`if (a1[97052]) { a1[97052] = 0; return; }`；`sub_476080` raw 90961-90965 另按 `set:CoexistMesSkip` 决定 `97050 = 0`），emulator 只写不消费 ⇒ 值永久驻留，`0x1b6` 永远读回 1，被 `set:CoexistMesSkip` 门控的 `97050`（跳读/自动模式）也永远不会被这条路径清零。 | tracked-done | T-0076(done) |
| 40 | `0x1bb` | missing-branch | P3 | 非法实参（既非 0 也非 1）的处理与体不同：体把 `SetTBの引数が不正です．
` 格式化进 `_this+8` 缓冲后交 `sub_4034D0` 走日志/消息汇（sub_4976A0 → sub_497620 → sub_438CC0 的 WriteFile），**不写字段、也不改控制流**，主循环随后照常 `ip += 4 * 95805` 推进；emulator 直接 `throw new Error`，指令不再推进并走 frame/loop.ts 的 onError 通路（同类非法实参在音频族是 `c.log` 后继续，见 handlers/audio.ts:440 的 0x1BA 写法）。错误串通路本身属宿主日志差异（emulator 有 `c.log` 与 `ShowMessageError`→`session.#onError` 两个等价通道），故只报控制流这一条；顺带：emulator 的错误串写的是不带 `
` 的 `SetTBの引数が不正です．`。 | tracked-done | T-0076(done) msgwin-backlog-cursor docs-new/03-engine/sound-system.md#§9 op-2-12 |
| 41 | `0x1bc` | missing-branch | P3 | 引擎释放在 `Engine[21290]`（byte 85160，语音子系统对象指针）非空时才发生；emulator 无条件对 3 个通道发 `voice-reset`（宿主会把正在播的语音停掉）。当 `Engine[21290] == 0`（语音对象未建）时引擎一个通道都不释放，emulator 仍会停播。 | audited-2026-09 | single-field-timers-audio-device docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/scene-start-flow.md:105 |
| 42 | `0x1bd` | missing-behavior | P3 | 引擎在本 handler 里维护通道 0 的**状态位**（`_this[21315]` 条件清 0、`_this[21318]` 置 0 或 `\| 0x10000`），emulator 的 `op_play_voice` 完全不写这两个字段（只有 `0x1BC`/`0x2F7`/`0x2FF`/`0x302` 会写）。这些位在本工程里没有读端，所以行为不可观测，但 `engineValues` 会与引擎内存漂移。 | audited-2026-09 | docs-new/03-engine/sound-system.md#§9 op6-06 |
| 43 | `0x1c9` | missing-consumer | P3 | op1（音频驱动 id）在引擎里是**装载键**（`sub_4B8490(Engine+7912, id, 数据, 大小)`），emulator 只把它写进日志、不落任何字段 ⇒ engineValues 里没有这个 id；同时 op1 对应的文件数据/大小（`v8`）也不存在宿主等价物。 | audited-2026-09 | single-field-timers-audio-device audio-module-topology-and-volume-routing:raw op-1/0x1C9-engine-field-offset-4x |
| 44 | `0x1ce` | approximation | P3 | 零分支的收尾目标窗：引擎把 `_this[122371]` **原样**交给 `sub_45A940`，而被调体内是 `_this[a2 + 261]`（无 `if (!a2) a2 = Font[307]` 这条默认窗回退，见 sub_4563D0/sub_456400/sub_45AD30 都有、sub_45A940 独缺）；emulator 改用 `m.resolveWin(m.lastArg)`，把 0 映射成 `defaultWin` ⇒ 当 `Engine[122371] == 0` 时两者操作的不是同一窗。 | tracked-done | T-0075(done) op-10-004 |
| 45 | `0x1d2` | missing-consumer | P3 | 一条记录只报这一个结构缺口：引擎 72 B 记录里 `+4/+8/+12/+16`（写入端 `sub_45EFA0` raw 74348 `memset(&v7[1], 0, 16)` 清零、重排路径 `sub_45F090` raw 74382-74389 填 `a4[0..3]`）与 `+44` 起的内嵌文本 std::string（`sub_45F090` raw 74395 `sub_40C210((int)v13, Src, strlen(...))`、存档恢复 raw 74609 `sub_40C120((const void **)v52, (const void **)(v19 + 44), 0, 0xFFFFFFFF)`）emulator 完全没建模——`TextItemRecord` 只有 win/v20/v24/v28/sel32/flags 五个数字字段，不进 `+4..+16`、也不存文本；而引擎的消费端（回看页重绘渲染循环 raw 76964-77027：按 `+40 & 4` 选 GDI 字体、从 `+20/+24/+28/+36/+16` 取排版态、把 `+44` 的字符串 memcpy 出来 `GetTextExtentPoint32A` 逐字排版后重绘）就是 **`0x1D1`（回看页重绘）**，该指令按既定处置**有意未实现**（`analysis/opcode-gaps.json` 的 0x1d1 disposition = deferred；`app/amayui-emulator/test/op-1d0-1d1-text-metrics.test.ts:10-15` 钉住「0x1D1 必须继续未注册」）。因此 emulator 侧这些字段不是「该建没建」，而是随 0x1D1 一起被划出范围；保存/读档路径 `sub_45F1B0`（写回 11 个 dword + 文本串）同样只服务这条链。 | audited-2026-09 | msgwin-backlog-cursor docs-new/03-engine/adv-text-rendering.md:488 docs-new/03-engine/sound-system.md#§9 op-2-12 op8-F1 |
| 46 | `0x1f5` | missing-branch | P1 | 引擎在解锁路径（锁深度<=0 且停靠标志置位）上还会判「队列恰剩 1 项（`497380 < 497384 && 497384 - 497380 == 1`）且非派发中」后调 `sub_40FB60` 放行脚本队列；emulator 的 0x1F5 只清 frameTickLock，注释里连这条队列长度判据都没有，`sub_40FB60` 未接线 ⇒ 停靠期间积累的脚本请求永不在 0x1F5 这一侧被放行。 | tracked-done | T-0057(done) script-queue-dispatch |
| 47 | `0x1f5` | missing-operand-io | P3 | 引擎解锁路径无条件清 `_this[429752]`（停靠标志）**在**判定派发之前，且清标志与派发是两次独立写；emulator 用同一个 `frameTickLock=0` 同时表达「清停靠标志」与「允许 0x1F4 重新刷时钟」，把引擎的两个字段（`429752` 停靠标志 / `429756` 锁深度）折叠成一个键 ⇒ 当锁深度为 0 而停靠标志为 0 时，引擎不写 429752，emulator 仍会写 frameTickLock=0（多写），字段结构被压平。 | audited-2026-09 | script-queue-dispatch |
| 48 | `0x1f6` | missing-behavior | P2 | `sub_4AB7A0` 清的第三张表 `sub_4A9D10(v1 + 270)`（Scene+1080，与 Scene+1096 同为 572B 节点表的 A/B 两份）在 emulator 完全没有对应容器：`scClearDrawContainer` 只清 drawItems、meshes、`Engine.l2dNodes`（= Scene+1096）与消息窗 ⇒ 若真机上有节点挂在 Scene+1080 那张表里，`i1f6` 之后它们仍会出画。 | tracked-done | T-0144(done) scene-drawtable-flush-and-dirty docs-new/03-engine/rendering.md:112 |
| 49 | `0x1f9` | missing-consumer | P2 | op3（颜色）在引擎里是**被消费**的：归一化后作 a5 传 sub_4A3800 → sub_49E9D0，最终作为第 4 实参进表面对象 vtable+28 的装载调用（raw 119762-119766），并写进槽记录 `Scene[5*slot+467]`（raw 123380）。emulator 把它交给 `native.setTextureObjectParam`，而该缝在 PixiBackend / HeadlessScene / StubNative 里**都没有实现**（只有 nativeTap 记录一次丢弃）⇒ 颜色没有任何落点。 | tracked-done | T-0086(done) texture-bind-synchronous-then-query:evidence op-2-05 |
| 50 | `0x1f9` | missing-branch | P3 | 引擎 set-texture 在按 op1 开文件之前先销毁**该槽的 movie 播放器对象**（`_this[op2 + 94672]`，与 0x20F play-movie 同一张 1000 槽表：sub_488FB0 + vtable 析构 + 置 0，raw 31211-31221）；emulator 只有 bindTexture，没有 per-slot movie 记录可清。 | audited-2026-09 | texture-bind-synchronous-then-query:evidence op-2-05 |
| 51 | `0x2` | missing-behavior | P3 | `v2 >= 0` 支缺「目标帧没装载脚本就逐级下退、退到 <0 抛退出」这条链：引擎对 `caller` 指向空帧不会直接切过去，emulator 直接 `cur = caller`，随后 stepOnce 会在空帧上抛错。 | tracked-done | T-0092(done) op-2-10 |
| 52 | `0x2` | missing-behavior | P3 | `v2 >= 0` 且是相邻返回（`v2 + 1 == cur`）时，引擎先 `sub_40EA00` 把被放弃帧的脚本数据/变量区/字符串区/三张表全部释放并把 `_this[frame+122372]` 置 -1；emulator 只切 `cur`，不释放也不置状态格。 | audited-2026-09 | op-2-10 |
| 53 | `0x2` | unclear | P3 | 引擎在 `>= 0` 支还维护 `if (_this[667852] > cur) _this[667852] = -1` 与 `*(_DWORD *)(_this + 120*cur + 383216) = -1`（帧状态格）；emulator 既不建模 667852 也不建模帧状态格，缺材料判断这两格在重写侧对应什么（回放下标？帧状态机？）。 | audited-2026-09 | op-2-10 |
| 54 | `0x201` | missing-behavior | P2 | 0x201 读的 `_this[166964]`（DrawMode）在 emulator 里无写入者：配置 `set:DrawMode` 未绑到该字段、引擎中唯一的脚本写入端 0x200（`sub_423170`）未实现 ⇒ 无论 INI / 宿主如何设置，getter 恒回 0（静默错值，与 T-0057 R4 同类缺陷）。 | tracked-done | T-0057(done) |
| 55 | `0x202` | missing-behavior | P2 | `0x202`/`0x203` 共用的回退源 `sub_4ADD60` 读 `DrawItem+0x60`（工作色 FROM），而引擎在颜色窗内**每帧把插值结果写回该格**（sub_49AA30 raw 117454-117456 与 raw 117470-117479 写 `*((_DWORD *)a2 + 24)` 与调用方指针）；emulator 只在 `freezeWindow`（窗末）写 `it.from = it.to`，窗内 `it.from` 保持配置时的旧值 ⇒ 窗内发出的 `set-draw-color <h> 0 <dur> -1 -1` 在引擎里淡入到“当帧插值色”，在 emulator 里到“窗起点色”。 | tracked-done | T-0102(done) |
| 56 | `0x204` | approximation | P3 | 直绘路径的空白字前进量：引擎在 `set:BlankExtentMode == 1` 且 `Font+201680 <= 1` 时对全角空格 `0x8140`/半角空格 `0x20` 调 `sub_404EE0` 逐字 GDI 量宽，emulator 的 `drawStringGlyphs` 一律用 `advance(ch, size)`（纯算术网格）⇒ 该模式下空格前进量与串落点整段偏移（不报错，只表现不对）。 | tracked-done | T-0085(done) docs-new/03-engine/adv-text-rendering.md:365 |
| 57 | `0x204` | missing-branch | P3 | 引擎门②「该槽的 CTexture 可锁定（vtable+32 返回真）才画」在 emulator 没有对应物：宿主只有「该槽有没有 create-texture 出来的 canvas 表面」这一道门，锁定失败的情形无法表达。 | tracked-done | T-0104(done) T-0085(done) docs-new/03-engine/adv-text-rendering.md:365 |
| 58 | `0x205` | host-invented | P1 | x 前进量被写回脚本操作数 op2：引擎把 op2 读进局部 `v8`、只更新 `&v8`（栈地址），从不回写操作数；emulator 的 `plan.setInt(2, nx)` 把 `x + 前进量` 写回脚本槽 ⇒ op2 是立即数时 `writeIntOperand` 硬抛（unsupported type 0x0），是可写槽时静默污染脚本状态（同一槽常在 1~3 行内被再读）。 | tracked-open | T-0147(open) op-10-006 |
| 59 | `0x205` | approximation | P3 | `set:BlankExtentMode == 1` 时引擎把整个 `cy` 换成逐字 GDI 量宽（半角 `2 × 量宽(0x20)`、全角 `量宽(0x8140)`），emulator 只接了门与公式、没有度量来源 ⇒ `numberCellExtent` 按 `measured: false` 返回原 `gridCy`，于是 x 前进量与串落点和引擎在 mode 1 下不同。 | tracked-done | T-0085(done) op-10-006 |
| 60 | `0x208` | approximation | P3 | 引擎 0x208 是**同步** getter（同一帧里 +1040/+1044 立刻可读）；emulator 的 PixiBackend/TextureCache 在图像尚未载入时返回 0×0 并只触发一次 preload（下一次查询才可能拿到真值），与引擎「同帧真实宽高」不等价。 | tracked-done | T-0005(done) T-0005(done) T-0025(done) texture-bind-synchronous-then-query:evidence |
| 61 | `0x20a` | missing-branch | P3 | 重贴门的取值来源：引擎是 `_this[124350] ? _this[95779] : _this[174801]`（脚本派发中时改读派发前保存的 effect_flags），emulator 只读 `effectFlags`（= `_this[174801]`）⇒ 当 `Engine[124350] != 0`（派发中）且两者 bit30 不同时，emulator 会贴/不贴错一格。 | tracked-done | T-0075(done) op-6-09 |
| 62 | `0x20a` | unclear | P3 | `set:DrawMode`（`dword_55E1BC + 667856`）在重排/重贴里开关两套绘制路径（raw 71551 的 D3D 自绘分支 vs 直绘），emulator 在这两条被调体里完全不读 DrawMode（只有宿主一条发布/光栅化通路）⇒ 无法判定它的通路对应引擎的哪一支。 | audited-2026-09 | op-6-09 |
| 63 | `0x20b` | missing-branch | P3 | sub_4A4C70 会先把矩形夹到表面记录 `v7[263..266]` 的边界（左/上取 max、右/下取 min），夹完为空（`*v5 >= v5[2]` / `v5[1] >= v5[3]`）就直接返回不画；emulator 把 x/y/w/h 原样交给 canvas 的 `ctx.fillRect(x, y, w, h)`，既不做夹取也没有空矩形早退。 | tracked-done | T-0076(done) docs-new/03-engine/rendering.md:32 op-7-0x20b-doc-semantics-but-unimplemented |
| 64 | `0x20e` | missing-branch | P3 | 引擎的三步包裹（`sub_4A50C0(Scene,0x26)` / `sub_498B60` / `sub_4A50C0(Scene,-1)`）以 `Engine[80684] == 1 && Engine[92322] == -1` 为条件，条件为真时设备被 Clear 两次、为假时只 Clear 一次；emulator 只有无条件的 `commitGraphics()`，两个判据字段都不存在，无法区分这两种情形。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 |
| 65 | `0x20f` | missing-operand-io | P2 | 引擎在每条 play-movie 上建/复用影片对象、解槽纹理并把 `_this[699204] \|= 0x2000`（影片播放位）与 `_this[675972] = 1`（影片刷新位）置起；emulator 只转发给一个「只打日志」的宿主缝，既不建对象也不置这两位 ⇒ 引擎里靠这两位分流的路径（raw 11369、raw 20660）在 emulator 完全没有对应状态。 | tracked-done | T-0077(done) T-0082(done) op-4-12 |
| 66 | `0x20f` | missing-branch | P3 | 引擎有两条『抛 C++ 异常、控制流离开本条指令』的分支：影片装载失败抛 asc_51F560、槽纹理表为空抛 asc_520248（`AVCommand_ShowMessage_Exception`）；emulator 无任何对应分支，未绑定纹理的槽也照样『起播』，脚本错误被静默吞掉。 | audited-2026-09 | op-4-12 |
| 67 | `0x20f` | approximation | P3 | op3 在引擎里不是单一『模式』值：先按位解码（0x10000/0x20000/0x40000/0x80000 → 模式 0..3，都不置位时取配置 set:DependMovieSound），再由 `sub_405460` 映射成『对应音源配置 ≥0 / ≠0』的 BOOL，并算音量 = `_this[20032] × sub_408350(op3) / 10000` 交给 `sub_4885A0`；emulator 把 op3 原样交给宿主日志，不读 `_this[20032]`、不做位解码。 | audited-2026-09 | op-4-12 |
| 68 | `0x213` | missing-branch | P2 | 对象表项为空时引擎两个写都跳过、emulator 却新建对象并把 (op2,op3) 写进新槽；这个伪造区间随后被 `itemRangesOf` 组装成 `itemRanges` 发给宿主，使宿主按「脚本删掉了 [base,base+count) 图元」去 detach 本窗正文。 | audited-2026-09 | op8-F1 op8-F5 |
| 69 | `0x21e` | stale-ledger | P3 | 派发表里 `0x21E` 的行内注释仍写「sx/sy/sz ÷256」，与同文件 handler 实现（÷100）和引擎除数 `dbl_5201F0 = 100.0` 都不符——该注释只存在于 `GFX_ITEM_OPS` 表，handler 的 docstring（gfx-item.ts:148）已订正；照注释改代码会把缩放幅度改错 2.56 倍。 | audited-2026-09 | op7-0x21e-stale-div256-comment |
| 70 | `0x223` | missing-behavior | P2 | 缺「写入转场记录前的惰性建层/渲染目标重绑」：引擎 `sub_4ADDB0` 在写记录前先看 `_this[op2 槽号 + 10614]`——为 0 时用 `sub_4A2C10(Scene, 槽号, 本帧目标[260], 目标[261], 目标[1048])` **现建**一个渲染层，非 0 但该层记的渲染目标与当前目标不同时**重绑**；emulator 的 `setTransition` 只写记录表，op2（纹理槽）仅作为记录 `[4]` 的数值保存。 | tracked-open | T-0076(done) T-0084(done) T-0091(doing) T-0076(done) T-0087(done) |
| 71 | `0x229` | missing-behavior | P2 | `0x229` 每次都会先执行 `sub_49A690` → `sub_49A300(Scene, Scene+1120)`，把 740 B 的 DrawItem 模板整块复位（含 `+720 = 0`、`+576 = -1`、`+492/496/500` → `+724/728/732`）；emulator 的 `scSetDrawModeBlock` 只把 5 元组整体替换进 `render4.drawMode`，模板复位这一半完全没有对应物。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 |
| 72 | `0x22d` | approximation | P2 | 引擎里 0x22D（Scene+323 缩放阵）与 0x22F（Scene+387 平移阵）是**两组独立矩阵**，收尾一起乘进 work（`D3DXMatrixMultiply(v73, v73, v118)` 再 `… , v120)`）⇒ 先 0x22D 再 0x22F 净效果 = `v·S + T`；emulator 的 `sceneXform.kind` 是**互斥判别**（kind === 'scale' ? scale : kind === 'axis-scale' ? axisScale : 1），0x22F 落地后 `kind='axis-scale'`、`axisTranslate` = 0x22F 的轴分量、`axisScale` 被 `sceneXformOf` 保留的初始 (1,1,1) ⇒ **同场景内先 i22d 后 i22f 时 0x22D 的缩放被整体丢弃**（引擎两个矩阵都在）。 | audited-2026-09 | op-4-05 |
| 73 | `0x22d` | missing-operand-io | P3 | `scSetSceneAxisTranslation` 建/取变换锚用的是 `sceneXformOf(s, 'axis-scale')`（0x22D 的种类名）而不是平移种类 ⇒ ① `SceneState.sceneXform.kind` 被标成 'axis-scale'，与 0x22D 无法在模型上区分（引擎里 `Scene[306] = 1` 两条都写、不可区分，但 `Scene+323` 与 `Scene+387` 两组矩阵格可分）；② 每次 0x22F 都会经 `sceneXformOf` 的 `??=` 路径保留/重置 axisScale 初值 (1,1,1) —— 这正是上一条 finding 里「0x22D 缩放被抹掉」的直接机制。 | audited-2026-09 | op-4-05 |
| 74 | `0x22f` | missing-behavior | P1 | 引擎在**层号 ∈ [20,30)** 的支里对 0x22F 的 op3/op4/op5 调 `D3DXMatrixRotationAxis(v120, a2 + 181, a2[184])`（轴 = 该三 float、角 = Scene[184]，两者都由 `sub_49A9C0` 从 op3/4/5 写入），该 64 字节阵随后在收尾 `D3DXMatrixMultiply(v73, v73, v120)` 乘进 work ⇒ **op3/4/5 对层 20..29 是 3D 旋转，不是屏幕空间 2D 平移**；emulator 把同一三个 float 当 `axisTranslate` 做 2D 平移（`presenter.ts:662` 的 `tx/ty`）。⇒ 台账与实现注释「op3/4/5 对该区间无任何效果，净效果就是 2D 缩放+平移」与体不符。 | tracked-done | T-0076(done) |
| 75 | `0x234` | approximation | P2 | 引擎 `D3DXMatrixRotationAxis(v88, a2 + 580, angle·π/180)`（轴 = op3/op4/op5 原样，**不归一化地交给 D3DX**）算三维旋转阵并乘进 work；emulator 只保留「绕屏幕 Z 轴、每周期一整圈」的一维角，方向仅取 `loopAxis.z` 的符号 ⇒ 任何含 x/y 分量的轴（真正的斜面/斜轴旋转）被投影成绕 Z 的正/反旋转，且轴的**模长**被丢弃（`i234 (h) 168 0 0 (local-int 2)` 的 z = 2 与 z = 1 在 emulator 得到完全相同的角速度）。 | audited-2026-09 | op-4-05 |
| 76 | `0x234` | unclear | P3 | 轴上界与自身缩放的**合成序**（`D3DXMatrixMultiply(v79, v79, v88)` 中 v79 是谁、该项自身 work 矩阵在 B 层旋转之前还是之后）依赖 `v79` 的实参绑定，本轮只读到 `sub_49BCC0` 的局部命名，未回溯调用方 `sub_4AEEA0` 的实参 ⇒ 无法判定「B 层旋转是否被 A 层缩放放大」。 | audited-2026-09 | op-4-05 |
| 77 | `0x23d` | missing-consumer | P2 | 『释放 slot 42..999』整条能力缺失：引擎既析构影片对象（`_this+4*slot+378688`）又经 `sub_49E980` 解绑 Scene 的槽→imgid 记录并把槽对象析构，emulator 的宿主缝未实现 ⇒ 这些槽的绑定与纹理全部留存，后续引用它们的绘制项仍会贴出旧图。 | audited-2026-09 | docs-new/03-engine/input-system.md:282 |
| 78 | `0x23f` | host-invented | P2 | 0x23F 的取值来源是 `Engine[op2 + 94672]`（该槽的**对象**表：0x236 sub_4246B0 与 0x20F play-movie 惰性建 entry，0x1F8/0x1F9/0x1FA 把它们置 0；尺寸由 sub_4080B0 按 `obj[+1084]` 类型分派取子对象 vtable+40/+68），而 emulator 用的是 0x1F8 记下的**表面**尺寸 `Engine.texSizes`（表面表是 `Scene + 4*slot + 42456`，另一张表，0x208/0x239 的来源）⇒ 引擎答「该槽对象的尺寸」、emulator 答「create-texture 表面的尺寸」。可证的相反情形：序列里**没有** 0x236/0x20F 建对象时（例如 emulator 守卫固化的 `create-texture 2a 78 78 0` → `i23f … 2a`），引擎因 `v2 == 0` 写 **−1**，emulator 写 w*1000。 | tracked-done | T-0076(done) op-2-02 |
| 79 | `0x23f` | missing-consumer | P2 | 引擎 0x23F 读的对象由 0x236（sub_4246B0）与 0x20F play-movie（sub_4237B0）惰性创建（`Engine[slot + 94672]`，raw 32245-32252 / 31627-31638），因此「有对象的槽」都有尺寸可查；emulator 的 playMovie 在 PixiBackend 里只 pushLog、没有 per-slot 对象状态（HeadlessScene/StubNative 同样只记日志，0x236 亦未建模）⇒ 这些槽 0x23F 恒 −1，引擎会给出对象尺寸×1000。 | audited-2026-09 | op-2-02 |
| 80 | `0x244` | missing-behavior | P2 | 引擎在 `0x244` 里还遍历 `Scene+1084` / `Scene+1100` 两张 572B 表，把 `flags & 2` 的节点 `+24`（窗起点）清 0；emulator 的 `scClearDrawItemAnimStarts` 只清绘制项表，Live2D 节点的窗起点不会被清。 | tracked-done | T-0077(done) docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/stub-reaudit-2026-09.md:311 op-5-005 |
| 81 | `0x249` | host-invented | P2 | 0x249 传给 sub_4A3800 的第 6 参是 **1**，callee 里 `if ( a6 ) v7[466] = -1;` ⇒ 引擎把该槽的 imgid 记录（`Scene[5*slot+466]`，0x216 读的那一格）写成 **−1**；emulator 反而 `e.texSlots.set(slot, imgid)` 写入真实 imgid ⇒ 与引擎相反。 | tracked-done | T-0086(done) T-0076(done) |
| 82 | `0x249` | missing-consumer | P2 | op3（颜色）在引擎里被真正消费（进表面对象 vtable+28 的装载调用 raw 119762-119766，并写 `Scene[5*slot+467]`）；emulator 交给 `native.setTextureObjectParam`，而该缝在 PixiBackend/HeadlessScene/StubNative 都没有实现（只有 nativeTap 记丢弃）⇒ 颜色在 emulator 没有任何落点。 | tracked-done | T-0086(done) |
| 83 | `0x258` | missing-operand-io | P3 | 引擎把 op2 的 bit0/bit1 分别以 0/1 写进**两张**镜像表（`Scene+1872` 与 `Scene+21872`）的四格，emulator 只写一张 `Engine.texSlotFlags`（值 = bit0\|bit1<<1），镜像表结构缺失。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 |
| 84 | `0x258` | noop-claim-unjustified | P3 | emulator 的字段注释写作「纹理槽标志对（建模）」并保留 `texSlotFlags` 状态，但引擎这四格是**只写不读**；照本项目自己的判据（`0x238`/`0x2FA`：写一个没人读的字段等于死写、不建模）这里的状态也应标注为「建模≠有消费端」，否则下游会误以为槽标志会影响渲染。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 |
| 85 | `0x259` | missing-consumer | P3 | `Engine.texSlotFlags` 无消费者：handler 在 gfx-misc.ts:87 清它，但全仓没有读点（渲染、装配、快照、读档都不含它）；引擎侧这对标志位却会随存档快照持久化（raw 17474）并在装载路径按 flag_a==1 决定重建槽（raw 19877）⇒ emulator 里这次复位是空转，且标志位本身活不过读档。 | tracked-done | T-0102(done) op-3-007 |
| 86 | `0x25b` | missing-operand-io | P3 | 引擎在 0x25B 体内写第二个字段 `_this[92379] = 2`（消息态模式 = 图像），emulator 只写 92381 ⇒ 模式位永远停在 0x25A 留下/初始的 0（不对称于 0x25A 的 `mediaMode=1`）。 | tracked-done | T-0093(done) op8-F8 |
| 87 | `0x25b` | missing-behavior | P2 | `if (!_this[167990]) { … sub_408440(_this, op1); }` 这条图像加载分支整段未建模：引擎会真解码资源并把图渲进 Scene 的固定帧纹理，失败时抛 `Command_ShowMessage_Exception`（影响控制流），emulator 只存 id、什么都不做也不报错。 | tracked-done | T-0093(done) T-0075(done) op8-F8 |
| 88 | `0x260` | missing-consumer | P1 | `vPad` 无消费者：引擎在竖排绘制时用 (+235112,+235120) 平移目标点、用 (+235116,+235124) 扩源矩形（`sub_45A940` 一族），emulator 只把 4 个值存进 `g.vPad` 就再无读者 ⇒ 竖排文本的这层修正完全丢失。 | tracked-done | T-0057(done) |
| 89 | `0x260` | missing-operand-io | P3 | 写入归属错：引擎的 4 格是 Font 级全局（`_this[80102..80105]` = Font+235112..235124），emulator 写进「写入时刻 `defaultWin` 的 geom 对象」，默认窗一改（0x80）这 4 个值就归到别的窗名下。 | tracked-done | T-0057(done) |
| 90 | `0x2bd` | missing-behavior | P2 | 漏掉同步写的第二个字重格：引擎在 op1≠0 时把 `_this[75953]`（Font+218516）与 `_this[21636]`（Font+1248，主字体 LOGFONTA 模板 `Font+0x15200` 的 lfWeight）**同时**写 700（假时同写 0）；emulator 只写前者，`Font+1248` 不建模。台账 `analysis/fields.json` 的 `logfont_main`（0x15200：「+1248=lfWeight」）与 `lf_weight_probe`（0x4A2C4：「与 +1248 同步写：op 0x2BD 非 0 ⇒ 两处都 700」）都写明这对是同步的。 | tracked-done | T-0057(done) text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 91 | `0x2bd` | missing-branch | P3 | `sub_459F40` 的首行早退门（`Font+1260` 字节为 0 则整个字体应用流程丢弃、连两个字重格都不写）在 emulator 没有对应物；emulator 无条件写字段并即时影响排版快照。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 92 | `0x2be` | missing-behavior | P3 | 漏掉同步写的第二个注音字重格：引擎 op1≠0 时把 `_this[75971]`（Font+218588）与 `_this[21327]`（Font+1308，注音 LOGFONTA 模板的 lfWeight）同时写 700；emulator 只写第一个。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 93 | `0x2be` | missing-branch | P3 | `sub_45A6E0` 的首行早退门（`Font+1320` 字节为 0 则注音字体重建整体 no-op）在 emulator 无对应物，字号/字重变更始终即时生效。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 op-2-07 |
| 94 | `0x2c7` | missing-branch | P3 | 负起点的错误路径缺失：引擎只把 `v3 >= v13 \|\| v4 <= 0` 判成空串（`v3` 是 signed），`v3 < 0` 且 `v4 > 0` 时会继续执行到 `sub_40C120`，那里的 `if ( v6 < a3 )` 是**无符号**比较 ⇒ 必然抛 `std::out_of_range`（引擎的脚本异常路径）；emulator 在 `sjisSubstr` 里把 `start < 0` 与 `start >= total` 合并成静默空串 ⇒ 该错误路径在 emulator 侧根本不存在（失败变成静默、脚本继续往下跑）。 | audited-2026-09 | op-5-007 |
| 95 | `0x2c8` | missing-branch | P3 | op2 的 **255 字节截断**这一支缺失：引擎先 `strcpy_s(Destination, 0x100u, v3)` 把 op2 拷进 256 字节栈缓冲（含结尾 0），随后的 `_mbstrlen(v4)` 与逐字节循环都只看**被截断**的串 ⇒ op2 超过 255 字节时字符总数与可拷内容都被截；emulator 直接用完整 JS 串算 `total`/迭代，不截断 ⇒ 长串的子串结果与字符计数分叉。 | audited-2026-09 | docs-new/03-engine/save-data.md:60 |
| 96 | `0x2e9` | missing-consumer | P2 | `_this[122464]`（ADV 自动翻页行基准）在 emulator 里只被写、无任何读者：引擎按「(当前窗行数 − 1 − 基准) × message:AutoMessageSpeed + message:AutoMessageMinTime（再夹到 ≥100ms）交计时器」算自动翻页停留时长，emulator 未实现该计时 ⇒ 自动翻页的节奏与真机不同（脚本只能靠写入这格来表达「本行比首行多停多久」）。 | tracked-done | T-0076(done) op-2-01 |
| 97 | `0x2eb` | missing-branch | P2 | 键存在但值为**空串**时引擎侧有「内建缺省 1.00 还在不在」这一层语义，而 emulator 的 `cfgStr` 对空串直接返回 `''`（`v === undefined ? fallback : String(v)`）⇒ op1 写成空串，TITLE 的第一段为空、`atoi("")` = 0 ⇒ 屏幕上是占位值 "0.00.0000"。而 emulator 自己生成的 overlay INI 恰好就写 `GameVersion=`（空值），所以这条路径在本机是稳定复现的。 | tracked-done | T-0124(done) |
| 98 | `0x2f4` | missing-behavior | P3 | 引擎按 op3（通道）读写 4 组字段：`_this[ch+21315]`（条件清 0 或置 `\| 0x10000`）、`_this[ch+21318]`（同形）、ADV 分支写 `[ch+122505] = op1` / `[ch+122508] = op2`、非 ADV 分支写 0；emulator 的 `op_voice_play_slot` 只发 `voice-defer`/`voice-play` 意图，`engineValues` 里这些字段保持不动 ⇒ 内存态与引擎漂移（且 `0x1BC` 的清零会把它们抹平，掩盖漂移）。 | audited-2026-09 | docs-new/03-engine/sound-system.md#§9 |
| 99 | `0x2f4` | approximation | P3 | op2 的位宽语义：引擎把 op2 **原值**交给 `sub_4BB840`，后者只取 `a4 & 1`（低 1 位）；emulator 在 handler 里先做 `(p.int(2) ?? 0) !== 0`，把 -1（引擎 `-1 & 1 = 1` ⇒ 循环）归一成 `true`、257（引擎 `257 & 1 = 1`）也归一成 `true`，而 2（引擎 `2 & 1 = 0` ⇒ 不循环）归一成 `true`。 | audited-2026-09 | docs-new/03-engine/sound-system.md#§9 |
| 100 | `0x2f6` | missing-operand-io | P3 | 0x2F6 的引擎字段回写全缺：`Engine[21315+ch]=0`、`[21318+ch]=0`、`[122505+ch]=0`、`[122508+ch]=0` 与 `[122501] = 三路语音是否有正忙的` 在 emulator 全部不做，导致同一条复位指令在 engineValues 里无痕（0x1BC 会写、0x2F6 不写）。 | tracked-done | T-0082(done) op-2-08 op8-F9 |
| 101 | `0x2f6` | missing-consumer | P3 | `Engine[122501]`（三路语音是否有正忙的）在 emulator 连字段都没登记读取/写入；引擎每次 0x2F6 都刷新它，emulator 没有对应可观测状态。 | audited-2026-09 | op-2-08 op8-F9 |
| 102 | `0x2f6` | overreach | P3 | 引擎按 ch 直接下标寻址、**不校验通道号值域**（`Engine[ch+21315]`），设备侧只认 ch<15；emulator 的 `#voiceChannel` 只认 0..2 并打日志丢弃，ch=3..14 时引擎会改字段、emulator 什么都不做。 | audited-2026-09 | op-2-08 op8-F9 |
| 103 | `0x2f8` | missing-branch | P3 | 引擎的通道上界是「设备通道号 < 15」（即语音通道 ch ≤ 2 之外还覆盖 3..14 的直通通道），emulator 的 `#voiceChannel` 只认 0..2 ⇒ ch=3..14 时引擎会写 `Sound[ch+375]` 并下发 SetPan，emulator 只打一条越界日志、什么都不做。 | audited-2026-09 | op-2-08 op-2-09 |
| 104 | `0x2fc` | missing-consumer | P3 | 无触点路径上引擎仍有副作用：`sub_477980` 遍历完缓冲且没有任何合法项时把触摸项计数 `_this[1694]` 清零（raw 92162-92163），即缓冲里的项全是手写笔项或缺 0x10 位时被丢弃并清计数；emulator 没有触摸缓冲，这条清空无从复现（也没有消费端读该计数）。 | audited-2026-09 | op-10-003 |
| 105 | `0x2fe` | missing-branch | P2 | 引擎对不在可选字体表（且不等于 `"AGE Extend"`）的注音面名会打印警告串「警告：[%s]は選択可能フォントの一覧に含まれていません。」；emulator 无条件接受任意面名、既不警告也不回退，于是「装不到的注音字体名」与「装得到的」表现完全相同（都静默走 FACE_MAP），排查字体问题时没有任何信号。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 |
| 106 | `0x2fe` | missing-behavior | P3 | `sub_432DD0` 除存面名外还把注音字形的**位置字段**按注音字号写死：`Font+1296 = 字号/-2`、`Font+102036` 同值、`Font+1292 = -字号`、`Font+102032` 同值（并且 `strcpy_s(_this + 1320)` 存原面名、`_this + 102060` 存 `"@" + 面名` 的竖排变体）；emulator 只存 `msgwin.font.rubyFace` 一个字段，既没有 `@` 变体也没有任何布局派生量。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop docs-new/03-engine/adv-text-rendering.md:488 |
| 107 | `0x303` | approximation | P3 | 引擎把 op2 **原样**写进消息窗对象的 `+288`，emulator 先把它归一成 `mode === 1 ? 1 : mode === 2 ? 2 : 0` 再存进 `align`：op2 取 1/2 之外的非 0 值时引擎会留下原值，emulator 存 0。 | audited-2026-09 | docs-new/03-engine/adv-text-rendering.md:488 |
| 108 | `0x305` | missing-branch | P1 | 缺总门分支：引擎仅在 `(Engine[489988] & 0x10001) == 65537`（bit0 由 `0x304` 置、bit16 由 `0x6E` 在 bit0 已置时补写）时才取回行游标并 `while (!sub_45BE20(...))` 把余下行贴出；emulator 无条件 `finishReveal` 所有窗并可能 `endCharReveal`，于是「`0x304` 后未插 `0x6E` 的 `0x305`」（以及注音插入被判定为空内容、bit16 未置的场合）会多贴一整段文本。 | tracked-done | T-0077(done) op-7-0x305-flags-not-cleared op3-008 op7-0x305-wrong-raw-anchor |
| 109 | `0x305` | missing-consumer | P2 | 出口侧的等待/节拍两段没建模：`EffectFlags` 的 `0x20000000` 位 + `sub_453A60(Engine+430572, MessageSpeed)` 节拍计时器（`MessageSpeed` 非 0 且 `0x8000000` 未置时），以及等待门分支写的计时器三格 `369352 = 0 / 369356 = 0 / 369344 = 1`。 | audited-2026-09 | op-7-0x305-flags-not-cleared op3-008 op7-0x305-wrong-raw-anchor |
| 110 | `0x308` | missing-behavior | P2 | 0x308 的宿主副作用完全没有实现：既不 `LoadLibraryA`+`GetProcAddress` 调注册/注销触摸的导出，也不按门 `op1 \|\| (GetConfig("system:limitTouch") & 1)` 选择注销（op1≠0 或配置位=1）还是注册（op1=0 且配置位=0）⇒ 触摸注册永远是空操作。 | tracked-open | T-0111(doing) docs-new/03-engine/stub-reaudit-2026-09.md:219 op-5-001 |
| 111 | `0x308` | missing-operand-io | P3 | `_this[1954] = op1`（三条出边都写，含失败路径）在 emulator 里没有对应格；该字段全反编译只写不读 ⇒ 对 VM 不可观测，属登记性缺口（与 0x2fa 的 Engine[1951] 同类）。 | tracked-open | T-0111(doing) docs-new/03-engine/stub-reaudit-2026-09.md:219 op-5-001 |
| 112 | `0x30a` | missing-branch | P3 | `op1 > 0x1F \|\| op2 > 7`（unsigned）时引擎抛 `SetGesKeyの引数が不正です．` 且**不写表**；emulator 的 no-op 静默接受任意实参（包括 op2 < 0 / op2 > 7 这种引擎会拒绝的索引）。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 op7-0x30a-noop-vs-body |
| 113 | `0x32` | missing-branch | P2 | 引擎按 `set:DrawMode`（`Engine+667856`）二选一：`== 0` 时把转送交给 `Scene+7912` 的 2D/GDI 对象（vtable+64），`!= 0` 才走 `sub_4A87A0`；emulator 的 handler 完全不读该字段、只实现后者，而本机配置是 `DrawMode=0` ⇒ 真机走的支路在 emulator 没有对应实现。 | audited-2026-09 | op-6-10 |
| 114 | `0x320` | missing-behavior | P3 | 引擎把顶点位置写成 `x - 0.5` / `y - 0.5`（`dbl_51D7F8 = 0.5`，raw 4197），emulator 直接取 op2/op3 的数组值当屏幕像素 ⇒ 整块几何相对真机偏移 (+0.5, +0.5)。 | audited-2026-09 | mesh-vertex-quad-and-per-vertex-color:fn |
| 115 | `0x320` | missing-behavior | P2 | 引擎新建 mesh 记录时把两格态色都初始化成 `-1`（= 0xFFFFFFFF，白不透明白），emulator 的 `makeMesh` 初始化成 `0`（全透明）⇒ "有几何但从未发过 0x322/0x323" 的 mesh，引擎按基础色绘制、emulator 因 `alpha = 0` 整块不画。 | audited-2026-09 | mesh-vertex-quad-and-per-vertex-color:fn |
| 116 | `0x320` | host-invented | P3 | emulator 自造了"顶点数 ≥ 3 才算建成几何"的判据（引擎只有 `vcount > 0` + `sub_4A2280` 的成功位），`vcount = 1..2` 时引擎会置绘制门 bit0 并建 36/72 字节顶点缓冲，emulator 连项都不建。 | audited-2026-09 | mesh-vertex-quad-and-per-vertex-color:fn |
| 117 | `0x321` | missing-behavior | P3 | 引擎在写格前会**自动创建**缺失的网格项（两级容器 find-or-create + `memset(v11, 0, sizeof(v11))` 零值初始化），emulator 只把这当成 `meshAttrs` 的一个 Map 键，没有「缺失即建零值网格项」的模型。 | audited-2026-09 | mesh-vertex-quad-and-per-vertex-color:fn |
| 118 | `0x321` | missing-branch | P2 | 引擎对 op2（项内槽号）**没有任何范围校验**，`result[op2 + 7] = op3` 是直接派生地址写；emulator 把它当任意 Map 键收下，没有任何「越界槽号」的守卫或报错口径，双方谁也没有把「合法槽号范围」写清楚。 | audited-2026-09 | mesh-vertex-quad-and-per-vertex-color:fn |
| 119 | `0x326` | approximation | P3 | 豁免描述只写「惰性建 ID3DXEffect(资源 202) 并重建 Snow」，漏掉**外层门 `Scene+46668 >= 1`**：不满足时整条 handler（含错误支）全不执行；该门是「3D/文字渲染冻结总闸」，emulator 侧 `Scene+46676` 都未建模，`+46668` 同样没有对应物。 | audited-2026-09 | lazy-3d-effect-202-snow scene-3d-weather-effects-rain-snow-leaf lazy-3d-effect-202-snow:why docs-new/03-engine/scene-start-flow.md:96 op-2-04 op-9-op805-325-noop-but-writes-effect-manager-fields op-10-005 |
| 120 | `0x326` | missing-branch | P3 | 体里 op4（纹理槽对象）为 0 时不是静默失败，而是 `sprintf_s` 组「関数：Set3DEffectSnow エラー：テクスチャが作成されていません．TEXTURE=%d」并 `sub_4034C0` 上报；no-op 后这条错误支在 emulator 永不发生。 | audited-2026-09 | lazy-3d-effect-202-snow scene-3d-weather-effects-rain-snow-leaf lazy-3d-effect-202-snow:why docs-new/03-engine/scene-start-flow.md:96 op-2-04 op-9-op805-325-noop-but-writes-effect-manager-fields op-10-005 |
| 121 | `0x327` | stale-ledger | P2 | `stubs.ts` 同一文件内自相矛盾：第 213-215 行的块注释仍写「同族的 `0x327`…与 `0x328`…**目前根本没注册** ⇒ 命中即 `NotImplementedOp`。**故意不上桩**」，而第 156-157 行已经把它们注册进 `ENGINE_INTERNAL_OPS`；这段注释没有机械消费者，下一位读者据此会误判「SETWEATHER 仍会硬停」并可能把它们撤表（撤了整段剧情重新硬停）。 | tracked-open | T-0093(done) T-0103(open) lazy-3d-effect-202-snow scene-3d-weather-effects-rain-snow-leaf op-8-F10 |
| 122 | `0x328` | missing-branch | P3 | `Scene+50708+4*id == 0` 时引擎逐 id 打「関数：Set3DEffectLeaf エラー：メッシュが作成されていません．MESH=%d」并 `sub_4034C0` 上报；no-op 让这条错误支永不发生（引擎侧是「leaf 挂上部分网格 + 报错」，emulator 侧是「什么都不发生、无日志」）。 | tracked-done | T-0093(done) lazy-3d-effect-202-snow scene-3d-weather-effects-rain-snow-leaf |
| 123 | `0x328` | approximation | P3 | 豁免理由写「emulator 无 3D 网格表（`Scene+80708` 的层/网格对象族 `sub_4AAA50`/`sub_4AAEC0`）」—— 事实上网格槽表已建模为 `scene.meshes`（`0x32A` 逐槽删、`0x32B` 清表都由它承接），缺的是 Effect3D 的 Leaf 对象与「把槽对象挂到 Leaf」这一步；照现描述会让人以为表本身不存在而重复造表。 | audited-2026-09 | lazy-3d-effect-202-snow scene-3d-weather-effects-rain-snow-leaf |
| 124 | `0x329` | missing-consumer | P3 | emulator 把网格槽表建模为 `scene.meshes`（`0x32A` `scRelease3DSlot` 逐槽删、`0x32B` `scClearMeshSlots` 清表），但**唯一往该表里装对象的路径（0x329）是 no-op** ⇒ 表恒空：真机上 `i329` 之后 `0x32A`/`0x32B` 有话可删（`clearMeshSlots: 释放 meshes=N` 日志 N>0），emulator 里 N 恒 0、`0x328` 也永远无网格可挂。 | tracked-done | T-0093(done) op-4-05 |
| 125 | `0x32c` | missing-branch | P3 | 体是**二段式**：视图矩阵初始化 + `D3DXMatrixLookAtLH` + 设备 vtable+176(SetTransform) **无条件**执行；只有「把视图/投影/世界三矩阵转发给 Effect3D 管理器」那一段被 `if (管理器 != 0)` 门住。豁免描述只提「3D 相机/天气参数面 + 设备 SetTransform」，没写这个门（以及 Rain/Snow 可分别缺的第二层门）⇒ 若将来实现 0x32C，容易漏掉「管理器为 0 时相机仍生效」这一半。 | audited-2026-09 | op-2-04 |
| 126 | `0x32d` | missing-operand-io | P2 | op2 的字节→颜色通道映射写反：引擎把 op2 的**最低字节**当蓝（`(unsigned __int8)v3`）、第三字节当红（`BYTE2(v3)`），emulator 把最低字节当红（`(rgb & 0xff)/255`）、第三字节当蓝，于是所有 `0x32d` 下发的颜色整体发生 R/B 交换。 | audited-2026-09 | docs-new/03-engine/scene-start-flow.md:96 |
| 127 | `0x32e` | overreach | P2 | `stubs.ts` 与台账把 0x32E 描述成「3D 图元/效果」（`sub_49A080(Scene, …)`），实际被调体是 **D3D `SetLight`**：它写 `Scene[26*op2+13687]` 的 104B 灯光结构（Position=op3..op5、Direction=归一化(op6,op7,op8)、Range=op11、Diffuse=A/R/G/B）并置 `Scene[op2+13677] = 1`，再调设备 vtable+204/212 —— 与 0x32F（同一 vtable+212 的 `LightEnable`）是同族；按现描述做实现会去建「图元/效果」层，而真正缺的是灯光表 + 宿主灯光缝。 | audited-2026-09 | op-7-0x20b-doc-semantics-but-unimplemented |
| 128 | `0x32e` | missing-branch | P3 | op9（α）是**钳位饱和**而非报错：`if ( v2 > 255 ) v2 = 255;` 之后才拼 ARGB —— 即 α>255 静默降到 255（与 0x32D 的 α<0 回退、>255 钳位是同族三态）；no-op 后钳位行为不存在，若将来实现而不带钳位，A 通道会溢出进 RGB 位。 | audited-2026-09 | op-7-0x20b-doc-semantics-but-unimplemented |
| 129 | `0x33f` | missing-operand-io | P2 | op2（α）与 op3（颜色）两格引擎都读并写进 `Scene+1264`，emulator 只读 op1、op2/op3 完全没读（`plan` 里也没声明），两格的下发能力整体缺失。 | tracked-done | T-0017(done) |
| 130 | `0x33f` | missing-branch | P3 | 引擎的两条负值回退分支完全没实现：op2 < 0 ⇒ 取 `sub_4ADD60(Scene, op1) >> 24`（该绘制项当前 α）、op3 < 0 ⇒ 取 `sub_4ADD60(Scene, op1)`（该绘制项当前色）；emulator 既没有绘制项 `Item + 96` 当前色模型，也不会去做这个回退。 | tracked-done | T-0017(done) |
| 131 | `0x341` | missing-branch | P2 | 引擎在 .MOC 读不到或解析失败时构建错误串并 `_CxxThrowException`（Command_ShowMessage_Exception），脚本侧是会弹消息窗并中断的异常路径；emulator 只写一条 log 并把槽留空，文件坏了在 emulator 里完全静默。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 132 | `0x341` | missing-behavior | P3 | `sub_4A1860` 成功后把改帧位 `M[11627]` 置 1（`sub_40BE10` raw 16022 读它决定是否继续出帧）；emulator 的 `l2dLoadModel` 只改 `l2dSlots`，不置任何等价位。 | tracked-done | T-0054(done) docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 133 | `0x346` | approximation | P2 | `0x346` 复位后 emulator 把旋转写成 axis=[0,0,1]/deg=0，而引擎**不碰**轴 `+464/+468/+472` 与角 `+488`，缺省值是轴 (0,0,0)、角 -1（`sub_49CA10` raw 118434 写 `*(_DWORD *)(v2 - 420) = -1` 与 raw 118487-118490 写轴三分量为 0.0）；带 0 长度轴的轴角矩阵在 D3DX 里是**未定义**（emulator 的 `affineRotation` 对 len==0 返回单位阵，nodeMatrix.ts:283），所以『引擎未定义 / emulator 恒单位阵』这一半也是自造，但两者在『节点未被 0x348 写过』这一状态下已经不同源。 | tracked-done | T-0096(done) docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 134 | `0x34a` | missing-behavior | P2 | 0x34a 改基础平移偏移后引擎会置 Scene+46508 脏位（`_this[11627] = 1`），emulator 的 l2dNodeBaseOffset 只改 baseOffset、不置 matrixDirty 也不通知宿主置脏 ⇒ 该指令单独发生时不保证重算/重画。 | audited-2026-09 | op-4-07 |
| 135 | `0x34e` | missing-behavior | P2 | 引擎在 .MTN 取不到/解析失败时抛 ShowMessage 异常（"L2Dモーションファイル %s の読み込みに失敗しました"）打断脚本执行；emulator 只记一条 log 后 return null（沉默跳过），脚本继续往下跑。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 136 | `0x34e` | missing-behavior | P2 | sub_478640 在本条指令装载动作时会把 `0x352` 预置的纹理号（实例 +24/+28）与动作号（+25/+32）**写进动作记录**（动作记录 +4/+8）并清标志；emulator 的 startMotion 只是把 pendingMotionNo/pendingTextureNo 置空，从未把它们应用到动作上 ⇒ `i352 … i34e` 这对语料失去效果。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 137 | `0x34e` | missing-branch | P2 | 引擎只有 op2 == 0（动作槽 0）这条支线调 sub_4784D0 消费循环位 op4（写实例 +20 与动作记录 +36）；op2 != 0 支线**完全不消费 op4**。emulator 对两个动作槽都无条件把 loop 传进 startMotion ⇒ 槽 1 的循环语义被凭空加上。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 |
| 138 | `0x34f` | missing-behavior | P2 | 引擎把 op2（或负值时查到的纹理色记录）当打包字节解码成三个 0..1 的乘色分量并逐部件下发（sub_478590 → sub_4BD150）；emulator 只把原始 int 存进 `textures` 表的 -1 号键，既不解码也不下发（`op2 < 0` 的查询回退同样缺失）。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 op-1/0x34F-negative-color-branch |
| 139 | `0x352` | missing-branch | P2 | sub_478540/sub_478560 都以 `if (*(_DWORD*)_this)` 为门：实例（Live2D 槽）不存在时 0x352 是彻底的 no-op；emulator 用 ensureSlot 建出空槽并写入 pending 值 ⇒ 引擎里不存在的槽在 emulator 里被创建。 | audited-2026-09 | docs-new/03-engine/stub-reaudit-2026-09.md:311 docs-new/03-engine/stub-reaudit-2026-09.md:15 |
| 140 | `0x53` | unclear | P3 | `op2 = INT_MIN(-2147483648)` 且 `op3 = -1` 时，引擎的 `v3 / v2` 编译成 x86 `idiv` ⇒ 商 +2147483648 溢出，CPU 抛整数溢出例外（真机异常/崩溃）；emulator 的 `Math.trunc(-2147483648 / -1)` 返回 2147483648（经 `p.setInt` 落池时不回绕，读回是 2147483648 而不是 int32），既不是例外也不是引擎的任一可观测输出。 | tracked-done | T-0057(done) op7-0x53-div-zero-throw-invented |
| 141 | `0x60` | missing-operand-io | P3 | 模数为 0 时，引擎在 `_CxxThrowException` 之前**先写了一次 op1=0**（sub_42B4B0 case 3 走全局 int 持久池：值 ENC 后写进 `_this+382976`），emulator 直接 `throw` 一格不写 ⇒ 少一次可观测的写序；若宿主或后续通路捕获该异常，`random (global-int X) 0` 在引擎里 X 已变 0、在 emulator 里 X 保持旧值。 | audited-2026-09 | op7-0x53-div-zero-throw-invented op-1/0x60-random-divzero-comment |
| 142 | `0x60` | approximation | P2 | 引擎的随机数是 CRT `rand()`（进程级 LCG 流），emulator 用宿主 `Math.random()` 近似 ⇒ 同一存档/同一脚本跑两次得到不同随机流，帧 digest 与录制的回放轨迹不可复现（T-0005 的 G3 回放判据要求 engine 段逐帧相等）。 | audited-2026-09 | op7-0x53-div-zero-throw-invented op-1/0x60-random-divzero-comment |
| 143 | `0x6e` | missing-consumer | P2 | 引擎 `sub_46CBF0` 的「同步排空」= 排进记录后**自旋到 `sub_45BE20` 报『该窗再无待显行』**，而 `sub_48F000` 还是按 `.{8.8x}` 条目在 `Font+258` 那张表里查 `a2=Engine[97055]` 的第 a3 项（非 0 才置 ADV）—— emulator 无「待显内容/条目表」这一层，`advanceReveal` 直接拿 `message:ReadTextSkip` 配置值当 ADV 判据（`readTextSkipOf(e) !== 0`），于是「门开着但这句话没有语音/条目（`sub_48F000` 返回 0）⇒ 引擎清 ADV、emulator 置 ADV」这一支不存在，`0x8000000` 的置位时机与真实引擎不同源。 | audited-2026-09 | adv-advance-opcodes adv-flag-lifecycle msgwin-config-gates docs-new/03-engine/message-config-gates.md#§1 op-10-002 op-7-0x305-flags-not-cleared op-10-004 |
| 144 | `0x6e` | missing-behavior | P3 | 引擎在「文本仍在显示」的非注音支里**写** `_this[122455] = 1`（raw 28359），emulator 的 `advanceReveal` 只有 `m.showing = readTextSkipOf(e) !== 0 ? 1 : 0`，且在 `readTextSkip` 为 0 时**无条件清 0**；引擎的 `_this[1415]`（跳读位，由 `0x88` 写）非 0 时 `sub_48F000` 路径会把 `122455` 留成 1，并在随后 LABEL_11 的 `if (_this[122455])` 里被当输入消费判据读用（`0x72` raw 28507/28515）。 | audited-2026-09 | adv-advance-opcodes adv-flag-lifecycle msgwin-config-gates docs-new/03-engine/message-config-gates.md#§1 op-10-002 op-7-0x305-flags-not-cleared op-10-004 |
| 145 | `0x6f` | missing-behavior | P3 | `sub_46AF90` 在非注音支把**当前 24B 文本记录的笔位**推进到下一行（`*(v5[12]-20) = v5[7]`、`*(v5[12]-16) += v7`、`++*(v5[12]-4)`、`++v5[71]`），emulator 的 `endLine` 只置段标志而**不写任何笔位/行号字段**；`▼` 字格图标在 followText 模式下正是靠「末条记录的 `-20`/`-16`」定位（`cellFrameOf` 注释自述改用排版重算绕过），所以这层字段在 emulator 里没有真源。 | audited-2026-09 | adv-advance-opcodes op-2-12 |
| 146 | `0x6f` | missing-consumer | P3 | `if (a3 >= 0)`（`a3 = _this[97055]` 记账门）成立时，引擎还会 `sub_4691A0(Font, 窗, gate\|8, 0, unk_51F030)` 往该行登记注音；emulator 的 `end-text-line` 完全不读 `ENGINE_FIELD.textBaseGate`、也不登记注音（注音只在 `show-text`/`0x196` 走 `addRuby`）。 | audited-2026-09 | adv-advance-opcodes op-2-12 |
| 147 | `0x70` | missing-operand-io | P2 | 引擎把 w/h **重复写进两组字段**：`obj+20/+24`（`v10[5]/v10[6]`）与 `obj+36/+40`（换行边界）；emulator 只写 `g.w/h` + `g.wrapRight/wrapBottom` 两组语义槽，引擎的「同一窗对象上 `+20/+24` 与 `+36/+40` 分别被不同读者取用（D3D 支 `+20/+24` 走 `sub_4A7170`，GDI 支 `+36/+40` 之外还写 `+124/+128`）」这一分层没有对应物。 | audited-2026-09 | msgwin-offscreen-surface-lifecycle:note docs-new/03-engine/adv-text-rendering.md:488 |
| 148 | `0x70` | missing-consumer | P3 | 落点 `sub_45D660` 的 D3D/GDI 互斥支（`sub_4A7170` 重建表面 / `sub_43C8D0`+`sub_43B070` 提交铺排）在 emulator 完全缺席：几何写完后不发任何「表面尺寸变化」意图，只发 `emitWin` 的文本载荷。 | audited-2026-09 | msgwin-offscreen-surface-lifecycle:note docs-new/03-engine/adv-text-rendering.md:488 |
| 149 | `0x71` | missing-consumer | P2 | `sub_41ED80` 尾部 `sub_48FFB0(_this + 80107)` 是**消息窗队列的冲刷点**（与 `0x72` raw 28529 的 `sub_4BB840` 三槽交付同属「待显消息槽」这一层），emulator 的 `op_message_show` 只清本窗文本 + 重排显现，没有任何队列冲刷/交付。 | audited-2026-09 | adv-flag-lifecycle op-4-10 |
| 150 | `0x71` | missing-behavior | P3 | 引擎在本条**必然**写 `_this[122496] = 0`（三条出口都写，raw 28444/28449/28457）并在跳读支写 `_this[122455] = 1`、`_this[174801] \|= 0x8000000`；emulator 的 handler 只经 `advanceReveal` 间接动 `effect_flags`，`122496` 一格完全不写，`122455` 只在 `advanceReveal` 里按 `readTextSkip` 赋 0/1（与引擎的 `sub_48F000` 判据不同源）。 | audited-2026-09 | adv-flag-lifecycle op-4-10 |
| 151 | `0x72` | missing-consumer | P2 | 引擎在 `!122496 && !(mask & 0x40)` 时对 3 个消息槽做交付循环：`sub_4BB840(Voice, i, _this[122505+i], _this[122508+i], *(int*)((char*)_this + 4*i - 469808))` 并**把槽清 0**，emulator 只做 `input.consumeEdges()`，`122505..122508` 这 6 格既无写者也无消费者 ⇒ 排队语音/消息的起播时点丢失（且 `0x19C`/`0xC4` 一族正是往这些槽里寄存的）。 | audited-2026-09 | adv-advance-opcodes docs-new/03-engine/flow-control.md:155 |
| 152 | `0x72` | missing-behavior | P3 | 共存消息支（`v7 = _this[97052] == 0; _this[97053] = 0; if (!v7) { …按 122501 选 message:AutoMessage*/MessagePitch 系数… if (v10 <= 100) v10 = 100; sub_453A60(_this+107545, v10); }`）在 emulator 的 handler 里没有对应物：`97052`/`97053` 的写点（`0x1B7` 与全局 teardown）存在，但本条既不读它们、也不写 `97053 = 0`、更不装 `Engine+107545` 的自动翻页节拍。 | audited-2026-09 | adv-advance-opcodes docs-new/03-engine/flow-control.md:155 |
| 153 | `0x72` | missing-branch | P3 | 引擎的早退判据是 `_this[122455]`（本页文本显示中），emulator 用的是 `advanceReveal(e)`（`readTextSkipOf(e) !== 0` 的等价物）；两者在「门关（`ReadTextSkip = 0`）但 `122455 = 1`」时不重合：引擎跳到 LABEL_17 武装但不消费输入，emulator 会 `clearAdv` 后继续走 `consumeEdges` + `finishPage`。 | audited-2026-09 | adv-advance-opcodes docs-new/03-engine/flow-control.md:155 |
| 154 | `0x75` | approximation | P3 | 引擎把字号同步写进**三到五格**：`Font+201684 = op1`、`Font+1232 = -op1`（主 lfHeight）、`Font+101972 = -op1`（注音 lfHeight），并在 `Font+201680 == 0` 时补 `Font+1236 = Font+101976 = op1 / -2`（半高）；emulator 只写 `ENGINE_FIELD.fontSize`（+ `msgwin.font.mainSize` 镜像），负数高度与半高格都不存在。 | audited-2026-09 | text-font-rebuild-cascade msgwin-attr-font-opcodes:noop |
| 155 | `0x7c` | missing-branch | P2 | 引擎**无条件**做深度校验：`帧[95796] != _this[430712]` 即抛「Depth が不正です」；emulator 只在 `redisplayScriptId != -1` 时校验，而该键在非 0x199 路径（读档/装载）下从不被写入（引擎侧同格的初值是 0，raw 17984/22605 的复位写 0）⇒ 读档后落到 local-ret 时，引擎会抛、emulator 静默放行。 | tracked-done | T-0076(done) T-0081(done) op-7-0x20b-doc-semantics-but-unimplemented op7-0x7c-local-ret-unimplemented |
| 156 | `0x7c` | missing-operand-io | P3 | 引擎在 0x7C 里清四格状态：`81776=-1 / 81768=0 / 51848=-1 / 51840=0`；其中 `51848` 在引擎侧**有真实读者**（raw 20005 `(v11 = *(_DWORD *)(_this + 51848), v11 >= 0) && *(_DWORD *)(_this + 23008) > v11` 与 raw 20286），emulator 四格全不建模 ⇒ 「重显示返回后把选择/列表框游标复位」这一步在重写侧不存在。 | tracked-done | T-0076(done) op-7-0x20b-doc-semantics-but-unimplemented op7-0x7c-local-ret-unimplemented |
| 157 | `0x7c` | missing-consumer | P3 | 引擎在 `_this[387940]` 置位时清 0，并在队列恰剩 1 项（`497380 < 497384 && 497384-497380 == 1`）时调 `sub_40FB60` 派发脚本队列；emulator 既不写 387940 也没有该派发点。 | tracked-done | T-0076(done) op-7-0x20b-doc-semantics-but-unimplemented op7-0x7c-local-ret-unimplemented |
| 158 | `0x82` | missing-branch | P2 | `op3 & 0x40`（bit6）会把起始记录下标整体后移窗的 `+132`（行游标）：引擎 `if ( (a4 & 0x40) != 0 ) v155 = *(_DWORD *)(v44 + 132) + v45;`；emulator 的 `start` 只用 `op2` 原值过越界门，bit6 完全未建模 ⇒ 带 bit6 的调用会从错误的记录起重画（或在 emulator 侧被那道门判成越界而整条跳过，引擎却会重画）。 | tracked-done | T-0104(done) |
| 159 | `0x82` | missing-branch | P2 | `op3 & 0x30`（bit4/bit5）时引擎额外调 `sub_4ABB60(Engine[1040], win+276, win+280)`（即 `0x25D` 登记的 DrawItem 区间）；emulator 只做 `emitWin(op1)`，不碰 `itemRanges`（`0x213`/`0x25D` 写入的那两个字段从不被重画路径重新登记）。 | tracked-done | T-0104(done) |
| 160 | `0x82` | missing-branch | P3 | `op3 & 1`（bit0）参与记录过滤：引擎 `if ( (v49 & 2) != 0 && (a4 & 1) == 0 ) goto LABEL_188;`（该记录自带 bit1 且 op3 未置 bit0 ⇒ 跳过这条记录）；emulator 无记录级过滤，`emitWin` 整窗重排。 | tracked-done | T-0104(done) |
| 161 | `0x82` | missing-branch | P3 | `*(_DWORD *)(*(_DWORD *)(_this + 4 * a2 + 1044) + 112) == 1` 时引擎走**专用路径** `sub_462040` 并直接 `return`（不进后面的 GDI 重画与设色）；emulator 无这条路径，任何窗都走同一条 `emitWin`。 | tracked-done | T-0104(done) |
| 162 | `0x9` | missing-behavior | P2 | 引擎的整体复位 sub_40DF10 把 `_this + 388252` 起的 11 个 Queue_int 槽（= 0x132/0x133/0x134 的容器）析构并置 NULL；emulator 的 op_exit_script 清了 scriptRequests（另一条 Queue_int @0x796DC）却不动 `Engine.dispatchQueues`，于是退出脚本前压进队的值会跨 exit-script 存活（引擎侧已被销毁、只能先 0x132 重建）。 | audited-2026-09 | docs-new/03-engine/flow-control.md:150 op-7-0x305-flags-not-cleared |
| 163 | `0x9` | missing-behavior | P3 | 体首无条件虚调用 sub_419720：清 `_this[174801]` 的 0x200 位，并对音乐子系统调 sub_489E50(…,100)（按 100 推进淡出/停播）+ sub_489B50（复位播放游标）；emulator 的 exit-script teardown 里没有对应的音乐/输入状态复位项（emulator 不建模音频子系统，也不建模 _this[174801] 的该位）。 | audited-2026-09 | docs-new/03-engine/flow-control.md:150 op-7-0x305-flags-not-cleared |
| 164 | `0x91` | missing-consumer | P2 | `effect_flags \|= 0x800000`（消息面显示态）在 emulator 里没有消费者：引擎侧该位的唯一读取端 `sub_4098E0`（raw 14055-14105）未实现——它按 `[174801] & 0x800000` 门控，左键走 `sub_404120`（取游标项 labelC **并把整表清空**）、无 enter/leave 也无点击时派发 `[12961]` 回退 label（`sub_403DD0` 的方向键/翻页键移动游标也在这条链上）。emulator 置位后无人读它 ⇒ i091 型「显示消息面并等玩家点击」的面板点击/回退派发永不发生（`Engine.routes.commitClickAndReset`/`fallbackLabel` 在 src 产品路径里无调用者）。 | audited-2026-09 | op7-0x92-step0-not-modeled |
| 165 | `0x91` | missing-branch | P2 | 体的显示分支末尾把本帧长度槽写 0（raw 29540）：派发器 raw 20165 `_this[...95782] += 4 * _this[...95805]` ⇒ **ip 停在本条**（阻塞等待 sub_4098E0 派发改 ip，重跑本条时 `[12956]` 已被置位才走①前进）。emulator 的 handler 正常返回 ⇒ `interpreter.stepOnce` 默认 `curFrame.ip += 1` 直接执行下一条 —— 「面板显示 = 脚本挂起」这一语义整个丢失（可观测面还包括 StepTrace.operandCount：emulator 恒报 2*argc+1=3，引擎该分支里是 0）。 | audited-2026-09 | op7-0x92-step0-not-modeled |
| 166 | `0x92` | missing-branch | P2 | 显示分支末尾写长度槽 0（raw 29565，5 → 0）⇒ raw 20165 `ip += 4*0` 使 ip 停在本条（显示后挂起等 sub_4098E0 派发）；emulator 的 handler 正常返回、`interpreter.stepOnce` 默认 `curFrame.ip += 1` ⇒ 直接跑下一条，`i092` 的「显示 + 等回退 label」语义丢失。 | audited-2026-09 | op7-0x92-step0-not-modeled |
| 167 | `0x92` | missing-consumer | P2 | `[12961] = op2`（回退 label / 面板 `[7467]`）在 emulator 里是死写：`RoutePanel.fallbackLabel`（route.ts:124）只在 panel.ts:168 被写、在 route.ts:349 被清零，**没有任何读者**；引擎侧的读者是 `sub_4098E0`（raw 14084，0x800000 显示态的「无 enter/leave 也无点击」出口）⇒ 回退派发永不发生。 | audited-2026-09 | op7-0x92-step0-not-modeled |
| 168 | `0x94` | missing-branch | P2 | 引擎的鼠标移动命中测试被 `if ( _this[12958] \|\| _this[12957] )` 门控（12958 = 面板 `[7464]` 待填充、12957 = `[7463]` 已显示，两者都由 0x94/0x91/0x92 置位、由 0x93 的 `sub_403EF0([7464]=0)` 与 toggle 清零）；emulator 的 `input.onCursorMove` 无条件 `routes.hitTest` ⇒ 面板关闭期间（两格皆 0：如 i093 之后、i094 之前）鼠标移动时，引擎**不**重算游标、emulator 会重算，`sub_403E70` 的悬停比较基准随之不同。同时 emulator 的 `fillPending`（=12958）是**没有读者的死写**——引擎里它的读者正是这道门。 | audited-2026-09 | single-field-timers-audio-device docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/scene-start-flow.md:105 |
| 169 | `0x94` | approximation | P3 | `sub_404020` 的首次分支在引擎里**无条件**执行 `GetCursorPos`+`ScreenToClient`+`sub_403C50(panel, x, y)`（鼠标在窗口外也取全局坐标、命中失败即写 `[7468] = -1`）；emulator 的 `showPanel` 写成 `if (e.input?.hasCursor) panel.hitTest(...)` ⇒ 无光标（鼠标出窗/未初始化，`readX/readY` 此时返回 -100000）时**跳过**这次命中测试，`hitDone` 仍置 1、游标保持旧值。 | audited-2026-09 | single-field-timers-audio-device docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/scene-start-flow.md:105 |
| 170 | `0x97` | missing-consumer | P3 | 语料里 6 条 i097 有 2 条把 ADV 推进绑到**掩码位 7/8**（`src/SN0000.txt:106` bit 8、`:114` bit 7；334 个 ADV 脚本同型 ⇒ 全部 2004 处只用到 {0,1,2,3,8,7}）。引擎里 bit(4+i) 是**按钮位**（`sub_4772E0` 的摇杆按钮循环 raw 91758-91759 `*a3 \|= 1 << (result + 4);`，按钮序号 i ⇒ 位 4+i；即 bit 7/8 = 按钮 3/4），而 emulator 的掩码组成只有键盘 0..6（input.ts:421 `m \|= this.keyEdge & 0x7f;`）+ 鼠标 bit4/5 + `joyEdge` 的 bit(4+i)。bit 7/8 的唯一产生者是 `InputManager.pressJoy`，但它在**产品路径上没有任何调用者**（全仓 grep 只在 test/ 命中：test/op-a5.test.ts:151、test/input.test.ts:239/:245、test/keyboard-mask.test.ts:121；渲染器/DOM 桥/调试 API 都不注入手柄）⇒ 这两条绑定永不命中 `pickByKey`。 | tracked-done | T-0052(done) single-field-timers-audio-device docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/scene-start-flow.md:105 |
| 171 | `0xa0` | approximation | P3 | emulator 要求真/假分支目标命中 labelMap（否则抛 `jcc: unknown true/false label`）；引擎对两个目标都不校验、直接以 `base + 4*目标` 写 ip。 | tracked-open | T-0145(open) live2d-enabled-config-flag |
| 172 | `0xa2` | missing-operand-io | P3 | op1 的**字符串型键**没被建模：引擎的键读法 `sub_41B640(_this, 1)` 对字符串族操作数直接返回池里的字符串（case 5/11 返回 `_this[95748]`/`[95791]` 槽、case 8/14 返回字符串指针所指、case 2 走立即串池）并原样当键；emulator 的操作数计划把 op1 声明为 `int`，`plan.int(1)` 对字符串型落到 `readIntOperand` 的 `atoi`/空串退路 ⇒ 键变成内容错误的十进制串（字符串键会与引擎的键空间不同），这类脚本的 `0xA3` 查表必然落到回退 label。附带：op1 声明成 `int` 后浮点型键被计划层直接拒绝（`0xa2 的第 1 位操作数计划声明为 int`），引擎的 `%lf` 分支同样拿不到。 | audited-2026-09 | docs-new/03-engine/input-system.md:282 |
| 173 | `0xa3` | missing-operand-io | P3 | op1 的**字符串型键**未建模：引擎 `sub_41B640(_this, 1)` 对字符串族操作数返回池里的原串当键，emulator 的操作数计划把 op1 声明为 `int` ⇒ `plan.int(1)` 对字符串型走 `atoi`/空串退路，键与引擎不同 ⇒ 用字符串键登记过的项永远查不中；浮点型键更直接被计划层拒绝（抛 `0xa3 的第 1 位操作数计划声明为 int`）。 | audited-2026-09 | docs-new/03-engine/input-system.md:282 |
| 174 | `0xa3` | missing-branch | P3 | 『表里存的值不在本帧 labelMap ⇒ 只顺序推进』这条 emulator 自加的静默分支：引擎命中后**无条件**执行 `ip = ip_base + 4*表值`（不校验目标是否合法），emulator 在 `labelPos(...) === null` 时 `return` 掉、控制流落回顺序执行 ⇒ 一旦某个 label 因帧重载/跨脚本（`0xa2` 只受 `0xa1` 复位）而不在本帧映射里，引擎与 emulator 会走到**不同**的指令上，且 emulator 侧没有任何诊断。 | audited-2026-09 | docs-new/03-engine/input-system.md:282 |
| 175 | `0xae` | missing-branch | P2 | 引擎在 sv1=1 分支里要求第二次 GetConfig 的结果**恰为 20**（`if (v3 != 1)` 之外的 `if (result == 20)`），并使用 `263*cur` 步长的槽组（`130199`/`130198`、savedCur/savedRet = `129624`/`129625`）；emulator 在「本工程槽记录带 instr」时用 `direct` 短路**整个版本选组**（`if (!direct && (!spec \|\| ...)) return;`），于是 sv2 的值完全不参与判据 ⇒ 若某份槽声明 sv1=1 而 sv2≠20，引擎按「不进这一支」处理，emulator 仍按 instr 直落。 | tracked-done | T-0059(done) T-0065(done) op7-0x7c-local-ret-unimplemented |
| 176 | `0xae` | missing-operand-io | P3 | 引擎版的 sv1/sv2 来源**只有**注册表两次调用（`(*(...)(_this[174405] + 4))(_this + 174405, aSetSaveversion)` / `aSetSaveversion_0`），全文件没有「把版本从存档容器头取出来当优先源」的读法；emulator 却把 `resume.sv1/sv2`（读档时从容器头 +284/+288 带进来）当**第一优先**（`resume?.sv1 ?? cfgInt(...)`）⇒ 当容器头声明的版本与注册表当前值不同时，两者选的槽位组不同。 | tracked-done | T-0065(done) op7-0x7c-local-ret-unimplemented |
| 177 | `0xae` | missing-operand-io | P3 | 引擎 `sub_40F750(_this, sv1', sv2)` 在装载下一帧时会**按槽位组还原调用返回栈**：`_this[_this[95776] + 97153] = _this[261*cur + 156525]` 并把 `261*cur+156526+j` 的每个项 `+3` 后写进 `_this[256*cur + 97193 + i]`（即「返回地址 = 记录值 + 3 dword」，与 0xAE 的 `95805 = 3` 步长同口径）；emulator 走 `resolveSlotRetStack` 自算返回栈（`nextRec.retStack` 或表 C 下标换算），不建模 `97153`/`97193` 这两组字段。 | tracked-done | T-0059(done) T-0063(done) op7-0x7c-local-ret-unimplemented |
| 178 | `0xb4` | missing-consumer | P3 | 引擎装载 SE 时经 `sub_4559C0` → `sub_454960` 把该 WAV 的统一文件 id 写进 FileDB「已使用文件」表（`0x19D` 查询的表），emulator 的 `0xB4` 路径不调 `Engine.markFileUsed` ⇒ 用 `0x19D` 统计/解锁的脚本少算这个 id。 | audited-2026-09 | audio-module-topology-and-volume-routing:raw |
| 179 | `0xb4` | missing-branch | P2 | 引擎的 SE 通道上限是 **0..14**（`if ( a2 > 0xE )` 才报错误串），emulator 的宿主只建 10 条 SE 通道（`SE_CHANNELS = 10`），通道 10..14 的 `se-load`/`se-play` 会被 `#seChannel` 判越界直接丢弃 ⇒ 引擎能出声的通道在 emulator 静音。 | audited-2026-09 | audio-module-topology-and-volume-routing:raw |
| 180 | `0xb5` | missing-branch | P2 | 同 0xB4：引擎 `sub_4B6020` 的通道值域判定是 `a2 > 0xE`（0..14 全合法），emulator 宿主只有 10 条 SE 通道 ⇒ 0xB5/0xBA 对通道 10..14 静默丢弃（引擎此时会正常起播）。 | audited-2026-09 | docs-new/03-engine/input-system.md:282 |
| 181 | `0xbc` | missing-branch | P1 | 缺 `sub_408CF0` 的「无需改动 ⇒ 整条早退」分支（raw 13523-13541 的 `if (a2) {...} else if (v3 >= 0) {...}` 结构）：引擎在 `a2 != 0 && sound:Music >= 0`（或 `a2 == 0 && sound:Music < 0`）时不做任何事，emulator 仍发 `bgm-stop` ⇒ 正在播的 BGM 被**停一次**（若卸载了 bgm-play），且宿主 `bgmMode(0)` 还会 `#stopBgm()`。玩家的音频开关（`0x1BA` 音乐分支与 `0xBC` 同段体）会踩到。 | tracked-done | T-0057(done) op-6-07 |
| 182 | `0xbf` | missing-behavior | P2 | 体开头对语音状态位 `Engine[21315]`/`Engine[21318]` 的翻转（有 0x10000 ⇒ 清 0；否则有 bit0 ⇒ 置 0x10000）在 emulator 完全没有：宿主侧只有 `voice-flag`/`voice-factor-*` 意图，没有把这两格（以及 0x2F7 的五参形式）建模成状态机，所以「一次性的 0x10000 已被消费 ⇒ 下一次播语音时清 0」的语义丢失。 | audited-2026-09 | op-1/0xBF-config-key-name |
| 183 | `0xbf` | missing-branch | P3 | `_this[122503]`（跳读态）这条判据在 emulator 里永远为假 —— 字段 `ENGINE_FIELD.skipReadActive`（= 122503）在整个 `app/amayui-emulator/src/` 里**从无写入点**，于是「跳读中不给语音让路」这条抑制分支不可达（引擎在跳读时 `fadeOnVoice` 不生效）。 | audited-2026-09 | op-1/0xBF-config-key-name |
| 184 | `0xc3` | stale-ledger | P3 | 实现注释与操作数计划的证据串把这条写成「`Music[259] = op1`」（附 `Engine[174713] = op1` 作为另一个动作），而体里**只有** `_this[174713] = result` 一条写；两处口径并存会被后人读成「引擎还写了 `Music[259]` 之外的字段」。 | audited-2026-09 | docs-new/03-engine/instruction-directions.md:102 |
| 185 | `0xc4` | missing-behavior | P1 | 体开头对 `_this[21315]`/`_this[21318]` 的翻转（0x10000 已置 ⇒ 清 0；否则有 bit0 ⇒ 置 0x10000）没做：宿主侧 `voice-flag`/`voice-factor-prepare` 是**只增不减**的状态（`audioEngine.ts` 的 `v.flag = 1` / `v.preparedFactor = ...`），配合 `0x2F7` 的五参形式（raw 33620-33637）缺失，语音通道状态位永远停在「已置位」。 | audited-2026-09 | docs-new/03-engine/sound-system.md#§9 |
| 186 | `0xc4` | missing-behavior | P2 | 末尾 `if (_this[21293]) _this[122501] = 1;` 没有对应物 —— `sound:Voice` 开关（`Engine[21293]`，boot raw 23693-23695）打开时，每次播语音都要把 `Engine[122501]` 记号置 1；emulator 既没有这个字段也没有这步写（`ENGINE_FIELD` 里查不到 122501）。 | audited-2026-09 | docs-new/03-engine/sound-system.md#§9 |
| 187 | `0xc5` | missing-branch | P3 | 越界 selector（≥5 或负）时引擎把 `GetVolumeの引数が不正です．` 连同 `(%s：%d行目)` 文件/行头经 `sub_4034D0`→`sub_4976A0`→`sub_497620` 报给宿主；emulator 直接在 `if (key === undefined) return;` 静默返回，既不报错也不留任何诊断——而同一份代码的注释自称「'skip' 只记不写」，实际并没有「记」。 | tracked-done | T-0082(done) |
| 188 | `0xc8` | approximation | P2 | 引擎把 `sleep n` 分成两条语义不同的路径：`n < 10` ⇒ 一次性 `Sleep(n)` 毫秒；`n >= 10` ⇒ 置 `effect_flags \|= 1` 并给定时器对象 `_this+107440` 设「帧间隔 = n」，实际等待由主循环的 `sub_453AF0`（`剩余 = 间隔*帧数 - 已过毫秒`，`>0` 则 `Sleep(剩余)`）逐帧摊还。emulator 不做区分，一律 `sleepUntil = nowMs + max(1, n)` 后由渲染帧循环一次性放行 ⇒ `n>=10` 时「按帧节流」与「按毫秒阻塞」不等价（引擎会按帧计数重排时间基准 `_this[5]=timeGetTime()`）。 | tracked-done | T-0082(done) |
| 189 | `0xd9` | missing-branch | P2 | 引擎 `if ( _this[124350] )` 的门在 emulator 读的是 `engineValues.get(ENGINE_FIELD.dispatchInProgress)`，而键 124350 全仓**无写点**（派发中状态由 `Engine.dispatching: boolean` 承载）⇒ 门恒为 0，`dispatchSavedFlags`（95779）的 0x1000 位永不被 0xD9 清；引擎在派发中（raw 18980 `*(_DWORD *)(_this + 497400) = 1;` / raw 25176 `_this[124350] = 1;`）时清。 | tracked-done | T-0079(done) single-field-timers-audio-device docs-new/03-engine/scene-start-flow.md:102 docs-new/03-engine/scene-start-flow.md:96 docs-new/03-engine/scene-start-flow.md:105 |
| 190 | `0xfa` | missing-consumer | P1 | `0xFA` 在「非跳读」时会把 `Engine[122505 + 3*i]` 的 **3 个待播语音槽**逐个交给 `sub_4BB840(Engine+21032, i, 触发的字符串 id, 音量/延迟参数, 该槽另两格)` 消费并清槽（`*v4 = 0`）；emulator 的 `op_poll_msg_advance` 完全不碰这 3 个槽，也没有对应的语音入队/播放通路 ⇒ 在真实路径上「ADV 退出时该补播的语音」在 emulator 里静默丢失。 | audited-2026-09 | adv-advance-opcodes adv-flag-lifecycle adv-input-pump-perframe:reads |
| 191 | `0xfa` | approximation | P2 | 引擎 `sub_478090(_this + 258, _this + 174802)` 之后**显式** `_this[174802] = 0`（把刚被消费刷吸取的那一格掩码清零）；emulator 只调 `consumeEdges()`（清鼠标/手把/键盘的**沿**），不写 `inputMask` 那一格 ⇒ 若下游有「读 `174802` 掩码格」的消费者，会看到与引擎不同的值。 | audited-2026-09 | adv-advance-opcodes adv-flag-lifecycle adv-input-pump-perframe:reads |
| 192 | `0xff` | missing-operand-io | P3 | 0xFF 未写 `Engine[cur+122327] = Engine[517]`（按键跳读的后备扫描游标镜像）—— 该格在 `sub_419D20` 里被读（raw 25096 `v2 = _this[_this[95776] + 122327] - 1;`）、并按 -1 判定续扫；emulator 全库没有对应字段，故这条写在重写侧无处落地。 | audited-2026-09 | docs-new/03-engine/rendering.md:112 op-4-06 op9-op255-input-reset-second-half-unmodeled |
| 193 | `cap:audio-device-init` | overreach | P2 | 条目 `engine.raw=none` 位置的引用 `raw 21426 → sub_4B69B0(Engine+18664)` 被标为「引擎 ctor」，实际是 **Engine 析构函数**里对 Sound 子对象的销毁（`sub_4B69B0` 是标量删除析构 `sub_4B6A30` 的实现，体里 `*_this = (HMODULE)&Sound___vftable_` + 15 次 `DeleteCriticalSection`）；初始化的真身是 `sub_4B5CF0`（raw 138415，`DirectSoundCreate`）与 `sub_4B6A60`（raw 139123，建 10 路 SoundBuffer），条目把它们全漏了，反而把 `sub_4B6940`（set-volume）列进 fns。 | tracked-done | T-0075(done) |
| 194 | `cap:bullet-dirty-from-freeze-or-pending` | approximation | P2 | 引擎的置脏无条件（只看 `46512\|46516` 非零），emulator 的等价物是「freeze 且存在可冻结项」才置脏：freeze 置位但场景里没有可冻结窗时 emulator 不置脏。 | tracked-open | T-0091(doing) T-0024(done) |
| 195 | `cap:bullet-dirty-from-freeze-or-pending` | missing-consumer | P1 | `Scene+46516`（pending）在 emulator 只被写进 `Engine.scenePending` 并只喂 0x400 等待门；引擎里它经 `(46512\|46516) ⇒ 46508 = 1` 强制本帧重画，而 `sceneNeedsRender` 完全不看它。 | tracked-done | T-0024(done) |
| 196 | `cap:clock-write-clock-freeze` | missing-behavior | P1 | 引擎主循环的时钟写入被 `Engine+667856 == 1 && (667860 != 0 \|\| effect_flags & 0x2400)` 门着，emulator 的驱动每帧无条件把 `host.now()` 写进 `e.nowMs`（并注入宿主），门的缺失使「非绘制帧时钟不动」这一引擎行为不存在。 | tracked-done | T-0078(done) |
| 197 | `cap:frame-pump-input-refresh` | stale-ledger | P1 | `frame-pump-input-refresh` 整条挂错子系统：它引的 `sub_4BBAB0` 是 DirectSound 语音（VOICE）通道的每帧泵（判忙 → 需装则读文件起播），而台账写成「每帧输入态刷新」、subsystem=输入、guard=test/input.test.ts；真正的输入态刷新在 `sub_478090`（消费刷）/`sub_4780D0`（实时刷）内调 `sub_4770A0`/`sub_477150`（raw 91551-91570 / 91592+），已由另一条 `input-keyboard-to-mask-bits`（analysis/engine-capabilities.json:4023-4047，guard=test/keyboard-mask.test.ts）覆盖 ⇒ 这一条是**误分类的重复条目 + 张冠李戴的守卫**。 | tracked-done | T-0124(done) |
| 198 | `cap:gfx-prim-mesh-and-render-state` | missing-consumer | P1 | A4 记录型的几条（0x1FC/0x1FE 图元变换、0x321 网格属性、0x32D 3D 颜色）写进 render4 后没有任何渲染消费者：presenter 的 drawMesh 只看 m.flags/m.verts，不看 meshAttrs/color3D；primTransform 也不参与 drawItem 的几何。 | audited-2026-09 | scene-3d-weather-effects-rain-snow-leaf |
| 199 | `cap:gfx-prim-mesh-and-render-state` | overreach | P3 | reads 漏列本族里唯二被建模的字段：0x238 写的 Engine[92338]/[92339] 与 0x243/0x24E 用的 Engine[92340]（即 Scene+46520/46524/46528），条目 reads 里只有 Scene 侧的变换/槽表/转场容器等。 | audited-2026-09 | scene-3d-weather-effects-rain-snow-leaf |
| 200 | `cap:lazy-transition-map-node` | stale-ledger | P1 | `lazy-transition-map-node` 的 `emulator.status="absent"`、`evidence="E0"`、`guard=""`、note「转场表未建模」全部过期：转场记录表 = `SceneState.render4.transitions`（Map），运行期惰性记录 = `render4.transitionRuntime`（`scTransitionTick` 首见即建并锁存起点），清表 = `scTransitionTick` 在「无在途 + 池挂起为假」时 `clear()` 两张表；写端逐格照抄引擎 `sub_4AAE10(Scene+1048, &id)[i]`。 | tracked-done | T-0124(done) |
| 201 | `cap:live2d-enabled-config-flag` | stale-ledger | P2 | `live2d-enabled-config-flag` 的 `emulator.evidence` 仍是 `E1`、`guard` 仍是空串，而 `test/live2d-enabled-flag.test.ts` 用真 boot 链 + 真资产断言了 `a9d0` 两条支路（含静态回落图 0x5273）⇒ 台账漏记一个**当真**的 E3 级守卫，该条实际不是「无守卫」。 | tracked-done | T-0124(done) T-0124(done) |
| 202 | `cap:live2d-enabled-config-flag` | overreach | P3 | 条目 whySilent 写「引擎侧没有 L2D 初始化断言 ⇒ 缺 L2D 时不会被当成故障」，但被它引作证据的入口 `sub_427BA0` 的失败支正是**抛 C++ 异常**（`L2Dモデルファイル %s の読み込みに失敗しました` + `_CxxThrowException`）⇒「装载失败不会被当成故障」在体里不成立；只有「开关关掉 / 不执行装载指令」才是静默的。 | tracked-done | T-0124(done) |
| 203 | `cap:live2d-node-draw-advance` | stale-ledger | P2 | note 的「**缺口**：宿主合成循环还没把『绘制节点』与 l2dAdvance 接起来」与代码相反：两个宿主都已在 `advanceModel` 里调 `scL2dTick`（它按 `l2dNodeDrawable` 过滤出本帧要画的节点再 `l2dAdvance`），并把结果键交给 presenter 出画。 | tracked-done | T-0054(done) |
| 204 | `cap:msgwin-backlog-cursor` | stale-ledger | P1 | 台账写「缺口：无回看页表与光标」，实际 TextItemTable 的 pages/cursor/baseCursor 都已建、已接线并被 T-0095 守卫钉住 —— 只剩消费者（滚轮）与 0x84 未接。 | tracked-done | T-0095(done) |
| 205 | `cap:msgwin-backlog-cursor` | missing-consumer | P1 | `TextItemTable.moveCursor`（逐行复刻 sub_459770）没有任何调用点，滚轮位 0x8/0x2 与 `effectFlags \|= 0x100000` 的写入者都不存在 ⇒ 回看功能整体不可达。 | tracked-done | T-0095(done) |
| 206 | `cap:passive-camera-and-effect-render-state` | stale-ledger | P2 | 台账称本条 emulator「没有等价物（absent/E1）」，但 `sub_4535F0` 的分支字段 `Scene+1260`（= 它读取的渲染模式）已由 `sceneDefaultBlend` 建模并被混合状态机消费且有守卫。 | tracked-done | T-0041(done) T-0017(done) |
| 207 | `cap:passive-camera-and-effect-render-state` | missing-behavior | P1 | 每帧的效果推进 `sub_453540`（按墙钟、上限 100 次、对三路效果对象各推进一次）在 emulator 完全没有：PixiBackend 每帧 present 不推进任何效果子系统。 | tracked-open | T-0111(doing) |
| 208 | `cap:render-3d-layer-dual-commit` | stale-ledger | P2 | `trigger` 说『其它子系统直接调用（本文件未见主循环调用点）』，但体里唯一调用点就是 0x222 handler sub_423EC0（raw 31933），即**脚本 i222 驱动**的一次区间提交；name『对偶逐帧提交』随之误导（逐帧提交是 sub_4B4040）。note 已订正此事，trigger/name 未同步。 | tracked-done | T-0076(done) |
| 209 | `cap:render-3d-layer-dual-commit` | missing-behavior | P1 | 0x222 的体是一次**完整提交 + 三表归并**：门 `Scene+46460 & 2` 与 `sub_497F50(Scene+1860)`；清 46508/46516；`sub_49DFD0` → `sub_4A1E90`（世界矩阵复位）→ `sub_4B06D0`（2D 绘制循环，内含转场两趟 36/37 与天气推进 136828-136829）；随后按 key 归并 Scene+1036/1068/1100 三张表，逐节点 `sub_4535F0(Scene+50704, key)` + `sub_453540` 推进 3D 天气/粒子管理器，并按 bit0 调 `sub_4B4020`/`sub_4AF1C0`/`sub_4B0360`（后者含 `j_D3DXMatrixOrthoLH`），收尾 `sub_49E170`/`sub_4A9BE0`。emulator 无 0x222 注册 ⇒ 语料 10 处命中即 NotImplementedOp；而若照同族 0x324-0x32E 的做法登记成 no-op，则会静默丢掉整条链。 | tracked-done | T-0076(done) |
| 210 | `cap:scene-3d-weather-effects-rain-snow-leaf` | missing-behavior | P1 | 整套 3D 天气/粒子效果子系统没有建模：管理器（Scene+50704，0x4F4 字节）、三效果槽、每帧推进（sub_4535F0(管理器,-1) + sub_453540，136828-136829）、逐节点推进、销毁判据 [310]/[311]/[312]、以及 0x326 的共享 ID3DXEffect(资源 202) 懒建与 `Scene+46668>=1` 门 —— emulator 只有 no-op 登记（stubs.ts:107/125/156-159/212），没有任何消费者。 | tracked-done | T-0076(done) T-0093(done) |
| 211 | `cap:scene-flag-46528-bits` | stale-ledger | P1 | 台账 `absent`/E0「冻结豁免位随 46512 一起未建模」为假：`Scene+46528` bit2 的判据已建模进冻结路径（`entryParam & 1` 豁免）且有守卫。 | tracked-open | T-0091(doing) T-0078(done) |
| 212 | `cap:scene-flag-46528-bits` | overreach | P2 | trigger 里 bit2 的极性写反：体是「bit2 == 0 才清零冻结值」（即 bit2 置位时忽略冻结），条目写成「bit2（=0 时）⇒ sub_49AA30 不提前收尾动画窗」。 | tracked-done | T-0078(done) |
| 213 | `cap:script-queue-dispatch` | stale-ledger | P1 | 台账称引擎脚本派发队列「未建模、0x143 是 no-op」，实际 `scriptRequests` + `dispatchNextRequest` + `op_dispatch_script_requests` 已逐条对齐 raw 18954-19016 并带守卫。 | tracked-open | T-0078(done) T-0111(doing) |
| 214 | `cap:script-queue-dispatch` | missing-behavior | P1 | `0x1F5` 计到 0 时的帧驱动出队（`if (!dispatch_in_progress) sub_40FB60(_this)`）未接线：emulator 的 op_frame_countdown 只清停靠锁，队列不会在帧循环里被消费。 | tracked-open | T-0057(done) T-0111(doing) |
| 215 | `cap:text-aa-config-gate` | overreach | P2 | note 断言字段 21662 在渲染侧没有消费者且“全库唯一出现点就是它的定义”—— 结论（无渲染读者）成立，但“唯一出现点”不成立：至少 :285 是写入点、:164-165 是定义点，AA-off 分支因此不是“不存在”而是“不可达”。 | tracked-done | T-0035(done) |
| 216 | `cap:text-blank-extent-mode-gate` | stale-ledger | P3 | trigger 里“先取 font_metrics_mode ? 字号 : -lfHeight”把字段名写错：体里是 `Engine[71745]`（度量模式字体档值），不是字号 `Engine[201684]`/`Font+201684`；按台账照抄会把 0x205 的 cy 口径接到字号字段上。 | tracked-done | T-0085(done) |
| 217 | `cap:text-white-level-on-composite` | stale-ledger | P1 | 台账仍写“机制未定位 / emulator 按 TEXT_WHITE_LEVEL = 0.89 实测值对齐”，而代码已改成覆盖率合成（TEXT_FILL_ALPHA = 225/255，渲染侧 globalAlpha），相关常量与三处 #e3e3e3 断言都已删除。 | tracked-done | T-0042(done) T-0042(done) |
| 218 | `cap:text-white-level-on-composite` | stale-ledger | P2 | guard 与 note 的代表性断言已迁走：note 说“三处都钉住 0xFFFFFF → #e3e3e3”，实际三处文件里的断言现在是“样式保留 #ffffff、压暗在 raster 的 α 合成”，0xFFFFFF → #e3e3e3 这条期望值在全仓零命中。 | tracked-done | T-0042(done) |
| 219 | `cap:texture-bind-synchronous-then-query` | missing-behavior | P1 | 0x249 之后没有纹理帧屏障：`#awaitTextureBound` 内写明的 0x249 分支永远不可达，因为唯一调用点被 `if (t.opcode === 0x1f9)` 卡住 ⇒ 紧随 0x249 的 0x208 仍会读到 0×0。 | tracked-done | T-0102(done) |

## 聚类（用于开票分组）

### 按 emulator 文件（finding 锚点所在文件）

- `src/vm/handlers/msgwin.ts`：68 条（P0 0 / P1 8 / P2 21 / P3 39）
- `src/vm/handlers/audio.ts`：23 条（P0 0 / P1 3 / P2 7 / P3 13）
- `src/vm/handlers/control.ts`：21 条（P0 0 / P1 1 / P2 2 / P3 18）
- `src/renderer/scene/ops.ts`：21 条（P0 0 / P1 2 / P2 9 / P3 10）
- `src/vm/handlers/gfx-texture.ts`：16 条（P0 0 / P1 0 / P2 10 / P3 6）
- `src/audio/audioEngine.ts`：15 条（P0 0 / P1 3 / P2 4 / P3 8）
- `src/vm/handlers/save-slot.ts`：11 条（P0 0 / P1 0 / P2 6 / P3 5）
- `src/vm/handlers/engine-fields.ts`：11 条（P0 0 / P1 1 / P2 4 / P3 6）
- `src/vm/handlers/frame.ts`：10 条（P0 0 / P1 2 / P2 3 / P3 5）
- `src/renderer/pixi/presenter.ts`：10 条（P0 0 / P1 2 / P2 4 / P3 4）
- `src/vm/handlers/stubs.ts`：10 条（P0 0 / P1 3 / P2 2 / P3 5）
- `src/vm/handlers/gfx-misc.ts`：9 条（P0 0 / P1 0 / P2 2 / P3 7）
- `src/vm/handlers/gfx-state.ts`：9 条（P0 0 / P1 0 / P2 4 / P3 5）
- `app/amayui-emulator/src/vm/handlers/stubs.ts`：9 条（P0 0 / P1 0 / P2 2 / P3 7）
- `src/live2d/runtime.ts`：8 条（P0 0 / P1 0 / P2 6 / P3 2）
- `src/renderer/pixi/textureCache.ts`：7 条（P0 0 / P1 0 / P2 1 / P3 6）
- `src/vm/engine.ts`：7 条（P0 0 / P1 2 / P2 4 / P3 1）
- `src/vm/handlers/agerc.ts`：6 条（P0 0 / P1 1 / P2 3 / P3 2）
- `src/vm/handlers/input.ts`：6 条（P0 0 / P1 1 / P2 3 / P3 2）
- `src/vm/input.ts`：5 条（P0 0 / P1 0 / P2 1 / P3 4）
- `src/frame/loop.ts`：5 条（P0 0 / P1 4 / P2 1 / P3 0）
- `src/vm/handlers/config-read.ts`：4 条（P0 0 / P1 1 / P2 1 / P3 2）
- `src/vm/handlers/arithmetic.ts`：4 条（P0 0 / P1 0 / P2 1 / P3 3）
- `src/renderer/drawitem/eval.ts`：4 条（P0 0 / P1 0 / P2 2 / P3 2）
- `src/renderer/scene/transition.ts`：4 条（P0 0 / P1 1 / P2 1 / P3 2）
- `src/renderer/pixiBackend.ts`：3 条（P0 0 / P1 0 / P2 3 / P3 0）
- `src/vm/operand.ts`：3 条（P0 0 / P1 0 / P2 2 / P3 1）
- `src/vm/handlers/memory.ts`：3 条（P0 1 / P1 0 / P2 0 / P3 2）
- `src/vm/handlers/menu.ts`：3 条（P0 0 / P1 0 / P2 0 / P3 3）
- `src/vm/handlers/panel.ts`：3 条（P0 0 / P1 0 / P2 2 / P3 1）
- `src/live2d/assetLoader.ts`：3 条（P0 0 / P1 0 / P2 3 / P3 0）
- `src/text/layout.ts`：3 条（P0 0 / P1 2 / P2 0 / P3 1）
- `src/vm/textItems.ts`：3 条（P0 0 / P1 2 / P2 0 / P3 1）
- `src/renderer/scene/state.ts`：3 条（P0 0 / P1 1 / P2 1 / P3 1）
- `src/text/fontSet.ts`：3 条（P0 0 / P1 1 / P2 1 / P3 1）
- `src/vm/msgwin.ts`：3 条（P0 0 / P1 0 / P2 2 / P3 1）
- `src/vm/native.ts`：2 条（P0 0 / P1 0 / P2 0 / P3 2）
- `src/vm/interpreter.ts`：2 条（P0 0 / P1 0 / P2 2 / P3 0）
- `src/vm/handlers/region-hittest.ts`：2 条（P0 0 / P1 0 / P2 0 / P3 2）
- `src/vm/handlers/resource-usage.ts`：2 条（P0 0 / P1 0 / P2 1 / P3 1）
- `src/text/sjis.ts`：2 条（P0 0 / P1 0 / P2 0 / P3 2）
- `src/vm/engineFieldIds.ts`：2 条（P0 0 / P1 1 / P2 0 / P3 1）
- `src/vm/handlers/gfx-item.ts`：2 条（P0 0 / P1 0 / P2 0 / P3 2）
- `src/renderer/drawitem/model.ts`：2 条（P0 0 / P1 0 / P2 1 / P3 1）
- `src/vm/handlers/live2d.ts`：2 条（P0 0 / P1 0 / P2 0 / P3 2）
- `src/live2d/mtn.ts`：2 条（P0 0 / P1 0 / P2 2 / P3 0）
- `src/vm/handlers/text-items.ts`：2 条（P0 0 / P1 0 / P2 1 / P3 1）
- `src/renderer/scene/blend.ts`：2 条（P0 0 / P1 0 / P2 2 / P3 0）
- `src/engineConfig.ts`：2 条（P0 0 / P1 1 / P2 1 / P3 0）
- `src/arch/nodeFileSource.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/vm/handlers/strings.ts`：1 条（P0 0 / P1 1 / P2 0 / P3 0）
- `src/vm/handlers/gfx-cg.ts`：1 条（P0 0 / P1 0 / P2 0 / P3 1）
- `test/opcode-gaps.test.ts`：1 条（P0 0 / P1 1 / P2 0 / P3 0）
- `test/mesh-vertex-quad.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `test/op-327-32e-setweather-noop.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `test/live2d-enabled-flag.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/renderer/headlessScene.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/renderer/audio/webAudioHost.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/configRegistry.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `test/adv-msgwin.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/vm/route.ts`：1 条（P0 0 / P1 0 / P2 0 / P3 1）
- `test/text-style-snapshot.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `test/render-draw-order.test.ts`：1 条（P0 0 / P1 0 / P2 0 / P3 1）
- `src/renderer/scene/snapshot.ts`：1 条（P0 0 / P1 1 / P2 0 / P3 0）
- `src/vm/engineSlot.ts`：1 条（P0 0 / P1 1 / P2 0 / P3 0）
- `test/texture-slot-resolve.test.ts`：1 条（P0 0 / P1 0 / P2 0 / P3 1）
- `src/vm/handlers/index.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `test/xval.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）
- `src/renderer/app/session.ts`：1 条（P0 0 / P1 1 / P2 0 / P3 0）
- `test/texture-frame-barrier.test.ts`：1 条（P0 0 / P1 0 / P2 1 / P3 0）

### 按 kind × severity

| kind | P0 | P1 | P2 | P3 | 合计 |
|---|---|---|---|---|---|
| approximation | 0 | 0 | 17 | 40 | 57 |
| host-invented | 0 | 1 | 3 | 9 | 13 |
| missing-behavior | 0 | 16 | 35 | 19 | 70 |
| missing-branch | 0 | 4 | 28 | 42 | 74 |
| missing-consumer | 0 | 8 | 16 | 25 | 49 |
| missing-operand-io | 1 | 3 | 11 | 22 | 37 |
| noop-claim-unjustified | 0 | 0 | 0 | 1 | 1 |
| overreach | 0 | 0 | 11 | 22 | 33 |
| stale-ledger | 0 | 16 | 18 | 8 | 42 |
| unclear | 0 | 0 | 1 | 6 | 7 |

### 影响面：语料可见 vs 当前不可见

- 影响**可观测**（impact 未声明不可见）：195 条
- 当前**不可见**（语料 0 处 / 退化输入才触发）：188 条 —— 这些按"登记 + 守卫防回归"处置，不占修复优先级
