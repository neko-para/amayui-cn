---
kind: session
state: live
---
# 03-engine · 修复计划（2026-09，依据 `audit-2026-09.md`）

> 输入：`docs-new/99-records/2026-09-audit/audit-2026-09.md`（+ 三份明细）—— 246 条保留结论，其中 P0 6 / P1 22。
> 本文规定**先做什么、哪些必须重构、每批的出口判据**。**逐轮执行总账已移入 `analysis/journal.jsonl`**；
> 当前批次状态见 `docs-new/00-overview/status.md`（生成物）。

## 0. 三条原则

1. **只按引擎证据改**：每处改动在代码注释里给 raw 行号 + 体里真实分支；没有依据的"看起来能跑"的补丁一律不做。
2. **不静默跳过**：任何不实现/近似实现都必须进缺口台账（`T-0081`），CI 能查到。
3. **区分补丁与重构**：结构性错误不逐条打补丁（见 §1），否则"修一处、坏一处"（读档画面那条线已经实证过一次）。

## 1. 必须「整体重构」而非强行修复的四处

| 编号 | 现状（审计证据） | 为什么补丁不行 | 重构目标 | 票 |
|---|---|---|---|---|
| **RF-A 操作数读取计划** | 13 处 int/float 混排、读写顺序、多写/少写错（`0x34B` `0x203` `0x100` `0x1A0` `0x2FC` `0x1CE` `0x1F9` `0x2E7` `0x305` `0x20F` `0x348` …）；另有 37 行「已核对但无 raw 引用」、多处 argc 与体不符（`0x1E` 文档 8 / 体只读 7） | 每修一处都要重新推导一遍顺序，且没有任何机制阻止下一次犯同一个错；文档 argc 与实现各自为政 | per-opcode **操作数计划**（argc + 逐位类型 int/float/ptr/str + 读写方向）由共享层统一执行，handler 只消费 `c.args`；把引擎每条指令都写的 **arity 槽 `frame.state[95805] = argc`** 建模为帧字段，并用它自动核验文档 argc 与 handler 读位数 | `T-0082` |
| **RF-B 缺口治理** | `ENGINE_INTERNAL_OPS` 9 条里 4 条体里有真实效果（`0x244` 清动画窗 / `0x325` 写效果销毁判据 / `0x10C` 写每帧扫描的按键表 / `0x324` 释放效果对象）；同时 21 条「引擎有体、emulator 零注册 ⇒ 命中即 `NotImplementedOp`」没有任何机读清单 | 靠注释与人脑记；两种失败模式（静默 no-op / 硬停）CI 都发现不了——审计一次就翻出 30+ 条 | 新增缺口真源 `analysis/opcode-gaps.json`（= 语料用到的未注册指令 + 注册成 no-op 但体有实效的指令 + 每条处置），配生成 md 与守卫棘轮；**新增未登记缺口即红** | `T-0081` |
| **RF-C 呈现 / 帧保留** | `#holdFrames` 是自己发明的启发式（引擎是每帧 `ClearTarget` 后从模型重组：`sub_498B60` = `IDirect3DDevice9::Clear`，另有 6 处无门调用）；装载点 `clearDrawContainer()`+`clearMeshSlots()` 无引擎对应（`sub_410160` 27 个 callee 里没有任何清容器） | hold 与 clear 互相"救场"：清了菜单 → 背景变上一屏残留像素（用户实测），调参只会让症状搬家 | 按引擎语义重做 present：去掉 hold 启发式；装载点改为「重建存档声明的槽 + **保留**绘制项」，把"上一屏不再合成"交给引擎真实机制（输出显示态复位 / 脚本自己的 `i1f6`/`detach-texture`）；必要时为 仮想ディスプレイ 建最小模型 | `T-0083`（+ `T-0072`/`T-0074`） |
| **RF-D 台账/文档↔实现一致性** | capabilities 11 条 P0/P1 失真（**两侧都错**：把已接线写成缺口、把硬停缺口写成 n/a-known）；机制文档 9 条 P0/P1 陈旧（把已实现写成未建模、把有读者的字段写成死写） | 手工同步必然再漂移（本次审计就是证据） | 把审计判据固化成检查器：capabilities 每条 `emulator.guard` 必须存在且含断言、`status` 与注册表/代码自动比对；文档里的 emulator 现状段落由生成块或测试校验 | `T-0078` + `T-0079` |

## 2. 执行顺序与出口判据

| 批次 | 内容 | 出口判据 | 结果 |
|---|---|---|---|
| **B0** | `T-0081` RF-B：缺口真源 + 生成 md + 守卫棘轮 | 未登记缺口即红 | ✅ 完成 |
| **B1** | `T-0076` 的 P0 两条：`0x2E9`、`0x228` | 各有单测 + 语料锚点 | ✅ 完成 |
| **B2** | `T-0082` RF-A：操作数计划 + 操作数记数槽建模 + argc 自动核验 | 三条守卫（`opcode-operands`/`opcode-arity`/`operand-plan`）挡住口径漂移 | 🔜 **计划层已落地**（`src/vm/operandPlan.ts`：声明式 `OperandPlan` + 共享执行器 + `Frame.operandCount` 建模 + `test/operand-plan.test.ts` 三条自动核验）；**存量 handler 迁移进行中：12/574** ⇒ 票未结（判据 1 要求覆盖"已核对"全部行） |
| **B3** | `T-0076` 剩余零注册指令 | 语料用到的指令全部有处置 | ✅ 完成（未实现 **0**） |
| **B4** | `T-0077` 凭空/错读 13 处 + Live2D 节点族 | 对照审计逐条打勾 | ✅ 完成 |
| **B5** | `T-0083` RF-C + 读档画面链 | 读档后无上一屏残留；A/B 对照一致 | ✅ 完成 |
| **B6** | `T-0078`/`T-0079` RF-D：台账 + 机制文档同步 | 四份 `--validate` 绿 | ✅ 完成 |
| **B7** | P2/P3 清尾（218 条） | 按 影响可见行为 > 回写操作数 > 纯记账 分批 | 🔜 进行中（`T-0097`/`T-0075`） |

## 3. 不做的事

- 不为"看起来正常"新增启发式（尤其不再给 `#holdFrames` 打补丁）；
- 不把缺口塞进 `ENGINE_INTERNAL_OPS` 静默跳过（B0 之后这是 CI 拦得住的）；
- 不做与审计结论无关的重命名/重排；
- 不改真实游戏数据（base SAVE.DAT / AGF 等只读不改）。
