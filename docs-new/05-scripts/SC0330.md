# 脚本台账 · `SC0330`

> 由 `analysis/scripts.json` 生成（`node scripts/build-scripts.mjs`）—— **勿手改本文件**。

| 项 | 值 |
|---|---|
| 脚本 | `$1$SC0330.BIN`（真源 `src/$1$SC0330.txt`） |
| 状态 | 🟠 部分 |
| 是什么 | 剧情脚本（本篇章节）：大量角色立绘的变换/表情/位置调整 + 文本推进。 |
| 怎么进/出 | 由章节调度 call-script；本脚本只登记与『立绘变换惯用法』相关的片段。 |

## 结构（读了哪些段）

| 行区间 | 锚点（必须出现在该区间内） | 职责 |
|---|---|---|
| `2007-2017` | `i21a (global-int f8025)` | 立绘变换惯用法：i21a 读回描画位置 → i218 读回 pivot → 两者加**同一 Δ** → i219 / i217 写回 → i1ff 0 0 0 平移归零。★这说明 pivot 与描画位置**同处一个坐标空间**（不是「相对项的局部量」） |

## 关键槽 / 局部量

| 名字 | 含义 |
|---|---|
| `global f8025 / f8026` | 两个立绘绘制项的 handle |
| `global f803b..f803d` | 描画位置（i21a 读 / i219 写） |
| `global f803e..f8040` | pivot（i218 读 / i217 写） |

## 不变量（拿它做回归断言）

- 同一个立绘项：pivot 与描画位置同步平移同一 Δ ⇒ 视觉锚点不变

## 坑（踩过一次，别再踩）

- `0x218`（pivot getter）/ `0x21A`（描画位置 getter）在 emulator **未注册** ⇒ 命中即硬报错；剧情路径大量使用这个惯用法（全库 i217 有 3353 处）

## 缺口

- 0x218 / 0x21A 未实现（会阻塞剧情脚本路径）

## 相关

- 引擎常态能力：`drawitem-world-matrix-composition`（见 `docs-new/03-engine/engine-capabilities.md`）
- 函数结论：`0x4ACF20`（见 `analysis/functions.json`）
- 函数结论：`0x4ACEE0`（见 `analysis/functions.json`）
- 函数结论：`0x4AC5F0`（见 `analysis/functions.json`）
- 主题文档：`docs-new/03-engine/opcode-table.md`

## 证据与备注

- 证据：src/$1$SC0330.txt:2007-2017；pivot 语义另见台账 drawitem-world-matrix-composition 与 docs/07-pointer-operand-model.md
- 备注：只登记了这一处惯用法；剧情脚本的整体结构（章节调度/演出）未读。
