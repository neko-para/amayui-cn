---
kind: generated
state: live
home: analysis/scripts.json
generated_by: scripts/build-scripts.mjs
---

# 脚本台账 · `SC0000`

> 由 `analysis/scripts.json` 生成（`node scripts/build-scripts.mjs`）—— **勿手改本文件**。

| 项 | 值 |
|---|---|
| 脚本 | `SC0000.BIN`（真源 `src/SC0000.txt`） |
| 状态 | 🟠 部分 |
| 是什么 | 章节（节）脚本：按 `global 3f3c` 分派到各「G」章头块做章节切换演出（云柱/缩放模糊/白晕/渐黑/人物轮廓），再 call-script 进 ADV 正文脚本（SN*/SG*）。 |
| 怎么进/出 | 由 SYSTEM4 的 mode-1 内联 ADV 管线 call-script（`1394` = SCINIT/SCJUMP 表按章选出的 `0x73`；新游戏首进还会先跑 G0000 写 `global 0=6`/`1394=0x74`）；节内 `ret` 后由 SYSTEM4 收尾（SETFATE / release）再按 `global 0` 分流到 ALLMAP→SCJUMP→下一个节。 |

## 结构（读了哪些段）

| 行区间 | 锚点（必须出现在该区间内） | 职责 |
|---|---|---|
| `1052-1084` | `create-mesh 19258 (global-float 9)` | 入口 `label_00003b38`：`i1f6` 拆场 → `create-mesh 19258` + `set-vertex-color 19258 0 ff 0`（瞬时 0xFF000000 满屏黑幕）→ `label_0006fb64` 层→槽表 → `call-script 3a SETCHARM` |
| `1000-1015` | `label_000047c4` | 入口尾：`call label_000047c4`（按节号分派的总入口） |
| `1256-1280` | `label_000047c4` | 节号分派表：`global 3f3c` 0/1/2/… ⇒ `label_000049f4`(G0000) / `label_00004ab8`(G0001) / … |
| `1281-1294` | `set-string (global-string bb9)` | G0000 プロローグ：只写章题串 `bb8="序章"` / `bb9="現に目覚めし女神"` + `global 2`/`global 0=6`/`1394=0x74`(SN0000)，**零绘制** |
| `1330-1374` | `create-texture (global-int 3f5f)` | G0001 第 1 段：层3 ← 图 76(AE910AA 256×128) + 全屏工作槽 `3f5f` 的 `draw-texture` → `i251` ZoomBlur（槽 64、1500ms、CenterU 500→400）→ `set-draw-color-alpha`/`set-draw-color` 淡入窗 |
| `1560-1595` | `i250 (local-int 0) (global-int 3f59)` | ★用户口径「又一次横向模糊」：层1 全屏工作槽 `3f59`（create-texture 500 2d0 1）+ `i250` SlideBlur（槽 9、3000ms），工作项 `0x18A9E` 同期 α255→0 淡出 |
| `1596-1638` | `set-vertex-color 19258 0 ff 0` | ★「渐黑」：`create-mesh 19258` → `set-vertex-color 19258 0 ff 0`（瞬时黑）→ `set-vertex-color-alpha 19258 0 bb8 0 (4fd5=0)`（3000ms）→ `i238 bb8` wait → `play-bgm 11` |
| `1640-1661` | `mov (global-int f8006) 7d` | ★「人物轮廓」：层1 ← 图 `0x7d` = EV002AD.AGF 1280×720 + `set-vertex-color-alpha 19258 0 960 (4fd4) (4fd5=0)`（2400ms 黑→透明）→ `i238 3e8` wait |
| `1694-1705` | `输入原文：「好厉害，真是年代久远的遗迹` | ★段5：G0001 的第一句话（本段**唯一**的文字；章名是贴片图）；随后 `wait-for-input` |
| `33253-33287` | `label_0006fb64` | 层→槽表初始化子程序：`3f54 + 3L + k`（层0=(4,5,6) / 层1=(7,8,9) / 层2=(0x2b,0x35,0x3f)…；k=2 = 转场工作槽） |
| `1420-1458` | `set-vertex-color 19258 0 ff ffffff` | ★段 2/3 的白晕开场：卡片项的 α 窗（`1420-1433`，1500ms）+ `create-mesh 19258`（`1444`，**同一 handle 第三次复用**）→ `set-vertex-color 19258 0 ff ffffff`（`1445`：瞬时**不透明白**）→ `1452` 改写成**透明白**（= FROM）→ `set-vertex-color-alpha 19258 0 3e8 …`（`1453`：1000ms → 不透明）= 白晕**淡入**；`i238 3e8` + `wait` 等满 |
| `1468-1510` | `mov (global-int f8006) 78` | 四张 1280×720 章节卡贴片（层 5/0/1/6 ← 图 `78`/`79`/`7a`/`7b`）依次上屏 + 白晕**淡出**（`1510`：`set-vertex-color-alpha 19258 0 7d0 0 (4fd5)` = 2000ms → 透明）⇒ 用户口径「白晕减弱 → 显现背景与文字」 |

## 关键槽 / 局部量

| 名字 | 含义 |
|---|---|
| `global 3f3c` | 节号（0=G0000 プロローグ、1=G0001 序章：遺跡に入る…）——分派表 `label_000047c4` 的键 |
| `global 1394 / 1395` | 要 call 的场景脚本 id（1394：G0000 段写 0x74=SN0000；SCJUMP 后由 SYSTEM4 置 0x73=SC0000） |
| `global f8006 / f801d` | `label_0006ff6c`（CG/背景层设置）的两个入参：图 id / 目标层号 |
| `global f8023..f802c` | 10 个 CG 层句柄（`INIT2.txt:6-17` 灌，池外全局）；`f8026..` 另作章头图元、`+2` 是转场工作纹理的全屏展示项 |
| `global 3f54` | 层→纹理槽表（`3f54 + 3L + k`；`label_0006fb64` 初始化） |
| `global 3f5f / 3f59 / 3f62` | 各层的全屏转场工作纹理槽（`create-texture <槽> 500 2d0 1`） |
| `global 4fd1..4fd5` | ★满屏 mesh 19258 的「当前色五元组」（跨脚本残留）：4fd4=α、4fd5=rgb |
| `global 13d7 / 13d8` | 逐节「已演」标志（G0000 写 13d7、G0001 写 13d8）——SCJUMP 用它选节 |
| `global f8047` | 逐层拆场门的门量（`!=0` 才做逐层 detach/release） |
| `mesh 19258` | 满屏四顶点网格（1280×720）：入口瞬时黑幕 + 白晕 + 渐黑 + 黑淡出都用它 |

## 不变量（拿它做回归断言）

- G0001 段一处都不写 `global 0`（停在 1）⇒ ADV 窗走「纸窗」支（T-0102 的白底同源）
- G0001 段没有任何文字绘制指令（无 i2da/i2eb/i23b/i2c7/i2ec/i304/i305）：章名是**贴片图**不是文本
- 段1 的「塔状云柱背景」不在本脚本：它是 SN0000:2042 的图 0x6c2 = BG010AA.AGF 1280×720
- 运行期素材（日志实证）：0x76=AE910AA(256×128)、0x77=AE910AB(768×256)、0x78=AE910AD(768×256)、0x79=AE900AB(1280×720)、0x7a=AE900AA(1280×720)、0x7b=AE902AA(1280×720)、0x7d=EV002AD(1280×720)
- 「渐黑」= 1607-1621（3000ms）；反向的「黑淡出显出人物轮廓」= 1640-1653（2400ms，state1=0）
- mesh `0x19258` 在本脚本里被复用四次（白晕 `1444`/`1503`、黑幕 `1607`、淡出 `1653`）⇒ **同一个 handle 承担「白晕 / 黑幕」两种角色**，判读一律看当时的 `state0`/`state1` 而不是 handle；★引擎对**已存在**的项执行 `create-mesh` 时是否重置两格态色**未逐字确证**（emulator 现按「只重建几何、不重置态色」实现）——本脚本的每一处 create 之后都立刻显式写两端色，故此处无观测差异

## 坑（踩过一次，别再踩）

- ★`jcc v mask label` 是 **v == 0 才跳**（`app/amayui-emulator/src/vm/handlers/control.ts:81-105`）⇒ 本段 `jcc 1 …` = 落下句（1452/1453 会执行）、`jcc 0 ffffffff label` = **跳**（1504-1507 与 1647-1651 **不执行**）。运行期日志（createMesh/setVertexColor/setTransition 的先后）逐条印证；这是本段最容易读反/改错的地方
- ★`0x322` 的 op2 是 **blend 选择子**（写 MeshEntry[9]），不是顶点下标；`0x323` 的 op2/op3 = **延迟/时长(ms)**、op4=目标 α、op5=目标 rgb（写 state1）
- ★`4fd1..4fd5` 是跨脚本残留的池外全局：入口(1066-1070)写黑、1446-1450 改白、1609-1613 再改黑 ⇒ 读它之前先看谁写过
- ★白晕是 mesh 19258 的**两段**窗：1444-1458 淡入 1000ms（state0 瞬 0xFFFFFFFF → 1452 改 0x00FFFFFF → 挂 state1=0xFFFFFFFF）+ 1503→1510 淡出 2000ms（=用户口径的「光晕减弱」），中间 1465-1466 有 1000ms wait
- ★「渐黑」是**渐**的（`1607-1616`）：`create-mesh 19258` → `set-vertex-color 0 ff 0`（瞬时黑）→ `set-vertex-color 0 0 0`（改回**透明**黑 = FROM）→ `set-vertex-color-alpha 0 bb8 ff 0`（**3000ms** → 不透明黑）；揭示靠 `1653` 的 `set-vertex-color-alpha 19258 0 960 0 (4fd5)`（**2400ms** → 透明）。这两个时长就是观感判据（`T-0103` 轮 18 逐帧核过：α 0→43→…→255，再 255→201→…→0）
- ★**卡片与人物轮廓共用同一个 draw item**（层 1 那一格，运行期 handle 101020）：`1641-1643` 只是把 `f8006` 从 `7a` 换成 `7d`、并重设槽 7 的图（`bindTexture imgid=0x7d`）⇒ 换图发生在黑幕 α=255 时，屏上不可见；别按「先撤卡片、再建轮廓」理解
- ★段 4 的第二次横向模糊（`1574`：工作槽 `3f59`=9、3000ms、源 = 卡片项 `f8024`）与 SN0000 段 1 是**同一分支**（`[13]=0` SlideBlur）⇒ 段 1/段 4 的「黑色遮罩」是同一根因（`T-0103` 轮 18）

## 相关

- 引擎常态能力：`clock-read-transition-window`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`mesh-vertex-quad-and-per-vertex-color`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`clock-read-transition-window`（见 `docs-new/03-engine/engine-capabilities.md`）
- 主题文档：`docs-new/03-engine/opcode-table.md`
- 主题文档：`docs-new/03-engine/scene-start-flow.md`
- 主题文档：`docs-new/03-engine/live2d.md`
- 主题文档：`docs-new/99-records/2026-09-transition/transition-render-spec-2026-09.md`
- 守卫测试：`app/amayui-emulator/test/t0102-chapter-chain.test.ts`

## 证据与备注

- 证据：src/SC0000.txt 的区间见 layout；运行期时间线（setTransition/createMesh/setVertexColor/detach 的先后）见 tickets/T-0103/evidence/l2d-residual-gamestart-vs-load.md 与实例日志的 [transition] 行（T-0103 轮 14）
- 备注：未读：G0002 之后的各章头块、各节的 ADV 正文段（本文件 33997 行 / 24501 指令，只读了入口 1008-1084 与 G0000/G0001 段 1256-1705）、`label_00070b40`（全层硬销毁）与 `label_0006fed8`/`label_00071120` 等辅助子程序的逐句；层 5/6 的素材未列全。
