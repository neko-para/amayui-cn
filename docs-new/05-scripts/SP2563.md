# 脚本台账 · `SP2563`

> 由 `analysis/scripts.json` 生成（`node scripts/build-scripts.mjs`）—— **勿手改本文件**。

| 项 | 值 |
|---|---|
| 脚本 | `SP2563.BIN`（真源 `src/SP2563.txt`） |
| 状态 | 🟠 部分 |
| 是什么 | 剧情 ADV 脚本（本体 SP*.txt 之一，17000+ 行）：立绘/文本推进 + 音频惯用法（音效「先装载后起播」、语音通道复位后静音）。 |
| 怎么进/出 | 由章节脚本 call-script 进入（调用方未追）；内部按 global 分支处理菜单/选择项。 |

## 结构（读了哪些段）

| 行区间 | 锚点（必须出现在该区间内） | 职责 |
|---|---|---|
| `405-411` | `play-sound-effect 2e 1` | 一段分支的收尾：`play-sound-effect 2e 1`（装载 SE 0x2e 到通道 1）→ `i0b5 1`（起播·播一次）→ ret |
| `17024-17033` | `i2f8 2 0` | 语音页收尾路径：`i2f6 2`（复位语音通道 2）→ `i2f8 2 0`（把该通道 pan 复位到中央）→ 按 global 判据跳转 |

## 关键槽 / 局部量

| 名字 | 含义 |
|---|---|
| `global 14acda / 14acdb` | 该段状态机（label_000389bc 置 3、收尾路径置 0） |
| `global a9d9 / 4fd6` | 语音收尾分支的判据（读后立即 jcc） |

## 不变量（拿它做回归断言）

- 音效惯用法成对出现：先 `play-sound-effect <id> <ch>` 装载，再 `i0b5 <ch>`（或 `i0ba <ch>` 循环）起播

## 坑（踩过一次，别再踩）

- `i0b5` 是 0xB5（SE 通道起播·播一次）而不是 0x5B `ne`：助记符缺失使反汇编显示 `i0b5`，误读会把「播一次」当成「比较不等」
- `i2f6` + `i2f8 … 0` 不是「清消息回调」而是「复位语音通道 + 把该通道 pan 复位到中央」（见 sound-system.md §4）

## 缺口

- 只读了 2 段（全 17307 行）：调用方、ADV 主流程、菜单分支均未读

## 相关

- 引擎常态能力：`audio-module-topology-and-volume-routing`（见 `docs-new/03-engine/engine-capabilities.md`）
- 函数结论：`0x420B00`（见 `analysis/functions.json`）
- 函数结论：`0x420B40`（见 `analysis/functions.json`）
- 函数结论：`0x426820`（见 `analysis/functions.json`）
- 函数结论：`0x4268D0`（见 `analysis/functions.json`）
- 主题文档：`docs-new/03-engine/sound-system.md`
- 主题文档：`docs-new/03-engine/opcode-table.md`

## 证据与备注

- 证据：src/SP2563.txt:405-411（`play-sound-effect 2e 1` + `i0b5 1`）与 :17024-17033（`i2f6 2` + `i2f8 2 0`）；指令语义见 functions.json 的 0x420B00/0x420B40/0x426820/0x4268D0
- 备注：只登记「音频惯用法」两段；ADV 主流程与菜单分支未读。
