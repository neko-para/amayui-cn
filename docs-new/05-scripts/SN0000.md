---
kind: generated
state: live
home: analysis/scripts.json
generated_by: scripts/build-scripts.mjs
---

# 脚本台账 · `SN0000`

> 由 `analysis/scripts.json` 生成（`node scripts/build-scripts.mjs`）—— **勿手改本文件**。

| 项 | 值 |
|---|---|
| 脚本 | `SN0000.BIN`（真源 `src/SN0000.txt`） |
| 状态 | 🟠 部分 |
| 是什么 | 序章脚本（含引擎『字格逐字显现』的真实用例）。 |
| 怎么进/出 | 由本篇流程 call-script；页与页之间用 i304/i305（文本块）与 wait-for-input 分段。 |

## 结构（读了哪些段）

| 行区间 | 锚点（必须出现在该区间内） | 职责 |
|---|---|---|
| `1075-1085` | `i073 8 0 (local-int 0) c 0 (local-int 1) 38 38 8 64` | 序章页的字格：窗 8、格 56×56（0x38）、8 格、节拍 100ms（0x64） |
| `575-580` | `i1ce 0` | 逐字开关收尾（0x1ce 0） |
| `1224-1236` | `show-text 0 @"由两个世界融合而生的"` | ★**第一条文案**（`SN0000.txt:1224` 的 `// 输入原文：…`）：本文件里第一条**被汇编**的 show-text（更早的 `:1210` 等都在 `/* 原文存档（对照用，不参与汇编） */` 注释块内）⇒ 指令下标 ip = 901，整页 3 行 58 字 |
| `1018-1049` | `i238 157c` | 开场序列：`i238 0x157c`(5500) → `wait` → `play-bgm d` → `i1f4` → 调两个子程序 → `i21a`/`i218` 读图元当前「位置/pivot」→ 校正后 `i219`/`i217` 写回 → `i1ff` 平移归零 → `i220 0 0x13880`(80s) 背景横移 → `i242` 登记 → `wait` |
| `1043-1048` | `i242 (global-int f8023) 1` | ★慢推 + 门：`i220 (global-int f8023) 0 13880 …`（80 000 ms 横移）→ `i242 (global-int f8023) 1`（`DrawItem+720` bit0 ⇒ 把这一项**排除出池挂起位** `Scene+46516`）→ `i238 64`（100 ms 计时器）→ `wait`。实测这道门驻留 **100 ms = `i238`**，不被 80 s 慢推钉住（`tickets/T-0024`） |
| `1112-1122` | `i1c7 (global-int f7ff5)` | ADV 等待循环：`i1c7`(ADV 激活) / `i1cc`(本页显示中) 写 `f7ff5`/`f7ff6` → `or` → `jcc`；随后 `i2f6`/`i2f8` 与 `play-voice`/`i2ff` 组页 |
| `1029-1045` | `i21a (global-int f8023) (global-int f803b) (global-int f803c) (global-int f803d)` | ★立绘/图元摆放：`i21a` 取**描画位置**、`i218` 取 **pivot** → 按 (100,370) 对齐 → `i219`/`i217` 写回 → `i220` 平移窗 → `i242`。跳过 `i21a`/`i218`（它们回写操作数）会让偏移基于 0 计算 ⇒ 位置全错 |
| `8-41` | `i258 3 1` | 开头 30+ 条 `i258 <槽> 1`：按位设置各纹理槽记录标志（`0x258`） |
| `121-196` | `i220 (local-int 0) 0 c8 0 0 0` | ★**侧边栏 UI 例程**（`label_00000c74` 展开 :121-155 / `label_00000e78` 收起 :160-196）：`i093` 清热点表 → 16 个槽的 `i220` 平移窗（dur 0xc8 = 200ms）→ `wait`（置 `0x400` 门）→ 写 `global 1399`（1/2）→ `call label_00000320` 按状态重登记热点表 → `ret`。这两段是 `SN0000.txt:52-66` 侧边栏热点的 labelA/labelB |
| `486-500` | `set-font (global-string bbb)` | ★ADV 主字体「标准样式块」（一次性）：set-font (global-string bbb) + i2bd 1（粗体）+ i2fe(bbc 注音面) + i08b 10 + i078 f8079 + i1a4 f807a f807a + i260/i261，末尾 mov (global-int 3f36) 2 标记已应用；进入条件 = jcc (3f36 != 2) 不跳转（即样式尚未应用时） |
| `3171-3184` | `i032 2 e 0 0 500 2d0 0 0 140 b4` | ★**存档缩略图的缩屏流水线**：`create-texture 2`（1280×720 全屏槽）→ `i20d 2`（设为渲染目标）→ `i20e` → `i20c` → `i20d -1`（还原）→ `create-texture e`（320×180）→ **`i032 2 e …` 缩放转送** → `release-texture 2` → `i1ae … e`（写 `.STH`）→ `release-texture e`。337 个 ADV 脚本同型（`tickets/T-0050`） |
| `1003-1067` | `label_000038e0` | ★★读档续跑门 + **被跳过带**：`i0ae` = 指令 737（line 1003）、存档落点 = 指令 794（line 1068 `i071 8`，0x71 表「重放这条消息」）⇒ **指令 738..793（line 1004..1067）在读档时不执行** —— 序章开场序列整段在其中：`i238 157c`/`wait`、`play-bgm d`(1022)、`i1f4`+`mov (global-int f801d) 0`+`mov (global-int f8006) b37`(1025-1026)、**`call label_0000e24c`(1027) = 全文件唯一的场景起始背景绘制**、80 秒慢推 `i220`(1039)、`i242`(1045) |
| `3667-3729` | `label_0000e24c` | ★★**CG/背景层设置**子程序（全文件唯一的背景绘制路径）：键 = `f8023[f801d]`（层 0 ⇒ **0x18A88**，表见 `src/INIT2.txt:6-17`）、槽 = `3f54[f801d][0]`（层 0 = 4）、图 = `f8006`（= 0xB37 = `BG050ABL.AGF` 2048×1152）、几何 = `i208` 取槽尺寸（2048×1152）+ pivot 回退算 dst（新项 ⇒ (0,0)）⇒ **src 全图 @ dst (0,0) 覆盖整屏**；写出点是 `set-texture`(3703) 与 `draw-texture`(3720)。调用点只有两处：指令 757（line 1027，**在被跳过带里**）与指令 1561（line 2043，剧情中途「换背景」块） |
| `2038-2046` | `mov (global-int f8006) 6c2` | ★段1 的「塔状云柱背景」：层2 ← 图 `0x6c2` = BG010AA.AGF 1280×720（槽 43） |
| `3009-3073` | `create-texture (global-int 3f5c) 500 2d0 1` | ★章切换块（读到 `exit`）：`1397=1` → `call label_0000b884`（detach 19640/19834 拆场）→ `i238 708` wait → `create-texture 3f5c` + `draw-texture` → `i250` SlideBlur（槽 63、2500ms、源项 0x18AEE）→ `set-draw-color-alpha`/`set-draw-color` 把工作项 α255→0 → `i238 9c4` wait → `global 0=2` → `exit`。★「渐黑」不在本脚本，在 `SC0000:1607-1621` |
| `76-114` | `i097 (local-int 0) (local-int 1) 1 1 3` | ★**ADV 键位绑定表**（6 条，逐条 `i090` 登记屏幕外 1×1 热点 + `i097` 绑掩码位）：位 0→`label_00002954`、1→`label_00002a70`、2→`label_00002b3c`、3→`label_00002c40`、8→`label_00002f84`、7→`label_00002da4`（仅当 `global f8009 > 0` 才登记）。热点 y 从 `0-0x3e8 = -1000` 起逐项 −1 ⇒ **在屏幕外**，鼠标永远命中不到；唯一读者是引擎 `sub_403D70`（键命中 → labelC，raw 9847-9863）⇒ 这是 ADV 的键位入口。★**位 3 = ←**（`0x10C` 默认表 VK37→位3）。因此 `set:WheelKeyUp = 3`（随包 INI）时**滚轮上滚 ≡ 按 ←**：走 `label_00002c40` → `call label_00000e78`（建 `global f8019` 的 16 项菜单 mesh）+ `i10a` 把光标挪到菜单项 ⇒**打开 ADV 侧边栏菜单**（= 2026-09-24 用户实测「滚轮上滚打开侧边栏并停在第一个按钮」的机制来源；位 0..3 的四段都是同一套菜单光标/展开逻辑，见 `label_00002954/2a70/2b3c/2c40`）。 |
| `3020-3024` | `mov (global-int f8043) 5dc` | 切章前把 **ADV 半透明遮罩**（mesh `0x19640`）淡掉：`mov (global-int f8043) 5dc`(1500) → `call label_0000c5e0`（该例程用 f8043 当时长写 `set-vertex-color-alpha 19640 …`；rgb 走「负值取当前」⇒ 保持黑）→ `call label_0000cf90` → `i238 708`(1800) + `wait`。实测时间线（`T-0103` 轮 18）：遮罩 α 128→85→42→0 用 ~1.5s，之后还有 ~1.8s 空拍（全亮背景）才进模糊 |
| `3026-3058` | `i250 (local-int 0) (global-int 3f5c) (global-int f8025) 1` | ★段 1「横向模糊」整块：`create-texture 3f5c`（1280×720，运行期槽 **63**）→ `draw-texture (f8025+2)`（**目标项** `0x18AEE`，全屏、引用该槽）→ `i250` 十个操作数的角色：`op1`=转场记录 key（=目标项）、`op2`=工作槽 `3f5c`、`op3`=**源区间起点** `f8025`（= 层 2 的云柱背景项）、`op4`=跨度 1、`op5..8`=四通道初值/终值（`[16]=0→[20]=200` = SlideBlur 的 **Length**、`[19]=[23]=0` = Angle 0 ⇒ **横向**）、`op9`=delay 0、`op10`=**2500ms**；随后 `3045/3051/3054` 写两端色窗：**目标项** FROM 不透明白 → TO **透明**（`local1 = 0*ff` 是故意取 0）⇒ 模糊图**由不透明淡到透明** = 用户口径的「向黑色渐变」。★emulator 的「模糊源 = 记录区间 A 的离屏副本」由此确定（`T-0103` 轮 17） |

## 关键槽 / 局部量

| 名字 | 含义 |
|---|---|
| `win 8` | 序章正文窗（字格逐字的目标窗） |
| `global f8023` | 开场背景图元的 handle（1015843；`i21a/i218/i219/i217/i220/i242` 的操作对象） |
| `global f803b..f8040` | 位置三元组（f803b/c/d）与 pivot 三元组（f803e/f8040）的暂存槽 |
| `global f7ff5 / f7ff6` | ADV 状态快照：`i1c7`(ADV 激活) / `i1cc`(本页显示中) |
| `global f801f / f801d / a9ba` | 当前立绘的**纹理槽**与由 `i216` 反查出的 **imgid**（`i2ff` 用它取资源/播语音） |
| `win 8` | 序章正文窗（字格逐字的目标窗，也是首文案所在的窗） |
| `global f8023..f802c` | 10 个 CG 层句柄 = 0x18A88/0x18A9C/0x18AEC/0x18B00/…/0x18B78（由 `src/INIT2.txt:6-17` 灌；**池外全局** ⇒ 真槽不还原、靠 INIT2 重跑） |
| `global f8006` | 当前 CG/背景图 id（`mov f8006 b37` 在被跳过带里 ⇒ 冷启动读档时是**陈值**） |
| `global f801d` | CG 层号（0 = 背景；1 = 立绘层；2 = 中层） |
| `global 3f54` | 「CG 层 → 纹理槽」约定表（`3f54[层][0]` = 槽号；层 0 = 4） |
| `global f8043` | ADV 半透明遮罩（mesh `0x19640`）淡出的时长（切章前 = `0x5dc` = 1500ms；由 `label_0000c5e0` 消费） |

## 不变量（拿它做回归断言）

- 页序列：i304（文本块开始）→ show-text → i305（文本块结束）→ i073（设字格）→ wait-for-input → 点击 → i071（清场）
- SN0000 的第一条被汇编 show-text 在 ip = 901（= `src/SN0000.txt:1225`），页面 3 行 58 字，全部落在窗 8
- 开场两道 `wait` 门的驻留 = 脚本 `i238` 的值（`i238 0x157c` ⇒ 5500 ms、`i238 64` ⇒ 100 ms），与 80 000 ms 的慢推窗无关（真实语料实测见 `tickets/T-0024/changes.md` 的 gateWaits 表）
- **读档续跑路径 = 指令 0..103 ∪ {737}（共 105 条）**，逐 token 扫过 `scripts/asm/opcodes.json` 的全部相关 code：其中 `create-texture`/`set-texture`/`draw-texture`/`i1f6`/`i1fa`/`i214`/`i21d`/`i1fd`/`i1ff`/`i23d`/`i32b` **一条都没有**（唯一的 i1f4/i1f5 是绘制块标记）⇒ **读档路径上 SN0000 一个绘制项都不创建、也不删除**；背景项 0x18A88 的创建点在指令 2850，不在该路径上
- SN0000 创建的绘制项键全部 ≥ **0x18A88**（≫ TITLE 的 0x135）⇒ 一旦被创建必然画在标题残留之上；最大键 = 0x30D40（200000，`create-mesh`）

## 坑（踩过一次，别再踩）

- ★`src/SN0000.txt:1209-1223` 是 `/* 原文存档（对照用，不参与汇编） */` 注释块 —— 里面也有 show-text，**不能**拿它当「第一条文案」的锚点（会被注释误导）；判据要用「第一条被汇编的 show-text」或 ip=901
- ★`i1c7`/`i1cc`/`i21a`/`i218`/`i215`/`i216` 都是**会回写操作数**的 getter：当 no-op 跳过时 op1/op2..4 保留上一条指令的旧值，脚本据此做的 `or`+`jcc` 与位置校正都会静默出错
- ★`i220 <handle> 0 13880 …`（80 000 ms 慢推）之后**必须**紧跟 `i242 <handle> 1`：引擎靠 `DrawItem+720` bit0 把这一项排除出池挂起位（`sub_49AA30` raw 117843-117844）⇒ `0x400` 等待门不会被整场慢推钉死（同时也让它不被「跳过」冻结截断，raw 117440-117442）。漏掉 `i242` ⇒ `wait` 会一直卡到平移结束（对照实测见 `tickets/T-0024` 的守卫）
- ★侧边栏 UI 例程以 `wait` + `ret` 结尾（`label_00000c74` :121-155 / `label_00000e78` :160-196），而**悬停全走「带返回点 -3」的派发**（`sub_405360`）⇒ label 的 `ret` **回到门指令重跑**。所以门指令（`0x72`）与页记账必须**幂等**：`tickets/T-0016` 修前悬停右侧侧边栏会让中间的整页文字被重新逐字（重放）—— 实测「悬停 400 帧 ⇒ 重放 2 次、页计数 0→1→2」
- ★ADV 正文的粗体来自这里（:491-492 的 i2bd 1，面名 = global-string bbb 即 ADV 字体分类），不是来自某条 show-text；但真机实测该页正文落在常体档（见 tickets/T-0035/notes.md 第 7 轮）⇒ 「脚本请求了 700」不等于「引擎画出来是粗体」，两者之间的落差在引擎绘制侧
- ★存档缩略图不是引擎自己缩的：`i032`（StretchTexture）把 1280×720 的全屏槽**缩放转送**到 320×180 的槽，随后 `i1ae` 才把它写成 `.STH` —— 缺 `i032` 的宿主只能写出占位缩略图（`tickets/T-0050`）
- ★`0x259`（指令 1）清的是两张 1000 条 5-dword **槽记录镜像**（raw 25357 `sub_41A3A0`：`Engine+81176+5k`/`+86176+5k` 的 +0/+4），**不是绘制项容器**（`Scene+0x408`）—— 别把它当「清画面」
- ★被跳过带里的 `play-bgm d`(1022) ⇒ 读档后必须补播 BGM（`handlers/audio.ts:140` 的既有实现）
- ★**章切换的「黑」不在本脚本**：`3009-3073` 只做「拆场 + 一次 i250 SlideBlur（槽 63、2500ms）+ 工作项 α 淡到 0」；用户口径的「向黑色渐变」是 `SC0000:1607-1621`（3000ms，满屏 mesh 19258 的顶点色窗），「白晕/人物轮廓」同样在 SC0000（见第三层 SC0000 条目）
- ★`jcc v mask label` = **v == 0 才跳**（`app/amayui-emulator/src/vm/handlers/control.ts:81-105`）⇒ 本文件 `jcc 1 …` 是落下句、`jcc 0 ffffffff label` 是跳；读 `3012`/`3037`/`3080` 一带的分支必须按此解（运行期日志逐条印证）
- ★段1 的「塔状云柱背景」= 层2 ← 图 `0x6c2` = BG010AA.AGF（1280×720，槽 43，`SN0000:2041-2043`）——不是章头素材，别去 SC0000 找它
- ★段 1 的「向黑」**不来自** SC0000 的黑幕（那在 2.5s 之后），而是本块 `3051-3054` 给**目标项**写的 α 窗（FROM 不透明 → TO 透明，2500ms）配上底下那张满屏黑 mesh `0x0`；改这两条中任一条都会让「渐黑」变成硬切（`T-0103` 轮 17/18）
- ★横向模糊的**暗色不是脚本给的**：`i250` 的采样权重是**归一化加权平均**（Σw = 32×1 + 中心 3 = 35），宿主必须用**加法**累加（`lighter`）+ 整数 α 表（Σα = 255）；用 source-over 会把 α 累成 `1-(34/35)^33 ≈ 0.616` ⇒ 合成幕只有 62% 不透明、黑底透出 38% = 用户实测的「横向模糊引入的黑色遮罩 / 背景色跳变」（`T-0103` 轮 18；段 4 是同一分支）

## 相关

- 引擎常态能力：`msgwin-char-reveal-grid`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`text-reveal-pump-409400`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`texture-bind-synchronous-then-query`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`mesh-vertex-quad-and-per-vertex-color`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`scene-pending-flag-0x400-gate`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`save-load-drawitem-clear-and-restore`（见 `docs-new/03-engine/engine-capabilities.md`）
- 引擎常态能力：`clock-read-transition-window`（见 `docs-new/03-engine/engine-capabilities.md`）
- 函数结论：`0x41F250`（见 `analysis/functions.json`）
- 函数结论：`0x45A940`（见 `analysis/functions.json`）
- 函数结论：`0x45BE20`（见 `analysis/functions.json`）
- 函数结论：`0x42D390`（见 `analysis/functions.json`）
- 函数结论：`0x42D410`（见 `analysis/functions.json`）
- 函数结论：`0x4303C0`（见 `analysis/functions.json`）
- 函数结论：`0x430450`（见 `analysis/functions.json`）
- 函数结论：`0x430340`（见 `analysis/functions.json`）
- 函数结论：`0x430380`（见 `analysis/functions.json`）
- 主题文档：`docs-new/03-engine/adv-text-rendering.md`
- 主题文档：`docs-new/03-engine/scene-start-flow.md`
- 主题文档：`docs-new/99-records/2026-09-transition/transition-render-spec-2026-09.md`
- 守卫测试：`app/amayui-emulator/test/char-reveal.test.ts`
- 守卫测试：`app/amayui-emulator/test/game-start-chain.test.ts`

## 证据与备注

- 证据：src/SN0000.txt:1075-1085 / 575-580；页序列断言见 test/char-reveal.test.ts 的『真实序章页序列（SN0000…）』
- 备注：★2026-09-24（T-0103 轮 14）：章切换链已读到 `exit`（见 layout 的 3009-3073 段）；「黑幕/白晕/人物轮廓/章节标题贴片」全在 **SC0000**（本轮新登记 SC0000 条目），本脚本只做拆场 + 一次 i250 SlideBlur；★`jcc` 极性（v==0 才跳）见 gotchas。★三处已修：① mesh 整屏黑 —— `0x320` 的 op2..op8 是**数组基址**（x/y/z 浮点数组 + 逐顶点色数组 + UV 数组），presenter 已按真实几何 + 真实颜色合成；且 `0x322/0x323` 有引擎的"负值 = 取当前 state0"回退（序章 50% 暗幕 `0x19640` = 0x80000000）。守卫 `test/mesh-vertex-quad.test.ts`（E3）。② 首文案跑到左上角 —— 文字块原点 = `0x7A` 覆盖值（`SN0000.txt:1087 i07a 8 8c 12c` = (140,300)；默认是 `SYSTEM4.txt:54 i079 8 8c 10` 的 (140,16)），行中心由 `SYSTEM4.txt:70 i303 8 1 1f4` 定为 140+500=640。守卫 `test/text-layout.test.ts`。③ 逐字过慢 —— 字格路径（`i073`）一拍 = **一个字格**（模数 = op9 = cells），整段 = cells × op10 = 800ms，不是"一字一拍"（58 字 ⇒ 5.8s）。守卫 `test/char-reveal.test.ts`。★2026-09 再修两处（同一轮用户复测）：④ **进场景时背景闪一下** —— 淡入幕 `0x19258` 的 `0x322 set-vertex-color` 写 state0=0xFF000000（全黑）后，`0x323 set-vertex-color-alpha` 才把 state1=0（透明）挂上 3.6s 淡出窗；两帧之间"无动画窗"时可见色必须是 state0（读到 state1 会让那一帧幕**透明** ⇒ 背景先露一下再被黑色盖上重淡）。引擎的可见色是**顶点缓冲里那份**：`sub_4AE2C0`(raw 132816-132823) 写完 state0 立刻以比例 0 刷 VB、`sub_4AE330`(raw 132834-132843) 不碰 VB ⇒ 未开窗时可见色 = state0。守卫 `test/mesh-vertex-quad.test.ts`。⑤ **逐字出现前整页先亮一次** —— 本页的 `i073` 在文本**之后**（`SN0000.txt:1240`，而 `:1081` 在页首），字格总门（`win+88`）自首屏起恒 1；但 `0x72 wait-for-input` 才进逐字模式（`effect_flags & 0x40000000`）、主循环才逐格 `sub_45A940` 拍字 ⇒ 在 `0x6E` 入队到 `0x72` 武装之间屏幕上一个字都没有。"无显现状态"必须读成"无"而不是"全部"（-1）—— 读成"全部"会让那一帧整页先亮、随后被逐字从头重播。判决点收敛在 `MsgWindow.revealedOf` 一处（字格门与 `0x300` 逐行泵并列为例外）。守卫 `test/char-reveal.test.ts`。（其余序章正文页、战斗/地图分支仍未读。） ★2026-09-24：ADV 键位绑定表已登记（layout 76-114）；滚轮/回看链的现状与缺口见 `tickets/T-0168`。 ★2026-09-24 追查补记：键位表里 **位 8 → `label_00002f84` → `call-script 31 // HISTORY`**（`SN0000.txt:831/836`）就是「回想」入口（位号来源 `SYSTEM4.txt:87-97` 的 `i10c 8 c9`，键码 0xC9 = PageUp）；位 0..3 = ↑/→/↓/← 的菜单导航 ⇒ 随包 `set:WheelKeyUp=3` 时滚轮上滚 = 菜单导航（不是回想）。HISTORY 的页渲染 `i1d1`（0x1D1）在 emulator 未实现 ⇒ 即便进了 HISTORY.BIN 也看不见画面（见 tickets/T-0168）。
