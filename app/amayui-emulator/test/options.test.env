# 测试专用的环境（Node `--env-file` 读它；见 package.json 的 test 脚本）
#
# ★为什么需要：`emulator.config.json` 是**本机私有**的运行开关（测试者会把它改成 showLogo=false
#   以省掉 LOGO 等待）。测试必须与它无关 ⇒ 一律指向这个"空配置"（= 全部默认值）。
# ★路径相对**仓库根**（`resolveOptionsPath(repoRoot, env)` 的口径）。
AMAYUI_EMULATOR_CONFIG=app/amayui-emulator/emulator.config.test.json

# ★★测试一律**静音**（`tickets/T-0103`）：`audio.enabled` 的真源是 `emulator.config.json`（人的偏好），
#   而"**这一次运行**出不出声"由**命令行**决定 ⇒ 测试用这个环境变量覆盖文件值（环境变量优先于文件）。
#   关掉的只是**宿主输出**（不建 AudioContext / 不起播 / 不建 <audio>），引擎侧的通道/延迟/仲裁/BGM 淡变
#   照跑（`decode` 改从容器头读时长 ⇒ 判据同量级）⇒ digest 与报告不受影响。
#   单跑某个测试时也可以：`AMAYUI_AUDIO_ENABLED=0 npx tsx --test test/xxx.test.ts`。
AMAYUI_AUDIO_ENABLED=0
