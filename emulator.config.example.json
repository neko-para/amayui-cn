{
  "$comment": "amayui-emulator 的**外置选项示例**（测试/调试用运行开关；**不是**游戏配置，绝不写回、不进存档）。用法：把本文件复制成同目录的 emulator.config.json 再改（后者是本机私有的，已 gitignore）。目前只有一个键。改完重跑 emulator 生效。路径也可用环境变量 AMAYUI_EMULATOR_CONFIG 覆盖。详见 docs-new/04-app/emulator.md 的「外置选项」节与 src/emulatorOptions.ts。",
  "boot": {
    "showLogo": true
  }
}
