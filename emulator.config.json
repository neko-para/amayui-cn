{
  "$comment": "amayui-emulator 的外置选项（测试/调试用运行开关；**不是**游戏配置，绝不写回、不进存档）。目前只有一个键。改完重跑 emulator 生效。路径可用环境变量 AMAYUI_EMULATOR_CONFIG 覆盖。详见 docs-new/04-app/emulator.md 的「外置选项」节与 src/emulatorOptions.ts。",
  "boot": {
    "showLogo": false
  }
}
