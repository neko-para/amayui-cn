# T-0016 · 过程文档（notes.md）

## 2026-09-14

## 复现与根因（2026-09 这一轮）

复现（真实语料）：`.tmp/t16-repro2.mts` —— 走 GAME START 链路到 SN0000 首文案的等待态，
把光标移到右侧侧边栏热点（`SN0000.txt:74` 的 `i090 4ce e9 32 104 …`，中心 ≈ (1255,363)），
逐帧记录 `ip / awaitingAdvance / revealedOf(8) / pages / routes / lastDispatch`。

| | 修复前 | 修复后 |
|---|---|---|
| 400 帧内"整页文字被重放" | **2 次**（`revealedOf(8)` 52→0，ip 回到 917） | **0 次** |
| 页计数 | 0→1→2（每次重跑门指令都记一页） | 0→1（只记一次） |
| 文本/内容 | 不变（只是被重新逐字） | 不变 |
| `revealRestarts`（链路 E3 指标） | 2 | 0 |

根因 = **悬停 label 的 `ret` 回到门指令重跑 `0x72`，而 emulator 的 `0x72` 把已显完的文字重新武装**。
引擎侧依据：`sub_405360`（返回点 -3）+ `0x72` 尾段只武装 ▼/等待门（raw 28539-28555），
文字游标由 `sub_45BE20` 推进、`0x71`/`sub_45EC60` 复位。

为什么"不断"重放：侧边栏展开 label（`SN0000.txt:121-155`）先 `i093` **清热点表**、再 `i220` 平移窗 + `wait`，
末尾 `call label_00000320` 按 `global 1399` **重登记**热点表 ⇒ 光标在新表里换项 ⇒ 又一次 hover-enter
⇒ 又一次重跑门指令。所以玩家看着就是"悬停着就一直重放"。

## 残余（本轮未动，登记在此）

- 门指令重跑时 `pages` 的记账时点仍是"重跑那一刻"（本页第一次 `0x72` 因为显现刚开始而提前 return）
  ⇒ 页计数语义正确（一页一次），但**时点**晚于"文字显完那一刻"。要用它做更细的时序断言时注意这一点。
- 验收 1（进 ADV 页即展开）按用户要求收窄到 `T-0028`（已 done）。
- "遮罩不消失"这一半由 `test/mesh-vertex-quad.test.ts` 的 ADV 暗幕断言（`#80000000`）间接覆盖；
  本轮未加"悬停期间暗幕不动"的专门断言。

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

用户明确要求排在 #1/#2/#3 之后处理（2026-09）。
2026-09 用户复核：验收 1（进入即展开）已在 T-0028 收窄为「进入时状态位/槽平移不一致」并单独跟踪；本票专注「hover 不得推进 ADV / 不得 cache 推进指令」。
