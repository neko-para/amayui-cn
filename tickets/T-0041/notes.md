# T-0041 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

本票是 `T-0017` 实施时**新发现**的未收敛项（`T-0017` 本身按真机可见行为交付）。

对照材料（都在 `.tmp/`，只作过程记录）：
- `t17before-0-title.png`（把混合档强制 normal = 修前行为，TITLE 正确）3.4MB；
- `t17fix2-0-title.png`（泄漏档打开）2.0MB —— logo 变成黑底方块、背景被覆盖；
- `t17v2-0-title.png` / `t17v2-6-gamestart.png`（默认档）与 `t17before` **逐字节相同** ⇒ 默认档没有引入回归；
- 同一份代码跑两次（`t17fix2` vs `t17fix3`）：静态帧（TITLE/GAMESTART/两处 hover）逐字节一致、淡入中途的帧（7/8）会差 ⇒ **比对必须以静态帧为准**（这条经验也值得记住）。

★2026-09-16 收敛：原"读法"与 E4 的矛盾根源是把**设备 vtable 间接调用**当成不存在(SetRenderState = `(*(*(device)+228))`，228 = 57×4)。真正的每项重设 = `ID3DXSprite::Begin(16)`（raw 123087-123088）。默认档（每项从默认开始）就是引擎事实，无需改动产品行为。
