# T-0035 · 过程文档（notes.md）

## 现象（用户实测 2026-09）

与真游戏对照：emulator 里**很多地方**的文字比引擎渲染的**更粗**，且**白色看着更亮**。

## 引擎的字体配置链（逐条核对，raw = `engine/天结_unpacked.exe_utf8.c`）

| 项 | 引擎 | 证据（raw） | emulator | 结论 |
|---|---|---|---|---|
| 面名 | `Font+1260`（主）/`+1320`（注音），来自 `message:Font` 与 `0x1A5`/`0x2FE` | 78859-78875 / 23666 | `msgwin.font.mainFace/rubyFace` + `resolveFace()` 映射到内置族 | ✅ 一致（映射政策另见 `fontSet.ts` 文件头） |
| 字号 | `Font+201684` 初值 24，`0x75` 写；`lfHeight = -size` | 78755-78872 / 24063 | `font.mainSize`（`0x75`） | ✅ 一致 |
| 字重 | `Font+1248` **初值 0**（FW_DONTCARE ⇒ 常规面），`0x2BD` 写 **700/0** | 78854 / 33394-33399 | `mainBold ? 700 : 400`（默认 400） | ✅ 等价（GDI 的 0 取常规面；400 = 常规面） |
| **抗锯齿** | `Font+1352` **初值 0**；**只有** `set:EnableAntiFont` 非 0 才读 `message:UseAntiFont` 并 `sub_4155B0` | 78755 / 23649-23656 / 22409-22422 | ❌（修复前）canvas **永远开 AA** | ★**不一致 → 已修** |
| 描边档 | `Font+1372` 初值 1（0 无 / 1 单向 / 2 同位叠 1/4 / 3 四向） | 78779 / 68091-68121 | `outlineMode` + `raster.ts` 逐档实现（不用 `strokeText`） | ✅ 一致 |
| 描边偏移 | `Font+1384/+1388` **初值 1/1** | 78780-78781 | `outlineDx/outlineDy`（默认走 `defaultWinStyle`） | ★**不一致 → 已修**（默认曾是 2/2） |
| 填充/描边色 | `Font+1360`（默认 `0xFFFFFF`）/`+1364` | 78891-78892 | `0x76`/`0x77` handler → 字段 21664/21665 | ✅ 一致 |

**判定：两处实质偏差** —— 主因是抗锯齿；另一处是**描边偏移的默认值**（引擎初始化 1/1，emulator 的
`defaultWinStyle()` 写的是 2/2 ⇒ 没有脚本显式设过偏移的文本，描边副本会偏出去 2px）。字重/字号/面名/颜色都对得上。

顺带核过、**不是**问题的一项：`Font+1236`（`lfWidth`）初值 **-12** = 高度 24 的一半 —— 对 CJK 字体来说
`tmAveCharWidth` 本来就是半角宽（= 高度/2），GDI 的字体匹配不会因此做横向压缩，与浏览器自然比例等价。

## 为什么本机的引擎是"无 AA"

```
真游戏 base 的 SYS4REG.INI：只有 [display]/[sound]/[message]/[system] 段，**没有 [set] 段**
  ⇒ GetConfig("set:EnableAntiFont") 缺键 ⇒ 引擎的缺键语义是写 0 并返回 0（sub_4957F0 raw 113008）
  ⇒ 23649 的 if 不进 ⇒ 根本不调 sub_4155B0 ⇒ Font+1352 保持初始化值 0 = 无 AA
本工程 overlay 的 SYS4REG.INI：[set] 段里写的是 EnableAntiFont=0（我们首跑生成的全量文件）
  ⇒ 同样无 AA
```

注意 `message:UseAntiFont=1` **确实存在**，但它只是"门后面的值"——门关着就不生效。这条很容易看错：
**只有先看 `set:EnableAntiFont`，才能决定 `message:UseAntiFont` 有没有意义。**

另两个"看起来相关"的键在渲染侧**没有读者**（别照抄成 AA 档位）：

- `message:AntiFontLevel` → raw 23653 写 `Engine+303796`；`grep 303796` 全库只有这一处写入、**零读取**；
- `set:Menu_UseAntiFont` → 只被配置表（`sub_434D00`）读写（raw 111703 / 112525），渲染侧无读者。

## 修复

1. **配置门**（`src/engineConfig.ts`）：`applyConfigToEngine` 里按引擎语义算两键门，写**字段 21662**
   （`Font+1352` = `Engine+85296+1352` ⇒ dword `(85296+1352)/4 = 21662`，与同族的 21664/21665/21667 一致）。
2. **样式携带**（`src/vm/handlers/msgwin.ts`）：`globalTextStyle().main.antiAlias = (字段21662 & 1) !== 0`；
   注音共用同一把开关；`FontSpec.antiAlias`（`text/layout.ts`）与 `DrawStringStyle.antiAlias`（`vm/native.ts`）
   把值带到两条渲染路径。快照语义与其它全局样式一致（入队时钉住）。
3. **锯齿字形化**（`src/renderer/text/raster.ts` 的 `thresholdAlpha`）：`antiAlias === false` 时，
   画完整窗后把画布 alpha 阈值化（`>= 128` 记满 255、否则 0，**颜色不动**）——
   canvas 没有"关文字 AA"的开关（`imageSmoothingEnabled` 只管图像缩放），所以按引擎的**结果**对齐：
   GDI 锯齿字形就是"每像素非 0 即满不透明"。消息窗（`rasterFrame`）与 `0x204/0x205` 直绘
   （`TextureCache.drawString`）两条路径同口径。
4. 阈值取 128（≈ GDI 在 50% 覆盖率处取整）。

## 判据

```text
npx tsx --test test/text-aa.test.ts        → 5/5 绿
   · 两键门五种组合（含缺 [set] 段 / 缺键）
   · AntiFontLevel 与 Menu_UseAntiFont 不影响 AA
   · 样式携带（字段 21662 → globalTextStyle().main.antiAlias）
   · 阈值化：127→0、200→255、颜色通道不动、幂等（无中间值时不回写）
npm run verify                             → 见 changes.md 的收尾实测
```

## 仍未做 / 待确认

- **E4（观感）**：本机没有"真机同帧对照"的可复现手段（截屏对照要人眼），所以这条的最终判据是
  **用户在真机/GUI 上的观感确认**；票据里不假装做过 E4。
- AA 的**灰度级**（`Font+218500`）、字形度量探针、内存字形缓存仍是"只记录、不追效果"（用户口径 R7b）。
- DrawMode==1（D3D 软件 AA 字形）那条路径的宿主实现未做：本机 `DrawMode=0`（overlay INI），
  且该路径只在 `set:DrawMode=1` 时启用。

## 2026-09-15

## ★第 3 轮调查（2026-09，用户提供 4 张对照截图后）——「粗」的主因不在抗锯齿，而在**字重/字面**

### 用户提供的信息

- 用户实测结论：**字体没有变化**（第 1 次变更的抗锯齿修复不改变观感）；同一轮里**存档缩略图已正常显示**（T-0036 收工）。
- 用户答复的口径：偏粗出现在 **ADV 正文** 与 **设置界面（OPTION/CONFIG）** 的文字（都是**运行时 canvas 文本**，不是汉化图片里的字）；
  对照对象是 **汉化版真机**（已装 Amayui CN、INI 里 `message:Font=Amayui CN`），即两边**同族同字重**。
- 用户提供 4 张截图：模拟器 ADV / 真机 ADV / 模拟器存档列表 / 真机存档列表（会话附件，不入库）。

### 量化测量（方法可复现，结论不依赖 .tmp）

方法：对同一行台词做「文本行分带 → 墨迹（亮度 > 170 的像素）覆盖率 + 横向游程中位/均值」统计。
★.tmp 里的对照图只是**临时产物，不作为证据**（票据证据 = 下面的数字与口径，复现步骤就在这段里）。

截图来源：
pm run shot -- --gamestart 产出的 `.tmp/<name>-9-sn0000-hover-out.png`（1600×902，与用户截图同场景），
真机侧用用户提供的 ADV 截图（1602×958）。三张图都经过相同的 125% 显示缩放。

| 样本 | 末行覆盖率 | 横游程中位 / 均值 | 备注 |
|---|---|---|---|
| 模拟器 · **真 Bold 面**（现状） | **0.330 – 0.398** | 5 / 7.1 – 7.6 | `[font] Amayui CN#700 ← Amayui-CN_cnjp-Bold.ttf` |
| 模拟器 · 强制 **Regular 面** | 0.220 – 0.245 | 3 / 4.6 | 实验：把 `weight` 临时钉成 400 |
| 模拟器 · 强制 **AA**（关掉阈值化） | 0.185 – 0.245 | 3 / 4.6 | 与「Regular+阈值化」**同值** ⇒ 阈值化本身在这条度量上不可见 |
| **真机**（用户截图） | **0.257** | 4 / 5.87 | 落在两者之间、明显偏向 Regular 一侧 |

**结论**：

1. **抗锯齿不是可见主因**：AA 开/关测得的墨迹与笔画统计完全相同（这也解释了用户「没有变化」的观感）。
   ⇒ 第 1 次变更保留（它按引擎语义是对的、且有守卫），但**不能指望它解决观感**。
2. **主因在字面/字重**：模拟器的真 Bold 比真机重 ~1.3×（笔画）/ ~1.5×（墨迹）；真机落在我们
   Regular 与 Bold 之间、偏向 Regular。用户在 OPTOIN/CONFIG 与 ADV 两处都看到同一现象，符合「一支字重主因」。

### 引擎侧已核到的事实（用于收敛假说）

- 脚本确实请求粗体：`i2bd 1` 出现在 ADV 旁白与 CONFIG 各处（`src/SN0000.txt:492/3242`、`src/$1$SC0330.txt:1121/17654`、
  `src/CONFIG.txt:158/209`、`CONFIG1.txt:2901/3030/3059` …）；`Font+1248` 初值 0（raw 78854），`0x2BD` 写 700（raw 33394/33399）。
- 引擎**从游戏数据里把字体当内存字体加载**：`AddFontMemResourceEx`（raw 23643），字体文件列表在 `Engine+489824`
  那个 vector 里（raw 22172-22237 的构建过程）——补丁会把 **Regular 与 Bold 两个文件**都放进游戏目录
  （`patch/patch.config.json:468-469`），`scripts/register-font.js` 也注册两面（会话级）。
- 未收敛的假说（**下一步要判定的就是这个**）：
  - **(a) 该机 GDI 只看到 Regular 面** ⇒ `lfWeight=700` 退化成 Regular（GDI 不做合成加粗）；
  - **(b) GDI 用的是真 Bold，但网格拟合/锯齿渲染比浏览器细 20-30%**；
  - **(c) ADV 文本用的不是主 LOGFONT**（`sub_459F40` 重建的 HFONT 集合 / 绘制路径用的句柄），
    需要读 raw 70941-71060（重建）与 ADV 绘制路径（68478/71172 一带）。

### 下一步（已计划，未开工）

1. **加一个可切换策略**：`emulator.config.json` 里 `fonts.bold: 'face' | 'regular'`（默认 `face` = 现状），
   决定 `lfWeight=700` 落到真 Bold 面还是 Regular 面；请用户 A/B 一次即可判定 (a) 还是 (b)。
2. 若 (b) 成立：查我们的光栅化是否把笔画「撑胖」（DPR=1.25 下阈值化可能让 stem 向上取整），
   必要时改成「按逻辑像素 1× 光栅化 + 整数对齐」。
3. 若 (c) 成立：读 `sub_459F40` 重建了哪些 HFONT、ADV 绘制用的是哪一个（`+1084`/`+1100` 之别）。

## 2026-09-15

## 2026-09-15 · 第 4 轮：引擎侧核对「加粗到底由谁决定」（本轮只读引擎，未改产品代码）

用户假说：真机上「加粗面」与「常规面」像是**独立枚举**出来的配置项 ⇒ 引擎内部应该不会尝试直接加粗，
只有用户选了粗体字体才会粗。

### 引擎代码核对（结论：假说只有一半成立）

1. **面名只有一个来源**：`GetConfigString("message:Font")` → `sub_465390(Font, …, Source)` → `strcpy_s(Font+1260)`
   （注音面另有硬编码 `"ＭＳ ゴシック"` → `Font+1320`；运行时还能改面名的是 `0x1A5` / `0x2FE`）。
   配置键注册在 raw 111454-111458，缺省值就是 `asc_52686C = "ＭＳ ゴシック"`。
   **全库带 `Font` 的配置键只有 7 个（raw 4236/4334/4335/4387/4388/4389/5006），没有任何一个是"粗体面名"**
   ⇒ 加粗不可能靠换面名实现。
2. **字重只有一个表达**：`0x2BD`（`sub_426200` raw 33385-33402）非 0 ⇒ `Font+1248`（主模板 lfWeight）与
   `Font+218516`（当前字重）都写 **700**，否则都 0；初值 **0 = FW_DONTCARE**（raw 78854，**不是** 400）。
   `0x2BE` 同理写注音的 `Font+1308` / `+218588`。
3. **引擎不做合成加粗**：没有双画、没有 lfWidth 加粗、没有字形膨胀；`sub_459F40`（raw 70941-71191）
   只是把模板 qmemcpy 成 8 个即席 LOGFONT（含竖排 `'@'`、escapement 2700/1800、面 `"AGE Extend"` 的变体）
   再 `CreateFontIndirectA`，`lfWeight` 一路照抄。绘制全文只有两处 `TextOutA`（raw 68095/68587）：
   档位 3 连画 **5 次**是**描边**、不是加粗。
4. **所以决策权在 GDI 的族内选面**：700 只是"请求"；该族有粗体面就选它，没有就静默用常体
   （GDI 不合成、不警告）。这与补丁说明第 6 步「两份族名同为 Amayui CN、系统按 Regular/Bold 配对；
   只装一份时『加粗』那部分不会变粗」是同一件事。

### 顺带查实（可当探针用）

字体选择器的候选表 `Font+201664`（`0x2DC`/`0x2DD`/`0x2DE`）是 `EnumFontFamiliesExA` 的**族**列表：
`Proc`（raw 74146-74171）只收 `lfCharSet == 0x86` 且非光栅的族，`sub_45F6C0`（raw 75028-75100）
再用探针字 `Font+218524` 把缺这些字形的族删掉 ⇒ **一个族一条，粗体不会作为独立条目出现**。
（顺带：`sub_45F6C0` 的调用点是启动配置应用段 raw 23800，喂给它的名单来自配置对象 vtable+48。）

⇒ **真机上的零成本判定（假设 (a) 还是 (b)）**：打开游戏字体选择器，看 `Amayui CN` 出现**一条**还是**两条**。

- **一条** ⇒ 两份 TTF 在 GDI 里配成了一族 ⇒ 真机的 `i2bd 1` 确实选到 Bold ⇒ (a) 不成立，
  偏细只能归因光栅化（(b)：查 DPR / 笔画取整）。
- **两条** ⇒ 该机没配对 ⇒ 700 静默退回常体 ⇒ (a) 成立，且此时 emulator 的「700 → 真 Bold 面」
  就是**过度加粗**，该走 `fonts.bold: 'regular'` 一类策略。

随包资产本身是配得成一族的：两份 TTF 的 `name ID 1` 都是 `Amayui CN`、`OS/2.usWeightClass` = 400 / 700、
`ulCodePageRange1` 一致（含 cp932）；这一前提已被 `test/font-bold-face.test.ts` 钉成棘轮。
⇒ (a) 若要成立，只能是**该机的安装状态与随包资产不符**（缺装 Bold、装了旧版、或字体缓存口径不同）。

### 落库

- 第二层：新增 `bold-is-lfweight-face-mapping`（whySilent 写两种静默错法：(a) 额外合成 ⇒ 过粗、
  (b) 只登记一面 / 把 Bold 当独立族 ⇒ 加粗静默失效）；
- 第一层：`Font+1252`（打包 lfItalic/lfUnderline/lfStrikeOut/lfCharSet，初值 0x1000000 ⇒ lfCharSet=1）
  由 tentative 转 confirmed；`Font+218516` 语义收敛为「当前字重」；`0x2BD` 的 purpose 订正（旧措辞「重排文本」是错的）；
- 主题文档：`docs-new/03-engine/adv-text-rendering.md` §1 新增「字重（加粗）落在哪：面名 × lfWeight 的分工」，
  §7 补一段指向（并写明「真机更细」不能归因到「引擎不加粗」）。

## 2026-09-15

## 2026-09-15 · 第 5 轮：量化判定「真机那次 700 有没有落到 Bold 面」——**没落到**（只读 + 复现测量，未改产品代码）

本轮用户答复：① 真机 ADV 对照截图是**两份字体都装好、并完全退出重启游戏之后**拍的；
② 装 700 后 ADV 有没有肉眼变粗「记不清」；③ 5 个字体分类**都设成了 Amayui CN**。

### 独立复现（用第三个光栅器，顺带把「DPR/取整撑胖笔画」这条假说排除）

用 PIL/FreeType 对**同一句台词**「ってきた世界の現神」按票里同一口径（墨迹 = 亮度 > 170；
文本行 bbox 覆盖率）重测：

| 渲染方式 | Regular | Bold | Bold/Regular |
|---|---|---|---|
| 30px 无 AA（hinted mono，最接近 GDI） | 0.241 | 0.379 | **1.57** |
| 30px AA + 阈值 128 | 0.236 | 0.345 | 1.46 |
| 38px AA + 阈值 128（≈DPR 1.25 的设备网格） | 0.221 | 0.350 | 1.59 |
| 38px 阈值化 → 降采样回 30px（≈我们的截图路径） | 0.209 | 0.345 | 1.65 |

- 与票里的实测**互相印证**：emulator 侧量到 Regular 0.220-0.245 / Bold 0.330-0.398，这里 0.209-0.241 / 0.345-0.379
  ⇒ 方法可靠，跨光栅器/DPR/提示网格的噪声 **≤8%**。
- 同一个族的两面覆盖率差 **1.46-1.59×**，比噪声带大一个量级 ⇒ 这个度量**足以判定用的是哪一面**。
- ★**「DPR/取整把笔画撑胖」被排除**：38px 设备网格（0.221 / 0.350）并不比 1× 无 AA（0.241 / 0.379）更胖，反而略轻。
  票里计划第 2 条（"按逻辑像素 1× 光栅化 + 整数对齐"）**不是**这条症状的解。

### 判定

真机 0.257 落在 **Regular 档（0.209-0.245）**，离 Bold 档（0.345-0.398）差 26-35%（≈2 倍噪声带）
⇒ **那张真机截图里，ADV 正文实际是用 Regular 面画的**：真机上那次 `i2bd 1` 的 700 **没有落到 Bold 面**
（GDI 静默退回常体，无错误、无警告）。

方向上与用户最初的怀疑一致（真机的加粗确实"没生效"），但**不是引擎机制的问题、也不是 emulator 面策略的问题**：

- 引擎侧：加粗 = `lfWeight=700` 的**请求**，落哪一面由 GDI 族内选面决定；族里没有可用的 Bold 面就静默退回常体。
  台账 `bold-is-lfweight-face-mapping`（本轮新增）、主题文档 `adv-text-rendering.md` §1「字重（加粗）落在哪」。
- 资产侧：随包两份 TTF 的 `name ID 1` 同为 `Amayui CN`、`OS/2.usWeightClass` 400/700、码页含 932；
  `res/fonts/Amayui-CN_cnjp-Bold.ttf` 与提交里的 LFS oid 一致（sha256 `6f336fad…`）⇒ **分发的那份就是这个**，
  在 Windows 上按定义能配成一族。守卫 `test/font-bold-face.test.ts`。
- ⇒ 真机没粗只剩**安装/缓存状态**这一类原因：仓库自己的 `docs/font-build.md` §8.5.2 / §8.6 就写着这个坑 ——
  「安装后若仍不生效：完全退出游戏 + **重启 FontCache 服务（或重启电脑）** 再测」；
  另外 `%LOCALAPPDATA%\Microsoft\Windows\Fonts\` 里若留着旧版（如缺 932 码页声明的早期构建，
  见 §8.6 的 `.tmp/font-backup/*.sarasa-nocp.bak`），游戏（日文 locale ⇒ `lfCharSet=DEFAULT_CHARSET` ⇒ 932 字符集选面）
  就看不到 Bold 面。

### 真机侧的两个零成本复核（等有环境时）

1. 游戏字体选择器里 `Amayui CN` 出现**一条**还是**两条**（引擎候选表 `Font+201664` 是 EnumFontFamiliesExA 的**族**列表）；
2. 重装两份字体 → **重启 FontCache 服务 / 重启电脑** → 完全退出游戏再进 ⇒ ADV 正文应**明显变粗**
   （按本票方法量同一台词，覆盖率应从 ~0.25 跳到 ~0.35-0.40）。

### 对票的处置

- 「emulator 更粗」这条症状的**基准不成立**：参照图来自「真机加粗没生效」的状态；
  emulator 现在的行为 = 引擎 + 正确安装的资产所应有的行为。
- 可选（由用户决定）：`fonts.bold: 'face' | 'regular'`（默认 `face`）只用于**在模拟器里复现"加粗没生效"的真机**，
  不是修 bug；原版日文游戏 ADV 用的是 メイリオ Bold（真粗体），所以 `regular` 与「对照原版」方向相反。

## 2026-09-15

### 第 5 轮·稳健性补充（排除「真机量的是另一行台词」这个残余风险）

同一族两面各测 8 句不同台词的墨迹覆盖率（30px 无 AA）：

| | 逐行范围 | 行间散布 |
|---|---|---|
| Regular | **0.216 – 0.277** | 0.060 |
| Bold | **0.341 – 0.414** | 0.073 |

两面区间**不重叠**（Regular 最大 0.277 < Bold 最小 0.341），行间散布（≈0.06-0.07）小于两面间距（≈0.13）。
⇒ 真机实测 **0.257 落在 Regular 区间内、离 Bold 区间差 ≥25%** —— 即使真机当时量的是**另一行**台词，结论也不变：
**真机那次 ADV 用的是 Regular 面，700 没落到 Bold 面。**

## 2026-09-15

## 2026-09-15 · 第 6 轮：用户答「真机只有一条 Amayui CN」——★**这条探针不成立**（本机实测订正）

用户答复：真机字体选择器里 `Amayui CN` **只有一条**。

### 实测 1：本机的字体安装状态（注册表 / 用户字体目录）

`HKCU\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Fonts` 里两条都在：

```text
Amayui CN (TrueType)       => %LOCALAPPDATA%\Microsoft\Windows\Fonts\Amayui-CN_cnjp.ttf
Amayui CN Bold (TrueType)  => %LOCALAPPDATA%\Microsoft\Windows\Fonts\Amayui-CN_cnjp-Bold.ttf
```

### 实测 2：照抄引擎口径枚举候选表 —— **两面都装好，候选表也只有一条**

口径完全按 `Proc`（raw 74146-74171）：`EnumFontFamiliesExA` + `lfFaceName=""` + `lfCharSet=0x86`(GB2312)
+ `lfOutPrecision=8`(OUT_TT_ONLY_PRECIS)，回调只收 `fontType != 1`（非光栅）且 `lfCharSet == 0x86`。

| 观测 | 结果 |
|---|---|
| 候选表条目总数（本机，charset 0x86） | 269 |
| 其中 `Amayui CN` | **1 条**（`w=400`；另有 `@Amayui CN` 竖排变体，被 `Proc` 的首字符判断滤掉 —— 与 `unk_52691C` 的用法吻合） |
| DEFAULT_CHARSET 全枚举 2925 条记录里，同一个 face 名以两种字重出现的族 | **0 个**（字重不同的族是**族名自带字重**，如 `Yu Gothic UI` / `Yu Gothic UI Semibold`） |

⇒ **「选择器里出现一条」是必然结果**，与 Bold 面在不在无关。票里把「一条/两条」当 (a)/(b) 判别器，
只有「两条」那半边成立（那需要两面的**族名不同**）；「一条」**推不出**「两面配成了一族」。

### 实测 3：★同机直接问 GDI「700 落到了哪一面」——**落到了 Bold**

新增工具 `tools/font-face-probe.ps1`：按 (face, lfWeight, lfHeight, lfCharSet) 建 `HFONT`，用 `GetFontData`
把 GDI **实际选中的那份字体数据**取回来，比对 sha256 与 `name` 表 `id6`（PostScript 名唯一标识一份文件）。

| 请求 | 取回的面 | sha256[:8] | bytes |
|---|---|---|---|
| `Amayui CN` lfWeight=400（charset 1 / 128 / 134 三次同值） | `id2=Regular` / `id6=Amayui-CN` | `b8b8cb3a…` | 24,133,900 |
| `Amayui CN` lfWeight=700（同上三字符集） | `id2=Bold` / `id6=Amayui-CN-Bold` | `6f336fad…` ⇐ 与随包 `res/fonts/Amayui-CN_cnjp-Bold.ttf` 的 LFS oid 一致 | 23,978,368 |
| 对照 `MS Gothic` lfWeight=400 与 700（Windows 自带、**无** Bold 面） | 两次**同一份** | `7c9ecef1…` | 8,990,124 |

⇒ 在这台机器上，GDI 的「700 请求」**确实**选到了 Bold 文件；而 `MS Gothic` 的对照说明
「静默退回常体」这件事**这条探针看得见**（不是探针失效）。

### 结论（对票的影响）

1. 「真机只有一条 Amayui CN」**既不证明也不否定**假设 (a)：装好两面的机器上同样只有一条。
   ⇒ 票里那条「零成本判定」作废，改用 `tools/font-face-probe.ps1`。
2. 同机探针显示 700 有 Bold 可用 ⇒ **若用户的「真机」就是这台 PC**，第 5 轮据 0.257 得出的
   「真机那次用的是 Regular 面」与它**冲突**，必须重测（同帧同缩放、并核对那句台词在脚本里确实是 `i2bd 1`）。
   若「真机」是**另一台**机器，则在那台上跑同一探针即可一次判定。
3. 已同步订正：`analysis/engine-capabilities.json` 的 `bold-is-lfweight-face-mapping`
   （`whySilent` + `emulator.note`）、`docs-new/03-engine/adv-text-rendering.md` §1 推论 2/3 与 §7。

### 复现命令

```powershell
pwsh -NoProfile -File tools/font-face-probe.ps1                     # 选面探针（默认 Amayui CN，400/700）
pwsh -NoProfile -File tools/font-face-probe.ps1 -Face "Amayui CN" -List   # 引擎口径的候选表枚举
pwsh -NoProfile -File tools/font-face-probe.ps1 -Face "MS Gothic" -CharsetProbe 1   # 对照组
```

## 2026-09-15

## 2026-09-15 · 第 6 轮·补：引擎**只注册一份内存字体** ⇒ 正文面名走系统字体表（堵掉最后一条逃生通道）

「本机 700 有 Bold 可用」之后，唯一还能解释「真机却更细」的逃生通道是：引擎把**自己的字体文件**用
`AddFontMemResourceEx` 注册进进程（那么游戏用的就不是系统装的那两份）。已核：

| 事实 | 证据（raw） |
|---|---|
| 全库**只有一处** `AddFontMemResourceEx` 调用 | grep 仅 raw 23643（函数 `sub_417800` raw 23466+，只由启动处 raw 142315 调用一次） |
| 它只注册配置字体表的**第 [0] 项** | raw 23606-23628（取 `Engine+489824` 的 `[0]` → FileSource 打开/读取整份）→ raw 23643 |
| 那份文件是游戏自带的 `AGE-EXTEND.TTF`，**不是** Amayui CN | `install/SYS4INI.BIN` 里唯一的 `.TTF` 名 = `AGE-EXTEND.TTF`；散文件 `install/AGE-EXTEND.TTF`（323,728 B）；面名 `AGE Extend` = raw 4455，被 `sub_459F40` 的即席 LOGFONT 用（raw 71097-71099） |
| ⇒ `message:Font`（汉化版 = `Amayui CN`）**不在进程内注册** | 同上：正文面名只能由**系统字体表**解析（也解释了选择器里能看到 Amayui CN —— 内存字体本来就不被 `EnumFontFamiliesExA` 枚举） |

⇒ 逃生通道关闭：真机正文「400/700 落哪一面」**完全由系统安装状态决定**（正是 `tools/font-face-probe.ps1` 探的那个），
而本机探针给出的是 **Bold**。

### 因此现在的判定与收口动作

- 本机 = 真机（用户 2026-09-15 确认）+ 两份 TTF 都装好 ⇒ 真机 ADV 的 `i2bd 1` 文本**本该**是 Bold。
- 第 5 轮那张「真机 0.257 ≈ Regular」的参照图，最可能是 **Bold 面生效之前**拍的（Bold 面 2026-09 才加入分发；
  装完还要重启 FontCache/电脑 + 完全退出游戏，见 `docs/font-build.md` §8.5.2/§8.6）⇒「emulator 更粗」的参照基准过期。
- 收口动作（不需要新分析）：按验收第 4 条②，在**当前**状态下重拍同帧对照 —— 真机 ADV（`i2bd 1` 的同一句台词）
  应与模拟器的 0.33-0.40 同档；若真机仍停在 ~0.25，再回到探针查该进程的字体状态。

### 落库

- 第二层新增 `text-face-source-memory-vs-system`（whySilent 写清「缺失/装错时不报错，选择器也看不出来」）；
- 第一层新增 `sub_417800`（PARTIAL：启动配置应用 + 唯一的进程内字体注册）与字段
  `Engine+0x77960/0x77964/0x77968`（字体文件索引表 begin/end/cap）、`+0x77970`（文件名表）、
  `+0x4E348`（注册标志）、`+0x4E368`（注册句柄）。

## 2026-09-15

## 2026-09-15 · 第 7 轮：用户给三图（老汉化 / 新汉化 / emulator）——**量化后结论翻转：emulator = GDI Bold，真机 = 常体档**

用户信息：老汉化版本用的是 `AGE-EXTEND.TTF`（引擎唯一在进程内注册的内存字体），观感**明显更瘦**；三图依次为老汉化、新汉化、emulator。

### 方法（可复现，数字即证据；`.tmp` 里的图不作证据）

- 口径沿用本票既有方法：亮度 > 170 记墨迹；取**首行**（三图首行都以「激烈的」开头 ⇒ 同内容）算墨迹/字、横游程（笔画宽）、并以**字距归一化**排除字号差。
- 新增 **GDI 参考渲染**：用引擎同款调用（`CreateFontIndirectA` + `TextOutW`，face=`Amayui CN`，白字、深灰底）
  把**同一句台词**渲染成位图，再用同一 Python 口径量 —— 这样"面/字重"的判据不再依赖别的光栅器。

| 样本 | 行高 | 字距 | 墨迹/字 | 游程中位 | 归一化笔画 | 中间/核心 | 文本白峰值 |
|---|---|---|---|---|---|---|---|
| 真机 · 老汉化（AGE Extend） | 34 | 36.2 | 415.8 | 4 | 0.111 | 0.18 | 230-231 |
| 真机 · 新汉化（Amayui CN） | 34 | 38.5 | **417.4** | 4 | 0.104 | 0.17 | 230-231 |
| emulator | 37 | 37.4 | **561.0** | 5 | 0.134 | 0.06 | **255** |
| GDI 400 · `Amayui CN` h-38 | 36 | 37.8 | 368.3 | 3 | 0.079 | 0.17 | 255 |
| GDI 700 · `Amayui CN` h-38 | 37 | 37.9 | **544.5** | 5 | 0.132 | 0.06 | 255 |

（同尺度 IoU：emulator 的首 3 字与 GDI 700 = **0.891**、与 GDI 400 = 0.502；真机新汉化与 GDI 400 = 0.546、与 GDI 700 = 0.431。
另：引擎真实 LOGFONT 还带 `lfWidth = lfHeight/2`（raw 71056-71058），按该值渲染 GDI 会把字距压到 28.9-29.9
—— 与真机字距 38.5 不符，说明引擎实际走的是 `lfWidth = 0` 那支（raw 71061-71064 的分支），故参考图取 lfWidth=0。）

### 结论

1. ★**emulator 与 GDI 的 Amayui CN Bold（700）三项重合**（561/0.134/0.06 vs 544/0.132/0.06，IoU 0.891）
   ⇒ emulator 侧「700 → 真 Bold 面」这条实现**本身是对的**。
2. 【★第 8 轮已订正：见下方第 8 轮 —— 该比较未计入「填充色 ≈230」与「引擎输出更软」，计入后真机实为 Bold 档】★**真机两个版本（老汉化 / 新汉化）在笔画上测不出差别**（墨迹/字 416 vs 417、游程中位都是 4），
   且都停在 GDI 400 与 GDI 700 之间、**偏常体**一侧；边缘特征（中间/核心 0.17）与 **GDI 400 完全一致**
   （Bold 档是 0.06）⇒ 真机 ADV 正文的最终像素落点**更像常体**。
   ⇒ ★**用户的口径（真机两版都比 emulator 瘦）与实测完全一致**：真机两版互为**同一档（常体档）**，比 emulator 轻约 26-35%（墨迹/字 416-417 vs 561）。老/新之间若仍有观感差异，来自**字形设计（不同字体基底）+ 白度**，不是字重。
3. **模糊 / 软件缩放解释不了这个差距**：给 GDI 700 加 0.4 / 0.6 / 0.8 / 1.0 px 高斯模糊后，
   墨迹/字 544→542/533/527/518、游程中位仍是 5、归一化仍是 0.132 ⇒ 不是"被缩放糊细了"。
4. **白度是独立的第二处偏差**：真机 230-231 且**不随局部背景亮度变化**（逐行背景中位 69-76 都一样）
   ⇒ 不是"背景透出"，更像填充色/合成档（≈0.90）或引擎侧合成；emulator 是 255 纯白。
   用户「白色看着更亮」当场成立 —— 另开票跟踪（T-0042）。
5. 引擎真源三条都已核实，**排除了"引擎没请求粗体"和"GDI 选不到 Bold"两种解释**：
   ① 该页的 ADV 标准样式块确实写 `i2bd 1`（`src/SN0000.txt:491-492`，已登记进第三层台账 layout）；
   ② `message:Font` 正文面**不在进程内注册**，由系统字体表解析（台账 `text-face-source-memory-vs-system`）；
   ③ 本机 GDI 对 `(Amayui CN, 700)` 实测选到 `Amayui-CN-Bold`（`tools/font-face-probe.ps1`）。
   ⇒ 落差只能在**引擎绘制合成那一侧**（重建后的 HFONT 有没有真的用在这条正文窗、描边 5 次 `TextOutA` 的实际组合、
   以及是否存在软件缩放合成）—— 这是本票剩下唯一要查的地方，已写进 acceptance。
6. 附带确认：老汉化能把中文画出来，正因为它替换了引擎**唯一在进程内注册的内存字体** `AGE-EXTEND.TTF`
   （与 `text-face-source-memory-vs-system` 完全吻合）。

### 复现

```powershell
# GDI 参考图（引擎同款调用；改 -Weight 400/700、-Height/-Width）
pwsh -NoProfile -File .tmp/gdi-render.ps1 -Weight 700 -Height -38 -Out .tmp/t0035-shots/gdi-w700-h38.bmp
python .tmp/measure-final.py        # 三图 + GDI 参考图的同口径表
python .tmp/iou-face.py             # 同尺度 IoU（面/字重判定）
python .tmp/blur-test.py            # 模糊能不能解释差距（不能）
```



## 2026-09-15

## 2026-09-15 · 第 8 轮：★订正第 7 轮 —— 计入「填充色」与「输出软化」后，**真机也是 Bold 档**

第 7 轮的结论（真机停在常体档）是**比较口径造成的**：拿 255 纯白、设备分辨率硬边的 GDI 参考图去比
真机的 230 白、软边输出，把「暗 + 软」误读成了「细」。

### 订正用的对照表（同一口径；每行都注明它模拟的是哪一环）

| 样本 | 字距 | 墨迹/字 | 游程中位 | 归一化笔画 | 中间/核心 |
|---|---|---|---|---|---|
| 真机 · 新汉化（实测） | 38.5 | **417.4** | **4** | **0.104** | **0.17** |
| emulator（实测） | 37.4 | 561.0 | 5 | 0.134 | 0.06 |
| GDI 700 h-38（255 白，1×） | 37.9 | 544.5 | 5 | 0.132 | 0.06 |
| GDI 700 → 填充压到 **230** | 37.9 | 520.5 | 4 | **0.106** | 0.07 |
| GDI 700 → 填充 230 → **放大 1.25×（lanczos）** | 37.4 | 485.5 | **4** | **0.107** | **0.18** |
| GDI 400 → 填充 230 → 放大 1.25× | 37.4 | 298.5 | 3 | 0.080 | 0.30 |

⇒ 真机的**笔画形态三项（中位笔画 4、归一化 0.104、边缘 0.17）与「700 + 填充230 + 放大」完全重合**
（4 / 0.107 / 0.18），而常体档（3 / 0.080 / 0.30）明显不符。**真机在用 Bold 面**，emulator 的选面实现是对的。
残余：墨迹/字 417 vs 486（−14%）尚未归因（放大核 / 内部分辨率 / 阈值对暗填充的敏感度），但它不是字重问题
—— 因为中位笔画与归一化笔画都已对齐。

### 同时核实的引擎真源（两条，第一条推翻了本轮中途的一个假设）

1. **描边画在填充之前、填充最后画**（`sub_455ED0` raw 68091-68128）：先 `SetTextColor(描边色)` + 1~5 次
   `TextOutA`（档位 3 = 四个偏移各一次），再 `SetTextColor(填充色)` + 中心 1 次 ⇒ **不存在「描边把填充啃掉一圈」的减法**，
   底纹不会让粗体变细。
2. **ADV 正文的填充色是配置色，不是纯白**：`src/SN0000.txt:487` 的样式块用 `i076 (global-int f807b)` 取色，
   而 SN0000 **从不写** `f807b`；`SYSTEM4.txt:272` 先给 `ffffff`，随后 `CONFIG1.txt` 的 `label_0000dc98`
   （:3197-3213，判 `a9dd` 开关位）用 `lookup-array … (global-int adcd) (global-int 14acda)` 覆盖填充/描边色，
   而 `adcd` 是**逐角色配色表**、由 `LOADCONFIG` 从 `SAVE.DAT` `load-int` 回来（见第三层 CONFIG2/CHECKCONFIG/LOADCONFIG 条目）。
   ⇒ 真机正文色 = 这台机器的配置值（实测核心亮度 230-231）；emulator 停在 `mov f807b ffffff` 的默认白（`diag:text` 显示 填充 #ffffff）。

### 修正后的结论

- **「emulator 的文字比真机粗、白」这条观感成立，但成分是这两条**：
  ① **白度/填充色**（T-0042，机制已定位到 `f807b ← adcd[14acda]`）；
  ② **输出软硬**：真机的字是**软边**（中间/核心 0.17，与放大后的参考图 0.18 一致），emulator 走阈值化 ⇒ 硬边（0.06）
  —— 同样的 Bold 笔画，硬边 + 纯白在视觉上更"实"、更粗。
- **选面/字重不需要改**：700 → `Amayui CN` Bold 与引擎一致。
- 第 7 轮那句「真机更像常体」以及第 5 轮那句「真机退回了常体」**都作废**（同一类口径错误：跨渲染管线的绝对墨迹不可直接比）。

### 复现

```powershell
pwsh -NoProfile -File .tmp/gdi-render.ps1 -Weight 700 -Height -30 -Out .tmp/t0035-shots/gdi-w700-h30.bmp
python .tmp/upscale-test.py     # 「内部尺寸渲染 → 填充230 → 放大1.25×」对照表
python .tmp/dim-blur-test.py    # 只压填充 / 只模糊 各自的影响
```

## 2026-09-15

## 2026-09-15 · 机制定位（T-0035 第 8 轮延伸）：230 不是「填充色」，是**文字层被按 α≈0.855 合成**

### 决定性测量：按通道解混合系数

对每个文字核心像素，用**同列上方 8px 的天空色**当背景 B，解 `α = (core − B)/(255 − B)`：

| 样本 | α(R) 中位 | α(G) 中位 | α(B) 中位 | 核心最亮 |
|---|---|---|---|---|
| 真机 新汉化 | **0.838** | **0.856** | **0.866** | 232 |
| emulator | **1.000** | **1.000** | **1.000** | 255 |

- 真机三通道解出的 α 一致（0.84-0.87）⇒ 文字是**半透明合成**，不是"填充色 230"。
- 真机核心颜色是 **(233,230,228)** —— **偏暖**（R>G>B），正是"白字以 ~85% 叠在暖色天空上"的结果；emulator 是纯 (255,255,255)。
- 这一条同时解释：核心**永远到不了 255**（α=0.855 ⇒ 上限 ≈ 0.855×255+0.145×bg ≈ 232，与实测最亮 232-235 吻合）、
  固定阈值下笔画"少 1px"（边缘被压到 170 以下）、以及观感"emulator 更粗更亮"。

### 同时排除（本轮新增，都有数据）

| 假设 | 判据 | 结论 |
|---|---|---|
| 整帧色调曲线（对比压缩） | 文字**描边**（引擎纯黑 0）在真机里最暗 = **3-4**（不是 25-30）⇒ 黑没被抬起 | ✗ 排除 |
| 内部低分辨率渲染 + 放大 | 扫了内部 13/15/16/18/22/24/26/30px × 各自放大倍数（bilinear/lanczos/nearest）、以及"内部 24px + σ0.8 滤波再放大"：**每一种都留 255** | ✗ 不能产生"无 255" |
| 配置色（adcd 调色板） | 真机 SAVE.DAT 全表 1000 条、28 种取值，**没有 0xE6E6E6**；`f807b` 的 336 处 local-ptr 赋值**全部**来自 `adcd` | ✗ 排除 |
| 截图链色调 | 同一张 PNG 的窗口菜单栏含纯 255 | ✗ 排除 |
| `set:DrawMode` 缺省 | 引擎注册缺省 = **0**（raw 111732-111733），与 emulator 一致 ⇒ 真机也是 GDI 路径 | ✗ 排除 |

### 本轮新读到的引擎事实（都写进能力台账了）

- `sub_455DB0`（raw 67944-67964）：**AA 门关时**（真机正是）文字由 **GDI 直接画进"窗表面"的 DC**
  （`sub_43B460` 锁表面 → `SelectObject(HFONT)` → `SetBkMode(TRANSPARENT)`）；**AA 开时这个函数什么都不做**（走另一条路）。
- 窗表面创建：`sub_43C8D0`（raw 48229+）用 `dwFlags = 7`（CAPS|WIDTH|HEIGHT，**不带像素格式** ⇒ 跟显示格式）、
  `dwCaps = 0x840`（OFFSCREENPLAIN|SYSTEMMEMORY）；贴出用**源色键**（`sub_43B070` → 表面 vtable+116，flag 8 = DDCKEY_SRCBLT）。
- 引擎自带的截图路径（raw 47895-47989）显示表面按 dd 掩码/移位转换（16bpp 时 5bit→8bit 是移位，白会变 248）——
  所以**格式转换也产生不了 230**。

### 剩下的唯一候选（也是最可能的一条）：**行淡入被冻在中途**

台账 `msgwin-line-fade-window` 的 note 原话：本工程的 DrawItem 颜色动画**已建模**，但"**缺的是把它接到消息窗行上**"。
引擎侧 `sub_45BE20` 附近（raw 72336-72348）给每行建色窗：`sub_4ACF60(item, rec[66], rec[67/68])` 设**起始色**，
`sub_4AD0C0(item, 0, speed×fade/100, -1)` 设**时长**（`MessageSpeed=50`、`MessageFade=250` ⇒ 125ms），
终点是 `0xFFFFFFFF`（不透明白）—— **若动画没跑完，文字就停在中间 α**。

真机的这三张参考图都是在**游戏窗口非激活**（用外部工具截的、菜单还显示着）的情况下拍的 ——
日式 ADV 常见行为是"窗口失活 ⇒ 暂停帧循环"，那么**色窗就冻在 α≈0.855 上**，而且三行共享起点 ⇒ 三行同为 0.855 ✓✓。

⇒ 判定方法（零成本，一步）：**把游戏窗口点成激活、让这一页显示满（≥1 秒）再截一张**，量同一句的文字核心：
- 若变成 **255/纯白**，且笔画指标回到 emulator 的档（中位 5、归一化 0.134）⇒ 230 是"淡入没跑完"的残留，
  **T-0042 与 T-0035 的观感差异都不是渲染器缺陷**，收口动作 = 在 emulator 里把行淡入真正接到消息窗行上（本就欠的那条）。
- 若仍是 230 ⇒ 是稳定的窗口/文字层 alpha，再去读窗 alpha 的来源（`message:MesWinAlpha=8` 目前**只在 op 0x131 被读、没有任何脚本用它**，
  疑为死键；需另找）。

### 复现

```powershell
python .tmp/alpha-solve.py     # 按通道解 α（真机 0.855 / emulator 1.000）
python .tmp/sweep-lowres.py    # 内部低分辨率+放大：任何组合都留 255
python .tmp/outline-dark.py    # 描边没被抬起 ⇒ 不是整帧曲线
```

## 2026-09-15

## 2026-09-15 · 机制追查（第 2 轮）+ 字重核对：**引擎的文字经过"内部尺寸→放大"，我们的没有**

### 一、那一档（0.89）的追查结果：定位到"文字图元合成"，但**具体代码点仍未找到**

新读到的（都有 raw 锚点）：

| 事实 | 位置 |
|---|---|
| 窗对象的**烘焙样式记录**字段全表：+168 填充色 / +172 描边色 / +176 第三色 / +180 描边档 / +184 主字号 / +188 **加粗标志** / +192+196 描边偏移 / +204 注音加粗 | 烘焙 `sub_45E870` raw 74093-74112；调用点 raw 79259-79283 |
| 绘制前会把这些**逐个还原成全局字段**（`Font+1360/1364/1368/1372`、`sub_4185F0(字号)`、`Font+1248/+218516` 字重、`Font+1384/1388` 偏移） | raw 81896-81948 |
| ⇒ **颜色链到 GDI 为止都是精确的**（序章/预览都是 `0xFFFFFF`）；0.89 发生在画完之后的文字图元合成 | — |
| 引擎里**没有** gamma / present 阶段的杠杆（无 `SetGammaRamp` 一类），且同屏**美术图逐像素一致** ⇒ 不是整帧效果 | grep gamma/Flip/Present = 0 |
| 引擎确实有**逐元素绘制色**机制（op 0x203 `set-draw-color-alpha`），且脚本用 `f807d` 当参数，而 `f807d ← a9df/a9e0/a9e1` = 真机存档里的 **192/160/128**（消息窗透明度档） | SN0000.txt:3755/3763/3771、CONFIG.txt:550；真机 SAVE.DAT 实测 a9df=192 a9e0=160 a9e1=128 |

⇒ 若那条 draw color 是**按 alpha 混合**，文字应落在 192/160/128 附近（远暗于实测 227）⇒ 与"常数、与背景无关"不符；
⇒ 说明 227 更可能来自**对该图元的颜色调制（modulate）**或**表面本身的像素值**。**具体代码点仍未找到**，acceptance 第 1 条继续挂着。

### 二、★字重核对：字号对得上，**笔画仍是 1px 差**，根因是"软化"

对照物：win 9 预览样例「天結神縁ＳＡＭＰＬＥ」（两边同串、同窗、同字号；坐标已扣掉窗口 chrome 的 58px 偏移）。

| 量 | 引擎(真机) | emulator(修复后) | 结论 |
|---|---|---|---|
| 整串 bbox 宽 / 字距 | 512 / 51.2 | 503 / 50.3 | **字号一致（−1.8%）** |
| 单字 'Ｅ' bbox | 51×27 | 51×27 | **完全一致** |
| 单字 'Ｅ' 横游程中位 | 5 | 5 | **一致**（在"文字填充像素"口径下） |
| ADV 正文 墨迹/字 | 414.6 | **436.9**（修复前 560.7） | **+5.4%**（修复前 +35%） |
| ADV 正文 游程中位 | **4** | **5** | **仍厚 1px** |
| ADV 正文 边缘（中间/核心） | **0.17** | **0.06** | ★**引擎的文字更软** |

⇒ 结论：**引擎的文字是"内部尺寸渲染 → 整体放大"的产物**（软化 ⇒ 固定阈值下笔画少 1px），
而我们是在**设备分辨率**上直接光栅化（硬边）。这与"引擎把帧按 1.25× 放大到窗口"一致（`Font+218596` = 缩放因子）。
⇒ 下一步（有机制依据、可预测）：**让 emulator 的文字也走"内部尺寸渲染 + 双线性放大"**（在文字光栅层把 30px 画布按 1.25 放大到设备尺寸），
预测：边缘 中间/核心 → **0.15-0.18**、游程中位 → **4**、墨迹/字 → ~415，与引擎对齐。

## 2026-09-16 · 从 `T-0042` 转来的一条颜色来源残留

`T-0042`（ADV 正文白度）第 3 轮把"字形覆盖率 α 的写入语义"补齐后（`A = max(A_dst, α)`，
见 `docs-new/03-engine/adv-text-rendering.md` §9.1 / 台账 `text-glyph-coverage-alpha-composite`），
剩下的差**不是机制**而是**颜色来源**：

| 量 | emulator | 真机 |
|---|---|---|
| ADV 正文核心 | **225**（= α·255，中性） | **229-232（暖）** |
| 机制推得的"填充之下"的颜色 | 黑描边（`Font+1364` 默认 0）/ 黑窗底 | 暖暗色 ≈ **`(63,32,16)`** |

即：真机 ADV 文本的填充 backdrop 是暖暗色（`225 + (30/255)·(63,32,16) = (232.4,228.8,226.9)` ✓ 实测
`(233,230,228)`），而 emulator 侧该颜色是黑 —— 要么 `Font+1364`（描边色）的真机来源不是默认 0
（配置调色板 `adcd` 里选出来的色），要么它是**消息窗底色**（`ddFillSurface(surfaces[20+win], 底色)`）。
两者都属于本票的「字体/颜色配置面」。定位入口：
`INITCONFIG*/CONFIG1.txt:2890-2910`（`i076 (global f807b)` / `i077 (global f807c)` / `i078 (global f8079)`）
与 `f807b/f807c ← adcd[...]`（真机 `SAVE.DAT` 已能解开，见 `T-0042` notes）。

## 2026-09-16 · 第 10 轮（本机 macOS）：**配置派生链读通 ⇒ "暖暗描边"假设被排除**；剩一条 (α, 填充色) 待判项

### 一、`CONFIG1.txt:3196-3213` `label_0000dc98` 逐行读通（文本颜色的唯一真源）

```text
f807b = ffffff ; f807c = 0                      ← 初值（白填充 + 黑描边）
if ((a9dd & 3) == 0) goto end
  c = adcd[14acda]                              ← 逐角色配色表；14acda = 当前角色/行下标
  if (c <= 0) goto end
  if (a9dd & 1) f807c = adcd[14acda]            ← 位 1 = 描边色用配色
  if (a9dd & 2) f807b = adcd[14acda]            ← 位 2 = 填充色用配色
end:
if (a9de == 0) { f8079=1; f807a=0; f807c = f807b }   ← 单向、偏移 0、描边色=填充色
else           { f8079=3; f807a=1 }                  ← 四向、偏移 1/1
i076 f807b; i077 f807c; i078 f8079; i1a4 f807a f807a
```

真机 `SAVE.DAT`：`a9dd = 2`（**只置填充位**）、`a9de = 1` ⇒
**`f807c = 0`（描边是纯黑）**、`f807b = adcd[14acda]`（暖白）、`f8079 = 3`、`f807a = 1`。

⇒ 第 8 轮/T-0042 一直挂着的候选"真机填充之下是暖暗色 `(63,32,16)`（= 描边色）"**被排除**：
描边色是 0，那个 `(63,32,16)` 只能是**场景/暗边**像素。真机文字核心的暖调只能来自**填充本身**
（逐角色配色表里的暖白）与合成档 α。

已经落库：`docs-new/03-engine/adv-text-rendering.md` §10（派生链表 + 两条路径的合成口径 + 待判项）；
CONFIG1 台账新增 `layout` 段（3196-3213）、一条 gotcha、一条 invariant；能力台账
`text-glyph-coverage-alpha-composite` 的 note 记了待判项。

### 二、★新的待判项：(α, 填充色) 在两条路径上给出**不一致**的组合

| 路径 | 合成口径 | 实测约束 |
|---|---|---|
| `0x204` 槽（设置界行标签/行值；填充与描边**都是白**） | A8R8G8B8 纹理 ⇒ 逐像素 α ⇒ **两次** α：`α·表面RGB + (1−α)·场景` | `255α + (1−α)·65 = 233` ⇒ **α ≈ 0.884（= 225/255，cov=15）** |
| 消息窗（ADV 正文 / 样例预览；描边黑） | dd 路径**色键**填面 + 色键贴出 ⇒ 不看 alpha ⇒ **一次** α：`α·填充色` | `α·填充色 = 227..233` ⇒ α=0.884 则填充色 257..264（**不可能**）；α=0.941（满覆盖 240）则填充色 241..247（**暖白，合理**） |

两个自洽解：① α=225/255 + 白填充（= emulator 现状，数值恰好落在真机区间：槽 233 ✔ / 消息窗 225 vs 真机 226-228 ✔）；
② α=240/255（GGO_GRAY4 满覆盖）+ 逐角色配色表的暖白填充（更贴脚本语义：`a9dd=2` 时填充就是配色色）。
**判别所需**：真机 `SAVE.DAT` 的 `adcd[14acda]` 实际值（本工程已能解 `format=3`）+ 同帧 ADV 像素。
⇒ 在拿到它之前**不动** `TEXT_FILL_ALPHA`：单独把 α 改成 240/255 会让白填充的模拟器从 225 变 240，
反而离真机 233 更远。

### 三、emulator 现状核对（本机实测，作为"链已对齐"的证据）

```text
$ npm run diag:text
  字号/字重     : main=30px weight=700 ruby=10px
  颜色          : 填充 #ffffff / 描边 #000000
  描边档位/偏移 : mode=3 dx=1 dy=1
  text win=9 ... outline=3 bg=- 行=1 字=10 已显示=10
```

⇒ 与 §1 的派生结论**逐项一致**（`a9de=1` ⇒ mode=3 + 1/1；描边黑；填充取默认白 = 本机 overlay 的
`adcd` 全白）。也就是说 emulator 的颜色链**没有实现缺口**：真机与本机的差别是**数据**
（真机 `adcd[14acda]` 是玩家选的暖白，本机 overlay 是白）——要复现真机的暖调，应把真机
`SAVE.DAT` 的配色表放进 overlay（`T-0030`/`T-0031` 那条既有链路），而不是改渲染代码。

### 四、仍未决：输出"更软"（acceptance 第 9 条）

已另开两条**引擎侧取证**（本会话的 fork 子代理，结果回来后再落库）：
① 消息窗底色/描边色在运行期的真实来源（本轮已基本自答：色键填面 + `a9dd=2` ⇒ 黑描边）；
② 文字是否"内部尺寸渲染 + 整体放大"（`Font+218596` 的语义、窗表面尺寸 vs 逻辑尺寸、色键贴出是否带缩放）。
本机可用的判据脚本：`.tmp/t0035-metrics.py`（ink/字、横游程中位、中间/核心，口径见脚本头注释；
注意本机截图 DPR=2 ⇒ 绝对值是 notes 里 1.25× 口径的 2×，比较要按"字距归一化"或同机前后对比）。

### 五、第 10 轮补充：「内部尺寸渲染 + 放大」假设**被证伪**（引擎侧取证）

两条独立取证（本会话 fork 子代理，只读 `engine/…_utf8.c`/`.lst` + `src/*.txt`）：

**1) 软化来源不可能是"整帧/文字的缩放"**

| 环节 | raw | 事实 |
|---|---|---|
| `Font+218592/218596` 初值 | 78765/78767 | 都是 **1.0** |
| 唯一写者 `sub_465840(Font)` | 78967-79030 | **仅当 `Renderer+1164 == 2`** 才取 `+1176/+1100`、`+1180/+1104` 作 X/Y 缩放；否则恒 1.0 |
| mode 从哪来 | 11889-11896 / 11906-11909 | `Engine+322736 = display:VirtualFullScreenType`；**内建默认 0**（111500-111502），真机 INI 无该键 |
| mode 0（真机）语义 | 114505-114800 | `+1248/+1252 = 1.0`、矩阵单位、**坐标不缩放** |
| 缩放站点是否区分文字/图元 | 75942-75960 / 71581-71584 / 71820-71823 / 72251-72254 | 坐标缩放**全部**包在 `if (DrawMode == 1)` 内（真机 DrawMode = 0）⇒ 既没有"整帧放大"，也**没有"只缩文字"的分支** |
| 消息窗离屏表面尺寸 | 73133-73180 `sub_45D660` | DrawMode == 0 ⇒ `sub_43C8D0(dd, win+20, w, h, 0)` = **逻辑/脚本尺寸**（不是设备尺寸） |

⇒ 第 8 轮"GDI 700 → 放大 1.25× 与真机三项重合"只是**指标拟合**，不是机制：
引擎在真机配置下就是 1:1，**没有**"内部尺寸 + 放大"。旁证：上一轮实测**字距** 引擎 51.2 vs emulator 50.3（−1.8%）
= 两侧有效尺度本来就一致，不需要再引入缩放。

**2) 那"引擎更软/更细"到底剩什么**

- 剩下的是**字形光栅化器本身**的差别：引擎走 GDI（`lfHeight` = **字符高** + `GetGlyphOutline` 的
  `GGO_GRAY4` 覆盖率 + grid-fit），emulator 走 canvas2D（`font = '30px'` = **em** + FreeType 的 AA）。
  两者的**边缘斜坡宽度**与**覆盖率分配**不同 ⇒ 固定阈值（亮度>170）下"墨迹/字 +5.4%、游程中位 5 vs 4"。
- ★但更可能的主因是**填充亮度**（= 本轮 §1 的配置色）：引擎的填充是逐角色配色（暖白 ≈ 241-247，乘 α 后 ≈ 213-222），
  emulator 是纯白（乘 α 后 225）⇒ 边缘像素更亮 ⇒ 超过阈值的像素更多 ⇒ **墨迹更多、游程更宽**。
  也就是说"更粗"与"更亮"是**同一个数据差**，不是两个渲染缺陷。
- 口径提醒：`.tmp` 里的原测量脚本随机器消失，"中间/核心 0.17 vs 0.06"这个数**依赖当时的口径**
  （若 core 取"恰好等于峰值"则它对填充亮度敏感 ⇒ 也可能部分是真机填充更暗造成的假象）。
  本机复现脚本 = `.tmp/t0035-metrics.py`（core 取 ≥0.95·峰值，亮度不敏感口径）。

### 六、结论（本票收口）

| 项 | 结论 |
|---|---|
| 面名/字号/lfWeight/加粗面（第 5-8 轮） | **一致**（真机也是 Bold 档；`i2bd 1` → lfWeight 700 → 族内选到 Bold） |
| 抗锯齿门（`set:EnableAntiFont` → `Font+1352`） | 门与阈值化**已实现**；运行期实际走覆盖率路径（T-0042 已证） |
| 描边档/偏移 | 一致（`SN0000.txt:486-500` → `i078 f8079`/`i1a4 f807a`；emulator `diag:text` = mode 3 / 1,1 ✓） |
| 填充/描边色 | **链一致，差在数据**：`CONFIG1.txt:3196-3213` 按 `a9dd`/`a9de` 从逐角色配色表 `adcd[14acda]` 派生；真机 `a9dd=2` ⇒ 描边黑、填充=配色（暖白），模拟器 overlay 的 `adcd` 全白 ⇒ 填充白。**这不是渲染缺陷**，要复现真机观感就把真机 `SAVE.DAT` 的配色表放进 overlay（走 T-0030/T-0031 既有链路） |
| "更软" | **证伪**为"内部尺寸+放大"；剩下的是 GDI vs canvas2D 的光栅化斜坡差（有界、不可用 canvas2D 复现）+ 填充亮度差（数据）。**不做**"照指标拟合"的改法（T-0042 第 1 次变更的教训：机制优先，不照症状对齐） |

### 七、附带核对：消息窗"底板"（纹理 + 半透明黑）在 emulator 侧是对的吗

第 10 轮的取证链指出真机的 ADV 底板 = 脚本画的 `create-mesh 19640` + `draw-texture 19640 11 …`
（纹理槽 0x11，1086×149 @(93,556)）+ `set-draw-color-alpha 19640 0 (f807d) (a9db)`（= alpha 192、
RGB 0 = 纯黑@75%）。核对 emulator 侧：

- 引擎的 `draw-texture`（`sub_422E70` → `sub_4ACE50`）把 **纹理槽 + 源/目标矩形写进"按 handle 索引的
  同一个场景元素"**；emulator 的 `op_draw_texture` 则总是建一个 **DrawItem**（handle 相同）。
- 换句话说：emulator 走的是"带纹理的图元"（tint=0、alpha=192 ⇒ 黑@75% 的纹理四边形），引擎走的是
  "同一元素的几何 + 纹理"；两者在**矩形/顶点一致**时结果相同（实测 emulator 的序章帧上半屏确实被压到
  ≈(72,31,31)，即 75% 黑底板生效 ✓）。
- ⇒ 没有发现可见缺口。**唯一残留的建模差异**：emulator 的 **mesh 路径**（`presenter.ts` 的 mesh 分支）
  只按顶点色填四边形、**不使用 mesh 上的纹理**；若将来遇到"非矩形/带纹理的 mesh"，这里会与引擎不同
  （当前语料里 `0x320` 站点都是轴对齐矩形，且带纹理的那种同时也有 DrawItem 覆盖，故无实测影响）。

## 2026-09-16 · 第 11 轮（用户提问驱动）：**"没有 SAVE.DAT 也有默认值"——是的，而且 emulator 侧已经是对的**

用户问：这部分设置（文本色/描边/角色配色）在界面里也改不了，那默认值应该是引擎内建提供的吧？
⇒ **对，全部核实**（也订正了第 10 轮 §10 里一处错判）：

| 环节 | 锚点 | 事实 |
|---|---|---|
| 标量默认 | `src/INITCONFIG2.txt:6-19` | `a9de=1` / `a9dd=2` / `a9db=0` / `a9df,a9e0,a9e1 = 192,160,128` / `a9e2=0` |
| 逐角色默认配色 | `src/CVINIT.txt`（`mov (global-int 14a8f1+n) <色>`；由 `INIT2:93` 调、`SYSTEM4:124` 触发，**每次启动**重建） | `14a8f1[0..]` = 内建默认：多数 `ffffff` + **26 条彩色**（`ffe100 84b1ff dcdcc6 71d8ff d3c3 4dbf67 2c9bdd d15e4b …`） |
| 默认 → 生效 | `src/INITCONFIG4.txt` | `3b4e[0..999]=0`、`a9e5[0..999]=0x2710`、`adcd[i] = 14a8f1[i]`（=0 则 `-1` 表未设），三张表逐个 `save-int` |
| **首次运行门** | `src/SYSTEM4.txt:71-82` | `load-int (global-int 5)`：**键 5 == 0 ⇒ `call-script 51dc // INITCONFIG`（含 INITCONFIG4）** + `save-int 5 = 1`；非 0 ⇒ `LOADCONFIG`；之后无条件 `CHECKCONFIG`（`:84`） |
| emulator 现状 | 本机 overlay `SAVE.DAT` 直接解表 | **三张表齐全**：`3b4e` 1000/1000、`a9e5` 1000/1000、**`adcd` 1000/1000（26 条非白，值与 `CVINIT` 逐条一致）** ⇒ 默认值这条链在 emulator 侧**本来就是对的** |

★**订正第 10 轮的一处错判**：那时写"真机 `adcd` 是玩家配的暖白 ⇒ 要复现就把真机配色表搬进 overlay"——
**不成立**。真机的配色表就是这套**内建默认**（配置界面不改这些值，与用户判断一致），而**旁白用 `adcd[0..3] = ffffff`（白）**
⇒ **两侧填充色都是白**。所以"暖 233"与配色表无关；它只能出在**填充之下垫的是什么**：
`SN0000.txt:3090-3098` 的**底板**（纹理槽 0x11、1086×149 @(93,556)、`set-draw-color-alpha alpha=f807d(192)/rgb=a9db(0)`
= 黑 @75%）压在场景上 ⇒ 文字背后 ≈`(63,32,16)` ⇒ `α·255+(1−α)·底板 = (232.4,228.8,226.9)` ✓ 实测 (233,230,228)；
而 emulator 的 canvas 是"不透明描边整块 + 填充按 α 叠加" ⇒ 填充之下是纯黑 ⇒ 225。
**判别（真机一步）**：只改消息窗透明度档（`a9df/a9e0/a9e1` ⇒ `f807d`），核心亮度跟着变 ⇒ 底板；不变 ⇒ 描边。
在那之前不改渲染代码（机制优先）。

过程插曲（值得记）：本轮核查时用**数字键**在 `SAVE.DAT` 里查 `adcd` 得到"0/1000"，据此一度以为默认值缺失；
`decodeSaveData` 的 int 表键其实是**字符串** `\x03` + hex8(idx)（引擎 `wsprintfA("%c%8.8x",3,idx)` 的口径）——
换成字符串键后三张表 1000/1000 齐全 ✓。⇒ 以后核对配置键一律按字符串键查。

落库：`docs-new/03-engine/adv-text-rendering.md` §10.2b/§10.2c（新增）+ §10.1/§10.3 订正；
脚本台账新增 `CVINIT`（partial）与 `INITCONFIG4`（analyzed）两条；能力台账 `text-glyph-coverage-alpha-composite` 的 note 订正。

### 第 11 轮补记（用户确认）：`192/160/128` = **背景整体遮罩**的透明度，不是文字层 alpha

用户 2026-09-16 确认：`a9df/a9e0/a9e1 = 192/160/128` 是「メッセージウィンドウ透明度」三档，对应**背景整体
遮罩**的不透明度（经 `f807d` → `set-draw-color-alpha 19640 0 (f807d) (a9db)`，遮罩色 `a9db` = 0 = 纯黑），
**不改文字层**。⇒ §10.2c 的判据可以量化：

- 切档 192→128 = 遮罩不透明度 0.75→0.50 ⇒ 露出的场景从 25% 变 50%（`f807d` 是遮罩项的 alpha 实参）；
- **若填充之下是遮罩后的场景**（引擎主张）⇒ 核心 `α·255+(1−α)·遮罩后场景` 会**明显上升 3-8 级**；
- **若填充之下是不透明黑描边**（emulator 现状）⇒ 遮罩被描边挡住 ⇒ 位移 `< 1` 级，基本看不出。

⇒ 真机一次切档即可定案（判据已写进 `docs-new/03-engine/adv-text-rendering.md` §10.2c 的表与
能力台账 `text-glyph-coverage-alpha-composite`）。仍然**不改**渲染代码。
