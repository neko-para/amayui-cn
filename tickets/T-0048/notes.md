# T-0048 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

为什么值得单独一单：① 它是 `0x109` 的逆，语义（移动系统光标）与「存档」无关、容易误判；② 1678 处语料 ⇒ 侧栏/光标记忆/回夹/居中四条路径都会踩；③ emulator 侧的「真实现」与「宿主缺口」要分开说清（引擎侧坐标/hover 生效，真实光标不动）。★未做（可另开）：给 0x10A 起助记符名（`set-mouse-pos`）需要走 `amayui-mnemonic-rename` 全流程（改表 → 生成 opcodes.json → rename-mnemonics.mjs 机械替换 src/data 的 1678 行 → 重建 + 重生成台账），本单只做实现与语义落库。 ★宿主侧那半件（浏览器不能移动真实系统光标）已单独登记为 **T-0053**（含四条替代路线的评估），本票只负责引擎侧语义与守卫。
