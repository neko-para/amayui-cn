# T-0034 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

2026-09 发现路径：做 T-0033 时跑 npm run verify，这两条红；核对后确认是**玩家数据被改**（base SYS4REG.INI 的 mtime 与 MessageSpeed 都变了），与本轮代码改动无关 —— 但按台账纪律要开票，不许放着不管。
