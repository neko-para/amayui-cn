# T-0100 · 过程文档（notes.md）

## 2026-09-20

### 轮 8 · 诊断收口（主 agent 归档）

- 全文已归档 `tickets/T-0100/design.md`（30KB，只读诊断，**未改任何仓库文件**）；现场证据 `evidence/sn0000-sc0000-boundary.md`（主 agent 从用户 GUI 实测日志摘出）。
- **本票的根因与最小修复集已写进 `acceptance`**（含 4 条改动、4 条守卫断言、E2/E3 复现路径、以及 **4 条"禁止的修法"**）。
- **H2 已排除**：`0x308` 的体 = `RegisterTouchWindow`/`UnregisterTouchWindow`（USER32.DLL）+ 写 `_this[1954]`（3 写 0 读，不碰文本/绘制/存档表）⇒ 与本症状**无因果**。（该 `0x308` 的处置另见 `tickets/T-0098/notes.md`。）
- ★**顺手订正项已无须再做**：诊断报告 §4.3 提到「`ops.ts:829` 注释把 `0x85` 写成『清行队列』与体不符」—— 那是**基于 T-0095 落地前的快照**；T-0095 已把该注释订正为「引擎 `0x301` 删绘制项区间 / `0x71` 开始新一段 / `sub_404F80`；★`0x85` 清的是回看页索引表 + 72B 记录表，与消息窗绘制项无关」（现 `ops.ts:829-834`）。**别再改一次。**
- **明确不改的（诊断给的理由）**：`exit`(0x2) 边界不 `msgwin.reset()` 是与引擎一致的；`scDetachTexture` 区间清与 `scClearDrawContainer` 都做对了；`#holdFrames` 不许打补丁。

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

来源 = 用户轮 7 的 E4 实测原话：「我验证了目前SN0000的路径，目前能够正确加载和保存，并且能够进入到第一个SC场景，但是进入后SN0000中心的文字没有被清除，可以记录等待后续处理完成后验证」。★本轮只记录，不修。
