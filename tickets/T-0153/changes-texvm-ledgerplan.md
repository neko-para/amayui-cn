# T-0153 · 台账落地计划（`changes-texvm.md` §6 → `analysis/opcode-gaps.json`）

> 由 scribe 子 agent 抄写；真源 = `engine/天结_unpacked.exe_utf8.c`（raw = 该文件 1-based 行号）。
> 计划文件：`.tmp/scribe-b/plan.json`（§6.1–6.6 主体，**已应用**）+ `.tmp/scribe-b/plan-delta.json`（收尾修正，**待应用**）。
> 生成器：`.tmp/scribe-b/gen-plan.mjs`（既有文本一律从现盘读出，零手抄）。
> 本票的台账部分**不新增 opcode 条目**，只在既有 `partial` 条目上删/改写 `missing[]` + 对齐 `note` 计数 + 落 `journal[]`。

## 1. 逐项对照（§6 ↔ 现盘 ↔ 计划）

| §6 项 | 要求 | 现盘（应用后） | 计划里的动作 |
|---|---|---|---|
| 6.1 `0x1F8`（504）| 删/改写 `raw: 122847-122848`；保留 `31171-31188` | ✅ `missing[1]` 已改写、`note` 已补 | `set.missing`（留 `31171-31188` + 改写条）+ `set.note` 追加 + `journal` |
| 6.2 `0x1FA`（506）| 删 `119585-119603`、`119591-119601` 两条；改写 `31245-31269` | ✅ 3 → **1** 条 | `set.missing`（整替为改写后的 1 条）+ `note` 3→1 + `journal` |
| 6.3 `0x23F`（575）| 删 `31627-31644`；改写 `40018-40032` | ✅ 2 → **1** 条 | 同上（`note` 2→1）|
| 6.4 `0x245`（581）| 删 `32672-32673`；保留 `32670` | ⚠ **现盘没有 581 条目** | **无动作**（空集；也不新增 —— 见 §3）|
| 6.5 `0x246`（582）| 删 `32696-32697`；保留两道门并补扩展点 | ✅ 3 → **2** 条 | `set.missing` + `note` 3→2 + `journal`；两道门在现盘是**一条**（`32679-32703`）⇒ 只补一句 |
| 6.6 `0x249`（585）| 改写 `123375-123380`；保留颜色那条并补一句 | ✅ 已改写/已补 | `set.missing`（三条整体重排，raw 收紧到 `123375-123380`）+ `journal` |
| 6.7 `0x23E`（578）/`0x208`（520）| 不动 | ✅ 578 不在册；520 未动 | **无动作** |

## 2. 我读体后对 §6.1 / §6.5 / §6.6 写法的三处收紧（都有 raw，详见 `.tmp/scribe-b/REPORT.md` §5）

1. **§6.1：`0x1F8` 写的是两格，不是三格。** `sub_4A2C10` raw 122847-122848 只有
   `Scene[5*slot+466] = -1`（= `_this + 20*a2 + 1864`）与 `Scene[5*slot+470] = 1`；
   **没有** `Scene[5*slot+467] = -1` —— 写 `467` 的是 `0x249` 路的 `sub_4A3800`（raw 123380 `v7[467] = a5`）。
   ⇒ 改写后的 `what` 写成「两格：`466` 已落地 / `470` 仍缺」，并单独说明颜色格 `467` 的写者不是这条路径。
2. **§6.5：现盘把「两道门」合成了一条**（`raw: "32679-32703"` 同时讲 `32689` 与 `32693`）
   ⇒ 扩展点那句只补一次，没有为了对齐报告而拆行。
3. **§6.6：颜色那条的 raw 在现盘是 `32716-32769`**（不是报告写的 `32750-32755`；`32750-32755` 是归一化那几行，
   已被并进同一条）⇒ 按内容定位后只追加一句，未改 raw。

## 3. 故意没做

- **`0x245`(581) / `0x23E`(578) 既不删也不建**：§6 只授权「删/改写」，而现盘无此二条；新建与否是
  T-0149 口径下的新决策（`0x245` 语料 **0** 处），不该由抄写员顺手做。`0x245` 的体我已读并记在报告里
  （`sub_4251E0` raw 32660-32677：门 `32670`、÷1000 在 `32672-32673`），需要时可直接补一条 `partial`。
- **`note` 里的缺口计数**：只在与 `missing[]` 实际条数不符时改（`506`:3→1、`575`:2→1、`582`:3→2）。
  `504`/`249` 的计数本来就对，未动其数字。

## 4. 收尾修正（`plan-delta.json`，待应用）

`0x246`（582）的 `note` 在上一轮补丁里**静默未命中**（该条写的是「仍缺 3 条」而不是「仍缺 `**3**` 条」），
至今仍写「仍缺 **3** 条分支」而 `missing[]` 只有 2 条 ⇒ `plan-delta.json` op ① 把它改成「仍缺 **2** 条」
（带前置断言，绝不会静默 no-op）。

## 5. 应用后的验证口径

```bash
node scripts/build-opcode-gaps.mjs      # ★缺口 md 不需要：renderGapMd 与现盘 md 逐字节相同（实测）
cd app/amayui-emulator && node --import tsx --test test/opcode-gaps.test.ts   # 棘轮（partial 必须有非空 missing[]）
```
