# T-0088 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

★发现路径：复核 `0x140`（曾是「语料最大的一条 deferred」）时把 `dword_55E1B4` 的全部读写点查清 —— 它**不是**函数表/对象指针/模块基址，而是一个 `__stdcall` **函数指针**（写点 `sub_4BA890` raw 141984；取值 `sub_453870` raw 65952）。⇒ 旧台账理由「间接调外部服务（`dword_55E1B4` 运行时解析，静态定不了目标）⇒ 需真机观察/动态调试」**已被推翻**：目标是静态确定的，真正卡住的是「只能由真人点选 + 两张表未建模 + 无宿主缝」。
未读到/不确定（沿用分析者披露）：① `0xDFC`（`SC0330.txt:1016`）身份未定（需按 `loadScriptFrame_40ED40` 的头部布局解 BIN 标签表 —— `install/$1$SC0330.BIN` 字符串段是混淆的）；② `sub_406220` 只读了开头 raw 11594-11615；③ `sub_455000`/`sub_454FA0` 未读体；④ AGERC 资源模板 8 的控件清单是从代码引用的 ID 反推的（`.rsrc` 未读）。
