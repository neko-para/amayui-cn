# T-0103 · 过程文档（notes.md）

## 2026-09-22 · ★E4 通道已打通（本票"帧级连拍"缺的不再是工具）

本轮在 `T-0029` 的收尾里证实：**本机可以真跑 Electron 界面**，两个跑手都有现成入口：

```bash
cd app/amayui-emulator
npm run shot   -- --gamestart --name t0103-gs          # 分段截图（每步一张，落 <仓库根>/.tmp/）
npm run record -- --scenario tools/scenarios/gamestart.json --out .tmp/t0103-record.jsonl --centered
npm run replay -- ../../.tmp/t0103-record.jsonl        # headless 复现（`engine 段逐帧相等` 的判据）
```

★两个坑（`T-0029/notes.md` §「轮 12」记了细节）：① `record` **必须加 `--centered`**，否则窗口被贴到屏幕外、
`record.cjs` 报"未找到游戏窗口"（trace 只有 1565 B）；② `--out` 按**仓库根**解析、`replay` 的轨迹路径按**包目录 cwd** 解析。

⇒ 本票判据 ① 要的"用存档 78 跑 `npm run shot` 连拍、把每个元素落成'第 N 帧 / 期望 vs 实际'"**现在可做**；
仍按用户要求**留到最后**处理（且第 1/4 段很可能就是 `T-0091` 的两条门）。

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

来源 = 用户轮 8 原话：「SN0000到SC0000的专场（章节切换动画）似乎有很多不一致的地方，这个有点难以描述，先记录，最后再处理」。★用户明确要求**最后再处理** ⇒ 本轮只登记。

## 2026-09-21

## 2026-09-21

## 用户实测补充（轮 9）：**逐段现象 + 可复现存档 78**

用户这次把"难以描述"的演出拆成了可核对的**四段**（真机 vs emulator 逐段对照）。这是本票判据 ①（取证）
要的那份清单 —— 下面是原话的结构化整理，**未加工成结论**。

### 复现配方：存档 78（★下一步就是 SN0000 → SC0000 的切换）

- 存档：`%LOCALAPPDATA%\Eushully\揤寢偄僉儍僢僗儖儅僀僗僞乕\SAVE\SAVE78.DAT`（1,016,868 B）+ `SAVE78.STH`
- 操作：**加载该存档 → 下一步发生的正是 SN0000 → SC0000 的章节切换**（用户明确给出）。
- ⇒ 以后不必再靠"玩到某个进度"复现：这是本票唯一需要的输入，配 `npm run shot` 连拍即可拿到逐帧对照。

### 四段对照（真机 = 用户口径；emulator = 用户实测）

| # | 真机（期望） | emulator（实际） | 差异性质 |
|---|---|---|---|
| 1 | 塔状**云柱背景** + **横向模糊（类似抖动）** + **向黑色渐变** | **只有横向模糊**，没有黑色渐变 | 缺一段"向黑"的收束 |
| 2 | 中间的**章节背景**：白色光晕**减弱** → 显现背景与文字 | **直接切到 TITLE 的 Live2D** | ★上屏残留（见下"归票"） |
| 3 | 文字**随光晕渐变**展示（先光晕、后文字） | 白色光晕 + 章节文字**直接叠加**；文字**不是渐变出现**，只是**被光晕遮住** | 层次/时序都不对 |
| 4 | 文字背后**渐变展示人物轮廓** | 先出现**又一次横向模糊**（"大概是前面白色光晕预期使用的，时序错位了"）→ 然后**突然**展示人物轮廓 | ★同一段效果被排到了后面（时序错位） |
| 5 | 渐变到 SC0000 的第一句话 | 能渐变到 SC0000，但**SC0000 刚打开仍是**：ADV 窗口背景**白色**（应半透明黑）、**文字颜色错误** | = `T-0102`（同源症状） |

用户结论原话：「这里**整个渲染顺序大概有明显的问题**」。

### 现象 → 归票（判据 ② 的初判，**待取证后再定**）

| 现象 | 最可能的归属 | 为什么这么判 |
|---|---|---|
| 第 2 段"切到 TITLE 的 Live2D" | **`T-0090`**（真槽读档后上一个画面的 Live2D 仍活着并被画） | 用户是从**存档 78 读档**进入的；T-0090 的症状就是"TITLE 的 node 0x14 画进 SN0000" |
| 第 1/4 段的横向模糊（`i250` 类别 3）与"时序错位/少一段" | **`T-0091`**（转场渲染剩余四项：类别 3 精确核 / `[4]` 非 create-texture 槽 / `Scene+46508·46512·46516` 三标志本身 / E4 可达路径） | 模糊就是 `setTransition` 的类别 3；而"同一段效果排到后面"正是**记录表时序/清表门**的形态 |
| 各段时长与"多/少一帧"的观感 | **`T-0099`**（sleep 门帧粒度：40ms 档每处多约一帧；本轮已修「到点当帧派发」） | 演出由 `0x238`/`wait`/`i6E` 节拍串起来，帧粒度残差直接改观感 |
| 第 5 段（ADV 白底 + 文字色） | **`T-0102`**（ADV 窗口背景白 + 角色名青色/紫色残留） | 用户本轮再次确认"SC0000 刚打开依然如此" ⇒ 与本票是**两件事**，别混在一起修 |

### 已有的锚点（动手时从这里开始，别从零读）

- **横向模糊的脚本侧**：`src/SN0000.txt:3036` 的 `i250 (local-int 0) (global-int 3f5c) (global-int f8025) 1 (local-int 1) (local-int 2) (local-int 4) (local-int 6) 0 9c4`
  —— 类别 3/类型 0 的**滑动模糊**；它前面是 `create-texture (global-int 3f5c) 500 2d0 1` + `draw-texture (local-int 0) (global-int 3f5c) …`
  （= 被模糊的那张全屏项），后面紧跟一段 `set-draw-color-alpha … 2 …` 的淡色逻辑。
- **章头素材与转场记录（现场日志，已归档）**：`tickets/T-0100/evidence/sn0000-sc0000-boundary.md`
  - `7974: bindTexture imgid=0x76 slot=44` / `7975: image 76 -> AE910AA.AGF (256x128)`（章头图 = 256×128）
  - `7982: createTexture slot=64 1280x720 mode=1 @1.25x`（★`[4]` 指向的槽**确实是 create-texture 出来的**）
  - `7984: setTransition id=0x18b02 writes=[[0,3],[1,0],[2,0],[3,1500],[4,64],[7,1],[13,1],[16,64],[17,500],[18,216],[19,0],[20,0],[21,400],[22,216],[23,0]]`
    ⇒ **类别 3（`[0]=3`）、`[13]=1`（ZoomBlur）、`[3]=1500`ms、目标槽 `[4]=64`**
- **相邻未收口项**：`T-0091` 的 G1/G2、`T-0099`（本轮已修一处）、`T-0102`（本轮推进）、`T-0090`（Live2D 残留）。

### 下一步（判据 ① 的剩余缺口）

用户给的是**元素级**描述；判据 ① 还差**帧级**：用存档 78 跑 `npm run shot`（连拍，或 `--name` 分段）
拿到逐帧截图，把上表每一行落成"第 N 帧 / 哪个元素 / 期望 vs 实际"。★在那之前**不要**动手改渲染顺序 ——
用户已明确"最后再处理"，且本票的第 1/4 段很可能就是 `T-0091` 的两条门（修完再回看会少一半现象）。
