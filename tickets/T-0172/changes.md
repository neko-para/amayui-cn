# T-0172 变更记录 —— `rendering.md` 行号整表逐行核（+ `04-app/emulator-copyright-effect.md` 三项）

> 纪律：**先读体后改**，每条都给"旧文档写什么 / 体里那一行是什么 / 据此改指哪一行或哪一段"。
> 不猜、不照抄旧数；核不动的写进 §6 卡点。
> 机器取证：改前 = `evidence/t0172-red-check.mjs`（输出 `evidence/t0172-red-check-out.txt`，**60/60 ❌**）
> + 上位旧表 `tickets/T-0112/evidence/b7b-red-check.mjs`（**26/26 ❌**）；
> 改后 = `evidence/check-anchors.mjs`（输出 `evidence/t0172-green-check-out.txt`，**46 条锚点 0 问题**）
> 与守卫 `app/amayui-emulator/test/doc-model.test.ts` 的「B7-B 棘轮（T-0172）」。

## 0. 命令与结果（红 → 绿）

```bash
node tickets/T-0112/evidence/b7b-red-check.mjs                      # 改前（上位旧表）：26 条 26 ❌
node tickets/T-0172/evidence/t0172-red-check.mjs                    # 改前（本票旧表）：60 条 60 ❌
node tickets/T-0172/evidence/check-anchors.mjs --quiet              # 改后：46 条锚点，❌ 0 处（[ok]）
cd app/amayui-emulator
  npm run typecheck:test                                            # exit 0（tsconfig.test.json）
  node --import tsx --test test/doc-model.test.ts                    # tests 10 / pass 10 / fail 0（含新增 T-0172 棘轮）
cd ../../ && node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --validate   # ✅ 见 §7
```

反编译真源 = `engine/天结_unpacked.exe_utf8.c`（文件名里的「天结」是**简体码位**，故按后缀 `_unpacked.exe_utf8.c` 查找，不硬编码）。

---

## 1. 判据①：`tickets/T-0112/changes.md`「待应用」节的 8 处已知错 —— **8/8 全部改掉**

逐条已被本票重新读体复核（不只是照抄 T-0112 给的 new 值）：

| 文档行 | 文档原写 | 体里那一行实际是什么 | 改指为 |
|---|---|---|---|
| 33 | `sub_426CF0`(0x323, line 33358) | 33358 = `int v16; // [esp-4h] [ebp-8h]`（局部声明） | **33888** = `int *__thiscall sub_426CF0(_DWORD *_this)` |
| 33/60 | `sub_4AF1C0` raw 131435、131491-131501 | 131435 = `bool v9; // zf`；131491 = `int v33; // [esp+24h]…` | **133458**（`//----- (004AF1C0)`）、**133505-133541**（`v40[0] & 2` 窗求值块，133526 调 `sub_4A2050`） |
| 58 | `sub_4A2050, 120438`；120475-120479 / 120502-120503 / 120482-120492 | 120438 = `v27 = *v23;`；120475 起是别的函数 | **122248**（函数头）；通道插值 **122289-122293**；快路径 **122294-122306**；写 VB+16 **122316-122317**（`CalcDiffuse エラー` 字串 = 122281） |
| 69 | 主循环 20445 / 20465-20467 / 20571-20576 / 20583 | 20445 = `{`、20465 = `void __thiscall __noreturn sub_412290(int _this)` | **20620**（`v3 = timeGetTime`）/ **20640-20642**（`v94 = v5`）/ **20746-20751**（`667860`+`369336`/`369332`）/ **20758**（`sub_4B4040`） |
| 79 | `sub_422E70`(30846) → `sub_4ACE50`(129796) | 30846 = `v43 = 1;`；129796 = `if ( v6 != *(_DWORD *)(i + 8) )` | **31270** → **131816**（31293 `SetRect`、31299 调 `sub_4ACE50`） |
| 86 | `sub_422E00` 30822 / `sub_4237B0` 31165 | 30822 = `int v43; // [esp+24h]…`；31165 = `void (__thiscall ***v4)…` | **31245** / **31604**（两处都是 `//-----` 函数头） |
| 100 | `sub_4ACF60`(129850) / `sub_4AD0C0`(129936) | 129850 = `{`；129936 = `//----- (004AA740)` | **131870** / **131956**（`|=2u` 131969、+48 131878、+56 131972、+96 131880、+100 131976） |
| 102 | `sub_4AEEA0`(131306) → `sub_4A2D50`(121065) | 131306 = `v4[60] = 0.0;`；121065 = 空行 | **133326** → **122890**（调 `sub_4A2D50` 于 133443；vtable+20 调用点 123146/123208） |

同批**顺带改掉**的、与上表同源的行号（不修就是自相矛盾）：

- 69：`sub_41A090`/`sub_41A1A0`/`sub_41A2C0` 24953/25014/25059 → **25193/25258/25307**（25206 置 107438、25207 取 timeGetTime、25267/25271）；逐帧链 `sub_4B4040`(134702)→`sub_4B06D0`(134755)→`sub_4AF1C0`(134875/133736/…) → **136741**（调 `sub_4B06D0` 于 136796）→ **134417** → **135596/135770/135993/136106/136299/136564/136667/136916**；`sub_4AF1C0`(131435-131622) → **133458-134274**。
- 70：窗起点锁存 131486 → **133511-133512**；收尾分支 131506-131513 → **133531-133538**；`_this[46512]` 132902 置1/134801 清0 → **134936 与 12796 置 1 / 136842 清 0**；present 帧始清脏 134751-134752 → **136792-136793**。
- 78：`sub_4B06D0`(132387) → **134417**（132387 落在 `sub_4AD9F0`(132364) 里，且是空行）。
- 79：`sub_4ADC20` 132507-132516 **保留**（读到记录 `+4`）。
- 81：`sub_432150`(40262)→**41011**、`sub_4ADFE0`(130665)→**132685**、（130708 `v19=sub_40DC30`）→**132753**、（130732-130761）→**132759-132782**、`sub_4AF1C0` 调用点 133562/… → **135596/135770/135993/136106/136299/136564/136667/136916**。
- 84：`sub_407E20`(12679)→**12762**（计时器读 12768、返回 12783）；脏标记 setter 129957/130051/130091 → **131881/131977/132823/132844**；`sub_4AF1C0` 置位（131503）→**133528**；present 清脏（134751-134752）→**136792-136793**。
- 93：主循环(20422 起) → **20464 起**（`while(1)` 级联于 **20615**）；其余同 69。
- 167：`sub_4AE2C0` raw 132815 → **132818**（`v5[9] = a3`）。
- 193：每项收尾 123250 → **123244**（123250 是 `// 4BC2D8: using guessed type` 注释）。

## 2. 判据②：**从未核过**的 8 处 —— 三种处置分开记

| 文档行 | 文档原写 | 处置 | 结论 / 新锚点 |
|---|---|---|---|
| 26 | 49942（`sub_441410`）、26981（`sub_453A60`） | **改指** | 49942 = `char *v18; // eax`；`sub_441410` 的 8 处调用点 = **27282/27311/27348/27383/27422/27559/27837/28192**（handler `sub_41D180`…`sub_41E7E0`）；`sub_453A60(_this+107461,…)` = **27273/27308/27345/27380/27418/28189** |
| 30 | 20619-20638 / 20723-20742（`0x2000` movie 分支） | **改指** | 20796-20813 与 20899-20917：`if ( (v17 & 0x2000) != 0 )` → 释放对象 → `*(_DWORD *)(_this + 378684) = 0` → `&= ~0x2000u`（两处） |
| 31 | 20651（`sub_453A60(_this+430012, 10)`） | **改指** | **20826**（同行 20827 `sub_489D10`、20835 `sub_489E50` 对 `_this+697816` 做音量渐变）⇒ 原文的"BGM/音乐淡入"判定正确，只是行号错 |
| 35 | 20934-20977（主循环 0x400 卫门） | **改指** | **21109-21153**：21109 `if ( (v35 & 0x400) == 0 ) break;`、21111 `if ( sub_407E20((_DWORD *)(_this + 322832)) \|\| v95 )`、21149 `goto LABEL_186`、21151 `&= ~0x400u` |
| 44 | 40262/130665/120518（create-mesh 链） | **改指** | **41011**（`sub_432150`，41035 读 op9）/ **132685**（`sub_4ADFE0`，132755 调 `sub_4A2280`）/ **122331**（`sub_4A2280`）⇒ 链路语义（handler→建 mesh→建几何）成立 |
| 51 | 119370-119399（`sub_49E390` 消费 `MeshEntry[9]`） | **对，保留** | 119370-119399 正是 `a8 == 1/2/3` 的选择子 if 链（119372/119383/119396）⇒ 已纳入棘轮 |
| 54 | 33865-33884（α 夹取 + 负值回退） | **对，保留** | 33865 `if ( v2 <= 255 )`、33870/33880 `sub_4AE3C0`（取当前 state0 通道）、33875 `v2 = 255`、33884 `sub_4AE2C0(...)` ⇒ 已纳入棘轮 |
| 58 | 见 §1 | 见 §1 | —— |

**顺带核了同文件其余显式行号锚点**（line 9/80/110–118 的 `1280`/`2026`/`1263` 是尺寸与年份，不是行号，未动）：80（131135-131139）、126（117425-117429）、160（123089-123121 / 119369-119399 / 119512-119578）、168（34442-34444）、169（133443、133617）、172（65861-65869）、178（123100-123117）、179（119474-119476）、180（134417-136734）、189（122816-122823）、190/191/192（123087-123088 / 123090-123120 / 123208、123224）、197（133617）**全部正确，保留并纳入棘轮**。

**改指的两处"分支归属"**（不是行号漂移，是原来就指到了**另一条分支**上，因此必须改）：

- line 198（§4.6 末条）：原写"`sub_49E390`/`sub_49E700` 在 `0`/≥4 时显式设 `(19,5)+(20,6)`（raw 119397-119400、119521-119540）"。
  体：119397-119398 是 **`a8 == 3`** 支（`(171,3)+(19,5)+(20,2)`），119521-119528 是 **`a8 == 2`** 支。
  `0`/≥4 支实际在 **119389-119394**（`(19,5)` 于 119392、`v19 = 6` 于 119393 → `goto LABEL_12`，`(20)` 于 119375）与 **119530-119534**（`(19,5)` 于 119533 → `(20)` 于 119517）⇒ 已改指。

## 3. 判据④：`04-app/emulator-copyright-effect.md` —— 三项 + 同源 5 处

| 文档行 | 文档原写 | 结论 |
|---|---|---|
| 59 | `sub_49A300`(115116) 是"逐帧颜色求值器" | **函数名错，已改 `sub_49AA30`(117239)**；`&2` 门控 117430、首帧锁存 117438、插值回写 117470-117479 |
| 55 | `sub_4B4040`(134702) → `sub_4B06D0`(132387) | **136741**（调 `sub_4B06D0` 于 136796）→ **134417**；`sub_4AF1C0` = 133458、`sub_4AEEA0` = 133326 |
| 58 | `sub_4AF1C0`(131491-131501) | **133505-133541**（`sub_4A2050` 调用点 133526）；`sub_4A2050` = 122248 |

**★ `115116` / `116878` 哪个是 `sub_49A300` 的真身**（判据④点名的那个问题）：

- `sub_49A300` **确实是真函数**，函数头在 raw **116878**（`//----- (0049A300)`，签名 116879 `int __thiscall sub_49A300(_DWORD *_this, int a2)`），体 = 逐字段清零 + `SetRect((LPRECT)(a2 + 8), 0, 0, 0, 0)` 的 **DrawItem 初始化/复位**，与逐帧颜色求值无关。
- `115116` **不是** `sub_49A300`：它落在 `sub_4983E0`（函数头 raw 115056）体内，内容 = `if ( v12 >= dbl_52A260 )`。
- 逐帧颜色求值器 = **`sub_49AA30`（raw 117239）** —— 两个函数**没有**被混成一个：一个是 DrawItem 复位（0x49A300），一个是每帧颜色动画求值（0x49AA30）。

同一文件里同源的行号错也一并改掉（否则与 `rendering.md` 自相矛盾）：

- line 15：主循环 `LABEL_216`(21041) → **21216**（21217 派发 1 条、21218 推进 ip）。
- line 63：`effect_flags` 位级联（20594-20633）→ **20769-21158**（级联起点 = 20769 `v17 = *(_DWORD *)(_this + 699204)`）。
- line 71：`0x400` 支（20934）→ **21111**。
- line 77：present 条件（20581）→ **20756**（`sub_40BE10((_DWORD *)(_this + 322832)) == 1 || v90 == 1`）。
- line 197：draw-item `&4` 支（`sub_4AEEA0` 131368）→ **133389-133390**（133390 `v10 = (LOBYTE(v26[0]) & 4) == 0;`；`sub_49BCC0` 调用 133394）。
- line 135/136（§5 注释）：132816-132823 / 132834-132843 **本来就对**，保留并纳入棘轮。

## 4. 判据③：把行号锚点做成两向棘轮（本票最厚的产出）

- **唯一真源表** = `tickets/T-0172/evidence/anchors.json`（46 条，字段 `{id, doc, line, row, cites[], raw[{line, has}]}`）。
- **命令行核对** = `tickets/T-0172/evidence/check-anchors.mjs`（三个方向：① 文档第 `line` 行仍含锚点文字 `row` 且仍写着 `cites[]` 每个行号；② `raw[].line` 处真含 `raw[].has`；③ `raw[].line` 必须被 `cites[]` 覆盖 —— 即"文档没写这个行号就不许拿它当断言"）。
- **守卫** = `test/doc-model.test.ts` 新增「B7-B 棘轮（T-0172）」，**读同一张 JSON 表**（不是另抄一份），断言形状与 T-0112 那条一致；`rendering.md` 与 `04-app/emulator-copyright-effect.md` 一并纳入。
- 敏感度已验证：本票自己在中途跑出过 10 处 ❌（含"raw 117438 不含 `_this + 46500`"这类 `has` 串写错、以及"cites 没写该行号"），逐条修正后才 0 问题 ⇒ 它不是"只看文档自洽"的假断言。

## 5. 锚点是 ABI：改动前查过谁锚在这两份文件上

```bash
node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --anchors-in docs-new/03-engine/rendering.md
#   T-0017  evidence[1] anchor="+48=混合模式" line=100
#   T-0110  evidence[2] anchor="从不设正"     line=84
node …/tickets.js --anchors-in docs-new/04-app/emulator-copyright-effect.md     # （无）
node .agents/skills/amayui-engine-analysis/scripts/scripts.js --anchors-in <两份>  # （无）
```

两个被锚定的字面串**都原样保留**（line 100 现为"flags`|=2`（启用位，131969）、+48=混合模式(0默认)（131878）"；line 84 现为"…此前"全文件只被清零、从不设正"是错的"），行号仍在原位（line 100/84 未移）⇒ 无需 retarget。`rendering.md`/`04-app` 的 `kind: narrative` / `state: live` 与 H1 均未动 ⇒ `00-overview/index.md` 无需重生成（doc-model 的索引同步项已证）。

## 6. 卡点（**结论性**，不在本票范围内的语义疑点）

1. **line 84 的"字节歧义"段存疑（只改了行号，没改结论）**：该段写"mesh 绘制 `sub_4AF1C0` 置的是 **46516**（131503），与 `sub_407E20` 返回的 **369348** 是**两个不同字节**"。但体里 `sub_4AF1C0` 同时有 raw **133528** `*(_DWORD *)(_this + 46516) = 1;` 与 raw **134271** `_this[11629] = 1;`；若 `sub_4AF1C0` 的 `_this` 是图形池基址（它用 `sub_40DC30(_this + 1064)` 访问 mesh 表、用 `_this + 46500` 读时钟，两者都只有池基址成立），则 `+46516` 字节与 `[11629]`（= +4×11629 = +46516）**是同一个字节**，也就等于 `sub_407E20` 返回的 `[11629]`。本票只把 `131503` 改指为 `133528`；"两个不同字节"这个判定**未在体上收敛**，需要一次专门的字节基址核对（建议另开票或并入 `T-0117`/帧循环票），不要在行号票里顺手改结论。
2. **`sub_4AF1C0` 还有第 9 处调用点**：raw **137221**（在 `sub_4B4460`，函数头 136968）。原文档列 8 处（本票 1:1 改指为 8 个），未擅自扩成 9 处（会改变"调用点"这条断言的覆盖面）；如需列全，属于语义补充而非行号修正。
3. **`rendering.md` line 9 的 `1280`/`640×480`/`1263`、line 110+ 的 `2026`** 是尺寸与年份，**不是** raw 行号，本票未纳入棘轮（避免把非锚点当锚点）。

## 7. 收尾实测

```bash
cd app/amayui-emulator
npm run typecheck:test                        # exit 0
node --import tsx --test test/doc-model.test.ts   # tests 10 / pass 10 / fail 0
cd ../..
node tickets/T-0172/evidence/check-anchors.mjs --quiet   # 46 条锚点，❌ 0 处（[ok]）
node tickets/T-0172/evidence/t0172-red-check.mjs         # 60 条 60 ❌（改前取证，永久可复跑）
node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --validate
```

改动文件：

- `docs-new/03-engine/rendering.md`（行号改指，未增删章节）
- `docs-new/04-app/emulator-copyright-effect.md`（行号改指）
- `app/amayui-emulator/test/doc-model.test.ts`（新增「B7-B 棘轮（T-0172）」一条）
- `tickets/T-0172/evidence/anchors.json`（锚点表，唯一真源）
- `tickets/T-0172/evidence/check-anchors.mjs`（命令行两向核对）
- `tickets/T-0172/evidence/t0172-red-check.mjs`（改前 60 条旧行号取证）+ 两份输出 `.txt`
- `tickets/T-0172/changes.md`（本文件）

**范围外**（未动，留给对应 owner）：`analysis/*.json`、`app/amayui-emulator/src/**`、`test/` 下其它文件、生成器脚本、其它票据目录。
