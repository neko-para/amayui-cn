# T-0112 变更记录 —— B7-B 残余：**逐格核 raw 之后**才改的 5 项

> 判据（`ticket.json` 的 `acceptance` ①–⑤）逐条对应下面 ①–⑤。
> 纪律：**先核后改**，每条都给"哪句错了 / 体里是什么 / raw 哪一行"；核不动的写卡点，不猜着改。
> 机器取证：改前的 26 条旧行号逐条对照真源 = `evidence/b7b-red-check.mjs`（输出 `evidence/b7b-red-check-out.txt`，**26/26 ❌**）；
> 改后的两向棘轮 = `app/amayui-emulator/test/doc-model.test.ts` 的
> 「★文档模型：机制叙述的 raw 行号锚点与反编译真源一致（B7-B 棘轮，T-0112）」（24 个锚点：① 文档那一行必须仍写着该行号；② 该行号处必须真的含那段代码串）。

## 0. 命令与结果（红 → 绿）

```bash
node tickets/T-0112/evidence/b7b-red-check.mjs          # 改前：26 条旧行号 26 ❌（[red]）
cd app/amayui-emulator && node --import tsx --test test/doc-model.test.ts
                                                        # 改后：9 例里 8 ✔ / 1 ✖ —— 见下方「并发基线红」；本票新增的 B7-B 棘轮 ✔、I9 ✔、索引同步 ✔
cd app/amayui-emulator && node --import tsx --test test/ticket-ledger.test.ts   # 6/6 ✔（含「done 必须带真实存在的守卫」「看板与真源同步」）
node .agents/skills/amayui-ticket-ledger/scripts/tickets.js --validate          # ✅ 票据自检通过（168 张）
node .agents/skills/amayui-engine-analysis/scripts/capabilities.js --validate   # [ok] 140 条
node .agents/skills/amayui-engine-analysis/scripts/scripts.js --validate        # [ok] 34 条
node scripts/build-tickets.mjs                                                  # 结算后重建看板（本票 open→done）
```

**并发基线红（与本票无关）**：`test/doc-model.test.ts` 的 `opcode-table.md 是 analysis/opcodes.json 的生成物` 一例在收尾时段变红 ——
`analysis/opcodes.json` 的 mtime（00:40:10）**晚于**其渲染物 `docs-new/03-engine/opcode-table.md`（00:15:33），即**另一个 agent 改了
opcode 真源、尚未重跑 `scripts/build-opcode-table.mjs`**（该生成器与本票的禁写清单一致，我没有跑）。本票的三个相关例（I9 沿革话术、
索引同步、新增的 B7-B raw 行号棘轮）全绿，与 `CONTEXT.md` §6 的「不新增红」口径一致。

---

## ① `adv-text-rendering.md` 的 `FontVWindow` 字段表：4 处可疑行号 **4/4 都要改**

体 = `sub_475D20`（raw 90437-90490，构造 `FontVWindow` 基类）。

| 行 | 文档原写 | 体里那几行实际是什么 | 真值 |
|---|---|---|---|
| `+4` 目标表面 id | 90444 | 90444 = `int v6[6]; // [esp+Ch] [ebp-1Ch] BYREF`（局部声明）、90445 = 空行 | **90446** `*(_DWORD *)(_this + 4) = 0;` |
| `+8` 描边源表面 id | 90445 | 同上（90445 是空行） | **90447** `*(_DWORD *)(_this + 8) = 2;` |
| `+116/+120` 缩放初值 | 90469 | 90469 = `*(_DWORD *)(_this + 112) = 0;` | **90463-90464** `*(float *)(_this + 116) = 1.0; *(float *)(_this + 120) = 1.0;` |
| `+1252` 打包 dword | 78866 | 78866 = `v27[4] = 0;` | **78861** `*(_DWORD *)(_this + 1252) = 0x1000000;` |

同批顺带核出并改掉的两处**指偏**（同一单元格内的二级证据，不能留着）：
- `+8` 行的 `86403/89152` 实测是**两个函数的收尾大括号**（`}`），不是读点；真正的读者是 `win+8` 当 **`sub_43D870` 的源表面**：
  raw **86385-86396**（`sub_46FA60`：`v11 = (unsigned int *)v8[a2 + 241];` → `sub_43D870(…, v11[2], …)`）与 raw **89135-89146**（`sub_4742F0` 同型，`v12[2]`）⇒ 已改指这两段。
- `+1252` 行的 `68747/68785/69789` 实测分别是 `if (*a2)`、`if (*a2)`、`sub_456DF0(...)` —— 都不是"照抄打包值"那一句；真正照抄的是 raw **68744 / 68782 / 69786**（`*(_DWORD *)&lplf->lfItalic = 0x1000000;`，三处即席 `LOGFONT`）⇒ 已改指这三处。
- 另：§3.4 的 `DrawMode=0` 行也拿 `90444` 当「`win+4` 恒 0」的证据 ⇒ 同批改 **90446**。

**卡点：无**（4/4 都核到体并改掉）。但**同表其余行本轮未逐格核**（不在判据内，避免超范围改动）；顺带抽查为**正确**的有：`+0` 90516、`+36/+40` 90431-90433、`+296` 90488、`+60..+99` 68304、`+88` 68301、`+96` 68302、`+92` 68303、`+12/+16` 73154-73155、`+20/+24` 73157-73158、`+124/+128` 73170-73171、`+1228` 78899/73150、`+44/+48` 的 83918。

---

## ② §3.5 的 `0x204 draw-string`「GDI 一次性整串」—— **证伪**，按体改口径

文档原句（§3.5 表）：``| `0x204 draw-string`（直绘进纹理槽） | **调用那一刻**（GDI 一次性整串） | raw 68470-68491 |``。

体：
- `0x204` 的 handler = `sub_423390`（raw **31454-31467**，`opcodes.json` 亦记 `raw .c 31454`）→ raw 31466 `sub_456710((int)(_this + 21324), v2, v3, v4, v5)`（`_this+21324` dword = `Font`）。
- `sub_456710`（raw **68470-68495**）体里**没有 `TextOutA`**：`GetTextMetricsA` 定位（68486）后把**整串 `a3`** 交给 raw 68491 `sub_471180`（有描边）或 raw 68493 `sub_46F2D0`（无描边）；两者是**软件字形光栅器** —— `sub_46F2D0`（头 raw 85933）逐字循环 raw **86044-86339**、每字一次 `GetGlyphOutline`（raw **86146**）、再 `sub_46D9F0` 逐像素合成进 Scene 的该槽纹理（`sub_4A3890` lock raw 86020）。`sub_456710` 体内**没有任何 `set:DrawMode`（`667856`）分支**。
- ⇒ 正确口径 = 「**整串一次调用、内部逐字光栅化**」，不是 GDI 整串，也与 `set:DrawMode` 无关。
- 对照：真正的「GDI 整串 `TextOutA`」是**另一条**路径 `sub_456820`（raw 68498-68634）：描边拷贝 raw 68587/68595/68601/68607，整串 raw **68616** `TextOutA(*(HDC *)(_this + 1104), v22, y, lpString, strlen(lpString));` —— 那是**消息窗排版**那一路（`sub_455ED0` 等调用点）。

改后：该行改成"整串一次调用 → 软件字形光栅器**逐字**合成进该槽纹理，**不经 GDI `TextOutA`、与 `set:DrawMode` 无关**"，证据 = raw 31454-31467 / 68470-68495 / 86044-86339。**卡点：无**。

---

## ③ `set:AutoLineFeed` —— 键存在；字段 = **`Font+1404`（= `Engine+86700`）**，已补进 §6 配置键表

1. **键确实存在**：raw **4385** `char aSetAutolinefee[17] = "set:AutoLineFeed";`；INI 解析 raw **112578-112581**（`_stricmp(aAutolinefeed, …)` → `sub_434D00(registry, aSetAutolinefee, &v)`）；注册缺省 raw **111548-111549**（`v13 = 1`）；另一处 SetConfig raw 142102。
2. **写的是哪个字段（读体后才写）**：raw **23660-23662** 启动时 `*(_DWORD *)(a1 + 86700) = (registry getter)(a1 + 697620, aSetAutolinefee);`，同段 raw 23667 用 `sub_465390(a1 + 85296, …)` 证明 `a1` = Engine 基址 ⇒ **`Engine+86700` = `Font+1404`**（`86700 − 85296 = 1404`）。⇒ 审计说的 `Font+1404` **成立**（此前 `fields.json` 里没有这条偏移，本轮新增）。
3. **语义（读体）**：raw **83001**（`sub_46B100`）与 raw **83609**（`sub_46BE30`）都是
   `if ( *(_DWORD *)(_this + 1404) == 1 && !sub_45BCD0((_DWORD *)_this, String) )`，
   位置在**超右边界（`v27 & 1`）分支内**、在 push 120B 文本记录（`sub_45E870`，raw 83655）之前 ⇒ **`1` = 允许自动换行**，并附带一次"行头禁则"查询：`sub_45BCD0`（raw **72113**）在 `Font+235084` 的 vector（`_this[58771]/[58772]`）里找当前字，返回"**该字是禁则**"。禁则表在本作恒空（`T-0110` 已证）⇒ 本作运行期等价于「**1 = 换行 / 0 = 不换行（继续越界画）**」。
4. 改动：`§6 配置门`表新增一行（随包值 `1`，证据 23660-23662 / 111548-111549 / 83001 / 83609 / `tickets/T-0031/evidence/generated-SYS4REG.ini:70`）；`§1` 的 `Font` 字段表新增 `+1404` 行。`analysis/fields.json` 新增 `Font/0x152AC`。

**卡点：无结论性卡点**，但"随包值"一栏的来源受限：**引擎发行包里的那份 `SYS4REG.INI` 没有进资源树**（`install/` 下没有 `SYS4REG.INI`；`.tmp/instances/*/overlay/` 是运行期回写物），所以该栏用**引擎注册缺省 `1`（raw 111549）**为准，并用 T-0031 归档的那份回写件（`[set]` 段 `AutoLineFeed=1`）互证。

---

## ④ `copyright-effect.md`：§6 索引表 **11 行整表逐行核** + 同批 §2/§3a/§3b/§5

函数头真值用 `grep -n '^//----- (00XXXXXX)'` 取得。

| §6 行 | 文档原写 | 实测真值 |
|---|---|---|
| 时钟每帧写 | 主循环 20445/20465/20571-20583 | **20620**（`v3 = timeGetTime`）/ **20640-20643**（tick `v94`、渲染门）/ **20746**（`else if (667860 \|\| flags&0x2400)`）/ **20750-20751**（`[369336]=[369332]; [369332]=v94`）/ **20758**（`sub_4B4040` present） |
| mesh 渲染 + CalcDiffuse | `sub_4AF1C0` 131435 / `sub_4A2050` 120438 | **133458**（逐帧窗 **133505-133541**）/ **122248**（`CalcDiffuse エラー` 字串 raw 122281） |
| draw-item 颜色动画求值 | `sub_49A300` 115116 | **函数名引错** ⇒ `sub_49AA30` **117239**（`&2` 门控 raw **117430**、插值 raw **117434-117483**） |
| set-vertex-color(-alpha) | 130789 / 130806 | **132809** / **132826** |
| set-draw-color-alpha | 129850 | **131870**（`+48` raw 131878、`+96` raw 131880） |
| set-draw-color | 129936 | **131956**（`\|=2u` raw 131969、`+56` 131972、`+76` 131974、`+100` 131976） |
| draw-item 渲染 | 131306 | **133326**（`sub_49AA30` 调用 raw 133389、`&4` 分支 raw 133390、交 `sub_4A2D50` raw 133443） |
| draw-texture 处理器 | `sub_422E70` 30846 → `sub_4ACE50` 129796 | **31270**（`SetRect` raw 31293、调用 raw 31299）→ **131816** |
| image 绘制器 | 121065 | **122890**（texture-slot vtable+20 调用点 raw 123146/123208） |
| 0x400 等待门 | 12762-12786 / 32303-32312 | **不变（本来就对）**：`sub_407E20` 体 12762-12786、`sub_4248C0` 体 32303-32312（32309-32310 写 `[92338]/[92339]`） |
| 淡出后的硬切 | 30822 / 31165 | **31245**（`sub_422E00`）/ **31604**（`sub_4237B0`） |

同一批结论在正文里的引用也一并核了（它们指向同一批体，不改就是自相矛盾）：
- §2 时钟 4 行（20445/20465/20468/20571/20575-20576/20583 → 20620/20640-20642/20643/20746/20750-20751/20758）。
- §3a：`sub_4AE2C0`(130789)/`sub_4AE330`(130806) → **132809/132826**；`sub_4AF1C0`(131435，131491-131501) → **133458（133505-133541）**；`CalcDiffuse` `sub_4A2050`(120438) → **122248**。
- §3b：`sub_4ACF60`(129850)→**131870**、`sub_4AD0C0`(129936)→**131956**、逐帧求值 `sub_49A300`(115116)→**`sub_49AA30`(117239)**、`sub_4AEEA0`(131306)→`sub_4A2D50`(121065) → **133326 → 122890**；字段语义表 7 格：`\|=2` 129948→**131969**、`&2` 门控 `sub_49A300` 115663→**`sub_49AA30` 117430**、`+52` 129949→**131970**、`+56` 129951→**131972**、`+76` 129953→**131974**、`+96` 129859→**131880**、`+100` 129955→**131976**。
- §5：`sub_4ACE50` 129805→**131816**（`*v11 \|= 1u` 在 131826）、`sub_4AEEA0` 131368→**133390**、`sub_4AE330` 130806→**132826**（`\|= 2u` 在 132836）、`sub_4AF1C0` 131491→**133505**。

**卡点：无**。改后 `grep` 复核该文件里 26 个旧行号 = **0 命中**。

---

## ⑤ `sub_40DF10` 的「10 个 Queue_int / 10 个 Stack_int 构造」—— 实测：**构造点各 1 处、各跑 10 次 ⇒ 对象各 10 个**

体 = `sub_40DF10`（raw 17932-18181）：

| 容器 | 构造点 | 循环 | 体里的分配 |
|---|---|---|---|
| `Queue_int` ×10 | **1 处**（raw 18082-18109 的 `do {} while`） | raw 18080-18081 `v18 = _this + 388252; v15 = 10;`，`do … while (!(v15-- == 1))` ⇒ **10 次** | `operator new(0x1Cu)` raw 18086、`*v5 = &Queue_int___vftable_` raw 18091、内缓冲 `operator new[](0x400)` raw 18092、`[4]=[5]=256`、`[2]=[6]=[3]=0`（18093-18097） |
| `Stack_int` ×10 | **1 处**（raw 18112-18137） | raw 18110-18111 `v18 = _this + 388292; v16 = 10;` ⇒ **10 次** | `operator new(0x14u)` raw 18116、`*v9 = &Stack_int___vftable_` raw 18123、`[1]=[2]=256`、内缓冲 `operator new[](0x400)` raw 18124、`[4]=-1` raw 18125 |

⇒ **对象数 10 + 10 成立**（文档的计数没错），错的是措辞与尺寸标注：原文括号里的 `Queue_int(0x28B)` / `Stack_int(0x1C)` **都不是体里的分配尺寸**（真值 `0x1C` / `0x14`），而"10 个 …构造"容易被读成"10 个构造点"。改法 = 把伪代码换成忠实的两条 `do-while`，注释里写实测数（**构造点各 1 处、计数各 10 ⇒ 对象各 10 个**）。**卡点：无**。

---

## 回写的数据层 / 守卫 / 范围申报

- `analysis/fields.json`：新增 `Font/0x152AC`（`auto_line_feed`，= `Engine+86700` = `Font+1404`），evidence = raw 23660-23662 / 111548-111549 / 83001 / 83609 / 72113，status `confirmed`。
- `app/amayui-emulator/test/doc-model.test.ts`：新增测试「★文档模型：机制叙述的 raw 行号锚点与反编译真源一致（B7-B 棘轮，T-0112）」——24 个锚点、两向断言（文档仍写着该行号 + 该行号处真含该代码串）。实现细节：反编译真源文件名里的「天结」是**简体字形**（与散文里的「天結」不同码位），故按后缀 `_unpacked.exe_utf8.c` 在 `engine/` 里查找，不硬编码文件名。
- 改了的手写文档：`docs-new/03-engine/adv-text-rendering.md`、`docs-new/03-engine/copyright-effect.md`、`docs-new/03-engine/engine-reset-mainloop.md`（三者都仍是 `kind: narrative` / `state: live`，H1 未动 ⇒ `docs-new/00-overview/index.md` 无需重生成，守卫已证）。
- 生成物未动：`opcode-table.md` / `engine-capabilities.md` / `opcode-gaps.md` 与对应 `analysis/*.json` 一律未改（`doc-model.test.ts` 的生成物同步项仍绿）。

## 待应用（**范围外或未核，本轮未改**）

1. `docs-new/03-engine/rendering.md` —— **同一批结论的旧行号仍在**（与本票 ④ 同源，本票判据不含它）：
   - line 33：`sub_4AF1C0` raw 131435、131491-131501 → **133458 / 133505-133541**；
   - line 58、60：`sub_4A2050` 120438 → **122248**、131491-131501 → **133505-133541**；
   - line 69：主循环 20445 / 20465-20467 / 20571-20576 / 20583 → **20620 / 20640-20642 / 20746 / 20750-20751 / 20758**；
   - line 79：`sub_422E70` 30846 → `sub_4ACE50` 129796 → **31270 → 131816**；
   - line 86：`sub_422E00` 30822 / `sub_4237B0` 31165 → **31245 / 31604**；
   - line 100：`sub_4ACF60` 129850 / `sub_4AD0C0` 129936 → **131870 / 131956**；
   - line 102：`sub_4AEEA0` 131306 → `sub_4A2D50` 121065 → **133326 → 122890**。
   ★该文件还有**其它未核行号**（line 26 的 49942/26981、line 30 的 20619-20638/20723-20742、line 31 的 20651、line 35 的 20934-20977、line 44 的 40262/130665/120518、line 51 的 119370-119399、line 54 的 33865-33884 …）⇒ 建议**单开一格「rendering.md 行号整表逐行核」**再做，避免半修。
2. `docs-new/04-app/emulator-copyright-effect.md` —— line 59 `sub_49A300`(115116) 是**函数名错**，应改 `sub_49AA30`(117239)；同文件 line 55 的 134702/132387、line 58 的 131491-131501 未核。
3. 若后续换反编译版本：`evidence/b7b-red-check.mjs` 的 `OLD` 表就是"哪些行号曾被写成什么"的清单，可当作重核起点。
