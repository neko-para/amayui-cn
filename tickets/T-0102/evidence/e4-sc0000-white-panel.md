# T-0102 · E4 复现：SN0000 → SC0000 之后 ADV 窗口本体是**平铺的 `#E3E3E3`**（用户口径的"白色背景"）

> 2026-09-22，用户指路后的第一次成功复现。**注意**：`SN0000` 内部没有这个问题（用户口径），
> 必须**过章节**才会出现 —— 所以要把序章末尾的存档载入后**点一下、再等约 15 s**（过场动画走完）。

## 复现命令（可重跑）

```powershell
cd app/amayui-emulator
# ★`--key Up` 已从 shot.cjs 移除（用户观察到"Up 一直像被按着"）⇒ 用 `--row <y>` 点行来选槽；
#   本帧是用 `--key Up` 那版跑出来的，槽位确实落在 078（日志：`save slot 78 -> 1016868 bytes`）。
npm run shot -- --load 78 --centered --name sc78 --burst 34
```

- `--burst <秒>` 是本次新加的（`tools/shot.cjs`）：点完之后每 2 s 连拍一张，用来抓**只出现一小段时间**的画面。
- 连拍帧落在仓库 `.tmp/`（**gitignored、会被后续运行覆盖**）：`sc78-16-t2s … t34s.png`，其中 `t16s`/`t18s` 是过场（几乎全黑），
  `t20s` 起进入 SC0000，`t34s` 是稳定态 —— 需要时用上面那条命令重跑即可。
- **本票只归档稳定态那一帧**：`tickets/T-0102/evidence/e4-sc0000-white-panel.png`（= `sc78-16-t34s.png`，sha256 见文末）。

## 现象（像素打点，`sc78-16-t34s.png`，1600×902）

| 采样点 | 颜色 | 说明 |
|---|---|---|
| (640, 700) | `#E3E3E3` | **ADV 窗口本体的平铺色**（用户看到的"白底"） |
| (1300, 700) | `#E3E3E3` | 同一块本体的右侧 |
| (640, 660) | `#FFFFFF` | 白字笔画（正文） |
| (900, 740) | `#E1C700` | 黄字（"接下维修委托算…"） |
| (640, 560) | `#604D4D` | 窗口上方的场景 |
| (240, 690) | `#4D2E20` | 窗口左缘外 |

- ★**`#E3E3E3` 正是本项目已记录的文字"白电平"**（`0xFFFFFF` 经覆盖率合成后落在 `233,230,228`
  一带，见 `raster.ts` 的 `TEXT_FILL_ALPHA` 注释与能力条目 `text-white-level-on-composite`）
  ⇒ 这块本体的来源**带文字光栅那条曲线**，不是"纯白占位块"（`#FFFFFF`）也不是"未绑定槽"。
- 同一帧里 **阿瓦罗的角色名已经是橘色**（`tickets/T-0102` 判据 4 的修复生效）✔ —— 也就是说：
  **字体颜色已对，只有窗口本体不对**。

## 已排除 / 已确证

- **不是** `presenter.#placeholder` 的纯白占位块：本轮全日志 `未绑定纹理槽` / `但纹理未就位` 各 **0 条**。
- **不是**上一轮修的那一类（消息窗正文区间 `[105000,105500)` 的无槽 DrawItem）：那条修的是
  `_load 79` 那一族的 `h=0x19a28…` 占位块，与本节这帧无关（两者都保留）。
- **不是** `rasterFrame` 的"窗底色"分支：那条分支恒不执行（`st.background` 语义上不存在）。
- 该帧的 `[present]` 汇总（示例）：
  `items={101020:a255 104000:a255 104001:a255 104501:a255 … 104200:a160 104203:a255} meshes={0x19258:0/#000000(0,0..1280,720)}`。
  ⇒ 窗口区（1042xx）确实有项在画，但**逐项归属**还没拿到（见下）。

## 下一步（缺一次"干净窗口"的日志）

本轮的日志被**用户同时在跑的实例**覆盖过（我读到的 `[present]` 那条其实是 TITLE 屏），
且 `shot` 的 `--load` 会在载入时把槽**回写**（日志里 `save slot 78 -> …`，其中一次写的是
**base**（真机目录）的 `SAVE78.DAT`）⇒ 需要：

1. **关掉你的实例 ~2 分钟**，让我跑一次带逐项日志的版本（我会临时打开"窗口区逐项日志"，
   跑完即撤），拿到 `1042xx` 那几项的 `handle / src / dst / tint / texture`；
2. 或者：你在**看到白底的那一帧**把 `E:\Games\Eushully\天結\.tmp\amayui-emulator.log` 复制给我
   （那里面就有 `configureDrawItem` / `setDrawColor*` 的完整序列）。

**当前最强候选**（等日志验证）：窗口本体项在"先设色、后建项"的顺序下丢了颜色 ——
`0x202`（`scSetDrawColor`）在 `flags & 1 == 0` 时**整笔丢弃**（日志 `[created-gated]`），
而 `0x203`（`scSetDrawColorAlpha`）**不丢**（引擎 `sub_4ACF60` 也确实是"建项 + 无条件写 `+48`/`+96`"；
但 `sub_4AD0C0`（0x202）的写入**全在 `flags & 1` 门内**）⇒ 两侧口径要逐条对齐后再定修法。
