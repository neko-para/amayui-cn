# T-0174 · 0x400 等待门的图形池旗标基址核对（变更记录）

> 主 agent 亲自核（read-only 读体 + 一处文档改写）。起因：`T-0172` 交回时的卡点（A8 指出 `rendering.md`
> line 84 与 line 92 口径不一致）＋ 我在整理 `T-0172` 的锚点表时发现「两个相邻 dword」的新线索。

## 一、三问的答案（全部落到体上）

### ① `sub_407E20`（raw 12762-12786）读的是哪一格？—— 返回 `[11629]`，但它自己**置**的是 `[11627]`

```c
v1 = _this[11631];                     // 等待时长
if ( !v1 ) goto LABEL_7;               // 没武装 ⇒ 直接看 [11629]
v2 = _this[11630] == 0;                // 起点是否已锁存
_this[11627] = 1;                      // ★副作用：置「有活动」位（12773）
if ( v2 ) _this[11630] = _this[11625]; // 起点 := 当前墙钟（池基 +46500）
if ( _this[11625] > (unsigned)(v1 + _this[11630]) || _this[11628] == 1 ) {
  _this[11630] = 0; _this[11631] = 0;  // 超时 ⇒ 拆表
LABEL_7:
  if ( (_this[11632] & 0x10000) != 0 && !_this[11629] ) _this[11632] = 0;
  return _this[11629];                 // ★返回 [11629]
}
return 1;                              // 计时器在跑 ⇒ 恒 1
```

⇒ **门 = 「等待计时器在跑」或「`[11629]` 非零」**。`[11627]` 只是它的副作用（"本帧有活动"），
`[11629]` 才是它**读**的那一格。

### ② `sub_4B4040` 族（present，raw 136785-136793）清的是哪两格？—— 两格都清，每帧一次

```c
*(_DWORD *)(_this + 46508) = 0;   // = [11627]
*(_DWORD *)(_this + 46516) = 0;   // = [11629]
sub_49DFD0((float *)_this); sub_4A1E90(_this); sub_4B06D0(_this);
```

⇒ 每帧 present 把**两个**旗标清 0，再由**本帧**的绘制/动画重新置起 ⇒ 这两格都是**帧内瞬态**，
不是跨帧累积量。

### ③ `sub_4AF1C0` 的 `_this` 是池基还是引擎基？—— **池基（= 场景/图形管理器，`Engine + 322832`）**

- `sub_407E20` 的调用点 raw **21111** 写作 `sub_407E20((_DWORD *)(_this + 322832))` —— **显式传池基**
  ⇒ 它的 `[11629]` = 池 + 46516 = 引擎 + 369348。
- `sub_4AF1C0` 的 9 个调用点全在**同一个大渲染函数**里（raw 135596/135770/135993/136106/136299/136564/
  136667/136916 与第 9 处 137221），且同函数内紧邻处就调 `sub_4AEEA0(_this, …)`（raw 135581）——
  两者共用同一个 `_this`（场景绘制对象）。
- 该函数（与 present 那个函数）访问 `_this + 46500`（= 池 + 46500 = 引擎 + 369332，动画墙钟）、
  `_this + 46508`、`_this + 46516` —— **同一基址**。

⇒ **`sub_4AF1C0` 在 raw 133528 写的 `_this + 46516` 与 `sub_407E20` 返回的 `[11629]` 是同一个 dword。**

## 二、由此推翻的旧说法（`rendering.md` line 84）

1. ❌「`46516` 与 `369348` 是**两个不同字节**」⇒ ✅ **同一个**（见 ③；`line 92` 早就写着 mesh-draw 用池基，
   本行当时却按 `base=引擎(0)` 解释同一格）。
2. ❌「`_this[369348]` 由**图形池绘制项 setter**（131881/131977/132823/132844…）置 1」⇒ ✅ 那些 setter 置的是
   **`[11627]`（= 池 + 46508）**；置 `[11629]` 的是 **mesh 绘制 `sub_4AF1C0`（133528）** 与 `sub_407E20` 自己
   的 `[11627]` 副作用要分开看。
3. ✅ **顺带收敛了那个「未收敛项」**：`rendering.md` line 84/89 把「~5s 静态显示的精确驱动者」列为未收敛 ⇒
   现在有了：present 每帧清两个旗标，而 mesh#2 的动画窗（delay `0x1194` = 4500ms ＋ dur `0x1f4` = 500ms）
   在它活跃的**每一帧**重新把 `[11629]` 置起 ⇒ 门持续到该窗走完（= 脚本停在那里的墙钟窗口 ≈ [4500, 5000]ms）。

## 三、落了什么

- `docs-new/03-engine/rendering.md` line 84：把「待核」段改成已核结论（含上面三问的要点），
  **保留了 `T-0172` 三向棘轮钉在这行的 10 个 cites**（12762 / 12768 / 12783 / 32303-32312 / 131881 /
  131977 / 132823 / 132844 / 133528 / 136792-136793）；`check-anchors.mjs` 复跑 **46 条 / ❌ 0**、
  `test/doc-model.test.ts` **10/10**。脚本：`app/amayui-emulator/.tmp/t0174-audit/fix-rendering.mjs`
  （自带"替换后 10 个 cites 必须仍在"的断言）。
- **未动**：`analysis/engine-capabilities.json`（若要给 `0x400 等待` 建/改能力条目，得等 `T-0108` 释放那六份
  台账；`[11627]`/`[11629]`/`[11631]` 三格的语义已在本文与 `rendering.md` 写清，可直接搬）。
- **未动**：任何 `src/**`（本条是分析票；emulator 侧 `0x400 门`的实现口径见 `waitFlags`/`frameRenderGate`，
  若要按本条订正实现，另开行为票）。

## 四、卡点

无。三问都在体上收敛。
