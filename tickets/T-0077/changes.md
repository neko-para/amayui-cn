# T-0077 · 过程文档（changes.md）

## 2026-09-19

2026-09-19 并行处置者完成验收 2 的 3 条（0x244 另一路在办）：① `0x324`（sub_41A470，raw 25402-25407）= 置 arity 槽后尾调 thunk `sub_453530` → 真身 `sub_453150`（raw 65367-65393；thunk 见清单 135085-135093）= 释放 Effect3D 的 `[258]`/`[259]`/`[260]`（字节 +0x408/+0x40C/+0x410 = Rain/Snow/Leaf 槽）并清 `[312]`（+0x4E0）；对象类型由析构 `sub_4537A0`（raw 65918-65923，`&Effect3D___vftable_`）证实 ⇒ 台账 disposition 改 `engine-internal`（有据跳过：emulator 无 Effect3D 子系统；★旧注 raw 33930 是邻居函数的空行，已订正）。② `0x325`（sub_426DC0，raw 33924-33938）= 写 Effect3D `+0x4D8`/`+0x4DC`；这两格作为**效果销毁判据**的消费点 = `sub_4535F0` raw 65890-65908（★旧注 raw 34000-34012 订正）⇒ 同样 `engine-internal`。③ `0x10C`（sub_4220B0，raw 30616-30634；汇编清单 53610-53659）= `Input[1176+Input[1432+键码]] = 位`（Input 是 Engine 的内嵌对象 = Engine+1032；目标 `Input[1176+VK]` 就是引擎每帧 `sub_4770A0`（raw 91551-91570）扫的 VK→掩码位表，中间键码表默认值见 `sub_476AA0` raw 91325-91423）⇒ 台账改 `engine-internal`（有据跳过：emulator 无键码表/VK→位表、宿主键盘也不进掩码 ⇒ 写入无消费者），但**引擎侧确有消费者** ⇒ note 里记明「T-0052 落地后必须从 ENGINE_INTERNAL_OPS 移进 OPS」，扩展点 = `handlers/input.ts` 的 `op_set_key_multi`。落点：`handlers/stubs.ts` 三条注释补 raw 依据 + 为什么不建模 + 扩展点；`analysis/opcode-gaps.json` 三条 disposition/note；`docs-new/03-engine/opcode-table.md` 三行；`test/opcode-gaps.test.ts` 的「4 条必须保持 unjustified」棘轮改写为「必须在册 + note 必须写清体内效果与不实现理由 + 仍 unjustified 必须带票」（不改它的话，本次正当处置反会把守卫弄红）；`test/opcode-operands.test.ts` 两条豁免理由串同步。另：`docs-new/03-engine/input-system.md` 的 0x10C 行原把两个操作数写反（`op1` 实为掩码位、`op2` 实为键码），已按体订正并补行号订正脚注。
