# T-0032 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

本条只修工具参数的路径基准与错误表现，不改录制格式。★证据里的报错文本来自实测日志（原文：`App threw an error during load` / `Error: EPERM: operation not permitted, mkdir '/Users/<user>/Documents/.tmp'`）。
