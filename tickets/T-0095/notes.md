# T-0095 · 过程文档（notes.md）

## 2026-09-20

### 轮 7 · 规格分析已收口（ANALYSIS-ONLY 子代理，全文见 `design.md`）

**结论：`0x1d0` 纯 VM、零宿主缝、现在就能做 ⇒ 建议按 `implemented` 落地，不降级。**

**前置（写端）已逐行读完**：
- `0x70` = `sub_41ED20`（raw 28401-28416）→ `sub_45D660`（raw 73133-73192）
- `0x71` = `sub_41ED80`（raw 28419-28462）→ `sub_45EC60`（raw 74197-74281）
- 页表 push 点**穷举** = 3 处：raw **73186**（0x70）/ **74271**（0x71）/ **74494**（读档 `sub_45F1B0`）

**★本主 agent 的独立复核（对子代理报告做了对抗性核对）**：
- ✅ 页表写者穷举成立。但子代理原话「`sub_45D240` 全库只有 3 个调用点」**不精确** —— 该函数是通用 8B/条 push_back，全库约 **17** 个调用点，其中指向页表（`+845`/`+3380`）的**确实只有那 3 处**（其余指向 `+95768`/`+95772`/`+383072` 等别的 vector）。已按此订正 `fields.json` 的 `Font/0x15A64` 证据文字。
- ✅ 顺带按子代理建议补全 `fields.json`：新增 `Font/0x15A68 backlog_pages_end`、`Font/0x15A6C backlog_pages_cap`；并给 `Font/0x15A74`（组首标记，**补写者** 73187/74275）、`Font/0x15A9C`（补 0x70 的 push 73189-73190）补证据。

**连带发现的三处既有缺陷**（本票一并修）：
1. `0x71` 的 SetTB 门在 emulator **没实现**：`m.textSlotArg` 全库只被初始化、**从未赋值** ⇒ 恒 0 ⇒ `>= 0` 恒真（引擎门 = `Engine[97055] >= 0`）。
2. `0x70` 在引擎里**也** push 页 + 置组首标记（raw 73186-73187），emulator 两件都没做。
3. `0x85`（`sub_418F50` → `sub_45EBE0` raw 74188-74193）= 清空**记录表 + 页表两张 vector**；`opcode-gaps.json` 里记的理由「GDI 文本对象的行/段容器」**是错的** ⇒ 纯 VM 可实现。

**语料计数（★两种写法都数过的实测）**：`i070` = **10** 处 / 3 文件（`i70` = 0）；`i071` = **90713** 处 / 340 文件（`i71` = 0）；`i1d0` = **5** 处 / 3 文件（HISTORY 3 / CONFIG 1 / REPLAYVOICE 1）；`i1d1` = 1；`i085` = **1**（`i85` = 0）。
★这正是交接文档 §5.4 那条纪律的又一实例：**台账助记符不补零、`src/*.txt` 是 `i` + 三位**。

**E3 载体已实测**：`install/CONFIG.BIN`（426 条指令，含 `0x1d0` 一条，实参与 `src/CONFIG.txt:22` 逐字对应）。★诚实披露：CONFIG 只**读**页表，页由主 ADV 的 `i071` 填 ⇒ 纯 CONFIG 必须先播合成前奏，否则 `i1d0` 合理返回 `-1,-1`。

**另一条订正**：`docs-new/03-engine/opcode-table.md:110` 把 `0x71` 的 op1 写成「文本」是**错的** ⇒ 实为**消息窗槽号**（语料 `i071 9` / `i071 2` / `i071 (local-int 4a9)` 且 4a9 = 3+i）。
