# T-0051 · 进展记录

## 2026-09-23：清单里与「存档缩略图」有关的那一条，**headless 半边已被自动化**

本票的 5 条都是"要在 Electron / 真机上亲眼看"的验证。同会话的 `T-0128` 没有（也不能）替代真机核验，
但它把其中 **0x32 · 存档缩略图** 的**前置条件**从"要人坐在窗口前"变成了"测试里可复现"：

| 项 | 之前 | 之后（`T-0128`） |
|---|---|---|
| 存档缩略图 E3 | `save-slot-chain.test.ts` 里那条 UI 驱动用例**在本机静默跳过**（列表首页没有占用槽 ⇒ 脚本根本不调 `0x1AF`；探针实测 `readSlotThumb` 调用数 = 0） | 拆成 `save-slot-thumb.test.ts`，并把真槽**复制到槽 0**（受控夹具）⇒ **任何机器上都是确定性 E3**（真链路 + 真 `.STH` 解码 + `setSlotPixels` 320×180） |
| 真槽定位 | 7~9 处闸门只查 `baseDir`（本机不存在）⇒ 静默跳过 | 收敛到 `test/realSlots.ts`（**base + overlay 两侧都看**）⇒ T1 档 skip 11 → 1 |

⇒ 本票清单里 **item 1** 现在只剩"真机那一半"：`.STH` 是**真截图**（内容 = 存档瞬间画面）而不是 `AMYTH1` 占位块，
以及 SAVE 界面把它显示出来 —— 这一半仍然要 Electron（`npm run shot` / `dbg:srv`）。

**其余四条不变**（仍需要真界面/真机）：

| # | 项 | 状态 |
|---|---|---|
| 2 | `0x32` 缩放插值（linear vs point） | ⬜ 待真机对照（可能推翻实现选择） |
| 3 | `0x207` 同尺寸槽→槽转送的像素落地（三处可见贴片） | ⬜ 待真界面 |
| 4 | `0x214` 换场收场（两立绘槽内容对调、次序不变） | ⬜ 待走到那一段章节 |
| 5 | `0x10A` 侧栏钉光标 | ⬜ **前置缺口**：不合成键盘掩码位 ⇒ 等 `T-0052`（本会话未动） |

★本票**保持 open**：E4 的定义就是"与真机/真界面核对过"，headless 再确定也替代不了它。
