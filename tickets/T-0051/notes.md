# T-0051 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

怎么用这张票：每条 acceptance 都是一次具体的人工操作 + 一个可判定的观察；做完把结论写进 `changes.md`（新条目放最上方）并按结果决定是否要改实现（第 2 条与第 3 条都可能反过来改代码）。★为什么它们没能在自动化里覆盖：headless 没有画布（第 1/2/3 条）、没有键盘掩码位（第 5 条）、到不了章节深处的块（第 4 条，boot SN0000 跑 2500 帧实测 0 次 `i214`）——都已在各自票据里写明，不在本票重复分析。
★2026-09 补（`T-0058`）：本清单里「0x10A 侧栏钉光标」那一项的**宿主侧前置条件已就绪** —— `native/win32-input` 的原生 `SetCursorPos` 已接线（渲染侧虚拟→客户区 + 主进程 DIP→物理；缺模块时降级为旧行为），真机核验见 `T-0058` 的 evidence（模块级往返、Electron 三跳换算、产品宿主日志）。⇒ E4 现在只需要在真界面里**看观感**：键盘走 ADV 侧栏时物理光标是否被钉进/钉出栏内。
