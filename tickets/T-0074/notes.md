# T-0074 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

复核范围与结论：① `0x2FA` 等价 no-op 成立（`_this[1951]` 全文件只有 raw 33724 一次写、无读）；② `0x137`（整型栈复位，同族 `0x138`/`0x13B..0x13D`，emulator 未建该栈）与 `0x244`（`sub_41A370 → sub_4AD9F0(Scene,2)` 遍历绘制项复位 `flags&2` 者的窗）是**未建模缺口**，不是等价 no-op（语料各 1 处；`opcode-table.md` / 偏离表第 3 行已按此措辞）；③ CALLBACK_LOAD 那一跳是引擎行为（已在对的位置）；④ 载荷项机制与「槽对象指针表」的关系写进 §7.10。留待实测的点：真机从标题读档的瞬间，上一屏（标题/菜单）的项是否可见 —— 静态读不出「谁清了它们」，候选是各屏自己的 `i1f6`/`detach-texture`（SAVE.BIN 的 `detach-texture 1d4c0 bb8` 在 `i1a1` 之后、读档路径不执行）与 `sub_403EF0` 家族。
