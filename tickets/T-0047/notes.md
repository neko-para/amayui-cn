# T-0047 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

为什么单独开单：T-0046 修的是 0x100 的分支逻辑（可见症状 = 右键无反应），本单改的是 0xCD 的节流时序；夹在一起会让「行为变了」归因不清，而且本单必须同时改三处测试的时钟模型（属 T-0020「收敛测试结构」的同一类问题 —— 那三处都是自造帧循环/冻结时钟）。建议落地顺序：① 写 `advanceThrottle`（一行）② 跑全量测试拿到失败清单（预期就是上面那三处）③ 逐个把它们的时钟改成「按帧/按指令批前进」，**不改断言** ④ 更新文档与 capability 台账（`adv-input-pump-perframe` 的 note 里补一句节流已建模）。
