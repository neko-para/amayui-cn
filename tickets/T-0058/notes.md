# T-0058 · 过程文档（notes.md）

## 从 ticket.json 的 `notes` 字段迁入（2026-09 文档模型）

过程笔记见 notes.md（含：官方资料口径摘要与链接、两个 Windows 坑、以及「为什么降级必须做扎实」）。★残余（已写进能力条目）：`ShowCursor` 的两处与另两处「光标居中」没接线（本作只在 0x10A 这条路上需要）；三条语料用途的**观感**要人在真界面看一次（`T-0051` 的 E4 清单）。★`.node` 不进 git（按需构建）—— 这也意味着**任何自动化都不能假定模块已构建**，降级路径是契约的一部分。
